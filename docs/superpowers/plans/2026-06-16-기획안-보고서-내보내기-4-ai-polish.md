# 기획안 → 보고서 AI 다듬기 (opt-in) Implementation Plan — 계획 4/4

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans. Steps use checkbox (`- [ ]`).

**Goal:** `vib report` 에 **opt-in AI 다듬기**를 추가한다. `--polish` 시 무료 provider 체인으로 보고서 본문 텍스트를 "개발 용어 → 비즈니스 보고 어조"로 다듬고, 실패하면 템플릿 원문으로 graceful fallback. 결과는 enrich 캐시에 저장해 반복 호출을 막는다. **기본 OFF, 무료 provider 전용(Claude 자동 호출 없음).**

**Architecture:** 증분 1 의 `build_report_model()` 가 만든 `ReportModel` 을 입력으로, 새 `polish.py` 가 블록별 텍스트를 다듬은 새 `ReportModel` 을 반환한다(순수 변환 위에 얹는 패스). provider 호출은 **기존 planning_cli 인프라 재사용**: `cli_adapters.build_cli_command` + `SubprocessPlanningCliRunner` + `response_policy.safe_planning_status`. 단 **provider 시도 순서는 free-only 로 새로 정의**(planning 의 `provider_try_order` 는 claude 를 포함하므로 재사용 금지). 캐시는 기획방 enrich.json 패턴(내용 해시 → 캐시) 미러.

**Tech Stack:** Python 3, pytest, dataclasses. 신규 외부 의존성 없음. 재사용: `vibelign.core.planning_cli.cli_adapters`, `.response_policy`.

**상위 설계:** `docs/superpowers/specs/2026-06-15-기획안-보고서-내보내기-design.md` §6 (하이브리드 변환). **비용 원칙(필수)**: AI 다듬기 기본 OFF; 켜도 무료 provider(codex→opencode→agy)만; Claude 는 `--cli claude` 명시 시에만(v2.4.3 정합).

**실행 브랜치:** `feat/report-export-ai` (base = `feat/report-export` = 증분 1 CLI). GUI/PDF(2a/2b)와 독립.

**범위:**
- `--polish`(기본 OFF) + `--cli {auto|codex|opencode|agy|claude}` 옵션.
- 다듬기 대상: `paragraph`·`summary` 블록 텍스트(블록당 1콜, 실패 시 원문 유지). `bullets` 항목 다듬기는 후속(MVP 제외 — 명시).
- enrich 캐시(`.vibelign/reports/{slug}-polish.json`): 동일 입력 재요청 시 provider 재호출 0.
- **비용 안전**: auto/기본 경로에서 claude 절대 호출 안 됨(테스트로 강제).

---

## 파일 구조
신규:
| 파일 | 책임 |
|---|---|
| `vibelign/core/reporting_cli/polish.py` | `polish_try_order`, `polish_block_text`, `polish_report_model` |
| `vibelign/core/reporting_cli/polish_cache.py` | `polish_cache_key`, `load_polish_cache`, `save_polish_cache` |
| `tests/core/reporting_cli/test_polish.py` | provider mock — 성공/실패/타임아웃/free-only/claude-차단 |
| `tests/core/reporting_cli/test_polish_cache.py` | 캐시 hit/miss |

수정:
| 파일 | 변경 |
|---|---|
| `vibelign/commands/vib_report_cmd.py` | `--polish`/`--cli` → polish_report_model(+캐시) |
| `vibelign/cli/cli_command_groups.py` | report 서브파서에 `--polish`/`--cli` 인자 |
| `vibelign/core/reporting_cli/__init__.py` | polish 공개 export |

---

## Task 1: polish.py (free-only provider 다듬기)

**Files:**
- Create: `vibelign/core/reporting_cli/polish.py`
- Test: `tests/core/reporting_cli/test_polish.py`

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/core/reporting_cli/test_polish.py`:
```python
from dataclasses import dataclass

from vibelign.core.reporting_cli.models import Block, ReportModel, Section
from vibelign.core.reporting_cli.polish import polish_report_model, polish_try_order


@dataclass
class FakeResult:
    status: str
    stdout: str


class FakeRunner:
    """SubprocessPlanningCliRunner 대역. attempts 에 (adapter, prompt) 기록."""

    def __init__(self, script):
        self.script = script  # dict: adapter -> FakeResult
        self.attempts = []

    def run(self, command, *, cwd, input_text, timeout_seconds):
        adapter = command[0]
        self.attempts.append(adapter)
        return self.script.get(adapter, FakeResult("not_installed", ""))


def _model():
    return ReportModel(
        title="예약 앱",
        report_type="work",
        date="2026-06-16",
        sections=[
            Section("개요", [Block(kind="summary", text="예약 앱 MVP")]),
            Section("핵심 내용", [Block(kind="bullets", items=["캘린더", "알림"])]),
            Section("배경", [Block(kind="paragraph", text="전화 예약 누락이 잦음")]),
        ],
    )


def test_try_order_auto_is_free_only_no_claude():
    order = polish_try_order("auto")
    assert "claude" not in order
    assert order[0] == "codex"  # design §6: codex → opencode → agy


def test_try_order_explicit_claude_allowed():
    # 명시 opt-in 일 때만 claude
    assert polish_try_order("claude")[0] == "claude"


def test_polish_rewrites_paragraph_and_summary_keeps_bullets():
    runner = FakeRunner({"codex": FakeResult("ok", "다듬은 문장")})
    out = polish_report_model(_model(), provider="auto", runner=runner)
    # summary/paragraph 텍스트가 다듬어짐
    assert out.sections[0].blocks[0].text == "다듬은 문장"
    assert out.sections[2].blocks[0].text == "다듬은 문장"
    # bullets 는 MVP 에서 손대지 않음
    assert out.sections[1].blocks[0].items == ["캘린더", "알림"]
    # claude 는 시도조차 안 됨
    assert "claude" not in runner.attempts


def test_polish_failure_keeps_original_text():
    runner = FakeRunner({})  # 모든 provider not_installed
    out = polish_report_model(_model(), provider="auto", runner=runner)
    assert out.sections[2].blocks[0].text == "전화 예약 누락이 잦음"  # 원문 유지


def test_polish_falls_back_to_next_free_provider():
    runner = FakeRunner({
        "codex": FakeResult("timeout", ""),
        "opencode": FakeResult("ok", "오픈코드 다듬음"),
    })
    out = polish_report_model(_model(), provider="auto", runner=runner)
    assert out.sections[2].blocks[0].text == "오픈코드 다듬음"
    assert "claude" not in runner.attempts


def test_polish_returns_new_model_does_not_mutate_input():
    original = _model()
    runner = FakeRunner({"codex": FakeResult("ok", "X")})
    out = polish_report_model(original, provider="auto", runner=runner)
    assert original.sections[0].blocks[0].text == "예약 앱 MVP"  # 입력 불변
    assert out is not original
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd /Users/usabatch/coding/VibeLign && python -m pytest tests/core/reporting_cli/test_polish.py -v`
Expected: FAIL — `ModuleNotFoundError: ...polish`

- [ ] **Step 3: 구현**

`vibelign/core/reporting_cli/polish.py`:
```python
from __future__ import annotations

from dataclasses import replace
from pathlib import Path

from vibelign.core.planning_cli import cli_adapters
from vibelign.core.planning_cli.response_policy import safe_planning_status
from vibelign.core.reporting_cli.models import Block, ReportModel, Section

# design §6: 무료 provider 전용. planning 의 provider_try_order 는 claude 를 포함하므로
# 재사용하지 않는다(비용 노출 방지). claude 는 명시 요청 시에만.
FREE_PROVIDERS: tuple[str, ...] = ("codex", "opencode", "agy")
_POLISH_BLOCK_KINDS = {"paragraph", "summary"}


def polish_try_order(provider: str | None) -> list[str]:
    """다듬기 provider 시도 순서. auto/None → 무료 전용. 명시 provider → 그것 우선 + 무료 보충.
    claude 는 provider 가 정확히 'claude' 일 때만 포함된다(자동 폴백으로는 절대 안 들어감)."""
    if not provider or provider == "auto":
        return list(FREE_PROVIDERS)
    order = [provider]
    for p in FREE_PROVIDERS:
        if p != provider:
            order.append(p)
    return order


def polish_block_text(text: str, *, provider: str, runner, root: Path, timeout_seconds: int) -> str | None:
    """텍스트 한 덩이를 비즈니스 보고 어조로 다듬는다. 실패 시 None."""
    if not text.strip():
        return None
    prompt = (
        "다음 보고서 문장을 의미는 그대로, 개발 용어를 업무 보고에 어울리는 비즈니스 "
        "어조로 자연스럽게 다듬어줘. 설명·따옴표 없이 다듬은 문장만 출력해.\n\n"
        f"{text}"
    )
    for adapter in polish_try_order(provider):
        command = cli_adapters.build_cli_command(adapter, prompt)
        if command is None:
            continue
        result = runner.run(command, cwd=root, input_text="", timeout_seconds=timeout_seconds)
        if safe_planning_status(result.status, result.stdout) == "ok":
            cleaned = result.stdout.strip()
            if cleaned:
                return cleaned
    return None


def polish_report_model(
    model: ReportModel,
    *,
    provider: str = "auto",
    runner=None,
    root: Path | None = None,
    timeout_seconds: int = 60,
) -> ReportModel:
    """ReportModel 의 paragraph/summary 블록 텍스트를 다듬은 새 ReportModel 을 반환한다.
    블록별 실패는 원문 유지(graceful). 입력 model 은 변경하지 않는다."""
    if runner is None:
        runner = cli_adapters.SubprocessPlanningCliRunner()
    if root is None:
        root = Path.cwd()

    new_sections: list[Section] = []
    for section in model.sections:
        new_blocks: list[Block] = []
        for block in section.blocks:
            if block.kind in _POLISH_BLOCK_KINDS and block.text:
                polished = polish_block_text(
                    block.text, provider=provider, runner=runner, root=root,
                    timeout_seconds=timeout_seconds,
                )
                new_blocks.append(replace(block, text=polished) if polished else block)
            else:
                new_blocks.append(block)
        new_sections.append(replace(section, blocks=new_blocks))
    return replace(model, sections=new_sections)
```

- [ ] **Step 4: 통과 확인**

Run: `cd /Users/usabatch/coding/VibeLign && python -m pytest tests/core/reporting_cli/test_polish.py -v`
Expected: PASS (6 passed)

- [ ] **Step 5: 커밋**

```bash
git add vibelign/core/reporting_cli/polish.py tests/core/reporting_cli/test_polish.py
git commit -m "feat(reporting): AI 다듬기 패스 (무료 provider 전용, claude 자동호출 차단)"
```

---

## Task 2: polish_cache.py (enrich 캐시)

**Files:**
- Create: `vibelign/core/reporting_cli/polish_cache.py`
- Test: `tests/core/reporting_cli/test_polish_cache.py`

- [ ] **Step 1: 실패하는 캐시 테스트**

`tests/core/reporting_cli/test_polish_cache.py`:
```python
from pathlib import Path

from vibelign.core.reporting_cli.models import Block, ReportModel, Section
from vibelign.core.reporting_cli.polish_cache import (
    load_polish_cache,
    polish_cache_key,
    save_polish_cache,
)


def _model(text="원문"):
    return ReportModel(
        title="t", report_type="work", date="2026-06-16",
        sections=[Section("S", [Block(kind="paragraph", text=text)])],
    )


def test_key_changes_with_content_and_provider():
    k1 = polish_cache_key(_model("a"), provider="auto")
    k2 = polish_cache_key(_model("b"), provider="auto")
    k3 = polish_cache_key(_model("a"), provider="codex")
    assert k1 != k2 and k1 != k3


def test_save_then_load_roundtrip(tmp_path: Path):
    polished = _model("다듬음")
    save_polish_cache(tmp_path, "예약-앱-work", key="abc", model=polished)
    hit = load_polish_cache(tmp_path, "예약-앱-work", key="abc")
    assert hit is not None
    assert hit.sections[0].blocks[0].text == "다듬음"


def test_load_miss_on_key_mismatch(tmp_path: Path):
    save_polish_cache(tmp_path, "s", key="abc", model=_model("x"))
    assert load_polish_cache(tmp_path, "s", key="zzz") is None


def test_load_miss_when_absent(tmp_path: Path):
    assert load_polish_cache(tmp_path, "none", key="abc") is None
```

- [ ] **Step 2: 실패 확인** — `python -m pytest tests/core/reporting_cli/test_polish_cache.py -v` → FAIL(모듈 없음).

- [ ] **Step 3: 구현**

`vibelign/core/reporting_cli/polish_cache.py`:
```python
from __future__ import annotations

import json
from dataclasses import asdict
from hashlib import sha256
from pathlib import Path

from vibelign.core.reporting_cli.models import Block, ReportModel, Section

_SCHEMA = 1


def polish_cache_key(model: ReportModel, *, provider: str) -> str:
    """모델 내용 + provider 의 안정 해시. 내용/ provider 가 바뀌면 키가 바뀐다."""
    h = sha256()
    h.update(provider.encode("utf-8"))
    h.update(b"\x1e")
    h.update(f"{model.title}\x1f{model.report_type}\x1f{model.date}".encode("utf-8"))
    for s in model.sections:
        h.update(b"\x1e")
        h.update(s.heading.encode("utf-8"))
        for b in s.blocks:
            h.update(b"\x1f")
            h.update(f"{b.kind}:{b.text}:{'|'.join(b.items)}".encode("utf-8"))
    return h.hexdigest()


def _cache_path(root: Path, slug: str) -> Path:
    return root / ".vibelign" / "reports" / f"{slug}-polish.json"


def save_polish_cache(root: Path, slug: str, *, key: str, model: ReportModel) -> Path:
    dest = _cache_path(root, slug)
    dest.parent.mkdir(parents=True, exist_ok=True)
    payload = {"schema_version": _SCHEMA, "key": key, "model": asdict(model)}
    dest.write_text(json.dumps(payload, ensure_ascii=False), encoding="utf-8")
    return dest


def load_polish_cache(root: Path, slug: str, *, key: str) -> ReportModel | None:
    path = _cache_path(root, slug)
    if not path.exists():
        return None
    try:
        data = json.loads(path.read_text(encoding="utf-8"))
    except (OSError, ValueError):
        return None
    if data.get("schema_version") != _SCHEMA or data.get("key") != key:
        return None
    m = data["model"]
    sections = [
        Section(heading=s["heading"], blocks=[Block(**b) for b in s["blocks"]])
        for s in m["sections"]
    ]
    return ReportModel(
        title=m["title"], report_type=m["report_type"], date=m["date"],
        source_plan_path=m.get("source_plan_path", ""), sections=sections,
    )
```

- [ ] **Step 4: 통과 확인** — PASS (4 passed). **Step 5: 커밋** `feat(reporting): 다듬기 결과 enrich 캐시`.

---

## Task 3: CLI 배선 (`--polish`/`--cli`)

**Files:**
- Modify: `vibelign/commands/vib_report_cmd.py`
- Modify: `vibelign/cli/cli_command_groups.py` (report 서브파서)
- Modify: `vibelign/core/reporting_cli/__init__.py`
- Test: `tests/cli/test_vib_report_cmd.py` (확장)

- [ ] **Step 1: 실패하는 커맨드 테스트 추가**

`tests/cli/test_vib_report_cmd.py` 끝에 추가(기존 `_args` 헬퍼에 `polish=False, cli="auto"` 기본 추가):
```python
def test_report_polish_off_by_default_no_provider_call(tmp_path, capsys, monkeypatch):
    monkeypatch.chdir(tmp_path)
    plan = tmp_path / "plan.md"
    plan.write_text(PLAN_MD, encoding="utf-8")
    called = {"n": 0}
    import vibelign.core.reporting_cli.polish as polish_mod
    monkeypatch.setattr(polish_mod, "polish_report_model", lambda *a, **k: called.__setitem__("n", called["n"] + 1) or a[0])
    run_vib_report(_args(plan, json=True))  # polish 미지정
    assert called["n"] == 0  # 기본 OFF → polish 호출 안 함


def test_report_polish_on_calls_polish(tmp_path, capsys, monkeypatch):
    monkeypatch.chdir(tmp_path)
    plan = tmp_path / "plan.md"
    plan.write_text(PLAN_MD, encoding="utf-8")
    seen = {}
    import vibelign.commands.vib_report_cmd as cmd
    def fake_polish(model, *, provider, root, **k):
        seen["provider"] = provider
        return model
    monkeypatch.setattr(cmd, "polish_report_model", fake_polish)
    run_vib_report(_args(plan, json=True, polish=True, cli="auto"))
    assert seen.get("provider") == "auto"
```

- [ ] **Step 2: 실패 확인** — argparse 에 `polish`/`cli` 없음 → AttributeError 또는 등록 전.

- [ ] **Step 3: 구현 — `vib_report_cmd.py`**

`ReportArgs` Protocol 에 추가:
```python
    polish: bool
    cli: str
```
import 추가: `from vibelign.core.reporting_cli import polish_report_model`.
`build_report_model(...)` 직후, `render_html` 전에:
```python
    if getattr(raw, "polish", False):
        from pathlib import Path as _Path
        model = polish_report_model(model, provider=getattr(raw, "cli", "auto") or "auto", root=_Path.cwd())
```
> 캐시 연동(선택, 같은 Step): polish 전 `load_polish_cache(root, slug, key=polish_cache_key(model, provider))` 히트면 그걸 쓰고, 미스면 polish 후 `save_polish_cache`. 캐시는 Task 2 함수 재사용. (테스트는 polish_report_model 모킹이라 캐시 분기는 통합테스트에서 옵션.)

- [ ] **Step 4: 구현 — `cli_command_groups.py`** (report 서브파서, 기존 `--json` 인자 뒤):
```python
    _ = r.add_argument("--polish", action="store_true", help="AI 로 어조 다듬기(무료 provider, 기본 OFF)")
    _ = r.add_argument("--cli", default="auto", choices=["auto", "codex", "opencode", "agy", "claude"], help="다듬기 provider (claude 는 명시 opt-in)")
```

- [ ] **Step 5: `__init__.py`** 에 `polish_report_model` export 추가.

- [ ] **Step 6: 통과 확인** — `python -m pytest tests/cli/test_vib_report_cmd.py tests/core/reporting_cli/ -v` → 전체 PASS.

- [ ] **Step 7: 커밋** `feat(reporting): vib report --polish/--cli 배선 (opt-in AI 다듬기)`.

---

## Task 4: 통합 검증 + 회귀

- [ ] **Step 1:** `python -m pytest tests/core/reporting_cli/ tests/cli/test_vib_report_cmd.py -q` → 전체 PASS.
- [ ] **Step 2:** `ruff check vibelign/core/reporting_cli/ vibelign/commands/vib_report_cmd.py tests/core/reporting_cli/` → clean.
- [ ] **Step 3 (라이브, provider 없이 graceful):** `python -m vibelign report /tmp/plan.md --type work --polish --json` — provider 미설치 환경이면 원문 그대로(템플릿 fallback)로 정상 생성되는지 확인(조용한 실패 없이). 출력 `{ok:true}`.
- [ ] **Step 4:** 전체 스위트 회귀 `python -m pytest tests/ -q` (기존 영향 0).

---

## Self-Review (작성 시점)
- **§6 커버:** ① 어조 다듬기(paragraph/summary 블록) ✓, 무료 provider 체인 ✓, 캐시 ✓, fallback ✓, 기본 OFF ✓. ② 임원용 요약 별도 생성은 MVP 에서 summary 블록 다듬기로 갈음(전용 생성은 후속). bullets 다듬기 후속(명시).
- **비용 안전(최重要):** `polish_try_order("auto")` 가 claude 미포함 — `test_try_order_auto_is_free_only_no_claude` + `test_polish_*` 의 `"claude" not in runner.attempts` 로 강제. planning 의 `provider_try_order`(claude 포함) **재사용 안 함**.
- **재사용:** `cli_adapters.build_cli_command`/`SubprocessPlanningCliRunner`/`safe_planning_status` 그대로. 신규 의존성 0.
- **불변성:** `polish_report_model` 은 `dataclasses.replace` 로 새 모델 반환(입력 불변, 테스트로 보장).
- **플레이스홀더:** 없음. 모든 스텝 실제 코드/명령.

## 후속
- bullets 항목 다듬기, 전용 임원 요약 생성(별도 프롬프트), GUI `--polish` 토글 노출(2a 모달에 체크박스).
