# 기획안 → 보고서 CLI (`vib report`) Implementation Plan — 계획 1/4

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 저장된 기획안 마크다운(`plans/*.md`)을 입력받아 보고서 종류(업무/제안/결과)에 맞는 HTML 보고서를 `.vibelign/reports/` 에 생성하는 `vib report` CLI 커맨드를 만든다.

**Architecture:** 3단 파이프라인을 순수 Python 모듈로 구현한다. `reader`(.md → `PlanningData`) → `templates`(`PlanningData` + 종류 → 포맷 독립 `ReportModel`) → `html_renderer`(`ReportModel` → HTML). `storage` 가 파일로 쓰고, `vib_report_cmd` 가 CLI 진입점이 된다. 이 계획은 GUI/Word/PPT/AI 없이 CLI 단독으로 완결·테스트된다.

**Tech Stack:** Python 3, argparse(기존 lazy_command 등록), pytest, dataclasses. 신규 외부 의존성 없음.

**상위 설계 문서:** `docs/superpowers/specs/2026-06-15-기획안-보고서-내보내기-design.md`

**이 계획의 범위(증분 1):**
- 보고서 종류 3종(work/proposal/result) 템플릿
- 출력 포맷: HTML 1종 (PDF 는 계획 2에서 이 HTML 을 웹뷰 인쇄로 처리)
- 변환: 템플릿 기반(결정적, AI 없음). AI 다듬기는 계획 4.
- `Block` 종류는 `paragraph`/`bullets`/`summary` 만. `table`/`keyvalue`/`slide_break` 는 Word/PPT 계획에서 추가.

---

## 파일 구조

신규 모듈 `vibelign/core/reporting_cli/`:

| 파일 | 책임 |
|---|---|
| `__init__.py` | 공개 API export |
| `models.py` | `PlanningData`, `ReportModel`, `Section`, `Block` 데이터클래스 |
| `reader.py` | `parse_plan_markdown(text) -> PlanningData` |
| `templates.py` | `SectionSpec`, `REPORT_TEMPLATES`, `build_report_model(...)` |
| `html_renderer.py` | `render_html(model) -> str` |
| `storage.py` | `write_report(...) -> Path` |

신규 커맨드/등록:
- `vibelign/commands/vib_report_cmd.py` — `run_vib_report(args)`
- `vibelign/cli/cli_command_groups.py` — `report` 서브파서 등록 (line 682 뒤)

신규 테스트:
- `tests/core/reporting_cli/conftest.py` (격리 fixture)
- `tests/core/reporting_cli/test_reader.py`
- `tests/core/reporting_cli/test_templates.py`
- `tests/core/reporting_cli/test_html_renderer.py`
- `tests/core/reporting_cli/test_storage.py`
- `tests/cli/test_vib_report_cmd.py`

---

## Task 1: 마크다운 리더 (`models.py` + `reader.py`)

**Files:**
- Create: `vibelign/core/reporting_cli/__init__.py`
- Create: `vibelign/core/reporting_cli/models.py`
- Create: `vibelign/core/reporting_cli/reader.py`
- Create: `tests/core/reporting_cli/__init__.py` (빈 파일, 패키지)
- Create: `tests/core/reporting_cli/conftest.py`
- Test: `tests/core/reporting_cli/test_reader.py`

- [ ] **Step 1: 격리 conftest 작성**

기존 `tests/core/planning_cli/conftest.py` 패턴을 그대로 복제한다.

`tests/core/reporting_cli/conftest.py`:
```python
import pytest


@pytest.fixture(autouse=True)
def _isolated_home(tmp_path_factory, monkeypatch):
    """reporting_cli 테스트가 실제 ~/.vibelign 설정을 읽지 않도록 격리."""
    home = tmp_path_factory.mktemp("home")
    monkeypatch.setattr("pathlib.Path.home", lambda: home)
    monkeypatch.setenv("HOME", str(home))
    return home
```

`tests/core/reporting_cli/__init__.py`: 빈 파일.

- [ ] **Step 2: 실패하는 리더 테스트 작성**

`tests/core/reporting_cli/test_reader.py`:
```python
from vibelign.core.reporting_cli.reader import parse_plan_markdown


SAMPLE = """# 예약 앱

## 한 줄 목표
미용실 예약을 앱으로 받고 싶어요.

## 대상 사용자
동네 미용실 사장님.

## 핵심 기능
- 예약 캘린더
- 알림 문자
- 아직 결정이 필요합니다.

## 사용자 흐름
- 손님이 시간 선택
- 사장이 확정

## 제외할 것
- 결제 연동

## 아직 결정이 필요한 질문
- 노쇼 정책은?
"""


def test_parse_extracts_title_and_idea():
    data = parse_plan_markdown(SAMPLE)
    assert data.title == "예약 앱"
    assert data.idea == "미용실 예약을 앱으로 받고 싶어요."


def test_parse_extracts_list_sections():
    data = parse_plan_markdown(SAMPLE)
    assert data.features == ["예약 캘린더", "알림 문자"]
    assert data.flows == ["손님이 시간 선택", "사장이 확정"]
    assert data.exclusions == ["결제 연동"]
    assert data.open_questions == ["노쇼 정책은?"]


def test_parse_drops_placeholder_items():
    data = parse_plan_markdown(SAMPLE)
    assert "아직 결정이 필요합니다." not in data.features


def test_parse_empty_text_returns_blank_data():
    data = parse_plan_markdown("")
    assert data.title == ""
    assert data.features == []
```

- [ ] **Step 3: 테스트 실패 확인**

Run: `cd /Users/usabatch/coding/VibeLign && python -m pytest tests/core/reporting_cli/test_reader.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'vibelign.core.reporting_cli'`

- [ ] **Step 4: 데이터 모델 구현**

`vibelign/core/reporting_cli/models.py`:
```python
from __future__ import annotations

from dataclasses import dataclass, field


@dataclass
class PlanningData:
    """기획안 .md 에서 추출한 정규화 데이터 (포맷·종류 무관)."""

    title: str = ""
    idea: str = ""
    target_users: str = ""
    problem: str = ""
    features: list[str] = field(default_factory=list)
    flows: list[str] = field(default_factory=list)
    decisions: list[str] = field(default_factory=list)
    exclusions: list[str] = field(default_factory=list)
    open_questions: list[str] = field(default_factory=list)
    context_notes: str = ""


@dataclass
class Block:
    """보고서 본문 블록. kind 에 따라 text 또는 items 사용."""

    kind: str  # "paragraph" | "bullets" | "summary"
    text: str = ""
    items: list[str] = field(default_factory=list)


@dataclass
class Section:
    heading: str
    blocks: list[Block] = field(default_factory=list)


@dataclass
class ReportModel:
    """포맷 독립 보고서 IR. 렌더러가 이걸 받아 PDF/Word/PPT 로 그린다."""

    title: str
    report_type: str
    date: str
    sections: list[Section] = field(default_factory=list)
```

- [ ] **Step 5: 리더 구현**

`vibelign/core/reporting_cli/reader.py`:
```python
from __future__ import annotations

from vibelign.core.reporting_cli.models import PlanningData

_FIELD_BY_HEADING = {
    "한 줄 목표": "idea",
    "대상 사용자": "target_users",
    "핵심 문제": "problem",
    "핵심 기능": "features",
    "화면 또는 사용 흐름": "flows",
    "사용자 흐름": "flows",
    "확정된 결정": "decisions",
    "제외할 것": "exclusions",
    "아직 결정이 필요한 질문": "open_questions",
    "구현 전에 AI가 알아야 할 맥락": "context_notes",
}

_LIST_FIELDS = {"features", "flows", "decisions", "exclusions", "open_questions"}


def _strip_placeholder(value: str) -> str:
    return "" if "아직 결정이 필요" in value else value


def _strip_bullet(line: str) -> str:
    stripped = line.strip()
    if stripped.startswith("- ") or stripped.startswith("* "):
        return stripped[2:].strip()
    return ""


def parse_plan_markdown(text: str) -> PlanningData:
    data = PlanningData()
    current: str | None = None
    buf: list[str] = []

    def flush() -> None:
        nonlocal buf
        if current and current not in _LIST_FIELDS:
            joined = " ".join(b.strip() for b in buf if b.strip())
            value = _strip_placeholder(joined)
            if value:
                setattr(data, current, value)
        buf = []

    for raw_line in text.splitlines():
        line = raw_line.rstrip()
        if line.startswith("# ") and not line.startswith("## "):
            flush()
            current = None
            data.title = line[2:].strip()
            continue
        if line.startswith("## "):
            flush()
            current = _FIELD_BY_HEADING.get(line[3:].strip())
            continue
        if current is None:
            continue
        if current in _LIST_FIELDS:
            item = _strip_placeholder(_strip_bullet(line))
            if item:
                getattr(data, current).append(item)
        else:
            buf.append(line)

    flush()
    return data
```

- [ ] **Step 6: `__init__.py` 작성 (부분 — 이후 Task 에서 확장)**

`vibelign/core/reporting_cli/__init__.py`:
```python
from vibelign.core.reporting_cli.models import (
    Block,
    PlanningData,
    ReportModel,
    Section,
)
from vibelign.core.reporting_cli.reader import parse_plan_markdown

__all__ = [
    "Block",
    "PlanningData",
    "ReportModel",
    "Section",
    "parse_plan_markdown",
]
```

- [ ] **Step 7: 테스트 통과 확인**

Run: `cd /Users/usabatch/coding/VibeLign && python -m pytest tests/core/reporting_cli/test_reader.py -v`
Expected: PASS (4 passed)

- [ ] **Step 8: 커밋**

```bash
git add vibelign/core/reporting_cli/__init__.py vibelign/core/reporting_cli/models.py vibelign/core/reporting_cli/reader.py tests/core/reporting_cli/
git commit -m "feat(reporting): 기획안 .md 파서 + 보고서 IR 모델"
```

---

## Task 2: 보고서 템플릿 (`templates.py`)

**Files:**
- Create: `vibelign/core/reporting_cli/templates.py`
- Modify: `vibelign/core/reporting_cli/__init__.py`
- Test: `tests/core/reporting_cli/test_templates.py`

- [ ] **Step 1: 실패하는 템플릿 테스트 작성**

`tests/core/reporting_cli/test_templates.py`:
```python
import pytest

from vibelign.core.reporting_cli.models import PlanningData
from vibelign.core.reporting_cli.templates import REPORT_TEMPLATES, build_report_model


def _sample() -> PlanningData:
    return PlanningData(
        title="예약 앱",
        idea="미용실 예약 앱",
        target_users="미용실 사장님",
        features=["예약 캘린더", "알림 문자"],
        flows=["손님 선택", "사장 확정"],
        decisions=["MVP 는 캘린더만"],
        open_questions=["노쇼 정책?"],
    )


def test_all_three_types_registered():
    assert set(REPORT_TEMPLATES) == {"work", "proposal", "result"}


def test_build_uses_title_and_date():
    model = build_report_model(_sample(), "work", date="2026-06-15")
    assert model.report_type == "work"
    assert model.title == "예약 앱"
    assert model.date == "2026-06-15"


def test_build_skips_empty_sources():
    data = PlanningData(idea="아이디어만 있음")
    model = build_report_model(data, "work", date="2026-06-15")
    headings = [s.heading for s in model.sections]
    # features/flows 가 비었으므로 그 섹션은 빠진다
    assert "개요" in headings
    assert "핵심 내용" not in headings


def test_build_bullets_block_for_list_source():
    model = build_report_model(_sample(), "work", date="2026-06-15")
    section = next(s for s in model.sections if s.heading == "핵심 내용")
    assert section.blocks[0].kind == "bullets"
    assert section.blocks[0].items == ["예약 캘린더", "알림 문자"]


def test_build_unknown_type_raises():
    with pytest.raises(ValueError):
        build_report_model(_sample(), "nope", date="2026-06-15")
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd /Users/usabatch/coding/VibeLign && python -m pytest tests/core/reporting_cli/test_templates.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'vibelign.core.reporting_cli.templates'`

- [ ] **Step 3: 템플릿 구현**

`vibelign/core/reporting_cli/templates.py`:
```python
from __future__ import annotations

from dataclasses import dataclass

from vibelign.core.reporting_cli.models import (
    Block,
    PlanningData,
    ReportModel,
    Section,
)


@dataclass(frozen=True)
class SectionSpec:
    heading: str  # 보고서에 보일 제목
    source: str  # PlanningData 필드명
    style: str  # "paragraph" | "bullets" | "summary"


REPORT_TEMPLATES: dict[str, list[SectionSpec]] = {
    "work": [
        SectionSpec("개요", "idea", "summary"),
        SectionSpec("대상 / 배경", "target_users", "paragraph"),
        SectionSpec("핵심 내용", "features", "bullets"),
        SectionSpec("진행 / 사용 흐름", "flows", "bullets"),
        SectionSpec("주요 결정", "decisions", "bullets"),
        SectionSpec("이슈 / 남은 질문", "open_questions", "bullets"),
    ],
    "proposal": [
        SectionSpec("제안 요약", "idea", "summary"),
        SectionSpec("배경 / 문제", "problem", "paragraph"),
        SectionSpec("대상", "target_users", "paragraph"),
        SectionSpec("핵심 기능 / 가치", "features", "bullets"),
        SectionSpec("사용 흐름", "flows", "bullets"),
        SectionSpec("기대 효과 / 결정 사항", "decisions", "bullets"),
        SectionSpec("범위 제외", "exclusions", "bullets"),
    ],
    "result": [
        SectionSpec("목표", "idea", "summary"),
        SectionSpec("수행 내용", "features", "bullets"),
        SectionSpec("진행 흐름", "flows", "bullets"),
        SectionSpec("주요 결정", "decisions", "bullets"),
        SectionSpec("제외 / 미결", "open_questions", "bullets"),
        SectionSpec("참고 맥락", "context_notes", "paragraph"),
    ],
}

REPORT_TYPE_LABELS: dict[str, str] = {
    "work": "업무 보고",
    "proposal": "제안서",
    "result": "결과 보고",
}


def _blocks_for(value: object, style: str) -> list[Block]:
    if isinstance(value, list):
        items = [v for v in value if v]
        return [Block(kind="bullets", items=items)] if items else []
    text = str(value or "").strip()
    if not text:
        return []
    kind = "summary" if style == "summary" else "paragraph"
    return [Block(kind=kind, text=text)]


def build_report_model(
    data: PlanningData, report_type: str, *, date: str
) -> ReportModel:
    specs = REPORT_TEMPLATES.get(report_type)
    if specs is None:
        raise ValueError(f"unknown report type: {report_type}")
    sections: list[Section] = []
    for spec in specs:
        blocks = _blocks_for(getattr(data, spec.source), spec.style)
        if blocks:
            sections.append(Section(heading=spec.heading, blocks=blocks))
    title = data.title or data.idea or "보고서"
    return ReportModel(
        title=title, report_type=report_type, date=date, sections=sections
    )
```

- [ ] **Step 4: `__init__.py` 확장**

`vibelign/core/reporting_cli/__init__.py` 의 import/`__all__` 에 추가:
```python
from vibelign.core.reporting_cli.templates import (
    REPORT_TEMPLATES,
    REPORT_TYPE_LABELS,
    build_report_model,
)
```
그리고 `__all__` 리스트에 `"REPORT_TEMPLATES"`, `"REPORT_TYPE_LABELS"`, `"build_report_model"` 추가.

- [ ] **Step 5: 테스트 통과 확인**

Run: `cd /Users/usabatch/coding/VibeLign && python -m pytest tests/core/reporting_cli/test_templates.py -v`
Expected: PASS (5 passed)

- [ ] **Step 6: 커밋**

```bash
git add vibelign/core/reporting_cli/templates.py vibelign/core/reporting_cli/__init__.py tests/core/reporting_cli/test_templates.py
git commit -m "feat(reporting): 보고서 종류 3종 템플릿 → ReportModel 빌더"
```

---

## Task 3: HTML 렌더러 (`html_renderer.py`)

**Files:**
- Create: `vibelign/core/reporting_cli/html_renderer.py`
- Modify: `vibelign/core/reporting_cli/__init__.py`
- Test: `tests/core/reporting_cli/test_html_renderer.py`

- [ ] **Step 1: 실패하는 렌더러 테스트 작성**

`tests/core/reporting_cli/test_html_renderer.py`:
```python
from vibelign.core.reporting_cli.html_renderer import render_html
from vibelign.core.reporting_cli.models import Block, ReportModel, Section


def _model() -> ReportModel:
    return ReportModel(
        title="예약 앱 <보고>",
        report_type="work",
        date="2026-06-15",
        sections=[
            Section("개요", [Block(kind="summary", text="미용실 예약 앱")]),
            Section(
                "핵심 내용",
                [Block(kind="bullets", items=["예약 캘린더", "알림 문자"])],
            ),
        ],
    )


def test_render_includes_title_and_type_label():
    html = render_html(_model())
    assert "예약 앱 &lt;보고&gt;" in html  # HTML 이스케이프됨
    assert "업무 보고" in html
    assert "2026-06-15" in html


def test_render_bullets_as_list_items():
    html = render_html(_model())
    assert "<li>예약 캘린더</li>" in html
    assert "<li>알림 문자</li>" in html


def test_render_is_full_document_with_print_css():
    html = render_html(_model())
    assert html.lstrip().startswith("<!DOCTYPE html>")
    assert "@media print" in html
    assert "</html>" in html.rstrip()
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd /Users/usabatch/coding/VibeLign && python -m pytest tests/core/reporting_cli/test_html_renderer.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'vibelign.core.reporting_cli.html_renderer'`

- [ ] **Step 3: 렌더러 구현**

`vibelign/core/reporting_cli/html_renderer.py`:
```python
from __future__ import annotations

from html import escape

from vibelign.core.reporting_cli.models import Block, ReportModel, Section
from vibelign.core.reporting_cli.templates import REPORT_TYPE_LABELS

_HEAD = """<!DOCTYPE html>
<html lang="ko">
<head>
<meta charset="utf-8">
<title>{title}</title>
<style>
  :root {{ --ink: #1A1A1A; --paper: #F7F7F2; --accent: #9B1B1B; }}
  * {{ box-sizing: border-box; }}
  body {{
    font-family: "Noto Serif KR", "Apple SD Gothic Neo", serif;
    color: var(--ink); background: var(--paper);
    max-width: 760px; margin: 0 auto; padding: 48px 40px; line-height: 1.7;
  }}
  h1 {{ font-size: 26px; border-bottom: 3px solid var(--accent); padding-bottom: 10px; }}
  h2 {{ font-size: 17px; color: var(--accent); margin-top: 28px; }}
  p.meta {{ color: #666; font-size: 13px; margin-top: 4px; }}
  p.summary {{ font-weight: 700; font-size: 16px; }}
  ul {{ padding-left: 20px; }}
  li {{ margin: 4px 0; }}
  @media print {{
    body {{ background: #fff; max-width: none; padding: 0; }}
    h2 {{ break-after: avoid; }}
    section {{ break-inside: avoid; }}
  }}
</style>
</head>
<body>"""

_TAIL = "</body>\n</html>"


def _render_block(block: Block) -> str:
    if block.kind == "bullets":
        items = "".join(f"<li>{escape(item)}</li>" for item in block.items)
        return f"<ul>{items}</ul>"
    if block.kind == "summary":
        return f'<p class="summary">{escape(block.text)}</p>'
    return f"<p>{escape(block.text)}</p>"


def _render_section(section: Section) -> str:
    blocks = "\n".join(_render_block(b) for b in section.blocks)
    return f"<section>\n<h2>{escape(section.heading)}</h2>\n{blocks}\n</section>"


def render_html(model: ReportModel) -> str:
    label = REPORT_TYPE_LABELS.get(model.report_type, model.report_type)
    parts = [
        _HEAD.format(title=escape(model.title)),
        f"<h1>{escape(model.title)}</h1>",
        f'<p class="meta">{escape(label)} · {escape(model.date)}</p>',
    ]
    parts.extend(_render_section(s) for s in model.sections)
    parts.append(_TAIL)
    return "\n".join(parts)
```

- [ ] **Step 4: `__init__.py` 확장**

import 에 `from vibelign.core.reporting_cli.html_renderer import render_html` 추가, `__all__` 에 `"render_html"` 추가.

- [ ] **Step 5: 테스트 통과 확인**

Run: `cd /Users/usabatch/coding/VibeLign && python -m pytest tests/core/reporting_cli/test_html_renderer.py -v`
Expected: PASS (3 passed)

- [ ] **Step 6: 커밋**

```bash
git add vibelign/core/reporting_cli/html_renderer.py vibelign/core/reporting_cli/__init__.py tests/core/reporting_cli/test_html_renderer.py
git commit -m "feat(reporting): ReportModel → HTML 렌더러 (인쇄 CSS 포함)"
```

---

## Task 4: 파일 저장 (`storage.py`)

**Files:**
- Create: `vibelign/core/reporting_cli/storage.py`
- Modify: `vibelign/core/reporting_cli/__init__.py`
- Test: `tests/core/reporting_cli/test_storage.py`

- [ ] **Step 1: 실패하는 저장 테스트 작성**

`tests/core/reporting_cli/test_storage.py`:
```python
from pathlib import Path

from vibelign.core.reporting_cli.models import ReportModel
from vibelign.core.reporting_cli.storage import write_report


def _model() -> ReportModel:
    return ReportModel(title="예약 앱", report_type="work", date="2026-06-15")


def test_write_default_path_under_reports_dir(tmp_path: Path):
    dest = write_report(tmp_path, _model(), "<html></html>", slug_source="예약 앱")
    assert dest.parent == tmp_path / ".vibelign" / "reports"
    assert dest.suffix == ".html"
    assert "work" in dest.name
    assert dest.read_text(encoding="utf-8") == "<html></html>"


def test_write_explicit_relative_output(tmp_path: Path):
    dest = write_report(
        tmp_path, _model(), "<x>", slug_source="x", output="out/report.html"
    )
    assert dest == tmp_path / "out" / "report.html"
    assert dest.read_text(encoding="utf-8") == "<x>"


def test_write_creates_parent_dirs(tmp_path: Path):
    dest = write_report(tmp_path, _model(), "<x>", slug_source="예약 앱")
    assert dest.exists()
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd /Users/usabatch/coding/VibeLign && python -m pytest tests/core/reporting_cli/test_storage.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'vibelign.core.reporting_cli.storage'`

- [ ] **Step 3: 저장 구현**

`vibelign/core/reporting_cli/storage.py`:
```python
from __future__ import annotations

from pathlib import Path

from vibelign.core.meta_paths import MetaPaths
from vibelign.core.planning_cli.storage import safe_plan_slug
from vibelign.core.reporting_cli.models import ReportModel


def write_report(
    root: Path,
    model: ReportModel,
    html: str,
    *,
    slug_source: str,
    output: str | None = None,
) -> Path:
    """보고서 HTML 을 디스크에 쓰고 최종 경로를 반환한다.

    output 이 주어지면 그 경로(절대/상대)를 쓰고, 없으면
    .vibelign/reports/{slug}-{report_type}.html 로 저장한다.
    """
    if output:
        dest = Path(output)
        if not dest.is_absolute():
            dest = root / dest
    else:
        slug = safe_plan_slug(slug_source)
        dest = MetaPaths(root).reports_dir / f"{slug}-{model.report_type}.html"
    dest.parent.mkdir(parents=True, exist_ok=True)
    dest.write_text(html, encoding="utf-8")
    return dest
```

- [ ] **Step 4: `__init__.py` 확장**

import 에 `from vibelign.core.reporting_cli.storage import write_report` 추가, `__all__` 에 `"write_report"` 추가.

- [ ] **Step 5: 테스트 통과 확인**

Run: `cd /Users/usabatch/coding/VibeLign && python -m pytest tests/core/reporting_cli/test_storage.py -v`
Expected: PASS (3 passed)

- [ ] **Step 6: 커밋**

```bash
git add vibelign/core/reporting_cli/storage.py vibelign/core/reporting_cli/__init__.py tests/core/reporting_cli/test_storage.py
git commit -m "feat(reporting): 보고서 파일 저장 (.vibelign/reports)"
```

---

## Task 5: CLI 커맨드 (`vib_report_cmd.py`) + 등록

**Files:**
- Create: `vibelign/commands/vib_report_cmd.py`
- Modify: `vibelign/cli/cli_command_groups.py` (line 682 `plan` 등록 뒤)
- Test: `tests/cli/test_vib_report_cmd.py`

- [ ] **Step 1: 실패하는 커맨드 테스트 작성**

`tests/cli/test_vib_report_cmd.py`:
```python
import json
from argparse import Namespace
from pathlib import Path

import pytest

from vibelign.commands.vib_report_cmd import run_vib_report

PLAN_MD = """# 예약 앱

## 한 줄 목표
미용실 예약 앱.

## 핵심 기능
- 예약 캘린더
- 알림 문자
"""


def _args(plan_path: Path, **over) -> Namespace:
    base = dict(
        plan=str(plan_path),
        type="work",
        format="html",
        output=None,
        date="2026-06-15",
        json=False,
    )
    base.update(over)
    return Namespace(**base)


def test_report_writes_html_and_json_output(
    tmp_path: Path, capsys: pytest.CaptureFixture[str], monkeypatch: pytest.MonkeyPatch
):
    monkeypatch.chdir(tmp_path)
    plan = tmp_path / "plan.md"
    plan.write_text(PLAN_MD, encoding="utf-8")

    run_vib_report(_args(plan, json=True))

    payload = json.loads(capsys.readouterr().out)
    assert payload["ok"] is True
    assert payload["report_type"] == "work"
    out_path = Path(payload["path"])
    assert out_path.exists()
    html = out_path.read_text(encoding="utf-8")
    assert "업무 보고" in html
    assert "<li>예약 캘린더</li>" in html


def test_report_unknown_type_reports_error_json(
    tmp_path: Path, capsys: pytest.CaptureFixture[str], monkeypatch: pytest.MonkeyPatch
):
    monkeypatch.chdir(tmp_path)
    plan = tmp_path / "plan.md"
    plan.write_text(PLAN_MD, encoding="utf-8")

    with pytest.raises(SystemExit):
        run_vib_report(_args(plan, type="nope", json=True))

    payload = json.loads(capsys.readouterr().out)
    assert payload["ok"] is False
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd /Users/usabatch/coding/VibeLign && python -m pytest tests/cli/test_vib_report_cmd.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'vibelign.commands.vib_report_cmd'`

- [ ] **Step 3: 커맨드 구현**

`vibelign/commands/vib_report_cmd.py`:
```python
from __future__ import annotations

import json
import sys
from datetime import date as _date
from pathlib import Path
from typing import Protocol, cast

from vibelign.core.project_root import resolve_project_root
from vibelign.core.reporting_cli import (
    build_report_model,
    parse_plan_markdown,
    render_html,
    write_report,
)
from vibelign.terminal_render import clack_intro, clack_success


class ReportArgs(Protocol):
    plan: str
    type: str
    format: str
    output: str | None
    date: str | None
    json: bool


def run_vib_report(args: object) -> None:
    raw = cast(ReportArgs, args)
    want_json = bool(raw.json)
    report_date = raw.date or _date.today().isoformat()

    plan_path = Path(raw.plan).expanduser()
    if not plan_path.exists():
        _fail(want_json, f"기획안 파일을 찾을 수 없어요: {plan_path}")
        return

    root = resolve_project_root(Path.cwd())
    data = parse_plan_markdown(plan_path.read_text(encoding="utf-8"))

    try:
        model = build_report_model(data, raw.type, date=report_date)
    except ValueError as exc:
        _fail(want_json, str(exc))
        return

    html = render_html(model)
    slug_source = data.title or data.idea or plan_path.stem
    dest = write_report(root, model, html, slug_source=slug_source, output=raw.output)

    if want_json:
        print(
            json.dumps(
                {"ok": True, "path": str(dest), "report_type": model.report_type},
                ensure_ascii=False,
            )
        )
    else:
        clack_intro("VibeLign 보고서")
        clack_success(f"보고서 저장: {dest}")


def _fail(want_json: bool, message: str) -> None:
    if want_json:
        print(json.dumps({"ok": False, "error": message}, ensure_ascii=False))
    else:
        print(message, file=sys.stderr)
    raise SystemExit(1)
```

- [ ] **Step 4: 커맨드 테스트 통과 확인**

Run: `cd /Users/usabatch/coding/VibeLign && python -m pytest tests/cli/test_vib_report_cmd.py -v`
Expected: PASS (2 passed)

- [ ] **Step 5: CLI 서브파서 등록**

`vibelign/cli/cli_command_groups.py` 에서 `plan` 커맨드 등록 블록(`p.set_defaults(func=lazy_command("vibelign.commands.vib_plan_cmd", "run_vib_plan"))`, line 682) **바로 뒤**에 추가:

```python
    r = sub.add_parser(
        "report",
        help="기획안을 보고서로 내보내기",
        description="기획안 마크다운을 업무/제안/결과 보고서 HTML 로 변환해요.",
        epilog=(
            "이렇게 쓰세요:\n"
            '  vib report plans/예약-앱.md --type work\n'
            '  vib report plans/예약-앱.md --type proposal --json'
        ),
    )
    _ = r.add_argument("plan", help="기획안 .md 경로")
    _ = r.add_argument(
        "--type",
        default="work",
        choices=["work", "proposal", "result"],
        help="보고서 종류 (work=업무, proposal=제안, result=결과)",
    )
    _ = r.add_argument(
        "--format", default="html", choices=["html"], help="출력 포맷"
    )
    _ = r.add_argument("--output", default=None, help="저장 경로 (기본: .vibelign/reports/)")
    _ = r.add_argument("--date", default=None, help="보고서 날짜 (기본: 오늘)")
    _ = r.add_argument("--json", action="store_true", help="JSON 으로 결과 출력")
    r.set_defaults(func=lazy_command("vibelign.commands.vib_report_cmd", "run_vib_report"))
```

- [ ] **Step 6: 등록 통합 테스트 추가**

`tests/cli/test_vib_report_cmd.py` 끝에 추가:
```python
def test_report_subcommand_is_registered():
    from vibelign.cli.vib_cli import build_parser

    parser = build_parser()
    ns = parser.parse_args(["report", "plan.md", "--type", "proposal"])
    assert ns.type == "proposal"
    assert callable(ns.func)
```

- [ ] **Step 7: 전체 reporting 테스트 통과 확인**

Run: `cd /Users/usabatch/coding/VibeLign && python -m pytest tests/cli/test_vib_report_cmd.py tests/core/reporting_cli/ -v`
Expected: PASS (전체 통과)

- [ ] **Step 8: 커밋**

```bash
git add vibelign/commands/vib_report_cmd.py vibelign/cli/cli_command_groups.py tests/cli/test_vib_report_cmd.py
git commit -m "feat(reporting): vib report CLI 커맨드 + 서브파서 등록"
```

---

## Task 6: 수동 end-to-end 검증

**Files:** (없음 — 실제 실행 확인)

- [ ] **Step 1: 실제 기획안으로 보고서 생성**

Run:
```bash
cd /Users/usabatch/coding/VibeLign && \
printf '# 테스트 보고서\n\n## 한 줄 목표\n예약 앱을 만들고 싶어요.\n\n## 핵심 기능\n- 예약 캘린더\n- 알림 문자\n' > /tmp/plan.md && \
python -m vibelign report /tmp/plan.md --type work --json
```
Expected: `{"ok": true, "path": ".../.vibelign/reports/...-work.html", "report_type": "work"}`

- [ ] **Step 2: 생성된 HTML 육안 확인**

Run: `open "$(python -m vibelign report /tmp/plan.md --type proposal --json | python -c 'import json,sys;print(json.load(sys.stdin)["path"])')"`
Expected: 브라우저에서 "제안서" 라벨 + 섹션이 보이는 HTML 보고서가 열림. 브라우저 인쇄(Cmd+P) 시 깔끔한 PDF 미리보기.

- [ ] **Step 3: 전체 테스트 스위트 회귀 확인**

Run: `cd /Users/usabatch/coding/VibeLign && python -m pytest tests/core/reporting_cli/ tests/cli/test_vib_report_cmd.py -q`
Expected: 전체 PASS, 기존 테스트 영향 없음.

---

## Self-Review 결과

- **Spec 커버리지:** §3 파이프라인(reader/templates/renderer/storage) ✓, §4 데이터 모델 ✓, §5 종류 3종 템플릿 ✓, §7 PDF=HTML(웹뷰 인쇄 입력) ✓. §6 AI 다듬기·§7 Word/PPT·§8 UI 는 의도적으로 후속 계획(2/3/4)으로 분리 — 이 계획 범위 밖임을 명시.
- **플레이스홀더:** 없음. 모든 스텝에 실제 코드/명령/기대출력 포함.
- **타입 일관성:** `PlanningData`/`ReportModel`/`Section`/`Block`/`SectionSpec` 명칭과 필드가 Task 1~5 전반에서 일치. 함수명 `parse_plan_markdown`/`build_report_model`/`render_html`/`write_report`/`run_vib_report` 일관.

## 후속 계획 (이 계획 완료 후)

- **계획 2:** GUI 연동 — `src/lib/vib/planning.ts` invoke 래퍼(`generatePlanningReport`) + Rust command(`vib report` sidecar 호출, `capture_with_stdin_timeout` 300s) + `PlanDocView` "📄 보고서로 내보내기" 버튼/모달(`GuardResultModal` 패턴) + 웹뷰 `printToPDF`.
- **계획 3:** Word/PPT 렌더러 — `python-docx`/`python-pptx` 를 vib-runtime 번들에 추가, `--format docx|pptx`, `Block` 에 `table`/`keyvalue`/`slide_break` 추가.
- **계획 4:** 하이브리드 AI 다듬기 — 무료 provider 체인으로 요약/어조, `enrich.json` 패턴 캐시, 실패 시 템플릿 fallback, `--polish` 토글.
