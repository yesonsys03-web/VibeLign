# 기획안 → 보고서 Word/PPT 렌더러 Implementation Plan — 계획 3a/4

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans. Steps use checkbox (`- [ ]`).

**Goal:** `vib report --format docx|pptx` 로 보고서를 **Word(.docx)·PowerPoint(.pptx)** 로 내보낸다. IR(ReportModel)을 입력으로 python-docx/pptx 가 바이너리 파일을 생성한다. 라이브러리 미설치 시 graceful 비활성(§9).

**Architecture:** 증분 1 의 `ReportModel`(Section/Block)을 입력으로, 새 `docx_renderer`/`pptx_renderer` 가 **bytes** 를 반환한다(python-docx/pptx, lazy import — 미설치면 명확한 에러). `storage` 에 바이너리 쓰기 경로를 추가하고, `vib_report_cmd` 가 `--format` 으로 html/docx/pptx 를 라우팅한다. Block kind 는 MVP 에서 기존 paragraph/bullets/summary 만 다룬다(table/keyvalue/slide_break 은 후속). pptx 는 **Section 당 슬라이드 1장**.

**Tech Stack:** Python 3, pytest, python-docx, python-pptx (둘 다 **optional extra** `vibelign[report]`). 네이티브 전이의존: lxml/Pillow.

**상위 설계:** `docs/superpowers/specs/2026-06-15-기획안-보고서-내보내기-design.md` §7(렌더러 표)·§9(sidecar 미가용 graceful). **번들 통합(§13.3: vib-runtime 에 휠 추가 + integrity manifest 재생성)은 이 계획 밖 — 계획 3b.** 이 계획은 dev venv/pip 환경에서 동작·테스트한다.

**실행 브랜치:** `feat/report-export-office` (base = `feat/report-export` = 증분 1 CLI). GUI/PDF/AI 와 독립.

**범위(3a):** docx·pptx 렌더러 + `--format` 라우팅 + 바이너리 저장 + optional extra. 미설치 graceful. **MVP block kind = paragraph/bullets/summary**(table/keyvalue/slide_break, GUI 노출, 번들은 후속).

---

## 파일 구조
신규:
| 파일 | 책임 |
|---|---|
| `vibelign/core/reporting_cli/docx_renderer.py` | `render_docx(model) -> bytes` |
| `vibelign/core/reporting_cli/pptx_renderer.py` | `render_pptx(model) -> bytes` |
| `tests/core/reporting_cli/test_docx_renderer.py` | 생성·재오픈 검증 + 미설치 graceful |
| `tests/core/reporting_cli/test_pptx_renderer.py` | 〃 |

수정:
| 파일 | 변경 |
|---|---|
| `vibelign/core/reporting_cli/storage.py` | dest 해석 공유 추출 + `write_report_bytes` |
| `vibelign/commands/vib_report_cmd.py` | `--format` 라우팅(html/docx/pptx) + graceful |
| `vibelign/cli/cli_command_groups.py` | `--format` choices 에 docx/pptx |
| `vibelign/core/reporting_cli/__init__.py` | 신규 export |
| `pyproject.toml` | `[project.optional-dependencies] report` |

---

## Task 1: storage 바이너리 쓰기

**Files:** Modify `vibelign/core/reporting_cli/storage.py`; Test `tests/core/reporting_cli/test_storage.py`(확장).

기존 `write_report(...html: str...)` 는 텍스트만 쓴다(`write_text`, `.html` 고정). dest 해석 로직을 공유 헬퍼로 빼고, bytes 용 함수를 추가한다.

- [ ] **Step 1: 실패 테스트 추가** — `test_storage.py` 끝에:
```python
def test_write_report_bytes_default_path_docx(tmp_path):
    from vibelign.core.reporting_cli.storage import write_report_bytes
    m = ReportModel(title="예약 앱", report_type="work", date="2026-06-16")
    dest = write_report_bytes(tmp_path, m, b"PK\x03\x04docx", slug_source="예약 앱", ext=".docx")
    assert dest.suffix == ".docx"
    assert dest.parent == tmp_path / ".vibelign" / "reports"
    assert dest.read_bytes() == b"PK\x03\x04docx"


def test_write_report_bytes_rejects_escape(tmp_path):
    from vibelign.core.reporting_cli.storage import write_report_bytes
    m = ReportModel(title="t", report_type="work", date="2026-06-16")
    import pytest
    with pytest.raises(ValueError, match="project-relative"):
        write_report_bytes(tmp_path, m, b"x", slug_source="x", ext=".pptx", output="../x.pptx")
```

- [ ] **Step 2: 실패 확인** — `python -m pytest tests/core/reporting_cli/test_storage.py -q` → FAIL(write_report_bytes 없음).

- [ ] **Step 3: 구현** — `storage.py`: dest 해석을 `_resolve_report_dest(root, model, *, slug_source, ext, output, force) -> Path` 로 추출(기존 write_report 의 71-90 로직을 ext 파라미터화: 기본경로 `f"{slug}-{model.report_type}{ext}"`, symlink containment·force·`..` 가드 동일). `write_report` 는 이 헬퍼(ext=".html")로 dest 구한 뒤 `write_text`. 신규:
```python
def write_report_bytes(
    root: Path, model: ReportModel, data: bytes, *,
    slug_source: str, ext: str, output: str | None = None, force: bool = False,
) -> Path:
    dest = _resolve_report_dest(root, model, slug_source=slug_source, ext=ext, output=output, force=force)
    dest.parent.mkdir(parents=True, exist_ok=True)
    dest.write_bytes(data)
    return dest
```
> 주의: `_relative_output_path`/`_unique_output_path`/symlink resolve 가드(86-87)·`.pdf`/`.html` suffix 제약 등 기존 보안 로직을 `_resolve_report_dest` 안에 그대로 보존. `output` 의 확장자 검증은 ext 와 무관하게 `_relative_output_path` 가 담당(절대/`..`/symlink 거부).

- [ ] **Step 4: 통과 확인** — `python -m pytest tests/core/reporting_cli/test_storage.py -q` → 전체 PASS(기존 + 신규 2). **Step 5: 커밋** `feat(reporting): storage 바이너리 쓰기(write_report_bytes)`.

---

## Task 2: docx_renderer.py

**Files:** Create `docx_renderer.py`; Test `test_docx_renderer.py`.

- [ ] **Step 1: 실패 테스트**
```python
import io
import pytest
from vibelign.core.reporting_cli.docx_renderer import render_docx, DOCX_AVAILABLE
from vibelign.core.reporting_cli.models import Block, ReportModel, Section


def _model():
    return ReportModel(
        title="예약 앱", report_type="work", date="2026-06-16",
        sections=[
            Section("개요", [Block(kind="summary", text="미용실 예약 앱")]),
            Section("핵심 내용", [Block(kind="bullets", items=["예약 캘린더", "알림 문자"])]),
            Section("배경", [Block(kind="paragraph", text="전화 예약 누락")]),
        ],
    )


@pytest.mark.skipif(not DOCX_AVAILABLE, reason="python-docx 미설치")
def test_render_docx_opens_and_contains_text():
    data = render_docx(_model())
    assert data[:2] == b"PK"  # docx = zip
    from docx import Document
    doc = Document(io.BytesIO(data))
    texts = "\n".join(p.text for p in doc.paragraphs)
    assert "예약 앱" in texts
    assert "업무 보고" in texts  # report_type 라벨
    assert "예약 캘린더" in texts
    assert "전화 예약 누락" in texts


def test_render_docx_missing_lib_raises_clear(monkeypatch):
    import vibelign.core.reporting_cli.docx_renderer as mod
    monkeypatch.setattr(mod, "DOCX_AVAILABLE", False)
    with pytest.raises(mod.ReportRendererUnavailable):
        render_docx(_model())
```

- [ ] **Step 2: 실패 확인.**

- [ ] **Step 3: 구현** — `docx_renderer.py`:
```python
from __future__ import annotations

import io

from vibelign.core.reporting_cli.models import ReportModel
from vibelign.core.reporting_cli.templates import REPORT_TYPE_LABELS

try:
    import docx  # noqa: F401
    DOCX_AVAILABLE = True
except ImportError:
    DOCX_AVAILABLE = False


class ReportRendererUnavailable(RuntimeError):
    """렌더러 라이브러리(python-docx/pptx) 미설치."""


def render_docx(model: ReportModel) -> bytes:
    if not DOCX_AVAILABLE:
        raise ReportRendererUnavailable(
            "Word 내보내기에 python-docx 가 필요합니다. (pip install python-docx)"
        )
    from docx import Document

    label = REPORT_TYPE_LABELS.get(model.report_type, model.report_type)
    doc = Document()
    doc.add_heading(model.title, level=0)
    doc.add_paragraph(f"{label} · {model.date}")
    for section in model.sections:
        doc.add_heading(section.heading, level=1)
        for block in section.blocks:
            if block.kind == "bullets":
                for item in block.items:
                    doc.add_paragraph(item, style="List Bullet")
            elif block.kind == "summary":
                p = doc.add_paragraph()
                run = p.add_run(block.text)
                run.bold = True
            else:  # paragraph
                doc.add_paragraph(block.text)
    buf = io.BytesIO()
    doc.save(buf)
    return buf.getvalue()
```
> python-docx API 세부(`add_heading`/`add_paragraph`/style "List Bullet")는 컴파일/런타임 에러를 보며 맞춘다(이 라이브러리는 안정적이라 위 코드가 대개 그대로 동작).

- [ ] **Step 4: 통과 확인.** **Step 5: 커밋** `feat(reporting): Word(.docx) 렌더러`.

---

## Task 3: pptx_renderer.py

**Files:** Create `pptx_renderer.py`; Test `test_pptx_renderer.py`.

- [ ] **Step 1: 실패 테스트**
```python
import io
import pytest
from vibelign.core.reporting_cli.pptx_renderer import render_pptx, PPTX_AVAILABLE
from vibelign.core.reporting_cli.models import Block, ReportModel, Section


def _model():
    return ReportModel(
        title="예약 앱", report_type="work", date="2026-06-16",
        sections=[
            Section("개요", [Block(kind="summary", text="미용실 예약 앱")]),
            Section("핵심 내용", [Block(kind="bullets", items=["예약 캘린더", "알림 문자"])]),
        ],
    )


@pytest.mark.skipif(not PPTX_AVAILABLE, reason="python-pptx 미설치")
def test_render_pptx_opens_with_slide_per_section():
    data = render_pptx(_model())
    assert data[:2] == b"PK"
    from pptx import Presentation
    prs = Presentation(io.BytesIO(data))
    # 제목 슬라이드 1 + 섹션 2 = 3
    assert len(prs.slides) == 3
    all_text = []
    for slide in prs.slides:
        for shape in slide.shapes:
            if shape.has_text_frame:
                all_text.append(shape.text_frame.text)
    blob = "\n".join(all_text)
    assert "예약 앱" in blob
    assert "예약 캘린더" in blob


def test_render_pptx_missing_lib_raises_clear(monkeypatch):
    import vibelign.core.reporting_cli.pptx_renderer as mod
    monkeypatch.setattr(mod, "PPTX_AVAILABLE", False)
    with pytest.raises(mod.ReportRendererUnavailable):
        render_pptx(_model())
```

- [ ] **Step 2: 실패 확인.**

- [ ] **Step 3: 구현** — `pptx_renderer.py`:
```python
from __future__ import annotations

import io

from vibelign.core.reporting_cli.docx_renderer import ReportRendererUnavailable
from vibelign.core.reporting_cli.models import ReportModel
from vibelign.core.reporting_cli.templates import REPORT_TYPE_LABELS

try:
    import pptx  # noqa: F401
    PPTX_AVAILABLE = True
except ImportError:
    PPTX_AVAILABLE = False


def _section_text(section) -> str:
    lines: list[str] = []
    for block in section.blocks:
        if block.kind == "bullets":
            lines.extend(block.items)
        elif block.text:
            lines.append(block.text)
    return "\n".join(lines)


def render_pptx(model: ReportModel) -> bytes:
    if not PPTX_AVAILABLE:
        raise ReportRendererUnavailable(
            "PPT 내보내기에 python-pptx 가 필요합니다. (pip install python-pptx)"
        )
    from pptx import Presentation

    label = REPORT_TYPE_LABELS.get(model.report_type, model.report_type)
    prs = Presentation()
    # 제목 슬라이드
    title_slide = prs.slides.add_slide(prs.slide_layouts[0])
    title_slide.shapes.title.text = model.title
    if len(title_slide.placeholders) > 1:
        title_slide.placeholders[1].text = f"{label} · {model.date}"
    # Section 당 슬라이드 1장 (Title and Content 레이아웃)
    for section in model.sections:
        slide = prs.slides.add_slide(prs.slide_layouts[1])
        slide.shapes.title.text = section.heading
        body = _section_text(section)
        if len(slide.placeholders) > 1 and body:
            slide.placeholders[1].text = body
    buf = io.BytesIO()
    prs.save(buf)
    return buf.getvalue()
```
> 슬라이드 레이아웃 인덱스(0=제목, 1=제목+내용)는 기본 템플릿 기준. placeholders 접근이 환경마다 다를 수 있어 런타임 에러를 보며 맞춘다.

- [ ] **Step 4: 통과 확인.** **Step 5: 커밋** `feat(reporting): PPT(.pptx) 렌더러 (Section 당 슬라이드)`.

---

## Task 4: CLI `--format docx|pptx` 라우팅 + extra

**Files:** Modify `vib_report_cmd.py`, `cli_command_groups.py`, `__init__.py`, `pyproject.toml`; Test `test_vib_report_cmd.py`.

- [ ] **Step 1: 실패 테스트** — `test_vib_report_cmd.py`(`_args` 에 format 기본 유지):
```python
def test_report_docx_writes_binary(tmp_path, capsys, monkeypatch):
    monkeypatch.chdir(tmp_path)
    plan = tmp_path / "plan.md"
    plan.write_text(PLAN_MD, encoding="utf-8")
    run_vib_report(_args(plan, json=True, format="docx"))
    payload = json.loads(capsys.readouterr().out)
    assert payload["ok"] is True
    out = Path(payload["path"])
    assert out.suffix == ".docx"
    assert out.read_bytes()[:2] == b"PK"


def test_report_docx_graceful_when_lib_absent(tmp_path, capsys, monkeypatch):
    monkeypatch.chdir(tmp_path)
    plan = tmp_path / "plan.md"
    plan.write_text(PLAN_MD, encoding="utf-8")
    import vibelign.core.reporting_cli.docx_renderer as dmod
    monkeypatch.setattr(dmod, "DOCX_AVAILABLE", False)
    with pytest.raises(SystemExit):
        run_vib_report(_args(plan, json=True, format="docx"))
    payload = json.loads(capsys.readouterr().out)
    assert payload["ok"] is False
    assert "python-docx" in payload["error"]
```

- [ ] **Step 2: 실패 확인.**

- [ ] **Step 3: 구현 — `vib_report_cmd.py`**: import `render_docx, render_pptx, write_report_bytes, ReportRendererUnavailable`. `build_report_model` 뒤 라우팅:
```python
    fmt = getattr(raw, "format", "html") or "html"
    try:
        if fmt == "docx":
            data = render_docx(model)
            dest = write_report_bytes(root, model, data, slug_source=slug_source, ext=".docx", output=raw.output, force=raw.force)
        elif fmt == "pptx":
            data = render_pptx(model)
            dest = write_report_bytes(root, model, data, slug_source=slug_source, ext=".pptx", output=raw.output, force=raw.force)
        else:  # html
            html = render_html(model)
            dest = write_report(root, model, html, slug_source=slug_source, output=raw.output, force=raw.force)
    except ReportRendererUnavailable as exc:
        _fail(want_json, str(exc)); return
    except (FileExistsError, ValueError) as exc:
        _fail(want_json, str(exc)); return
```
(slug_source 는 이 분기 전에 계산. 기존 html 분기 코드를 위 라우팅으로 대체.)

- [ ] **Step 4: `cli_command_groups.py`** — report 서브파서 `--format` choices `["html"]` → `["html", "docx", "pptx"]`.

- [ ] **Step 5: `__init__.py`** — `render_docx`, `render_pptx`, `write_report_bytes`, `ReportRendererUnavailable` export.

- [ ] **Step 6: `pyproject.toml`** — 추가:
```toml
[project.optional-dependencies]
report = ["python-docx>=1.0", "python-pptx>=1.0"]
```

- [ ] **Step 7: 통과 확인** — `python -m pytest tests/cli/test_vib_report_cmd.py tests/core/reporting_cli/ -q` → 전체 PASS. **Step 8: 커밋** `feat(reporting): vib report --format docx|pptx 라우팅 + report extra`.

---

## Task 5: 통합 검증 + 회귀
- [ ] **Step 1:** `python -m pytest tests/core/reporting_cli/ tests/cli/test_vib_report_cmd.py -q` → PASS.
- [ ] **Step 2:** `ruff check vibelign/core/reporting_cli/ vibelign/commands/vib_report_cmd.py tests/core/reporting_cli/` → clean.
- [ ] **Step 3 (라이브):** `python -m vibelign report /tmp/plan.md --type work --format docx --json` → `.docx` 생성, `file` 이 "Microsoft Word"/zip, `python -m vibelign report ... --format pptx` → `.pptx`. 둘 다 재오픈 가능.
- [ ] **Step 4:** 전체 스위트 회귀 `python -m pytest tests/ -q`.

---

## Self-Review (작성 시점)
- **§7 커버:** Word(python-docx)·PPT(python-pptx, 슬라이드 분할=Section 단위) 렌더러 ✓, `--format` 동등 라우팅 ✓. **§9 graceful:** 미설치 → `{ok:false}` 명확 안내(조용한 실패 없음) ✓ — `DOCX_AVAILABLE`/`PPTX_AVAILABLE` + `ReportRendererUnavailable`.
- **보안:** 바이너리 저장도 `_resolve_report_dest`(symlink resolve + containment + `..` 거부)를 그대로 거친다 — html 과 동일 가드.
- **의존성:** optional extra `vibelign[report]`. 네이티브 전이(lxml/Pillow)는 §13.3 — **번들(3b) 에서 vib-runtime 추가 + integrity manifest 재생성**(이 계획 밖, 명시).
- **MVP 경계:** block kind table/keyvalue/slide_break, GUI `--format` 노출은 후속(design §4.2). pptx 는 Section 당 1슬라이드(slide_break 휴리스틱 후속).
- **플레이스홀더:** 없음. 단 python-docx/pptx API 세부는 "런타임 보며 맞춤" 명시.

## 후속
- 번들 통합(3b): vib-runtime 에 python-docx/pptx + lxml/Pillow 휠(win_amd64 / macOS arm64+x86_64), macOS integrity manifest 재생성, Word/PPT 비활성 조건 GUI 표시.
- block table/keyvalue/slide_break, GUI 모달 `--format` docx/pptx 선택, pptx 슬라이드 분할 휴리스틱.
