# VibeLign — CLI Strategy

Development model:

CLI First → Engine Stable → GUI Later

Why CLI first:

- easier debugging
- easier automated tests
- easier packaging (pip / uv)
- engine stabilizes before UI

CLI command name:

vib