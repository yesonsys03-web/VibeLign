# 작동 검증 + 개선 루프 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 실행해보기 작동 검증을 사용자 명시 확인("✓ 잘 돼요")으로 만들고, 미리보기에서 부족함을 느꼈을 때 작업방으로 개선 요청을 보내는 제스쳐를 추가해 가이드 5️⃣의 두 축(안전 guard + 작동 run)을 VibeLign이 함께 인지하게 한다.

**Architecture:** M3b 의 `runErrorFix` 핸드오프 채널을 `workHandoff{kind: "error"|"improve"}` 로 일반화해 개선 요청을 재사용한다. 작동 검증은 `guardStatus` 와 동형인 `runVerified` 신호(변경 지문으로 stale 리셋)로 추가하되 `inferStep` 게이팅은 보존한다(추가 축).

**Tech Stack:** React + TypeScript (Tauri v2 프런트). 순수 함수 vitest, tsc --noEmit, eslint.

설계 근거: `plans/2026-06-12-작동검증-개선루프-design.md`

---

## File Structure

- **Create** `vibelign-gui/src/lib/run-preview/improveInstruction.ts` — `buildImproveInstruction`(개선 요청 프레이밍, 순수)
- **Create** `vibelign-gui/src/lib/run-preview/improveInstruction.test.ts`
- **Create** `vibelign-gui/src/lib/run-preview/workHandoff.ts` — `WorkHandoff` 타입 + `buildHandoffInstruction`(kind 분기 디스패처, 순수)
- **Create** `vibelign-gui/src/lib/run-preview/workHandoff.test.ts`
- **Modify** `vibelign-gui/src/pages/WorkRoom.tsx` — `errorFix` 모드 → `handoff` 모드 일반화
- **Modify** `vibelign-gui/src/pages/RunPanel.tsx` — 핸드오프 채널 일반화 + "✓ 잘 돼요"·"✏ 부족해요" 제스쳐 + 완주/nudge
- **Modify** `vibelign-gui/src/App.tsx` — `runErrorFix`→`workHandoff` 일반화 + `runVerified` 신호

기존 `errorFixInstruction.ts`(buildRunErrorFixInstruction)·`runView.ts`·`run.ts` 는 변경 없음.

---

## Task 1: buildImproveInstruction (순수 함수)

**Files:**
- Create: `vibelign-gui/src/lib/run-preview/improveInstruction.ts`
- Test: `vibelign-gui/src/lib/run-preview/improveInstruction.test.ts`

- [ ] **Step 1: 실패 테스트 작성**

`vibelign-gui/src/lib/run-preview/improveInstruction.test.ts`:
```ts
import { describe, expect, it } from "vitest";
import { buildImproveInstruction } from "./improveInstruction";

describe("buildImproveInstruction", () => {
  it("frames the request as improving already-built code, not building anew", () => {
    const out = buildImproveInstruction({ requestText: "버튼 색을 더 진하게", planPath: null });
    expect(out).toContain("이미 만든 것");
    expect(out).toContain("버튼 색을 더 진하게");
  });

  it("references the plan only when planPath is present", () => {
    expect(buildImproveInstruction({ requestText: "x", planPath: "plans/p.md" })).toContain("plans/p.md");
    expect(buildImproveInstruction({ requestText: "x", planPath: null })).not.toContain("기획안은");
  });

  it("does NOT instruct the agent to re-run or preview (re-entrancy trap)", () => {
    expect(buildImproveInstruction({ requestText: "x", planPath: null })).toContain("직접 띄우지 마세요");
  });

  it("degrades when the request is empty", () => {
    expect(buildImproveInstruction({ requestText: "  ", planPath: null })).toContain("구체적인 요청이 없");
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd vibelign-gui && ./node_modules/.bin/vitest run src/lib/run-preview/improveInstruction.test.ts`
Expected: FAIL — `buildImproveInstruction` 미정의(모듈 없음).

- [ ] **Step 3: 구현**

`vibelign-gui/src/lib/run-preview/improveInstruction.ts`:
```ts
// === ANCHOR: IMPROVE_INSTRUCTION_START ===
// 개선 요청 → 작업방 핸드오프 지시문 (plans/2026-06-12-작동검증-개선루프-design.md §4).
// "써보니 부족함" 다듬기. buildRunErrorFixInstruction(에러)와 짝, 순수 함수.
export interface ImproveInstructionInput {
  readonly requestText: string;
  readonly planPath: string | null;
}

// === ANCHOR: IMPROVE_INSTRUCTION_BUILD_START ===
export function buildImproveInstruction({ requestText, planPath }: ImproveInstructionInput): string {
  const trimmed = requestText.trim();
  return [
    "이미 만든 것을 직접 써보니 아래 점을 개선하고 싶어요. 기존 동작을 유지하면서 요청한 부분만 다듬어 주세요.",
    "",
    "[개선 요청]",
    trimmed || "(구체적인 요청이 없어요 — 코드를 보고 사용성·완성도가 떨어지는 부분을 한 가지만 골라 개선해 주세요.)",
    "",
    ...(planPath
      ? [`참고: 이 작업의 기획안은 ${planPath} 에 있어요 — 기획안 범위를 벗어나지 않게 개선해 주세요.`, ""]
      : []),
    "작업 기준:",
    "- 무엇을 바꿀지 먼저 한두 줄로 설명한 뒤, 최소한의 변경으로 개선하세요.",
    "- 앵커 경계를 지키고, 약속한 범위 밖 파일은 건드리기 전에 확인하세요.",
    "- 개선 뒤 다시 실행이나 미리보기는 사용자가 직접 합니다 — dev 서버를 직접 띄우지 마세요.",
  ].join("\n");
}
// === ANCHOR: IMPROVE_INSTRUCTION_BUILD_END ===
// === ANCHOR: IMPROVE_INSTRUCTION_END ===
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `cd vibelign-gui && ./node_modules/.bin/vitest run src/lib/run-preview/improveInstruction.test.ts`
Expected: PASS (4 tests).

- [ ] **Step 5: 커밋**

```bash
git add vibelign-gui/src/lib/run-preview/improveInstruction.ts vibelign-gui/src/lib/run-preview/improveInstruction.test.ts
git commit -m "feat(gui): buildImproveInstruction — 개선 요청 핸드오프 지시문(순수)"
```

---

## Task 2: workHandoff 디스패처 (순수 함수 + 타입)

**Files:**
- Create: `vibelign-gui/src/lib/run-preview/workHandoff.ts`
- Test: `vibelign-gui/src/lib/run-preview/workHandoff.test.ts`

- [ ] **Step 1: 실패 테스트 작성**

`vibelign-gui/src/lib/run-preview/workHandoff.test.ts`:
```ts
import { describe, expect, it } from "vitest";
import { buildHandoffInstruction } from "./workHandoff";

describe("buildHandoffInstruction", () => {
  it("routes error kind to the start-failure framing", () => {
    const out = buildHandoffInstruction({ kind: "error", text: "boom" }, null);
    expect(out).toContain("테스트 실패가 아니라"); // buildRunErrorFixInstruction 표식
    expect(out).toContain("boom");
  });

  it("routes improve kind to the improvement framing", () => {
    const out = buildHandoffInstruction({ kind: "improve", text: "더 빠르게" }, null);
    expect(out).toContain("이미 만든 것"); // buildImproveInstruction 표식
    expect(out).toContain("더 빠르게");
  });

  it("passes planPath through to both kinds", () => {
    expect(buildHandoffInstruction({ kind: "error", text: "x" }, "p.md")).toContain("p.md");
    expect(buildHandoffInstruction({ kind: "improve", text: "x" }, "p.md")).toContain("p.md");
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd vibelign-gui && ./node_modules/.bin/vitest run src/lib/run-preview/workHandoff.test.ts`
Expected: FAIL — `buildHandoffInstruction` 미정의.

- [ ] **Step 3: 구현**

`vibelign-gui/src/lib/run-preview/workHandoff.ts`:
```ts
// === ANCHOR: WORK_HANDOFF_START ===
// 실행해보기 → 작업방 핸드오프 채널 (plans/2026-06-12-작동검증-개선루프-design.md §4).
// M3b 의 단일 채널(error)을 kind 로 일반화 — error(시작 실패 고치기) / improve(써보니 다듬기).
import { buildRunErrorFixInstruction } from "./errorFixInstruction";
import { buildImproveInstruction } from "./improveInstruction";

export type WorkHandoffKind = "error" | "improve";

export interface WorkHandoff {
  readonly kind: WorkHandoffKind;
  readonly text: string;
}

/** 핸드오프 → 작업방 지시문. kind 로 프레이밍 분기. */
export function buildHandoffInstruction(handoff: WorkHandoff, planPath: string | null): string {
  return handoff.kind === "error"
    ? buildRunErrorFixInstruction({ errorText: handoff.text, planPath })
    : buildImproveInstruction({ requestText: handoff.text, planPath });
}
// === ANCHOR: WORK_HANDOFF_END ===
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `cd vibelign-gui && ./node_modules/.bin/vitest run src/lib/run-preview/workHandoff.test.ts`
Expected: PASS (3 tests).

- [ ] **Step 5: 커밋**

```bash
git add vibelign-gui/src/lib/run-preview/workHandoff.ts vibelign-gui/src/lib/run-preview/workHandoff.test.ts
git commit -m "feat(gui): workHandoff 디스패처 — error/improve kind 분기(순수)"
```

---

## Task 3: 핸드오프 채널 일반화 (App + WorkRoom + RunPanel 동시)

> 타입 리네임이라 세 파일이 함께 컴파일돼야 한다 — 한 커밋으로 묶는다. no-handoff 경로는 M3b 와 바이트 동일을 유지한다.

**Files:**
- Modify: `vibelign-gui/src/App.tsx` (runErrorFix → workHandoff)
- Modify: `vibelign-gui/src/pages/WorkRoom.tsx` (errorFix 모드 → handoff 모드)
- Modify: `vibelign-gui/src/pages/RunPanel.tsx` (onRequestErrorFix → onRequestWorkHandoff)

- [ ] **Step 1: App.tsx — 상태·배선 교체**

`src/App.tsx` 의 runErrorFix 상태(약 197행)를:
```tsx
  // 실행해보기 실패 → 작업방 핸드오프(루프 닫기 §6). RunPanel 이 에러 tail 을 싣고,
  // WorkRoom 이 마운트 시 consume-once 로 받아 비운다.
  const [runErrorFix, setRunErrorFix] = useState<string | null>(null);
```
→ 로 교체:
```tsx
  // 실행해보기 → 작업방 핸드오프(§4·§6): error(시작 실패 고치기)·improve(써보니 다듬기).
  // RunPanel 이 싣고, WorkRoom 이 마운트 시 consume-once 로 받아 비운다.
  const [workHandoff, setWorkHandoff] = useState<WorkHandoff | null>(null);
```
App.tsx 상단 import 에 추가(다른 import 옆):
```tsx
import type { WorkHandoff } from "./lib/run-preview/workHandoff";
```
RunPanel 라우팅(`{page === "run" && <RunPanel ...`)의 `onRequestErrorFix={(text) => { setRunErrorFix(text); navigate("work"); }}` 를:
```tsx
onRequestWorkHandoff={(h) => { setWorkHandoff(h); navigate("work"); }}
```
WorkRoom 라우팅(`{page === "work" && <WorkRoom ...`)의 `runErrorFix={runErrorFix} onErrorFixConsumed={() => setRunErrorFix(null)}` 를:
```tsx
workHandoff={workHandoff} onWorkHandoffConsumed={() => setWorkHandoff(null)}
```

- [ ] **Step 2: WorkRoom.tsx — props·상태·지시문·배너 일반화**

`src/pages/WorkRoom.tsx` import 추가:
```tsx
import { buildHandoffInstruction, type WorkHandoff } from "../lib/run-preview/workHandoff";
```
(기존 `import { buildRunErrorFixInstruction } from "../lib/run-preview/errorFixInstruction";` 줄은 삭제 — workHandoff 가 흡수.)

WorkRoomProps 인터페이스의:
```tsx
  /** 실행해보기 실패 핸드오프(§6) — 에러 tail. 마운트 시 consume-once 로 받는다. */
  runErrorFix?: string | null;
  /** 핸드오프 소비 완료 신호 — App 의 runErrorFix 를 비워 재진입 시 재발동 방지. */
  onErrorFixConsumed?: () => void;
```
→ 교체:
```tsx
  /** 실행해보기 → 작업방 핸드오프(§4): error/improve. 마운트 시 consume-once 로 받는다. */
  workHandoff?: WorkHandoff | null;
  /** 핸드오프 소비 완료 — App 의 workHandoff 를 비워 재진입 재발동 방지. */
  onWorkHandoffConsumed?: () => void;
```
함수 시그니처 구조분해의 `runErrorFix, onErrorFixConsumed,` → `workHandoff, onWorkHandoffConsumed,`.

로컬 상태:
```tsx
  // 실행해보기 실패 핸드오프로 받은 에러 tail(§6) — 있으면 실행 지시문을 에러-수정용으로 바꾼다.
  const [errorFix, setErrorFix] = useState<string | null>(null);
```
→ 교체:
```tsx
  // 실행해보기 핸드오프(§4) — 있으면 실행 지시문을 핸드오프(error/improve)용으로 바꾼다.
  const [handoff, setHandoff] = useState<WorkHandoff | null>(null);
```

effectiveInstruction:
```tsx
  const effectiveInstruction =
    errorFix != null
      ? buildRunErrorFixInstruction({ errorText: errorFix, planPath: planningOutputPath })
      : instruction;
```
→ 교체:
```tsx
  // != null 로 — 핸드오프 text 가 빈 문자열이어도 핸드오프 모드다(즉시 실패/빈 요청).
  // 빈 text 는 각 builder 가 "출력/요청 없음"으로 degrade.
  const effectiveInstruction =
    handoff != null ? buildHandoffInstruction(handoff, planningOutputPath) : instruction;
```

캡처 effect:
```tsx
    if (runErrorFix == null) return;
    resetForNextRun();
    setErrorFix(runErrorFix);
    onErrorFixConsumed?.();
    // runErrorFix 변화에만 반응 ...
  }, [runErrorFix]);
```
→ 교체:
```tsx
    if (workHandoff == null) return;
    resetForNextRun();
    setHandoff(workHandoff);
    onWorkHandoffConsumed?.();
    // workHandoff 변화에만 반응 — reset/consumed 는 안정 함수(매 렌더 새로 만들어져도 의미 동일).
  }, [workHandoff]);
```

resetForNextRun 의 `setErrorFix(null);` → `setHandoff(null);`.

실행 버튼 라벨 `{errorFix != null ? "이 에러 고치기" : "AI에게 작업 시키기"}` →:
```tsx
{handoff != null ? (handoff.kind === "error" ? "이 에러 고치기" : "이 개선 작업하기") : "AI에게 작업 시키기"}
```

362 섹션 배너 `{errorFix != null ? (` → `{handoff != null ? (` 로 바꾸고, 배너 내부를 kind 별로:
```tsx
      {handoff != null ? (
        <section className="card" style={{ display: "grid", gap: 6, padding: 12, background: "#FDECEA", border: "2px solid #1A1A1A" }}>
          <div style={{ fontSize: 12, fontWeight: 900, color: "#b42318" }}>
            {handoff.kind === "error" ? "실행 에러를 고칠게요" : "요청하신 개선을 작업할게요"}
          </div>
          <div style={{ fontSize: 12, color: "#444", lineHeight: 1.6 }}>
            {handoff.kind === "error"
              ? "실행해보기에서 앱이 켜지지 않았어요 — 아래 실행 출력을 AI 에게 넘겨 원인을 찾아 고칩니다."
              : "미리보기에서 부족했던 점을 AI 에게 넘겨 다듬습니다."}
            {planningOutputPath ? " 기획안 범위 안에서 작업해요." : ""}
          </div>
          <pre style={{ margin: 0, maxHeight: 140, overflowY: "auto", background: "#1A1A1A", color: "#FCA5A5", padding: 8, borderRadius: 4, fontFamily: "IBM Plex Mono, monospace", fontSize: 11, lineHeight: 1.5, whiteSpace: "pre-wrap", wordBreak: "break-word" }}>
            {handoff.text || (handoff.kind === "error"
              ? "(캡처된 실행 출력이 없어요 — AI 가 코드·설정에서 시작 실패 원인을 추정해요)"
              : "(구체적 요청이 없어요 — AI 가 완성도 낮은 부분을 한 가지 골라 개선해요)")}
          </pre>
        </section>
      ) : instruction ? (
```

- [ ] **Step 3: RunPanel.tsx — onRequestErrorFix → onRequestWorkHandoff**

`src/pages/RunPanel.tsx` import 에 추가:
```tsx
import type { WorkHandoff } from "../lib/run-preview/workHandoff";
```
props 인터페이스의:
```tsx
  /** 실행 실패 시 "이 에러 고쳐줘 →" — 에러 tail 을 작업방 핸드오프로(루프 닫기 §6). */
  onRequestErrorFix?: (errorText: string) => void;
```
→ 교체:
```tsx
  /** 실행해보기 → 작업방 핸드오프(§4·§6): error(실패 고치기)·improve(개선 요청). */
  onRequestWorkHandoff?: (handoff: WorkHandoff) => void;
```
함수 구조분해 `onRequestErrorFix` → `onRequestWorkHandoff`.

에러 버튼:
```tsx
          {status === "failed" && onRequestErrorFix && (
            <div ...>
              <button className="btn" onClick={() => onRequestErrorFix(collectErrorTail(lines))}>
                이 에러 고쳐줘 → 작업방
              </button>
```
→ 교체:
```tsx
          {status === "failed" && onRequestWorkHandoff && (
            <div style={{ display: "flex", gap: 8, alignItems: "center", flexWrap: "wrap" }}>
              <button className="btn" onClick={() => onRequestWorkHandoff({ kind: "error", text: collectErrorTail(lines) })}>
                이 에러 고쳐줘 → 작업방
              </button>
```
(나머지 안내 span 은 그대로.)

- [ ] **Step 4: 빌드 검증 (tsc + vitest + eslint)**

Run:
```bash
cd vibelign-gui && rtk tsc --noEmit && ./node_modules/.bin/vitest run src/lib/run-preview && ./node_modules/.bin/eslint src/App.tsx src/pages/WorkRoom.tsx src/pages/RunPanel.tsx
```
Expected: tsc 0, vitest 통과(기존 16 + Task1/2 의 7), eslint 0.

- [ ] **Step 5: 커밋**

```bash
git add vibelign-gui/src/App.tsx vibelign-gui/src/pages/WorkRoom.tsx vibelign-gui/src/pages/RunPanel.tsx
git commit -m "refactor(gui): 핸드오프 채널 runErrorFix→workHandoff{kind} 일반화"
```

---

## Task 4: RunPanel "✏ 뭔가 부족해요" 개선 입력

**Files:**
- Modify: `vibelign-gui/src/pages/RunPanel.tsx`

- [ ] **Step 1: 개선 입력 상태 추가**

RunPanel 함수 본문 상태 선언부(다른 useState 옆)에 추가:
```tsx
  const [improveOpen, setImproveOpen] = useState(false);
  const [improveText, setImproveText] = useState("");
```

- [ ] **Step 2: running 중 "✏ 부족해요" 제스쳐 + 입력창 렌더**

`electronRunning` 안내 블록 다음(같은 action 카드 안)에 추가:
```tsx
          {status === "running" && onRequestWorkHandoff && (
            <div style={{ display: "grid", gap: 6 }}>
              {!improveOpen ? (
                <button className="btn btn-ghost btn-sm" style={{ justifySelf: "start" }} onClick={() => setImproveOpen(true)}>
                  ✏ 뭔가 부족해요 — 고치고 싶어요
                </button>
              ) : (
                <div style={{ display: "grid", gap: 6 }}>
                  <textarea
                    value={improveText}
                    onChange={(e) => setImproveText(e.target.value)}
                    placeholder="써보니 어떤 점이 부족했나요? (예: 버튼이 너무 작아요 / 로그인 후 화면이 비어요)"
                    style={{ width: "100%", minHeight: 64, fontSize: 13, padding: 8, border: "2px solid #1A1A1A", borderRadius: 4, fontFamily: "inherit", boxSizing: "border-box" }}
                  />
                  <div style={{ display: "flex", gap: 8, alignItems: "center", flexWrap: "wrap" }}>
                    <button
                      className="btn"
                      disabled={improveText.trim().length === 0}
                      onClick={() => onRequestWorkHandoff({ kind: "improve", text: improveText.trim() })}
                    >
                      작업방에서 개선하기 →
                    </button>
                    <button className="btn btn-ghost btn-sm" onClick={() => onNavigate("planning")} style={{ fontSize: 11 }}>
                      큰 방향을 바꿀래요 → 기획방
                    </button>
                    <button className="btn btn-ghost btn-sm" onClick={() => { setImproveOpen(false); setImproveText(""); }} style={{ fontSize: 11 }}>
                      닫기
                    </button>
                  </div>
                </div>
              )}
            </div>
          )}
```

- [ ] **Step 3: 빌드 검증**

Run:
```bash
cd vibelign-gui && rtk tsc --noEmit && ./node_modules/.bin/eslint src/pages/RunPanel.tsx
```
Expected: tsc 0, eslint 0.

- [ ] **Step 4: 커밋**

```bash
git add vibelign-gui/src/pages/RunPanel.tsx
git commit -m "feat(gui): RunPanel 개선 요청 입력 → 작업방 직행(하이브리드 §4)"
```

---

## Task 5: runVerified 신호 (App)

**Files:**
- Modify: `vibelign-gui/src/App.tsx`

- [ ] **Step 1: runVerified 상태 + 지문 ref 추가**

`src/App.tsx` 의 guardStatus 블록(약 238-256행) **바로 아래**에 동형으로 추가:
```tsx
  // 작동 검증(써봤다 ✓) 신호 — guardStatus 와 짝이되 inferStep 게이팅은 안 건드린다(추가 축).
  // 리셋 규칙도 guardStatus 동형: 검증 시점 지문 ≠ 현재 지문이면 stale → 무효화(코드 또 고치면 재검증).
  const [runVerified, setRunVerified] = useState(false);
  const runVerifiedFingerprintRef = useRef<string | null>(null);
  useEffect(() => {
    if (!runVerified || changedFingerprint === null) return;
    if (changedFingerprint !== runVerifiedFingerprintRef.current) {
      setRunVerified(false);
      runVerifiedFingerprintRef.current = null;
    }
  }, [changedFingerprint, runVerified]);
  useEffect(() => {
    setRunVerified(false);
    runVerifiedFingerprintRef.current = null;
  }, [projectDir]);
```

- [ ] **Step 2: RunPanel 라우팅에 runVerified·onRunVerified·guardStatus 전달**

`{page === "run" && <RunPanel ...` 를:
```tsx
                {page === "run" && <RunPanel projectDir={projectDir} onNavigate={navigate} guardStatus={guardStatus} runVerified={runVerified} onRunVerified={() => { runVerifiedFingerprintRef.current = changedFingerprint; setRunVerified(true); }} onRequestWorkHandoff={(h) => { setWorkHandoff(h); navigate("work"); }} />}
```
(기존 onRequestWorkHandoff 는 Task 3 에서 추가됨 — 이 줄로 통합.)

- [ ] **Step 3: 빌드 검증 (RunPanel prop 은 Task 6 에서 추가 — 일시적 타입오류 허용 안 함: Task 6 와 함께 컴파일)**

> Task 5·6 은 RunPanel 의 새 prop(guardStatus/runVerified/onRunVerified)을 함께 정의해야 컴파일된다. Task 6 까지 끝낸 뒤 한 번에 검증·커밋한다. 이 단계에서는 저장만.

---

## Task 6: RunPanel 작동 검증 제스쳐 + 완주/nudge

**Files:**
- Modify: `vibelign-gui/src/pages/RunPanel.tsx`

- [ ] **Step 1: props 추가**

RunPanel props 인터페이스에 추가:
```tsx
  /** 안전 축 상태(가이드 신호) — 완주 🎉·교차 nudge 표시용. */
  guardStatus?: "ok" | "issue" | null;
  /** 작동 축 — 사용자가 "✓ 잘 돼요"로 확인했는지. */
  runVerified?: boolean;
  /** 작동 검증 보고 — guardStatus 동형 채널(App). */
  onRunVerified?: () => void;
```
함수 구조분해에 `guardStatus, runVerified, onRunVerified` 추가.

- [ ] **Step 2: running 중 "✓ 잘 돼요" 제스쳐 + 완주/nudge**

Task 4 의 "✏ 부족해요" 블록 **앞에** 추가:
```tsx
          {status === "running" && onRunVerified && (
            runVerified ? (
              <div style={{ fontSize: 13, fontWeight: 800, color: "#166534" }}>
                {guardStatus === "ok"
                  ? "🎉 안전·작동 둘 다 확인했어요 — 한 사이클 완주!"
                  : "✓ 작동 확인 완료 — 안전도 확인해볼까요?"}
                {guardStatus !== "ok" && (
                  <button className="btn btn-ghost btn-sm" style={{ fontSize: 11, marginLeft: 6 }} onClick={() => onNavigate("home")}>
                    상태 확인 →
                  </button>
                )}
              </div>
            ) : (
              <div style={{ display: "flex", gap: 8, alignItems: "center", flexWrap: "wrap" }}>
                <button className="btn btn-sm" onClick={() => onRunVerified()}>
                  ✓ 잘 돼요 — 작동 확인 완료
                </button>
                <span style={{ fontSize: 11, color: "#888", fontWeight: 700 }}>
                  미리보기에서 직접 써보고 잘 되면 눌러주세요
                </span>
              </div>
            )
          )}
```

- [ ] **Step 3: 빌드 검증 (Task 5 + 6 합산)**

Run:
```bash
cd vibelign-gui && rtk tsc --noEmit && ./node_modules/.bin/vitest run src/lib/run-preview && ./node_modules/.bin/eslint src/App.tsx src/pages/RunPanel.tsx
```
Expected: tsc 0, vitest 통과, eslint 0.

- [ ] **Step 4: 커밋**

```bash
git add vibelign-gui/src/App.tsx vibelign-gui/src/pages/RunPanel.tsx
git commit -m "feat(gui): 작동 검증 runVerified 신호 + RunPanel ✓잘돼요/완주/nudge"
```

---

## Task 7: 전체 검증 + 회귀 리뷰

**Files:** 없음(검증만)

- [ ] **Step 1: 풀 빌드**

Run:
```bash
cd vibelign-gui && rtk tsc --noEmit > /tmp/tsc.txt 2>&1; echo "tsc: $?"; ./node_modules/.bin/vitest run > /tmp/vt.txt 2>&1; echo "vitest: $?"; grep -aE "Tests " /tmp/vt.txt | tail -1; ./node_modules/.bin/eslint src/pages/RunPanel.tsx src/pages/WorkRoom.tsx src/App.tsx src/lib/run-preview/*.ts > /tmp/es.txt 2>&1; echo "eslint: $?"
```
Expected: tsc 0, vitest 0(전체 통과), eslint 0.

- [ ] **Step 2: WorkRoom 회귀 리뷰 (code-reviewer, 별도 컨텍스트)**

집중: no-handoff 경로(평소 기획→작업방)가 일반화 전과 바이트 동일한가; workHandoff consume-once 생명주기; effectiveInstruction kind 분기; runVerified 리셋이 guardStatus 동형으로 stale 을 막나.

- [ ] **Step 3: 리뷰 지적 반영 후 재검증·커밋**

---

## Self-Review (작성자 체크)

- **스펙 커버리지**: §2 runVerified=Task5/6 · §3 RunPanel 제스쳐=Task4/6 · §4 핸드오프 일반화=Task1/2/3 · §5 완주/nudge=Task6(+M3b의 "▶실행해보기"가 guard→run nudge) · §6 순수함수=Task1/2. 전부 매핑됨.
- **inferStep 미변경**: 어느 Task 도 guide.ts inferStep 을 건드리지 않음(설계 §5 보존 조건 충족).
- **타입 일관성**: `WorkHandoff{kind,text}` 는 Task2 정의, Task3/RunPanel/WorkRoom 동일 사용. `onRequestWorkHandoff`/`onWorkHandoffConsumed`/`onRunVerified` 이름 전 Task 일치.
- **no-handoff 바이트 동일**: Task3 가 `handoff==null` 분기로 기존 instruction 경로 보존(M3b 리뷰 기준 재적용, Task7 리뷰가 확인).
