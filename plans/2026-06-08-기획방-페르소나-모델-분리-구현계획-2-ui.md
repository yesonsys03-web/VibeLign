# 기획방 페르소나↔모델 분리 — 구현계획 2 (Settings UI)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 사용자가 Settings 화면에서 기획방 페르소나별로 (1) 어떤 모델(provider)이 그 역할을 맡을지 재배정하고 (2) ON/OFF 할 수 있게 한다. 설정은 전역 `~/.vibelign/gui_config.json` 의 `planning_personas` 섹션에 저장되어, 이미 구현된 백엔드(구현계획 1)의 대화·계획서 두 경로가 즉시 따른다.

**Architecture:** Rust Tauri 커맨드 3종(`get_planning_personas`/`set_planning_personas`/`probe_planning_providers`)이 전역 설정을 읽고 쓰고 설치된 provider 를 탐지한다. 얇은 TS 래퍼로 프런트에 노출하고, Settings 안에 독립 컴포넌트 `PlanningPersonaSettings` 를 마운트한다. 순수 머지/파싱 로직은 HOME 비의존 헬퍼로 분리해 단위 테스트한다.

**Tech Stack:** Rust(Tauri, serde_json, cargo test), React/TypeScript(Vitest), 기존 `read_gui_config`/`write_gui_config`/`find_executable` 재사용.

**관련:** 스펙 `plans/2026-06-08-기획방-페르소나-모델-분리-design.md` §9.1, 구현계획 1 `...-구현계획-1-backend.md`(완료).

## 범위 / 비범위 (YAGNI)

- **포함:** Settings "기획방 페르소나" 섹션(페르소나별 provider 드롭다운 + ON/OFF + 설치 여부 주석), 이를 받치는 Tauri 커맨드/TS 래퍼.
- **제외(→ 구현계획 3):** 기획방 안의 빠른 변경(칩 인라인 편집), 폴백 발생 "OO로 대체됨" 배지. 배지는 `provider_used` 를 Rust `PersonaRun`→채팅 메시지→프런트 타입까지 새로 관통시켜야 하는 별도 교차스택 작업이라 분리한다.

## 데이터 계약 (구현계획 1 과 동일)

`~/.vibelign/gui_config.json`:
```jsonc
"planning_personas": {
  "version": 1,
  "personas": {
    "chloe":    { "enabled": true, "provider": "claude" },
    "gio":      { "enabled": true, "provider": "codex" },
    "mina":     { "enabled": true, "provider": "agy" },
    "deepseek": { "enabled": true, "provider": "opencode" }
  }
}
```
- provider 후보(고정): `claude`, `codex`, `agy`, `opencode`.
- 페르소나 기본 provider(드롭다운 기본 표시): chloe→claude, gio→codex, mina→agy, deepseek→opencode.
- `enabled` 미지정/비-boolean → true (구현계획 1 의 파리티 규칙과 동일).

## 파일 구조

- `vibelign-gui/src-tauri/src/commands/settings.rs` (수정) — 순수 헬퍼 `read_planning_personas_value`/`merge_planning_personas_value` + Tauri 커맨드 `get_planning_personas`/`set_planning_personas`.
- `vibelign-gui/src-tauri/src/commands/planning_persona.rs` (수정) — `probe_planning_providers()` (설치된 provider id 목록) + 그 Tauri 커맨드. 기존 `provider_spec`/`find_executable` 재사용.
- `vibelign-gui/src-tauri/src/lib.rs` (수정) — 새 커맨드 3종 invoke_handler 등록.
- `vibelign-gui/src/lib/vib/planning-personas.ts` (신규) — TS 타입 + `getPlanningPersonas`/`setPlanningPersonas`/`probePlanningProviders` 래퍼.
- `vibelign-gui/src/lib/vib/index.ts` (수정) — 재노출.
- `vibelign-gui/src/components/PlanningPersonaSettings.tsx` (신규) — 섹션 컴포넌트.
- `vibelign-gui/src/pages/Settings.tsx` (수정) — 컴포넌트 마운트.
- 테스트: settings.rs 인라인 `#[cfg(test)]`, `vibelign-gui/src/components/__tests__/PlanningPersonaSettings.test.tsx`(신규).

---

## Phase A — Rust 백엔드 브리지

### Task 1: 순수 헬퍼 — planning_personas 읽기/머지

**Files:**
- Modify: `vibelign-gui/src-tauri/src/commands/settings.rs`
- Test: 동일 파일 인라인 `#[cfg(test)] mod tests`

순수 함수라 HOME 비의존 → 단위 테스트 쉬움. Tauri 커맨드(Task 2)는 이를 `read_gui_config`/`write_gui_config` 와 결합.

- [ ] **Step 1: 실패 테스트 작성.** settings.rs 끝(`// === ANCHOR: SETTINGS_END ===` 직전, 기존 test 모듈이 있으면 거기에, 없으면 새로) 추가:

```rust
#[cfg(test)]
mod planning_personas_tests {
    use super::{merge_planning_personas_value, read_planning_personas_value};

    #[test]
    fn read_returns_personas_object() {
        let cfg = serde_json::json!({
            "planning_personas": {"version": 1, "personas": {"chloe": {"enabled": false, "provider": "codex"}}}
        });
        let personas = read_planning_personas_value(&cfg);
        assert_eq!(personas["chloe"]["provider"], "codex");
        assert_eq!(personas["chloe"]["enabled"], false);
    }

    #[test]
    fn read_missing_section_is_empty_object() {
        let cfg = serde_json::json!({"recent_projects": []});
        assert_eq!(read_planning_personas_value(&cfg), serde_json::json!({}));
    }

    #[test]
    fn merge_sets_section_without_dropping_other_keys() {
        let cfg = serde_json::json!({"recent_projects": ["/a"], "cli_path_installed_version": "9"});
        let update = serde_json::json!({"chloe": {"enabled": true, "provider": "agy"}});
        let merged = merge_planning_personas_value(cfg, update);
        assert_eq!(merged["recent_projects"], serde_json::json!(["/a"]));
        assert_eq!(merged["cli_path_installed_version"], "9");
        assert_eq!(merged["planning_personas"]["version"], 1);
        assert_eq!(merged["planning_personas"]["personas"]["chloe"]["provider"], "agy");
    }
}
```

- [ ] **Step 2: 컴파일 실패 확인.** Run: `cd vibelign-gui/src-tauri && cargo test planning_personas_tests 2>&1 | head -20` — Expected: FAIL (헬퍼 미정의).

- [ ] **Step 3: 구현.** settings.rs 의 `write_gui_config` (`:250-253`) 아래에 추가:

```rust
/// gui_config 값에서 planning_personas.personas 객체를 꺼낸다(부재 시 빈 객체).
pub(crate) fn read_planning_personas_value(cfg: &serde_json::Value) -> serde_json::Value {
    cfg.get("planning_personas")
        .and_then(|s| s.get("personas"))
        .filter(|p| p.is_object())
        .cloned()
        .unwrap_or_else(|| serde_json::json!({}))
}

/// personas 업데이트를 gui_config 에 병합한다(다른 키 보존, version 고정).
pub(crate) fn merge_planning_personas_value(
    mut cfg: serde_json::Value,
    personas: serde_json::Value,
) -> serde_json::Value {
    if !cfg.is_object() {
        cfg = serde_json::json!({});
    }
    cfg["planning_personas"] = serde_json::json!({
        "version": 1,
        "personas": personas,
    });
    cfg
}
```

- [ ] **Step 4: 테스트 통과 확인.** Run: `cd vibelign-gui/src-tauri && cargo test planning_personas_tests 2>&1 | tail -12` — Expected: 3 passed.

- [ ] **Step 5: 커밋.**
```bash
git add vibelign-gui/src-tauri/src/commands/settings.rs
git commit -m "feat(gui): planning_personas config read/merge helpers

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 2: Tauri 커맨드 — get/set planning personas

**Files:**
- Modify: `vibelign-gui/src-tauri/src/commands/settings.rs`

`read_gui_config`/`write_gui_config` 는 settings.rs 내부 private 이므로 커맨드도 같은 파일에 둔다.

- [ ] **Step 1: 구현 (커맨드는 thin wrapper라 헬퍼 테스트로 커버됨 — 별도 테스트 없이 진행).** settings.rs 의 Task 1 헬퍼 아래에 추가:

```rust
#[tauri::command]
pub(crate) fn get_planning_personas() -> serde_json::Value {
    read_planning_personas_value(&read_gui_config())
}

#[tauri::command]
pub(crate) fn set_planning_personas(personas: serde_json::Value) -> Result<(), String> {
    let merged = merge_planning_personas_value(read_gui_config(), personas);
    write_gui_config(&merged)
}
```

- [ ] **Step 2: 빌드 확인.** Run: `cd vibelign-gui/src-tauri && cargo build 2>&1 | tail -6` — Expected: 컴파일 성공(아직 등록 안 해 unused 경고 가능 — Task 4에서 해소).

- [ ] **Step 3: 커밋.**
```bash
git add vibelign-gui/src-tauri/src/commands/settings.rs
git commit -m "feat(gui): get/set planning personas tauri commands

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 3: provider 설치 탐지 커맨드

**Files:**
- Modify: `vibelign-gui/src-tauri/src/commands/planning_persona.rs`
- Test: 동일 파일 인라인 `mod tests`

기존 `provider_spec`(provider→실행파일명) + `find_executable`(augmented PATH 탐색)을 재사용해, 4개 provider 중 설치된 것만 돌려준다. 대화 경로와 같은 탐지라 일관.

- [ ] **Step 1: 실패 테스트 작성.** `planning_persona.rs` 의 `mod tests` 에 추가(`use super::{...}` 에 `installed_providers_from` 추가):

```rust
    #[test]
    fn installed_providers_filters_by_resolver() {
        // codex, agy 만 설치된 것으로 가정한 resolver
        let resolver = |name: &str| matches!(name, "codex" | "agy");
        let installed = installed_providers_from(resolver);
        assert_eq!(installed, vec!["codex".to_string(), "agy".to_string()]);
    }
```

- [ ] **Step 2: 컴파일 실패 확인.** Run: `cd vibelign-gui/src-tauri && cargo test planning_persona 2>&1 | head -20` — Expected: FAIL (`installed_providers_from` 미정의).

- [ ] **Step 3: 구현.** `planning_persona.rs` 에 추가(테스트 가능한 순수 코어 + PATH 사용 래퍼 + 커맨드):

```rust
/// provider 후보 순서(드롭다운/탐지 공용).
const PLANNING_PROVIDERS: &[&str] = &["claude", "codex", "agy", "opencode"];

/// resolver(실행파일명→설치여부)로 설치된 provider id 만 추린다.
fn installed_providers_from(resolver: impl Fn(&str) -> bool) -> Vec<String> {
    PLANNING_PROVIDERS
        .iter()
        .filter(|provider| {
            provider_spec(provider)
                .map(|(executable, _)| resolver(executable))
                .unwrap_or(false)
        })
        .map(|p| (*p).to_string())
        .collect()
}

/// 실제 PATH(augmented)에서 설치된 planning provider 목록.
pub(crate) fn probe_planning_providers() -> Vec<String> {
    installed_providers_from(|executable| find_executable(executable).is_some())
}
```

`probe_planning_providers` 의 Tauri 커맨드 래퍼는 `commands` 모듈 관례를 따른다. 같은 파일(앵커 내부)에 추가:

```rust
#[tauri::command]
pub(crate) fn planning_provider_status() -> Vec<String> {
    probe_planning_providers()
}
```

- [ ] **Step 4: 테스트 통과 + 빌드 확인.** Run: `cd vibelign-gui/src-tauri && cargo test planning_persona 2>&1 | tail -12 && cargo build 2>&1 | tail -4` — Expected: 테스트 PASS(신규 1 포함), 빌드 성공.

- [ ] **Step 5: 커밋.**
```bash
git add vibelign-gui/src-tauri/src/commands/planning_persona.rs
git commit -m "feat(planning): probe installed planning providers command

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 4: 커맨드 등록 (lib.rs)

**Files:**
- Modify: `vibelign-gui/src-tauri/src/lib.rs`

- [ ] **Step 1: 등록.** `lib.rs` 의 `invoke_handler(tauri::generate_handler![...])` 목록(`detect_installed_tools` 가 등록된 곳, 약 `:159` 부근)에 3개 추가. 모듈 경로는 기존 항목 표기를 따른다(예: `commands::settings::get_planning_personas`). 등록 라인 예:

```rust
            commands::settings::get_planning_personas,
            commands::settings::set_planning_personas,
            commands::planning_persona::planning_provider_status,
```
(기존 `detect_installed_tools` 등록 라인과 동일한 콤마/모듈 표기 스타일을 정확히 맞춘다. `commands::settings::...` 경로가 다른 settings 커맨드 등록 표기와 일치하는지 파일에서 확인 후 동일하게.)

- [ ] **Step 2: 빌드 확인 (unused 경고 사라짐).** Run: `cd vibelign-gui/src-tauri && cargo build 2>&1 | tail -6` — Expected: 성공, planning 커맨드 관련 unused 경고 없음.

- [ ] **Step 3: 커밋.**
```bash
git add vibelign-gui/src-tauri/src/lib.rs
git commit -m "feat(gui): register planning persona config commands

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Phase B — 프런트엔드

### Task 5: TS 래퍼 + 타입

**Files:**
- Create: `vibelign-gui/src/lib/vib/planning-personas.ts`
- Modify: `vibelign-gui/src/lib/vib/index.ts`

- [ ] **Step 1: 래퍼 작성.** 기존 lib 모듈처럼 `@tauri-apps/api/core` 의 `invoke` 사용. 새 파일:

```typescript
// vibelign-gui/src/lib/vib/planning-personas.ts
import { invoke } from "@tauri-apps/api/core";

export type PlanningProviderId = "claude" | "codex" | "agy" | "opencode";

export interface PlanningPersonaConfig {
  enabled: boolean;
  provider: string;
}

/** 페르소나 id → 설정. 비어있는 항목은 호출자가 기본값으로 채운다. */
export type PlanningPersonaConfigMap = Record<string, Partial<PlanningPersonaConfig>>;

export async function getPlanningPersonas(): Promise<PlanningPersonaConfigMap> {
  return (await invoke<PlanningPersonaConfigMap>("get_planning_personas")) ?? {};
}

export async function setPlanningPersonas(personas: PlanningPersonaConfigMap): Promise<void> {
  await invoke("set_planning_personas", { personas });
}

export async function probePlanningProviders(): Promise<string[]> {
  return (await invoke<string[]>("planning_provider_status")) ?? [];
}
```

> 주의: Tauri 커맨드 인자명은 snake_case 파라미터가 camelCase 로 매핑된다. 커맨드 `set_planning_personas(personas: ...)` 의 인자명이 `personas` 이므로 invoke 인자 키도 `personas` 다(위와 일치). 단일 단어라 변환 이슈 없음.

- [ ] **Step 2: index.ts 재노출.** `vibelign-gui/src/lib/vib/index.ts` 에 한 줄 추가(기존 `export { ... } from "./onboarding";` 형식을 따라):

```typescript
export { getPlanningPersonas, setPlanningPersonas, probePlanningProviders, type PlanningPersonaConfig, type PlanningPersonaConfigMap, type PlanningProviderId } from "./planning-personas";
```

- [ ] **Step 3: 타입체크 확인.** Run: `cd vibelign-gui && npx tsc --noEmit 2>&1 | tail -10` — Expected: 새 파일 관련 에러 없음(기존 무관 에러가 있다면 무시하되 새 모듈 라인엔 에러 없어야 함).

- [ ] **Step 4: 커밋.**
```bash
git add vibelign-gui/src/lib/vib/planning-personas.ts vibelign-gui/src/lib/vib/index.ts
git commit -m "feat(gui): planning persona config lib wrappers

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 6: PlanningPersonaSettings 컴포넌트 (+ 테스트)

**Files:**
- Create: `vibelign-gui/src/components/PlanningPersonaSettings.tsx`
- Create test: `vibelign-gui/src/components/__tests__/PlanningPersonaSettings.test.tsx`

페르소나 4종(`PLANNING_PERSONAS`)을 행으로 렌더. 각 행: 이름/역할 + provider `<select>`(claude/codex/agy/opencode, 미설치는 "(미설치)" 주석) + ON/OFF 버튼. 변경 시 `setPlanningPersonas` 로 전체 맵 저장.

순수 머지 로직 `applyPersonaChange(map, id, patch)` 를 export 해 단위 테스트한다(컴포넌트 통합테스트는 invoke 모킹 필요 — 핵심 로직은 순수 함수로 검증).

- [ ] **Step 1: 실패 테스트 작성.**

```tsx
// vibelign-gui/src/components/__tests__/PlanningPersonaSettings.test.tsx
import { describe, it, expect } from "vitest";
import {
  PLANNING_PROVIDER_OPTIONS,
  applyPersonaChange,
  effectivePersona,
} from "../PlanningPersonaSettings";

describe("PlanningPersonaSettings logic", () => {
  it("exposes the four provider options", () => {
    expect(PLANNING_PROVIDER_OPTIONS).toEqual(["claude", "codex", "agy", "opencode"]);
  });

  it("effectivePersona falls back to default provider and enabled=true", () => {
    expect(effectivePersona({}, "chloe")).toEqual({ enabled: true, provider: "claude" });
    expect(effectivePersona({ chloe: { provider: "codex" } }, "chloe")).toEqual({
      enabled: true,
      provider: "codex",
    });
    expect(effectivePersona({ gio: { enabled: false } }, "gio")).toEqual({
      enabled: false,
      provider: "codex",
    });
  });

  it("applyPersonaChange writes a full entry without touching others", () => {
    const next = applyPersonaChange({ mina: { provider: "agy" } }, "chloe", { enabled: false });
    expect(next.chloe).toEqual({ enabled: false, provider: "claude" });
    expect(next.mina).toEqual({ provider: "agy" });
  });
});
```

- [ ] **Step 2: 테스트 실패 확인.** Run: `cd vibelign-gui && npx vitest run src/components/__tests__/PlanningPersonaSettings.test.tsx 2>&1 | tail -15` — Expected: FAIL (모듈/심볼 없음).

- [ ] **Step 3: 컴포넌트 구현.**

```tsx
// vibelign-gui/src/components/PlanningPersonaSettings.tsx
import { useEffect, useState } from "react";
import {
  getPlanningPersonas,
  setPlanningPersonas,
  probePlanningProviders,
  type PlanningPersonaConfigMap,
} from "../lib/vib";
import { PLANNING_PERSONAS } from "../pages/planning/PlanningPersonas";

export const PLANNING_PROVIDER_OPTIONS = ["claude", "codex", "agy", "opencode"] as const;

const DEFAULT_PROVIDER: Record<string, string> = {
  chloe: "claude",
  gio: "codex",
  mina: "agy",
  deepseek: "opencode",
};

export interface EffectivePersona {
  enabled: boolean;
  provider: string;
}

/** 저장된 맵 + 기본값으로 페르소나의 실효 설정을 만든다. */
export function effectivePersona(map: PlanningPersonaConfigMap, id: string): EffectivePersona {
  const entry = map[id] ?? {};
  return {
    enabled: entry.enabled ?? true,
    provider: entry.provider ?? DEFAULT_PROVIDER[id] ?? "claude",
  };
}

/** 한 페르소나에 patch 를 적용해 완전한 엔트리로 기록(다른 페르소나는 보존). */
export function applyPersonaChange(
  map: PlanningPersonaConfigMap,
  id: string,
  patch: Partial<EffectivePersona>,
): PlanningPersonaConfigMap {
  const current = effectivePersona(map, id);
  return { ...map, [id]: { ...current, ...patch } };
}

export function PlanningPersonaSettings() {
  const [map, setMap] = useState<PlanningPersonaConfigMap>({});
  const [installed, setInstalled] = useState<string[] | null>(null);
  const [saving, setSaving] = useState(false);

  useEffect(() => {
    getPlanningPersonas().then(setMap).catch(() => setMap({}));
    probePlanningProviders().then(setInstalled).catch(() => setInstalled([]));
  }, []);

  async function persist(next: PlanningPersonaConfigMap) {
    setMap(next);
    setSaving(true);
    try {
      await setPlanningPersonas(next);
    } finally {
      setSaving(false);
    }
  }

  const change = (id: string, patch: Partial<EffectivePersona>) =>
    void persist(applyPersonaChange(map, id, patch));

  return (
    <div className="card" style={{ marginTop: 16 }}>
      <div style={{ fontWeight: 700, fontSize: 12, marginBottom: 8, textTransform: "uppercase", letterSpacing: 1 }}>
        기획방 페르소나
      </div>
      <div style={{ fontSize: 11, color: "#777", lineHeight: 1.6, marginBottom: 12 }}>
        각 역할을 어떤 AI가 맡을지 고르고, 끄고 켤 수 있어요. 고른 AI가 없거나 로그인이 안 되어 있으면
        설치된 다른 AI로 자동 대체돼요. {saving && <span style={{ color: "#4DFF91" }}>저장 중…</span>}
      </div>
      {PLANNING_PERSONAS.map((persona) => {
        const eff = effectivePersona(map, persona.id);
        return (
          <div
            key={persona.id}
            style={{ display: "flex", alignItems: "center", gap: 10, marginBottom: 10, flexWrap: "wrap" }}
          >
            <span style={{ fontWeight: 700, fontSize: 12, minWidth: 64 }}>{persona.label}</span>
            <span style={{ fontSize: 11, color: "#888", minWidth: 40 }}>{persona.role}</span>
            <select
              value={eff.provider}
              onChange={(e) => change(persona.id, { provider: e.target.value })}
              style={{ background: "#1A1A1A", color: "#7DFF6B", border: "1px solid #333", borderRadius: 4, padding: "3px 8px", fontSize: 11 }}
            >
              {PLANNING_PROVIDER_OPTIONS.map((p) => (
                <option key={p} value={p}>
                  {p}
                  {installed && !installed.includes(p) ? " (미설치)" : ""}
                </option>
              ))}
            </select>
            <button
              type="button"
              className="btn btn-sm"
              onClick={() => change(persona.id, { enabled: !eff.enabled })}
              style={{
                background: eff.enabled ? "#4DFF91" : "#1A1A1A",
                color: eff.enabled ? "#1A1A1A" : "#888",
                border: "2px solid #1A1A1A",
                fontWeight: 700,
                minWidth: 52,
              }}
            >
              {eff.enabled ? "ON" : "OFF"}
            </button>
          </div>
        );
      })}
    </div>
  );
}
```

- [ ] **Step 4: 테스트 통과 확인.** Run: `cd vibelign-gui && npx vitest run src/components/__tests__/PlanningPersonaSettings.test.tsx 2>&1 | tail -12` — Expected: 3 passed.

- [ ] **Step 5: 커밋.**
```bash
git add vibelign-gui/src/components/PlanningPersonaSettings.tsx vibelign-gui/src/components/__tests__/PlanningPersonaSettings.test.tsx
git commit -m "feat(gui): PlanningPersonaSettings component with provider reassign + on/off

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 7: Settings 화면에 마운트

**Files:**
- Modify: `vibelign-gui/src/pages/Settings.tsx`

- [ ] **Step 1: import 추가.** Settings.tsx 상단 import 그룹(`ToolSetupSelector` import 아래)에:

```tsx
import { PlanningPersonaSettings } from "../components/PlanningPersonaSettings";
```

- [ ] **Step 2: 마운트.** "AI 도구 설정" 카드(`{/* AI 도구 설정 ... */}` 블록, `</div>` 로 닫히는 곳) 바로 다음, `page-content` div 가 닫히기 전(`</div>` 두 개 + return 닫힘 직전)에 추가:

```tsx
        <PlanningPersonaSettings />
```
(구체적으로 `AI 도구 설정` 카드의 닫는 `</div>` 와, 그 아래 `page-content` 컨테이너의 닫는 `</div>` 사이에 한 줄로 삽입. 들여쓰기는 형제 카드들과 동일하게 8스페이스.)

- [ ] **Step 3: 타입체크 + (가능하면) 기존 Settings 테스트 회귀.** Run:
```bash
cd vibelign-gui && npx tsc --noEmit 2>&1 | tail -10
npx vitest run 2>&1 | tail -15
```
Expected: 타입 에러 없음, 전체 vitest 통과(신규 포함, 회귀 0).

- [ ] **Step 4: 커밋.**
```bash
git add vibelign-gui/src/pages/Settings.tsx
git commit -m "feat(gui): mount PlanningPersonaSettings in Settings page

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## 최종 검증

- [ ] **Rust 전체 테스트 + 빌드.** Run: `cd vibelign-gui/src-tauri && cargo test 2>&1 | tail -15 && cargo build 2>&1 | tail -4` — Expected: 전부 PASS, 빌드 성공.
- [ ] **프런트 타입체크 + vitest.** Run: `cd vibelign-gui && npx tsc --noEmit 2>&1 | tail -6 && npx vitest run 2>&1 | tail -12` — Expected: 통과.
- [ ] **수동 라운드트립(선택, GUI 기동 가능 시):** Settings → 기획방 페르소나에서 클로이 provider 를 codex 로, 미나를 OFF 로 바꾼 뒤 `~/.vibelign/gui_config.json` 의 `planning_personas.personas` 가 갱신됐는지 확인. 기획방 대화에서 미나가 호출되지 않는지 확인(구현계획 1 의 enabled 스킵).

## 자기 검토 메모 (작성자용)

- 스펙 §9.1(Settings 페르소나 섹션: provider 드롭다운 + ON/OFF + 설치 주석): Task 6 컴포넌트 + Task 3 탐지. ✅
- 단일 소스(전역 gui_config) 기록: Task 1/2 가 `planning_personas` 섹션만 갱신, 다른 키 보존. ✅
- 구현계획 1 백엔드가 이 설정을 이미 소비 → UI 저장 즉시 대화·계획서 반영. ✅
- 설치 탐지는 대화 경로와 동일한 `find_executable`/`provider_spec` 재사용 → 일관. ✅
- **이관:** 기획방 인라인 빠른 편집 + 폴백 "대체됨" 배지(`provider_used` 교차스택 관통)는 **구현계획 3** 로 분리. 배지는 Rust `PersonaRun` 에 `provider_used` 추가 → `PlanningChatMessage`/프런트 타입 확장 → `PlanningPersonaStatus`/메시지 렌더에 배지. 별도 계획에서 TDD.
