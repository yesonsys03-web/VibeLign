# 기획방 페르소나↔모델 분리 — 구현계획 1 (백엔드/설정/폴백)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 페르소나와 공급자(CLI/모델)를 분리하고, 전역 설정(`~/.vibelign/gui_config.json`)을 단일 소스로 Rust 대화 경로와 Python 계획서 경로가 함께 읽어, 페르소나별 모델 재배정·ON/OFF·자동 폴백을 백엔드에서 동작시킨다. (UI는 구현계획 2에서.)

**Architecture:** 공유 설정 파일을 두 런타임이 각자 읽고, 동일한 폴백 알고리즘(지정 provider → 없거나 실패 시 내부 우선순위로 다음 가용 provider)을 양쪽에 미러 구현한다. provider 실행 능력은 양쪽 통일(claude·codex·agy·opencode), persona 집합은 경로별 유지(대화 4, 계획서 3).

**Tech Stack:** Python(pytest), Rust(cargo test, Tauri), serde_json.

**관련 스펙:** `plans/2026-06-08-기획방-페르소나-모델-분리-design.md`

---

## 파일 구조

신규/수정 파일과 책임:

- `vibelign/core/planning_cli/cli_adapters.py` (수정) — provider 레지스트리에 `opencode` 추가.
- `vibelign/core/planning_cli/planning_config.py` (신규) — 전역 설정 `planning_personas` 리더. 책임: 파일 읽기 + 기본값 병합 + 안전 폴백.
- `vibelign/core/planning_cli/fallback.py` (신규) — provider 시도 목록 생성(내부 우선순위 상수 포함). 양쪽 의미의 단일 정의처(Python 측).
- `vibelign/core/planning_cli/orchestrator.py` (수정) — 페르소나별 폴백 실행 + 설정(enabled/provider) 반영.
- `vibelign-gui/src-tauri/src/commands/planning_persona.rs` (수정) — provider 레지스트리 분리 + 설정 리더 + 폴백 resolver.
- `vibelign-gui/src-tauri/src/commands/planning_chat.rs` (수정) — 비활성 페르소나 호출 스킵.
- 테스트: `tests/core/planning_cli/test_cli_adapters_opencode.py`(신규), `tests/core/planning_cli/test_planning_config.py`(신규), `tests/core/planning_cli/test_fallback.py`(신규), `tests/core/planning_cli/test_orchestrator.py`(확장), Rust 인라인 `#[cfg(test)] mod tests`.

---

## Phase A — Python (계획서 경로)

### Task 1: cli_adapters에 opencode provider 추가

**Files:**
- Modify: `vibelign/core/planning_cli/cli_adapters.py` (`resolve_cli_executable`, `probe_cli_candidates`, `select_adapter`, `build_cli_command`)
- Test: `tests/core/planning_cli/test_cli_adapters_opencode.py`

- [ ] **Step 1: 실패 테스트 작성**

```python
# tests/core/planning_cli/test_cli_adapters_opencode.py
from vibelign.core.planning_cli import cli_adapters


def test_resolve_opencode_executable(monkeypatch):
    monkeypatch.setattr(
        "vibelign.core.planning_cli.cli_adapters.shutil.which",
        lambda name: f"/bin/{name}" if name == "opencode" else None,
    )
    assert cli_adapters.resolve_cli_executable("opencode") == "/bin/opencode"


def test_build_opencode_command(monkeypatch):
    monkeypatch.setattr(
        "vibelign.core.planning_cli.cli_adapters.shutil.which",
        lambda name: f"/bin/{name}",
    )
    cmd = cli_adapters.build_cli_command("opencode", "프롬프트")
    assert cmd == [
        "/bin/opencode",
        "run",
        "-m",
        "opencode/deepseek-v4-flash-free",
        "프롬프트",
    ]


def test_select_adapter_accepts_opencode():
    assert cli_adapters.select_adapter("opencode") == "opencode"


def test_probe_includes_opencode(monkeypatch):
    monkeypatch.setattr(
        "vibelign.core.planning_cli.cli_adapters.shutil.which",
        lambda name: None,
    )
    adapters = [c.adapter for c in cli_adapters.probe_cli_candidates()]
    assert "opencode" in adapters
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `python -m pytest tests/core/planning_cli/test_cli_adapters_opencode.py -v`
Expected: FAIL (opencode 미지원 → `resolve_cli_executable("opencode")` 가 None, `build_cli_command` 가 ValueError, `select_adapter` 가 ValueError).

- [ ] **Step 3: opencode 지원 추가**

`resolve_cli_executable` (현재 `cli_adapters.py:157-163`) 의 매핑에 opencode 추가:

```python
def resolve_cli_executable(adapter: str) -> str | None:
    executable_name = {
        "codex": "codex",
        "claude": "claude",
        "agy": "agy",
        "opencode": "opencode",
    }.get(adapter)
    if executable_name is None:
        return None
    return shutil.which(executable_name)
```

`probe_cli_candidates` (현재 `:166-180`) 의 루프 튜플에 opencode 추가:

```python
    for adapter in ("codex", "claude", "agy", "opencode"):
```

`select_adapter` (현재 `:183-192`) 의 케이스에 opencode 추가:

```python
        case "codex" | "claude" | "agy" | "opencode":
            return cli_choice
```

`build_cli_command` (현재 `:201-215`) 의 match 에 opencode 케이스 추가:

```python
        case "opencode":
            return [executable, "run", "-m", "opencode/deepseek-v4-flash-free", prompt]
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `python -m pytest tests/core/planning_cli/test_cli_adapters_opencode.py -v`
Expected: PASS (4 passed)

- [ ] **Step 5: 커밋**

```bash
git add vibelign/core/planning_cli/cli_adapters.py tests/core/planning_cli/test_cli_adapters_opencode.py
git commit -m "feat(planning): add opencode provider to cli adapters

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 2: 전역 설정 리더 (planning_config.py)

**Files:**
- Create: `vibelign/core/planning_cli/planning_config.py`
- Test: `tests/core/planning_cli/test_planning_config.py`

설정 스키마(스펙 §5): `~/.vibelign/gui_config.json` 의 `planning_personas.personas[id] = {enabled, provider}`.

- [ ] **Step 1: 실패 테스트 작성**

```python
# tests/core/planning_cli/test_planning_config.py
import json
from pathlib import Path

from vibelign.core.planning_cli.planning_config import (
    PersonaConfig,
    load_persona_config,
)


def _write_config(home: Path, payload: dict) -> None:
    cfg_dir = home / ".vibelign"
    cfg_dir.mkdir(parents=True, exist_ok=True)
    (cfg_dir / "gui_config.json").write_text(json.dumps(payload), encoding="utf-8")


def test_missing_file_returns_empty_config(tmp_path, monkeypatch):
    monkeypatch.setenv("HOME", str(tmp_path))
    monkeypatch.setattr("pathlib.Path.home", lambda: tmp_path)
    cfg = load_persona_config()
    assert cfg.get("chloe") is None  # 호출자가 기본값을 채움


def test_reads_enabled_and_provider(tmp_path, monkeypatch):
    monkeypatch.setattr("pathlib.Path.home", lambda: tmp_path)
    _write_config(
        tmp_path,
        {
            "planning_personas": {
                "version": 1,
                "personas": {
                    "chloe": {"enabled": False, "provider": "codex"},
                },
            }
        },
    )
    cfg = load_persona_config()
    assert cfg["chloe"] == PersonaConfig(enabled=False, provider="codex")


def test_corrupt_file_returns_empty(tmp_path, monkeypatch):
    monkeypatch.setattr("pathlib.Path.home", lambda: tmp_path)
    cfg_dir = tmp_path / ".vibelign"
    cfg_dir.mkdir(parents=True)
    (cfg_dir / "gui_config.json").write_text("{ not json", encoding="utf-8")
    assert load_persona_config() == {}


def test_partial_entry_defaults(tmp_path, monkeypatch):
    monkeypatch.setattr("pathlib.Path.home", lambda: tmp_path)
    _write_config(
        tmp_path,
        {"planning_personas": {"personas": {"gio": {"provider": "claude"}}}},
    )
    cfg = load_persona_config()
    assert cfg["gio"] == PersonaConfig(enabled=True, provider="claude")
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `python -m pytest tests/core/planning_cli/test_planning_config.py -v`
Expected: FAIL (`ModuleNotFoundError: planning_config`)

- [ ] **Step 3: 리더 구현**

```python
# vibelign/core/planning_cli/planning_config.py
# === ANCHOR: PLANNING_CONFIG_START ===
from __future__ import annotations

import json
from dataclasses import dataclass
from pathlib import Path


@dataclass(frozen=True)
class PersonaConfig:
    enabled: bool
    provider: str | None


def _config_path() -> Path:
    return Path.home() / ".vibelign" / "gui_config.json"


def load_persona_config() -> dict[str, PersonaConfig]:
    """전역 gui_config.json 의 planning_personas 섹션을 읽는다.

    파일/섹션 부재·손상 시 빈 dict 를 반환한다(호출자가 기본값을 채운다).
    """
    path = _config_path()
    try:
        raw = path.read_text(encoding="utf-8")
    except OSError:
        return {}
    try:
        data = json.loads(raw)
    except (ValueError, TypeError):
        return {}
    section = data.get("planning_personas") if isinstance(data, dict) else None
    personas = section.get("personas") if isinstance(section, dict) else None
    if not isinstance(personas, dict):
        return {}
    out: dict[str, PersonaConfig] = {}
    for persona_id, entry in personas.items():
        if not isinstance(entry, dict):
            continue
        enabled = entry.get("enabled", True)
        provider = entry.get("provider")
        out[str(persona_id)] = PersonaConfig(
            enabled=bool(enabled),
            provider=str(provider) if isinstance(provider, str) and provider else None,
        )
    return out
# === ANCHOR: PLANNING_CONFIG_END ===
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `python -m pytest tests/core/planning_cli/test_planning_config.py -v`
Expected: PASS (4 passed)

- [ ] **Step 5: 커밋**

```bash
git add vibelign/core/planning_cli/planning_config.py tests/core/planning_cli/test_planning_config.py
git commit -m "feat(planning): read persona config from global gui_config.json

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 3: 폴백 시도 목록 생성기 (fallback.py)

**Files:**
- Create: `vibelign/core/planning_cli/fallback.py`
- Test: `tests/core/planning_cli/test_fallback.py`

내부 우선순위 상수와, "지정 provider 우선 + 우선순위 보충, 중복 제거" 시도 목록 생성.

- [ ] **Step 1: 실패 테스트 작성**

```python
# tests/core/planning_cli/test_fallback.py
from vibelign.core.planning_cli.fallback import (
    INTERNAL_PROVIDER_PRIORITY,
    provider_try_order,
)


def test_preferred_first_then_priority():
    order = provider_try_order("agy")
    assert order[0] == "agy"
    assert order == ["agy"] + [p for p in INTERNAL_PROVIDER_PRIORITY if p != "agy"]


def test_no_duplicates():
    order = provider_try_order("claude")
    assert len(order) == len(set(order))


def test_unknown_preferred_still_tries_priority():
    order = provider_try_order("zzz")
    assert order[0] == "zzz"
    assert INTERNAL_PROVIDER_PRIORITY[0] in order


def test_none_preferred_uses_priority_only():
    assert provider_try_order(None) == list(INTERNAL_PROVIDER_PRIORITY)
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `python -m pytest tests/core/planning_cli/test_fallback.py -v`
Expected: FAIL (`ModuleNotFoundError: fallback`)

- [ ] **Step 3: 구현**

```python
# vibelign/core/planning_cli/fallback.py
# === ANCHOR: FALLBACK_START ===
from __future__ import annotations

# 폴백 시도 우선순위. 사용자 지정 provider 가 항상 맨 앞에 오고,
# 그 뒤를 이 순서로 보충한다. v1 에서는 UI 비노출 내부 상수.
INTERNAL_PROVIDER_PRIORITY: tuple[str, ...] = ("claude", "codex", "agy", "opencode")


def provider_try_order(preferred: str | None) -> list[str]:
    """preferred 를 맨 앞에 두고 내부 우선순위로 보충한 시도 목록(중복 제거)."""
    order: list[str] = []
    if preferred:
        order.append(preferred)
    for provider in INTERNAL_PROVIDER_PRIORITY:
        if provider not in order:
            order.append(provider)
    return order
# === ANCHOR: FALLBACK_END ===
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `python -m pytest tests/core/planning_cli/test_fallback.py -v`
Expected: PASS (4 passed)

- [ ] **Step 5: 커밋**

```bash
git add vibelign/core/planning_cli/fallback.py tests/core/planning_cli/test_fallback.py
git commit -m "feat(planning): provider fallback try-order generator

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 4: orchestrator에 폴백 + 설정 반영

**Files:**
- Modify: `vibelign/core/planning_cli/orchestrator.py` (`_apply_agents_to_result` 실행 루프 `:124-154`, `_resolve_adapters` `:191-209`)
- Test: `tests/core/planning_cli/test_orchestrator.py` (확장)

동작: `cli_choice` 가 명시(`--cli`)면 기존 동작 유지(명시 오버라이드 우선). `auto`/미지정이면 페르소나별로 (1) 설정의 provider, 없으면 persona.adapter 를 1순위로 (2) `provider_try_order` 로 폴백 순회. 설정의 `enabled=False` 페르소나는 제외.

- [ ] **Step 1: 실패 테스트 작성 (폴백 + enabled)**

```python
# tests/core/planning_cli/test_orchestrator.py 에 추가
from vibelign.core.planning_cli.cli_adapters import PlanningCliResult
from vibelign.core.planning_cli.models import PlanningInput
from vibelign.core.planning_cli.orchestrator import create_planning_with_agents


def test_orchestrator_falls_back_when_preferred_not_installed(tmp_path, monkeypatch):
    # claude 만 미설치, 나머지는 설치된 것으로
    monkeypatch.setattr(
        "vibelign.core.planning_cli.cli_adapters.resolve_cli_executable",
        lambda adapter: None if adapter == "claude" else f"/bin/{adapter}",
    )
    runner = FakeRunner([PlanningCliResult("ok", "대체 설계입니다.", "", 0, 10)])

    result = create_planning_with_agents(
        tmp_path,
        PlanningInput(idea="@chloe 예약 앱"),
        runner=runner,
    )

    # chloe 기본 provider 는 claude(미설치) → 폴백으로 codex 가 답함
    assert result.agents_used == ("chloe",)
    assert result.agent_statuses["chloe"] == "ok"
    assert runner.commands[0][0] == "/bin/codex"


def test_orchestrator_falls_back_on_runtime_failure(tmp_path, monkeypatch):
    monkeypatch.setattr(
        "vibelign.core.planning_cli.cli_adapters.resolve_cli_executable",
        lambda adapter: f"/bin/{adapter}",
    )
    # 1순위(claude) 는 로그인 안 됨, 2순위(codex) 는 ok
    runner = FakeRunner(
        [
            PlanningCliResult("not_logged_in", "", "not logged in", 1, 10),
            PlanningCliResult("ok", "코덱스 설계입니다.", "", 0, 10),
        ]
    )

    result = create_planning_with_agents(
        tmp_path,
        PlanningInput(idea="@chloe 예약 앱"),
        runner=runner,
    )

    assert result.agents_used == ("chloe",)
    assert result.agent_statuses["chloe"] == "ok"
    assert runner.commands[0][0] == "/bin/claude"
    assert runner.commands[1][0] == "/bin/codex"


def test_orchestrator_skips_disabled_persona(tmp_path, monkeypatch):
    monkeypatch.setattr(
        "vibelign.core.planning_cli.cli_adapters.resolve_cli_executable",
        lambda adapter: f"/bin/{adapter}",
    )
    monkeypatch.setattr(
        "vibelign.core.planning_cli.orchestrator.load_persona_config",
        lambda: {"gio": __import__(
            "vibelign.core.planning_cli.planning_config",
            fromlist=["PersonaConfig"],
        ).PersonaConfig(enabled=False, provider=None)},
    )
    runner = FakeRunner([PlanningCliResult("ok", "클로이 설계입니다.", "", 0, 10)])

    result = create_planning_with_agents(
        tmp_path,
        PlanningInput(idea="@chloe @gio 예약 앱"),
        runner=runner,
    )

    assert "gio" not in result.agents_used
    assert result.agents_used == ("chloe",)
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `python -m pytest tests/core/planning_cli/test_orchestrator.py -v -k "fall_back or falls_back or disabled"`
Expected: FAIL (폴백/enabled 미구현 — claude 미설치 시 `not_installed` 로 스킵됨, disabled 무시 안 됨)

- [ ] **Step 3: orchestrator 수정**

`orchestrator.py` 상단 import 에 추가:

```python
from vibelign.core.planning_cli.fallback import provider_try_order
from vibelign.core.planning_cli.planning_config import load_persona_config
```

`_resolve_adapters` (`:190-209`) 를 "명시 오버라이드 우선, auto면 설정+기본" 으로 변경. 반환은 페르소나별 **1순위 provider** dict 유지(폴백은 실행 루프에서):

```python
# === ANCHOR: ORCHESTRATOR__RESOLVE_ADAPTERS_START ===
def _resolve_adapters(
    cli_choice: str,
    personas: tuple[PlanningPersona, ...],
# === ANCHOR: ORCHESTRATOR__RESOLVE_ADAPTERS_END ===
) -> dict[str, str]:
    cli_ids = _parse_csv(cli_choice)
    if cli_ids and cli_ids != ("auto",):
        # --cli 명시 오버라이드: 기존 동작 유지
        if len(cli_ids) == 1:
            adapter = cli_adapters.select_adapter(cli_ids[0])
            return {persona.id: adapter for persona in personas}
        mapped: dict[str, str] = {}
        for index, persona in enumerate(personas):
            mapped[persona.id] = (
                cli_adapters.select_adapter(cli_ids[index])
                if index < len(cli_ids)
                else persona.adapter
            )
        return mapped
    # auto: 설정의 provider 우선, 없으면 persona 기본
    config = load_persona_config()
    resolved: dict[str, str] = {}
    for persona in personas:
        entry = config.get(persona.id)
        resolved[persona.id] = (
            entry.provider if entry and entry.provider else persona.adapter
        )
    return resolved
```

`_apply_agents_to_result` (`:112-117`) 에서 personas 해석 직후 enabled 필터를 추가:

```python
    mention_result = resolve_persona_mentions(message)
    personas = _resolve_personas(
        default_persona_ids or mention_result.persona_ids,
        agents_choice,
    )
    personas = _filter_enabled_personas(personas)
    adapters = _resolve_adapters(cli_choice, personas)
```

실행 루프(`:124-154`)의 단일 어댑터 실행을 폴백 순회로 교체:

```python
    for turn_index, persona in enumerate(personas, start=1):
        preferred = adapters[persona.id]
        run_result = _run_persona_with_fallback(
            persona=persona,
            preferred=preferred,
            runner=active_runner,
            root=root,
            message=mention_result.clean_text or message,
            markdown=markdown,
            timeout_seconds=timeout_seconds,
        )
        statuses[persona.id] = run_result.status
        runs.append(
            AgentRunMetadata(
                turn_index, persona.id, run_result.adapter, run_result.status, run_result.stdout
            )
        )
        if save_transcript:
            write_turn_transcript(
                root,
                session_id=base.session_id,
                turn_index=turn_index,
                adapter=run_result.adapter,
                response=run_result.stdout,
            )
        if run_result.status != "ok":
            continue
        markdown = append_persona_section(markdown, persona.section_title, run_result.stdout)
        used_agents.append(persona.id)
```

> 주의: `adapters[persona.id]` 가 `_finalize_agent_result` (`:243`) 에서도 쓰인다. 폴백으로 실제 사용된 어댑터를 반영하려면 루프에서 `adapters[persona.id] = run_result.adapter` 로 갱신한다(루프 본문 끝에 추가).

새 헬퍼 + enabled 필터를 `_parse_csv` 위(`:211` 부근)에 추가:

```python
from dataclasses import dataclass


@dataclass(frozen=True)
class _PersonaRun:
    adapter: str
    status: str
    stdout: str


# === ANCHOR: ORCHESTRATOR__RUN_PERSONA_WITH_FALLBACK_START ===
def _run_persona_with_fallback(
    *,
    persona: PlanningPersona,
    preferred: str,
    runner: PlanningCliRunner,
    root: Path,
    message: str,
    markdown: str,
    timeout_seconds: int,
) -> _PersonaRun:
    prompt = build_persona_prompt(persona, message, markdown)
    last_status = "not_installed"
    last_adapter = preferred
    for adapter in provider_try_order(preferred):
        command = cli_adapters.build_cli_command(adapter, prompt)
        if command is None:
            last_status, last_adapter = "not_installed", adapter
            continue
        cli_result = runner.run(
            command, cwd=root, input_text="", timeout_seconds=timeout_seconds
        )
        status = safe_planning_status(cli_result.status, cli_result.stdout)
        if status == "ok":
            return _PersonaRun(adapter=adapter, status="ok", stdout=cli_result.stdout)
        last_status, last_adapter = status, adapter
    return _PersonaRun(adapter=last_adapter, status=last_status, stdout="")
# === ANCHOR: ORCHESTRATOR__RUN_PERSONA_WITH_FALLBACK_END ===


def _filter_enabled_personas(
    personas: tuple[PlanningPersona, ...],
) -> tuple[PlanningPersona, ...]:
    config = load_persona_config()
    return tuple(
        persona
        for persona in personas
        if (config.get(persona.id).enabled if config.get(persona.id) else True)
    )
```

- [ ] **Step 4: 테스트 통과 확인 (신규 + 회귀)**

Run: `python -m pytest tests/core/planning_cli/test_orchestrator.py -v`
Expected: PASS (신규 3 + 기존 회귀 전부). 특히 `test_orchestrator_runs_requested_personas_in_fixed_order`, `test_orchestrator_keeps_template_when_all_agents_fail` 가 여전히 통과해야 함.

- [ ] **Step 5: 전체 planning_cli 회귀**

Run: `python -m pytest tests/core/planning_cli/ -v`
Expected: PASS (all)

- [ ] **Step 6: 커밋**

```bash
git add vibelign/core/planning_cli/orchestrator.py tests/core/planning_cli/test_orchestrator.py
git commit -m "feat(planning): per-persona provider fallback and enabled filter

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Phase B — Rust (대화 경로)

### Task 5: provider 레지스트리를 persona spec에서 분리

**Files:**
- Modify: `vibelign-gui/src-tauri/src/commands/planning_persona.rs` (`PersonaSpec` `:18-24`, `persona_spec` `:79-107`, `persona_cli` `:109-112`, tests `:217-277`)
- Test: 동일 파일 인라인 `mod tests`

목표: `PersonaSpec` 에서 `executable`/`args_before_prompt` 제거하고 `default_provider: &str` 추가. provider 실행정보는 `provider_spec(provider_id)` 레지스트리로 이동.

- [ ] **Step 1: 실패 테스트 작성 (레지스트리 분리)**

기존 `persona_spec_maps_*` 테스트(`:221-246`)를 provider 레지스트리 기준으로 교체:

```rust
    use super::{build_persona_prompt, persona_spec, provider_spec, PlanningChatLine};

    #[test]
    fn persona_default_provider_mapping() {
        assert_eq!(persona_spec("chloe").unwrap().default_provider, "claude");
        assert_eq!(persona_spec("gio").unwrap().default_provider, "codex");
        assert_eq!(persona_spec("mina").unwrap().default_provider, "agy");
        assert_eq!(persona_spec("deepseek").unwrap().default_provider, "opencode");
    }

    #[test]
    fn provider_spec_returns_executable_and_args() {
        assert_eq!(provider_spec("codex").unwrap(), ("codex", &["exec"][..]));
        assert_eq!(provider_spec("claude").unwrap(), ("claude", &["-p"][..]));
        assert_eq!(provider_spec("agy").unwrap(), ("agy", &["-p"][..]));
        assert_eq!(
            provider_spec("opencode").unwrap(),
            ("opencode", &["run", "-m", "opencode/deepseek-v4-flash-free"][..])
        );
    }
```

- [ ] **Step 2: 컴파일 실패 확인**

Run: `cd vibelign-gui/src-tauri && cargo test planning_persona 2>&1 | head -30`
Expected: FAIL (컴파일 에러 — `provider_spec` 미정의, `PersonaSpec.default_provider` 없음)

- [ ] **Step 3: 레지스트리 분리 구현**

`PersonaSpec` (`:18-24`) 변경:

```rust
#[derive(Clone, Copy)]
struct PersonaSpec {
    name: &'static str,
    role: &'static str,
    default_provider: &'static str,
}
```

`persona_spec` (`:79-107`) 의 각 항목에서 `executable`/`args_before_prompt` 를 `default_provider` 로 교체:

```rust
fn persona_spec(persona_id: &str) -> Option<PersonaSpec> {
    match persona_id {
        "chloe" => Some(PersonaSpec {
            name: "클로이",
            role: "제품 설계자. 사용자의 막연한 아이디어를 기능 흐름과 화면 구조로 구체화한다.",
            default_provider: "claude",
        }),
        "gio" => Some(PersonaSpec {
            name: "지오",
            role: "기획 검토자. 빠진 조건, 위험한 가정, 구현 전에 정해야 할 결정을 짚는다.",
            default_provider: "codex",
        }),
        "mina" => Some(PersonaSpec {
            name: "미나",
            role: "사용자 탐색자. 실제 사용자가 겪을 상황, 입력 방식, 엣지케이스를 질문한다.",
            default_provider: "agy",
        }),
        "deepseek" => Some(PersonaSpec {
            name: "딥시기",
            role: "조교. 다른 페르소나의 설명과 결정을 사용자가 알기 쉽게 풀어 주고, 사용자의 질문에 차분히 답하며 논의를 정리한다.",
            default_provider: "opencode",
        }),
        _ => None,
    }
}

fn provider_spec(provider_id: &str) -> Option<(&'static str, &'static [&'static str])> {
    match provider_id {
        "claude" => Some(("claude", &["-p"])),
        "codex" => Some(("codex", &["exec"])),
        "agy" => Some(("agy", &["-p"])),
        "opencode" => Some(("opencode", &["run", "-m", "opencode/deepseek-v4-flash-free"])),
        _ => None,
    }
}
```

`persona_cli` (`:109-112`) 를 persona 기본 provider 경유로 변경:

```rust
pub(crate) fn persona_cli(persona_id: &str) -> Option<(&'static str, &'static [&'static str])> {
    let spec = persona_spec(persona_id)?;
    provider_spec(spec.default_provider)
}
```

`run_persona_response` 내부(`:37`)의 `find_executable(spec.executable)` 와 `cmd.args(spec.args_before_prompt)` (`:47-48`)는 Task 6에서 폴백 resolver로 대체되므로, 이 단계에서는 컴파일을 위해 임시로 기본 provider 경유로 바꿔둔다:

```rust
    let (executable_name, args_before_prompt) = match provider_spec(spec.default_provider) {
        Some(pair) => pair,
        None => return failed_persona_run(),
    };
    let Some(executable) = find_executable(executable_name) else {
        return PersonaRun {
            content: format!(
                "{} CLI를 찾지 못했어요. 설치 또는 PATH 설정을 확인해 주세요.",
                spec.name
            ),
            status: "failed".to_string(),
        };
    };
    let prompt = build_persona_prompt(spec, lines);
    let mut cmd = Command::new(executable);
    cmd.args(args_before_prompt);
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `cd vibelign-gui/src-tauri && cargo test planning_persona 2>&1 | tail -20`
Expected: PASS (레지스트리 테스트 통과, 기존 prompt 테스트 통과)

- [ ] **Step 5: 커밋**

```bash
git add vibelign-gui/src-tauri/src/commands/planning_persona.rs
git commit -m "refactor(planning): split provider registry from persona spec

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 6: Rust 설정 리더 + 폴백 resolver

**Files:**
- Modify: `vibelign-gui/src-tauri/src/commands/planning_persona.rs`
- Test: 동일 파일 인라인 `mod tests`

`run_persona_response` / `run_active_ai` / `pick_judge_cli` 가 "설정 provider 우선 → 폴백 순회" 로 동작하도록.

- [ ] **Step 1: 실패 테스트 작성 (폴백 순서/설정 파싱)**

순수 함수로 테스트 가능한 부분을 분리: `provider_try_order(preferred)` 와 `read_persona_provider(config_json, persona_id)`.

```rust
    use super::{provider_try_order, persona_provider_from_value, INTERNAL_PROVIDER_PRIORITY};

    #[test]
    fn try_order_puts_preferred_first_no_dupes() {
        let order = provider_try_order("agy");
        assert_eq!(order[0], "agy");
        let mut seen = std::collections::HashSet::new();
        assert!(order.iter().all(|p| seen.insert(p.clone())));
        for base in INTERNAL_PROVIDER_PRIORITY {
            assert!(order.iter().any(|p| p == base));
        }
    }

    #[test]
    fn persona_provider_reads_config_value() {
        let v: serde_json::Value = serde_json::from_str(
            r#"{"planning_personas":{"personas":{"chloe":{"enabled":true,"provider":"codex"}}}}"#,
        )
        .unwrap();
        assert_eq!(persona_provider_from_value(&v, "chloe"), Some("codex".to_string()));
        assert_eq!(persona_provider_from_value(&v, "mina"), None);
    }
```

- [ ] **Step 2: 컴파일 실패 확인**

Run: `cd vibelign-gui/src-tauri && cargo test planning_persona 2>&1 | head -20`
Expected: FAIL (`provider_try_order`, `persona_provider_from_value`, `INTERNAL_PROVIDER_PRIORITY` 미정의)

- [ ] **Step 3: 폴백 + 설정 구현**

`planning_persona.rs` 상단(use 아래)에 상수/헬퍼 추가:

```rust
pub(crate) const INTERNAL_PROVIDER_PRIORITY: &[&str] = &["claude", "codex", "agy", "opencode"];

fn provider_try_order(preferred: &str) -> Vec<String> {
    let mut order: Vec<String> = Vec::new();
    if !preferred.is_empty() {
        order.push(preferred.to_string());
    }
    for provider in INTERNAL_PROVIDER_PRIORITY {
        if !order.iter().any(|p| p == provider) {
            order.push((*provider).to_string());
        }
    }
    order
}

fn persona_provider_from_value(config: &serde_json::Value, persona_id: &str) -> Option<String> {
    config
        .get("planning_personas")?
        .get("personas")?
        .get(persona_id)?
        .get("provider")?
        .as_str()
        .filter(|s| !s.is_empty())
        .map(|s| s.to_string())
}

fn persona_enabled_from_value(config: &serde_json::Value, persona_id: &str) -> bool {
    config
        .get("planning_personas")
        .and_then(|s| s.get("personas"))
        .and_then(|p| p.get(persona_id))
        .and_then(|e| e.get("enabled"))
        .and_then(|v| v.as_bool())
        .unwrap_or(true)
}

/// 전역 gui_config.json 을 읽는다. 부재/손상 시 빈 객체.
fn read_gui_config_value() -> serde_json::Value {
    let Some(home) = std::env::var_os("HOME").or_else(|| std::env::var_os("USERPROFILE")) else {
        return serde_json::json!({});
    };
    let path = std::path::PathBuf::from(home).join(".vibelign").join("gui_config.json");
    std::fs::read_to_string(path)
        .ok()
        .and_then(|t| serde_json::from_str(&t).ok())
        .unwrap_or_else(|| serde_json::json!({}))
}

/// persona 의 1순위 provider(설정 우선, 없으면 기본)로 시도 목록을 만든다.
fn resolve_provider_order(persona_id: &str, spec: &PersonaSpec) -> Vec<String> {
    let config = read_gui_config_value();
    let preferred = persona_provider_from_value(&config, persona_id)
        .unwrap_or_else(|| spec.default_provider.to_string());
    provider_try_order(&preferred)
}
```

`run_persona_response` (`:26-70`) 의 단일 실행을 폴백 순회로 교체:

```rust
pub(crate) fn run_persona_response(
    project_dir: &Path,
    persona_id: &str,
    lines: &[PlanningChatLine<'_>],
) -> PersonaRun {
    let Some(spec) = persona_spec(persona_id) else {
        return PersonaRun {
            content: "선택한 페르소나를 찾지 못했어요.".to_string(),
            status: "failed".to_string(),
        };
    };
    let prompt = build_persona_prompt(spec, lines);
    for provider in resolve_provider_order(persona_id, &spec) {
        let Some((executable_name, args)) = provider_spec(&provider) else {
            continue;
        };
        let Some(executable) = find_executable(executable_name) else {
            continue;
        };
        let mut cmd = Command::new(executable);
        cmd.args(args);
        cmd.arg(&prompt);
        cmd.current_dir(project_dir);
        cmd.stdin(Stdio::null());
        cmd.env("PATH", augmented_vib_path());
        cmd.env("NO_COLOR", "1");
        hide_console(&mut cmd);
        if let Ok(output) = cmd.output() {
            if output.status.success() {
                let content = String::from_utf8_lossy(&output.stdout).trim().to_string();
                if !content.is_empty() {
                    return PersonaRun {
                        content,
                        status: "ok".to_string(),
                    };
                }
            }
        }
        // 실패 → 다음 provider 로 폴백
    }
    PersonaRun {
        content: "AI 응답을 가져오지 못했어요. 설치된 AI가 없거나 로그인이 필요할 수 있어요.".to_string(),
        status: "failed".to_string(),
    }
}
```

`resolve_persona_cli` (`:188-192`) 를 폴백 순회로 변경(judge/active 경로도 일관):

```rust
fn resolve_persona_cli(persona_id: &str) -> Option<(PathBuf, Vec<String>)> {
    let spec = persona_spec(persona_id)?;
    for provider in resolve_provider_order(persona_id, &spec) {
        let (executable, args) = match provider_spec(&provider) {
            Some(pair) => pair,
            None => continue,
        };
        if let Some(path) = find_executable(executable) {
            return Some((path, args.iter().map(|a| a.to_string()).collect()));
        }
    }
    None
}
```

> 참고: Task 5에서 임시로 둔 `run_persona_response` 본문은 이 단계에서 폴백 버전으로 완전히 대체된다. `failed_persona_run()` 헬퍼(`:72-77`)는 더 이상 쓰이지 않으면 제거한다(미사용 경고 방지).

- [ ] **Step 4: 테스트 통과 확인**

Run: `cd vibelign-gui/src-tauri && cargo test planning_persona 2>&1 | tail -20`
Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add vibelign-gui/src-tauri/src/commands/planning_persona.rs
git commit -m "feat(planning): config-driven provider with fallback in chat path

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 7: 비활성 페르소나 호출 스킵 (chat append)

**Files:**
- Modify: `vibelign-gui/src-tauri/src/commands/planning_chat.rs` (`append_planning_chat_turn` 루프 `:170-188`)
- Modify: `vibelign-gui/src-tauri/src/commands/planning_persona.rs` (공개 헬퍼 노출)
- Test: `planning_persona.rs` 인라인 (enabled 파서) — Task 6의 `persona_enabled_from_value` 재사용

설정에서 `enabled=false` 인 페르소나는 요청돼도 응답 생성을 건너뛴다.

- [ ] **Step 1: 실패 테스트 작성 (enabled 파서)**

```rust
    use super::{persona_enabled_from_value};

    #[test]
    fn persona_enabled_defaults_true_and_reads_false() {
        let v: serde_json::Value = serde_json::from_str(
            r#"{"planning_personas":{"personas":{"gio":{"enabled":false}}}}"#,
        )
        .unwrap();
        assert!(!persona_enabled_from_value(&v, "gio"));
        assert!(persona_enabled_from_value(&v, "chloe")); // 미지정 → true
    }
```

- [ ] **Step 2: 컴파일/실패 확인**

Run: `cd vibelign-gui/src-tauri && cargo test planning_persona 2>&1 | head -20`
Expected: FAIL (`persona_enabled_from_value` 가 `pub(crate)` 아니면 비공개라 import 불가)

- [ ] **Step 3: 공개 헬퍼 + chat 스킵 구현**

`planning_persona.rs` 에 공개 래퍼 추가:

```rust
/// 전역 설정에서 해당 페르소나가 활성인지. 부재 시 true.
pub(crate) fn is_persona_enabled(persona_id: &str) -> bool {
    persona_enabled_from_value(&read_gui_config_value(), persona_id)
}
```

그리고 Task 6에서 추가한 `persona_enabled_from_value` 를 `pub(crate) fn` 으로 표기.

`planning_chat.rs` import (`:15`) 에 `is_persona_enabled` 추가:

```rust
use super::planning_persona::{is_persona_enabled, run_persona_response, PlanningChatLine};
```

`append_planning_chat_turn` 루프(`:170`) 시작에 스킵 분기 추가:

```rust
        for agent in request.agents {
            if !is_persona_enabled(&agent) {
                continue;
            }
            let lines = messages
```

- [ ] **Step 4: 테스트 통과 + 빌드 확인**

Run: `cd vibelign-gui/src-tauri && cargo test planning_persona 2>&1 | tail -15 && cargo build 2>&1 | tail -5`
Expected: 테스트 PASS, 빌드 성공(에러 없음)

- [ ] **Step 5: 커밋**

```bash
git add vibelign-gui/src-tauri/src/commands/planning_persona.rs vibelign-gui/src-tauri/src/commands/planning_chat.rs
git commit -m "feat(planning): skip disabled personas in chat turn

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## 최종 검증

- [ ] **Python 전체 회귀**

Run: `python -m pytest tests/ -q`
Expected: PASS (신규 포함 회귀 없음)

- [ ] **Rust 전체 테스트 + 빌드**

Run: `cd vibelign-gui/src-tauri && cargo test 2>&1 | tail -20`
Expected: PASS

- [ ] **Conformance 수동 확인 (양쪽 동일 선택)**

시나리오: `chloe` 의 provider 가 claude 인데 claude 미설치, codex 설치됨.
- Python: 위 `test_orchestrator_falls_back_when_preferred_not_installed` 가 codex 선택을 보장.
- Rust: `provider_try_order("claude")` 가 `["claude","codex","agy","opencode"]` → claude 실패 후 codex 시도. (코드 리뷰로 확인)

---

## 자기 검토 메모 (작성자용)

- 스펙 §6.1 provider 통일: Task 1(Python opencode) + Task 5(Rust provider_spec) 로 양쪽 claude·codex·agy·opencode 보유. ✅
- 스펙 §7 폴백: Python Task 3+4, Rust Task 6. 설치 안 됨·런타임 실패 둘 다 다음 provider 로. ✅
- 스펙 §5 설정 스키마: Python Task 2, Rust Task 6 리더. 부재/손상 안전 폴백. ✅
- 스펙 §7 enabled 필터: Python Task 4(`_filter_enabled_personas`), Rust Task 7. ✅
- `provider_used` 노출(배지) 및 Settings/기획방 UI: **구현계획 2 (UI)** 로 이관.
