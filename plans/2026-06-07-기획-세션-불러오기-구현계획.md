# 기획 세션 불러오기 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 이전 기획 세션(대화 + 카드)을 목록 모달에서 골라 그대로 복원해 이어서 수정할 수 있게 한다.

**Architecture:** 세션은 이미 `.vibelign/planning/chat_<id>/`에 통째로 영속된다(messages/cards/session.json+output_path). 새 명령 `list_planning_chat_sessions`(목록)와 `load_planning_chat_session`(id로 복원)을 추가하고, `load_latest`의 "디렉터리→응답" 코어를 공유 헬퍼로 빼서 재사용한다. 프론트는 피커 모달이 목록을 띄우고 선택 시 그 세션을 복원해 기획방에 진입한다.

**Tech Stack:** Rust (Tauri command, serde_json, std::fs), React/TypeScript, 테스트 `cargo test` + `tsc`/`vitest`. 순수 파일 IO + UI(AI 서브프로세스 없음) → 크로스플랫폼 안전.

---

## 배경 명세

`plans/기획-세션-불러오기-design.md`(spec) 구현. 새 저장 포맷·마이그레이션 없이 기존 세션
저장을 노출만 한다. 모든 세션(저장됨+초안) 목록, 홈 버튼→모달.

## 파일 구조
- **수정** `commands/planning_chat_types.rs` — `PlanningSessionSummary` 타입.
- **수정** `commands/planning_chat.rs` — `list_planning_chat_sessions`·`load_planning_chat_session` 명령 + `load_session_from_dir` 헬퍼 리팩터.
- **수정** `lib.rs` — 두 명령 등록.
- **수정** `src/lib/vib/types.ts` — `PlanningSessionSummary`.
- **수정** `src/lib/vib/planning.ts` + `index.ts` — 두 래퍼.
- **생성** `src/pages/planning/PlanningSessionPicker.tsx` — 피커 모달.
- **수정** `src/App.tsx` + `src/pages/Home.tsx` — 버튼 + 상태 + 복원 배선.

---

## Task 1: 세션 목록 명령 (Rust)

**Files:**
- Modify: `vibelign-gui/src-tauri/src/commands/planning_chat_types.rs`
- Modify: `vibelign-gui/src-tauri/src/commands/planning_chat.rs`
- Modify: `vibelign-gui/src-tauri/src/lib.rs`

- [ ] **Step 1: 요약 타입 추가**

`planning_chat_types.rs`의 `PlanningChatSessionResponse` 정의 아래에 추가:

```rust
#[derive(Serialize, Deserialize, Debug, PartialEq, Eq)]
#[serde(rename_all = "camelCase")]
pub(crate) struct PlanningSessionSummary {
    pub(crate) session_id: String,
    pub(crate) title: String,
    pub(crate) output_path: Option<String>,
    pub(crate) saved: bool,
    pub(crate) created_at: String,
    pub(crate) message_count: usize,
    pub(crate) card_count: usize,
}
```

> `planning_chat_types.rs` 상단은 이미 `use serde::{Deserialize, Serialize};`를 가진다.

- [ ] **Step 2: 실패 테스트 작성 (planning_chat.rs)**

`planning_chat.rs` 맨 아래(`timestamp_ms` 함수 아래, `PLANNING_CHAT_END` 앵커 주석 위)에 테스트 모듈을 추가:

```rust
#[cfg(test)]
mod tests {
    use super::*;

    fn write_session(root: &std::path::Path, id: &str, idea: &str, output: Option<&str>, msgs: usize, cards: usize) {
        let dir = root.join(".vibelign/planning").join(id);
        std::fs::create_dir_all(&dir).expect("mkdir");
        let output_json = match output {
            Some(p) => format!("\"{p}\""),
            None => "null".to_string(),
        };
        std::fs::write(
            dir.join("session.json"),
            format!(
                "{{\"schema_version\":1,\"session_id\":\"{id}\",\"idea\":\"{idea}\",\"mode\":\"chat\",\"created_at\":\"1\",\"output_path\":{output_json}}}"
            ),
        )
        .expect("session");
        let msg = "{\"id\":\"m\",\"role\":\"user\",\"personaId\":null,\"content\":\"hi\",\"status\":\"ok\",\"createdAt\":\"1\"}";
        let arr = vec![msg; msgs].join(",");
        std::fs::write(dir.join("messages.json"), format!("[{arr}]")).expect("messages");
        let card = "{\"id\":\"c\",\"title\":\"t\",\"summary\":\"\",\"reason\":\"\",\"state\":\"draft\",\"createdAt\":\"1\",\"updatedAt\":\"1\"}";
        let carr = vec![card; cards].join(",");
        std::fs::write(dir.join("cards.json"), format!("{{\"cards\":[{carr}]}}")).expect("cards");
    }

    #[test]
    fn list_sessions_returns_summaries_with_saved_flag_and_counts() {
        let root = tempfile::tempdir().expect("root");
        write_session(root.path(), "chat_1", "예약 앱", None, 2, 1);
        write_session(root.path(), "chat_2", "카드 흐름", Some("plans/a.md"), 4, 3);

        let summaries = list_sessions(root.path());

        assert_eq!(summaries.len(), 2);
        let saved = summaries.iter().find(|s| s.session_id == "chat_2").expect("chat_2");
        assert!(saved.saved);
        assert_eq!(saved.output_path.as_deref(), Some("plans/a.md"));
        assert_eq!(saved.title, "카드 흐름");
        assert_eq!(saved.message_count, 4);
        assert_eq!(saved.card_count, 3);
        let draft = summaries.iter().find(|s| s.session_id == "chat_1").expect("chat_1");
        assert!(!draft.saved);
        assert_eq!(draft.message_count, 2);
    }

    #[test]
    fn list_sessions_skips_dirs_without_messages() {
        let root = tempfile::tempdir().expect("root");
        let dir = root.path().join(".vibelign/planning/chat_x");
        std::fs::create_dir_all(&dir).expect("mkdir");
        std::fs::write(dir.join("session.json"), "{}").expect("session");
        assert!(list_sessions(root.path()).is_empty());
    }
}
```

- [ ] **Step 3: 테스트 실패 확인**

Run: `cd vibelign-gui/src-tauri && cargo test planning_chat`
Expected: FAIL — `list_sessions` 미정의.

- [ ] **Step 4: list_sessions + 명령 구현**

`planning_chat.rs` 상단 `use`에 추가(기존 store import 줄에 `planning_dir` 추가, 타입 import에 `PlanningSessionSummary` 추가):

```rust
use super::planning_chat_store::{
    latest_chat_session_file, planning_dir, read_json, write_json, StoredPlanningChatSession,
};
```
(이미 `planning_dir`가 import돼 있으면 중복 추가하지 말 것. `latest_chat_session_file`/`read_json`/`write_json`/`StoredPlanningChatSession`는 기존 그대로.)

타입 import은 기존 `use super::planning_chat_types::{...}` 목록에 `PlanningSessionSummary`를 추가.

`timestamp_ms` 함수 위(또는 아래, 테스트 모듈 위)에 추가:

```rust
fn summary_title(idea: &str) -> String {
    idea.lines().next().unwrap_or("").trim().chars().take(60).collect()
}

fn list_sessions(project_dir: &std::path::Path) -> Vec<PlanningSessionSummary> {
    let dir = planning_dir(project_dir);
    let Ok(entries) = std::fs::read_dir(&dir) else {
        return Vec::new();
    };
    let mut rows: Vec<(std::time::SystemTime, PlanningSessionSummary)> = Vec::new();
    for entry in entries.flatten() {
        let session_dir = entry.path();
        let session_path = session_dir.join("session.json");
        let messages_path = session_dir.join("messages.json");
        if !session_path.exists() || !messages_path.exists() {
            continue;
        }
        let Ok(session) = read_json::<StoredPlanningChatSession>(&session_path) else {
            continue;
        };
        let messages = read_json::<Vec<PlanningChatMessage>>(&messages_path).unwrap_or_default();
        let cards = super::planning_chat_cards::read_cards(&session_dir);
        let modified = messages_path
            .metadata()
            .and_then(|meta| meta.modified())
            .unwrap_or(UNIX_EPOCH);
        rows.push((
            modified,
            PlanningSessionSummary {
                title: summary_title(&session.idea),
                saved: session.output_path.is_some(),
                output_path: session.output_path.clone(),
                created_at: session.created_at.clone(),
                message_count: messages.len(),
                card_count: cards.len(),
                session_id: session.session_id,
            },
        ));
    }
    rows.sort_by(|a, b| b.0.cmp(&a.0));
    rows.into_iter().map(|(_, summary)| summary).collect()
}

#[tauri::command]
pub(crate) async fn list_planning_chat_sessions(project_dir: String) -> Vec<PlanningSessionSummary> {
    let project_dir = PathBuf::from(project_dir);
    if !project_dir.is_absolute() {
        return Vec::new();
    }
    tauri::async_runtime::spawn_blocking(move || list_sessions(&project_dir))
        .await
        .unwrap_or_default()
}
```

- [ ] **Step 5: lib.rs 등록**

`lib.rs`의 `commands::planning_chat::load_latest_planning_chat_session,` 다음 줄에 추가:

```rust
            commands::planning_chat::list_planning_chat_sessions,
```

- [ ] **Step 6: 테스트 + 빌드**

Run: `cd vibelign-gui/src-tauri && cargo test planning_chat && cargo build`
Expected: PASS (목록 2개 포함), 빌드 성공.

- [ ] **Step 7: 커밋**

```bash
git add vibelign-gui/src-tauri/src/commands/planning_chat_types.rs vibelign-gui/src-tauri/src/commands/planning_chat.rs vibelign-gui/src-tauri/src/lib.rs
git commit -m "feat(planning): list_planning_chat_sessions command + summary type"
```

---

## Task 2: id로 세션 복원 (Rust)

**Files:**
- Modify: `vibelign-gui/src-tauri/src/commands/planning_chat.rs`
- Modify: `vibelign-gui/src-tauri/src/lib.rs`

- [ ] **Step 1: load_session_from_dir 헬퍼로 리팩터**

현재 `load_latest_planning_chat_session`의 `spawn_blocking` 본문은 (최신 session.json 찾기 →
session/messages 읽기 → markdown → cards → success)를 인라인으로 한다. "디렉터리→응답" 코어를
헬퍼로 뺀다. `load_latest_planning_chat_session` 함수 **위**에 헬퍼 추가:

```rust
fn load_session_from_dir(
    project_dir: &std::path::Path,
    session_dir: &std::path::Path,
) -> PlanningChatSessionResponse {
    let session = match read_json::<StoredPlanningChatSession>(&session_dir.join("session.json")) {
        Ok(parsed) => parsed,
        Err(error) => return planning_chat_error(error),
    };
    let messages = match read_json::<Vec<PlanningChatMessage>>(&session_dir.join("messages.json")) {
        Ok(parsed) => parsed,
        Err(error) => return planning_chat_error(error),
    };
    let markdown = read_saved_markdown(project_dir, &session);
    let cards = super::planning_chat_cards::read_cards(session_dir);
    planning_chat_success(session, messages, markdown, cards)
}
```

그리고 `load_latest_planning_chat_session`의 `spawn_blocking` 본문을 아래로 교체:

```rust
    tauri::async_runtime::spawn_blocking(move || {
        let Some(session_path) = latest_chat_session_file(&project_dir) else {
            return planning_chat_error("planning chat session not found");
        };
        let session_dir = session_path
            .parent()
            .map(|path| path.to_path_buf())
            .unwrap_or_else(|| project_dir.clone());
        load_session_from_dir(&project_dir, &session_dir)
    })
    .await
    .unwrap_or_else(|error| planning_chat_error(error.to_string()))
```

- [ ] **Step 2: 빌드 + 회귀 확인 (리팩터는 동작 동일)**

Run: `cd vibelign-gui/src-tauri && cargo build && cargo test planning_chat`
Expected: PASS. 경고 없음. (load_latest 동작은 이전과 동일 — 디렉터리 기준으로 동일 파일 읽음.)

- [ ] **Step 3: load_planning_chat_session 명령 추가**

`load_latest_planning_chat_session` 함수 아래에 추가:

```rust
#[tauri::command]
pub(crate) async fn load_planning_chat_session(
    project_dir: String,
    session_id: String,
) -> PlanningChatSessionResponse {
    let project_dir = PathBuf::from(project_dir);
    if !project_dir.is_absolute() {
        return planning_chat_error("projectDir must be absolute");
    }
    if session_id.trim().is_empty() {
        return planning_chat_error("sessionId is required");
    }
    tauri::async_runtime::spawn_blocking(move || {
        let session_dir = planning_dir(&project_dir).join(&session_id);
        if !session_dir.join("session.json").exists() {
            return planning_chat_error("planning chat session not found");
        }
        load_session_from_dir(&project_dir, &session_dir)
    })
    .await
    .unwrap_or_else(|error| planning_chat_error(error.to_string()))
}
```

- [ ] **Step 4: 복원 테스트 추가**

`tests` 모듈(Task 1에서 만든 것)에 추가:

```rust
    #[test]
    fn load_session_from_dir_reads_messages_and_cards() {
        let root = tempfile::tempdir().expect("root");
        write_session(root.path(), "chat_9", "복원 테스트", Some("plans/x.md"), 3, 2);
        let session_dir = root.path().join(".vibelign/planning/chat_9");

        let response = load_session_from_dir(root.path(), &session_dir);

        let json = serde_json::to_value(&response).expect("json");
        assert_eq!(json["ok"], true);
        assert_eq!(json["sessionId"], "chat_9");
        assert_eq!(json["messages"].as_array().expect("messages").len(), 3);
        assert_eq!(json["cards"].as_array().expect("cards").len(), 2);
        assert_eq!(json["outputPath"], "plans/x.md");
    }
```

- [ ] **Step 5: lib.rs 등록**

`lib.rs`의 `commands::planning_chat::list_planning_chat_sessions,`(Task 1에서 추가) 다음 줄에 추가:

```rust
            commands::planning_chat::load_planning_chat_session,
```

- [ ] **Step 6: 테스트 + 빌드**

Run: `cd vibelign-gui/src-tauri && cargo test planning_chat && cargo build`
Expected: PASS (list 2 + load 1 + skip 1), 빌드 성공.

- [ ] **Step 7: 커밋**

```bash
git add vibelign-gui/src-tauri/src/commands/planning_chat.rs vibelign-gui/src-tauri/src/lib.rs
git commit -m "feat(planning): load_planning_chat_session by id (shared load_session_from_dir)"
```

> 참고: `read_json`/`write_json`이 둘 다 import돼 있어야 한다(write_json은 기존 명령이 사용). 미사용 경고가 나면 무시하지 말고 실제 사용처를 확인 — 이 task는 `write_json`을 추가/제거하지 않는다.

---

## Task 3: TS 타입 + 래퍼

**Files:**
- Modify: `vibelign-gui/src/lib/vib/types.ts`
- Modify: `vibelign-gui/src/lib/vib/planning.ts`
- Modify: `vibelign-gui/src/lib/vib/index.ts`

- [ ] **Step 1: 타입 추가**

`types.ts`의 `PlanningChatSessionResponse` 인터페이스 아래에 추가:

```typescript
export interface PlanningSessionSummary {
  sessionId: string;
  title: string;
  outputPath?: string | null;
  saved: boolean;
  createdAt: string;
  messageCount: number;
  cardCount: number;
}
```

- [ ] **Step 2: 래퍼 추가**

`planning.ts`의 type import 목록에 `PlanningSessionSummary`를 추가하고, 파일 끝의
`// === ANCHOR: PLANNING_END ===` 주석 **바로 위**에 추가:

```typescript
// === ANCHOR: PLANNING_SESSIONS_START ===
export function listPlanningChatSessions(projectDir: string): Promise<PlanningSessionSummary[]> {
  return invoke<PlanningSessionSummary[]>("list_planning_chat_sessions", { projectDir });
}

export function loadPlanningChatSession(projectDir: string, sessionId: string): Promise<PlanningChatSessionResponse> {
  return invoke<PlanningChatSessionResponse>("load_planning_chat_session", { projectDir, sessionId });
}
// === ANCHOR: PLANNING_SESSIONS_END ===
```

(`PlanningChatSessionResponse`는 이미 import돼 있다.)

- [ ] **Step 3: index.ts export**

`index.ts`의 `export { ... } from "./planning";` 목록에 `listPlanningChatSessions, loadPlanningChatSession`를 추가.

- [ ] **Step 4: 타입체크**

Run: `cd vibelign-gui && npx tsc --noEmit`
Expected: 에러 없음.

- [ ] **Step 5: 커밋**

```bash
git add vibelign-gui/src/lib/vib/types.ts vibelign-gui/src/lib/vib/planning.ts vibelign-gui/src/lib/vib/index.ts
git commit -m "feat(planning): session list/load invoke wrappers + summary type"
```

---

## Task 4: 피커 모달 (TS)

**Files:**
- Create: `vibelign-gui/src/pages/planning/PlanningSessionPicker.tsx`

- [ ] **Step 1: 컴포넌트 구현**

`PlanningSessionPicker.tsx`:

```tsx
import { useEffect, useState } from "react";

import { listPlanningChatSessions } from "../../lib/vib";
import type { PlanningSessionSummary } from "../../lib/vib/types";

interface PlanningSessionPickerProps {
  readonly projectDir: string;
  readonly onSelect: (sessionId: string) => void;
  readonly onClose: () => void;
}

function fileName(path: string): string {
  const normalized = path.replace(/\\/g, "/");
  const slash = normalized.lastIndexOf("/");
  return slash >= 0 ? normalized.slice(slash + 1) : normalized;
}

export function PlanningSessionPicker({ projectDir, onSelect, onClose }: PlanningSessionPickerProps) {
  const [sessions, setSessions] = useState<PlanningSessionSummary[] | null>(null);

  useEffect(() => {
    let active = true;
    void listPlanningChatSessions(projectDir).then((rows) => {
      if (active) {
        setSessions(rows);
      }
    });
    return () => {
      active = false;
    };
  }, [projectDir]);

  return (
    <div
      style={{ position: "fixed", inset: 0, background: "rgba(0,0,0,0.6)", zIndex: 9999, display: "flex", alignItems: "center", justifyContent: "center", padding: 24 }}
      onClick={onClose}
    >
      <div
        style={{ background: "#FEFBF0", border: "2px solid #1A1A1A", boxShadow: "8px 8px 0 #1A1A1A", width: "100%", maxWidth: 520, maxHeight: "80vh", display: "flex", flexDirection: "column" }}
        onClick={(event) => event.stopPropagation()}
      >
        <div style={{ background: "#1A1A1A", padding: "12px 18px" }}>
          <span style={{ fontFamily: "IBM Plex Mono, monospace", fontWeight: 700, fontSize: 13, color: "#fff", letterSpacing: 1 }}>이전 기획 불러오기</span>
        </div>
        <div style={{ padding: 16, overflow: "auto", display: "grid", gap: 8 }}>
          {sessions === null && <div style={{ fontSize: 12, opacity: 0.7 }}>불러오는 중…</div>}
          {sessions !== null && sessions.length === 0 && (
            <div style={{ fontSize: 12, opacity: 0.7 }}>아직 저장된 기획이 없어요.</div>
          )}
          {sessions?.map((session) => (
            <button
              key={session.sessionId}
              type="button"
              onClick={() => onSelect(session.sessionId)}
              style={{ textAlign: "left", border: "2px solid #1A1A1A", background: "#fff", padding: "10px 12px", cursor: "pointer", display: "grid", gap: 4 }}
            >
              <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", gap: 8 }}>
                <strong style={{ fontSize: 12 }}>{session.title || "(제목 없음)"}</strong>
                <span
                  style={{
                    fontSize: 10,
                    fontWeight: 800,
                    padding: "2px 8px",
                    border: "1.5px solid #1A1A1A",
                    whiteSpace: "nowrap",
                    background: session.saved ? "#1E9E5A" : "#E6E3D8",
                    color: session.saved ? "#fff" : "#6B6657",
                  }}
                >
                  {session.saved ? "✓ 저장됨" : "작성중"}
                </span>
              </div>
              <div style={{ fontSize: 11, opacity: 0.75 }}>
                {session.outputPath ? `${fileName(session.outputPath)} · ` : ""}
                대화 {session.messageCount} · 카드 {session.cardCount}
              </div>
            </button>
          ))}
        </div>
        <div style={{ padding: "12px 18px", borderTop: "2px solid #1A1A1A", display: "flex", justifyContent: "flex-end" }}>
          <button className="btn btn-ghost btn-sm" type="button" onClick={onClose}>닫기</button>
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: 타입체크**

Run: `cd vibelign-gui && npx tsc --noEmit`
Expected: 에러 없음.

- [ ] **Step 3: 커밋**

```bash
git add vibelign-gui/src/pages/planning/PlanningSessionPicker.tsx
git commit -m "feat(planning): session picker modal"
```

---

## Task 5: App + Home 배선 (TS)

**Files:**
- Modify: `vibelign-gui/src/App.tsx`
- Modify: `vibelign-gui/src/pages/Home.tsx`

- [ ] **Step 1: App.tsx — import + 상태 + 복원 함수**

`App.tsx` 상단 import에 추가:

```tsx
import { PlanningSessionPicker } from "./pages/planning/PlanningSessionPicker";
```
그리고 `lib/vib` import 목록(기존 `loadLatestPlanningChatSession` 등이 있는 줄)에 `loadPlanningChatSession`를 추가.

컴포넌트 본문, 다른 `useState` 근처에 추가:

```tsx
  const [showSessionPicker, setShowSessionPicker] = useState(false);
```

`openPlanningRoom` 함수 근처에 복원 함수 추가:

```tsx
  async function resumeSession(sessionId: string) {
    if (!projectDir) return;
    setShowSessionPicker(false);
    const result = await loadPlanningChatSession(projectDir, sessionId);
    if (!result.ok) return;
    setReviewSourcePath(null);
    setPlanningPrompt(result.prompt || result.messages[0]?.content || "기획방");
    setPlanningResult(result);
    setPage("planning");
  }
```

- [ ] **Step 2: App.tsx — Home에 prop 전달 + 모달 렌더**

`<Home ... />`를 렌더하는 곳(`page === "home"`)의 props에 추가:

```tsx
onOpenPlanningHistory={() => setShowSessionPicker(true)}
```

그리고 최상위 렌더 트리(반환 JSX의 바깥쪽, 다른 전역 오버레이가 있다면 그 근처)에 모달을 추가:

```tsx
        {showSessionPicker && projectDir && (
          <PlanningSessionPicker
            projectDir={projectDir}
            onSelect={(sessionId) => void resumeSession(sessionId)}
            onClose={() => setShowSessionPicker(false)}
          />
        )}
```

- [ ] **Step 3: Home.tsx — prop 타입 + 버튼**

`Home.tsx`의 props 인터페이스에 추가:

```tsx
  readonly onOpenPlanningHistory?: () => void;
```

구조분해 목록에 `onOpenPlanningHistory`를 추가하고, 기획 시작 UI(아이디어 입력/시작 버튼) 근처에
버튼을 추가(조건부 — 핸들러가 있을 때만):

```tsx
{onOpenPlanningHistory && (
  <button type="button" className="btn btn-ghost btn-sm" onClick={onOpenPlanningHistory} style={{ fontSize: 11 }}>
    이전 기획 불러오기
  </button>
)}
```

- [ ] **Step 4: 타입체크 + 빌드**

Run: `cd vibelign-gui && npx tsc --noEmit`
Expected: 에러 없음.

- [ ] **Step 5: 커밋**

```bash
git add vibelign-gui/src/App.tsx vibelign-gui/src/pages/Home.tsx
git commit -m "feat(planning): wire session picker (home button + resume) into app"
```

> 주의: `App.tsx`/`Home.tsx`는 큰 파일이다. 먼저 읽어 정확한 삽입 위치를 찾을 것. `projectDir`,
> `setPlanningResult`, `setPlanningPrompt`, `setReviewSourcePath`, `setPage`, `loadLatestPlanningChatSession`
> import은 이미 App.tsx에 존재한다(기존 `loadProjectPlanning`/`openPlanningRoom`가 사용). Home의 기획
> 시작 영역이 어디인지 `onStartPlanning` 사용처로 찾을 것.

---

## Task 6: 라이브 수동 검증

- [ ] **Step 1: 앱에서 확인**

`npm run tauri dev`로 띄우고:
1. 기획방에서 대화 몇 줄(카드 생기게) → 기획안 저장.
2. 홈으로 → **"이전 기획 불러오기"** 클릭 → 모달에 그 세션이 **✓ 저장됨** 배지·대화/카드 수와 함께 뜸.
3. 행 클릭 → 기획방 진입, **대화·카드가 그대로 복원**됨.
4. 이어서 수정 → 저장(덮어쓰기) 동작 확인.
5. 저장 안 한 다른 세션도 **작성중** 배지로 목록에 뜨고 복원되는지.

- [ ] **Step 2: 결과 기록**

복원이 대화+카드까지 정확하면 통과.

---

## Self-Review (작성자 점검)

**Spec 커버리지:**
- §1 list 명령 + load-by-id + load_session_from_dir 리팩터 → Task 1·2. ✅
- §2 피커·래퍼·타입 → Task 3·4. ✅
- §3 App/Home 배선 → Task 5. ✅
- §6 테스트 → Task 1·2 단위 + Task 6 수동. ✅
- §9 크로스플랫폼 → Task 4 `fileName`이 `\`/`/` 모두 분리(`replace(/\\/g,"/")`). ✅

**플레이스홀더 스캔:** 없음. 모든 코드 스텝 완전 코드. ✅

**타입 일관성:** `PlanningSessionSummary`(Rust camelCase serde ↔ TS: sessionId/title/outputPath/saved/createdAt/messageCount/cardCount) 일치. `list_planning_chat_sessions`/`load_planning_chat_session`/`load_session_from_dir`/`listPlanningChatSessions`/`loadPlanningChatSession`/`resumeSession`/`onOpenPlanningHistory` 시그니처가 정의·사용에서 일치. ✅

**회귀 주의:** Task 2의 `load_session_from_dir` 리팩터는 `load_latest` 동작을 바꾸지 않아야 함 — Task 2 Step 2에서 `cargo test planning_chat` + build로 확인.
