# 강 PD 가이드 레이어 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 초보자가 "지금 뭘 해야 하는지" 항상 알 수 있도록, 6단계 표준 여정을 추론해 상단 스트립·홈 허브 카드·사용법 따라하기로 안내하는 가이드 레이어를 구현한다.

**Architecture:** `stages.ts`와 같은 패턴의 순수 모듈 `guide.ts`(여정 정의 + 추론 + override + baseline 비교, TDD)를 토대로, React 바인딩 훅(`useGuide.ts`)과 표시 컴포넌트 2개(`GuideStrip`, `JourneyHowto`)를 만들고 App.tsx에서 조립한다. 기존 상태 스트립(App.tsx:337-355)은 GuideStrip이 대체·통합한다. **신호 배선은 v1 범위**: `changedFileCount`는 git changed-set(`listChangedFiles`)을 최신 체크포인트 시점 baseline(localStorage)과 비교, `guardStatus`는 홈 guard 실행 콜백 — 전부 기존 프론트 API라 백엔드(Rust/Python) 변경 0.

**Tech Stack:** React + TypeScript (Tauri GUI), vitest(순수 함수), tsc + tauri dev(시각 검증), localStorage(영속).

> **검증 방침(중요):** 이 프로젝트는 UI 레이아웃에 단위테스트를 요구하지 않는다(CLAUDE.md "가장 작은 패치"). **순수 로직(`guide.ts`)만 TDD**, 컴포넌트·통합은 `npx tsc --noEmit` + 육안 렌더.

**근거 spec:** `plans/2026-06-10-강PD-가이드레이어-design.md` (커밋 cbb64e3 + 2026-06-10 critic 리뷰 반영판)
**현재 코드 기준:** 744b8d6. App.tsx 상태 스트립=337~355행, `navigate()`=187~190행, `planningStatus`=284~288행, backupList effect=121~144행(의존성 `[projectDir]`뿐 — **체크포인트 저장해도 갱신 안 되는 갭, Task 4에서 page 의존성 추가로 해소**), Home(manual) 호출=378행, Settings 호출=395행.
**critic 리뷰 반영 사항(v2):**
1. **신호 배선 v1 승격(M1·M2)** — `changedFileCount`=마지막 체크포인트 baseline 대비 대칭차(`listChangedFiles`+`backupList`+localStorage, Task 4). git HEAD 절대 기준은 커밋 안 하는 초보자에게 단조증가라 부적합. `guardStatus`=홈 `handleRunGuard`(Home.tsx:63~78) 결과 콜백(엔진 `status` 필드 기준, 어휘는 pass/warn/fail — **pass만 "ok"**). ⚠️ **비-git 프로젝트는 null이 아니라 0** — `git_status.rs`는 비-git·git 미설치에 빈 목록을 반환(에러 아님)하므로 `.catch → null` 경로는 진짜 예외만 탄다. 비-git은 "영원히 변경 0" → 자동 5️⃣ 전환 없음, 4️⃣ affordance가 유일한 검증 진입로(critic 재리뷰 C1).
2. **타입 사실(C1)** — `Home.onNavigate`는 `(page: "backups") => void`로 좁혀져 있음(Home.tsx:24, AdvancedHomeCards.tsx:42 동일). Task 6에서 `(page: Page) => void`로 확장(leaf인 CheckpointCard/UndoCard는 `"backups"`만 호출하므로 안전).
3. **5️⃣ 목적지 정정** — guard 버튼은 진단 탭에 없고 홈에만 있음 → 5️⃣ targetPage="home".
4. **스트립 시인성(M3)** — 가이드 활성 구간 13px + `#FBBF24` 강조.
5. **북극성 점검 반영(v5)** — ① 4️⃣ AI 도구 미보유 분기: `detectInstalledTools()` 재사용(0개 확정 시에만), 스트립 "설치 도움받기 →"(설정 이동) + 하우투 ⑤. 4️⃣는 가이드가 앱 밖으로 내보내는 유일한 지점 = 입문자 최대 이탈 절벽인데 기존 신호가 미연결이었음. ② 첫 사이클 완주 축하: 6️⃣→4️⃣ 전환 감지(`shouldCelebrate`, TDD), 프로젝트당 1회. ③ 사용자 노출 카피에서 영문 용어 제거(guard→'상태 확인', undo→'되돌리기' — UI 라벨과 동일 어휘).

---

## File Structure

**신규:**
- `vibelign-gui/src/lib/nav/guide.ts` — 여정 6단계 정의(`JOURNEY_STEPS`), `inferStep()`, `resolveOverride()`, `cardStepState()`, `latestCheckpointId()`, `countChangesSinceBaseline()`, `guideRelevantPaths()`, localStorage 키. 순수.
- `vibelign-gui/src/lib/nav/guide.test.ts` — vitest.
- `vibelign-gui/src/lib/nav/useGuide.ts` — guide.ts의 React 바인딩(localStorage 영속). 얇은 훅.
- `vibelign-gui/src/components/nav/GuideStrip.tsx` — 상단 스트립(가이드 + 기존 상태 신호 통합, 4️⃣ affordance 포함).
- `vibelign-gui/src/components/nav/JourneyHowto.tsx` — 사용법 "순서대로 따라하기" 아코디언.

**수정:**
- `vibelign-gui/src/App.tsx` — 신호 배선(`backupLoaded`·최신 체크포인트·changed baseline·guardStatus), `useGuide` 조립, 상태 스트립 교체, props 전달.
- `vibelign-gui/src/pages/Home.tsx` — `onGuardResult` 콜백(Task 4), `onNavigate` 타입 확장 + `guideStep` prop + manual_list 뷰에 JourneyHowto(Task 6).
- `vibelign-gui/src/components/home/AdvancedHomeCards.tsx` — `onNavigate` 타입 확장(Task 6, 타입만).
- `vibelign-gui/src/components/nav/StageHubCards.tsx` — `currentStep` prop + 차례 표시.
- `vibelign-gui/src/pages/Settings.tsx` — 초보자 가이드 ON/OFF 토글 카드.

---

## Task 1: 여정·추론 순수 모듈 (`guide.ts`) — TDD

**Files:**
- Create: `vibelign-gui/src/lib/nav/guide.ts`
- Test: `vibelign-gui/src/lib/nav/guide.test.ts`

- [ ] **Step 1: 실패하는 테스트 작성**

`vibelign-gui/src/lib/nav/guide.test.ts`:
```ts
import { describe, it, expect } from "vitest";
import {
  JOURNEY_STEPS,
  journeyStep,
  inferStep,
  resolveOverride,
  cardStepState,
  guideOverrideKey,
  guideBaselineKey,
  guideCelebratedKey,
  latestCheckpointId,
  countChangesSinceBaseline,
  guideRelevantPaths,
  shouldCelebrate,
  type GuideSignals,
} from "./guide";

const base: GuideSignals = {
  hasPlanDoc: false,
  planningPending: false,
  hasCheckpoint: false,
  changedFileCount: null,
  guardStatus: null,
};

describe("JOURNEY_STEPS", () => {
  it("6단계가 순서대로", () => {
    expect(JOURNEY_STEPS.map((j) => j.step)).toEqual([1, 2, 3, 4, 5, 6]);
  });
  it("journeyStep은 step 번호로 조회", () => {
    expect(journeyStep(3).label).toBe("체크포인트 저장");
    expect(journeyStep(4).targetPage).toBe("code");
  });
  it("5️⃣ 목적지는 홈 — guard 버튼은 진단 탭에 없음 (spec §2 정정)", () => {
    expect(journeyStep(5).targetPage).toBe("home");
  });
  it("1️⃣은 자동 단계 — 이동 목적지 없음", () => {
    expect(journeyStep(1).targetPage).toBeNull();
    expect(journeyStep(1).cardStage).toBeNull();
  });
});

describe("inferStep", () => {
  it("기획안 없으면 2", () => {
    expect(inferStep(base)).toBe(2);
  });
  it("기획 진행 중이면 기획안 있어도 2", () => {
    expect(inferStep({ ...base, hasPlanDoc: true, planningPending: true })).toBe(2);
  });
  it("기획안 있고 체크포인트 없으면 3", () => {
    expect(inferStep({ ...base, hasPlanDoc: true })).toBe(3);
  });
  it("기획안+체크포인트, 추가 신호 없으면 4", () => {
    expect(inferStep({ ...base, hasPlanDoc: true, hasCheckpoint: true })).toBe(4);
  });
  it("변경 감지되면 5 (뒤 단계 우선 — spec §4-1)", () => {
    expect(inferStep({ ...base, hasPlanDoc: true, hasCheckpoint: true, changedFileCount: 3 })).toBe(5);
  });
  it("변경 + guard 통과면 6", () => {
    expect(
      inferStep({ ...base, hasPlanDoc: true, hasCheckpoint: true, changedFileCount: 3, guardStatus: "ok" }),
    ).toBe(6);
  });
  it("체크포인트 없으면 변경 감지돼도 5로 점프하지 않음", () => {
    expect(inferStep({ ...base, hasPlanDoc: true, changedFileCount: 3 })).toBe(3);
  });
  it("신호 사용 불가(null=조회 예외)는 보수적 생략 — 4 유지 (spec §4-5)", () => {
    expect(
      inferStep({ ...base, hasPlanDoc: true, hasCheckpoint: true, changedFileCount: null, guardStatus: null }),
    ).toBe(4);
  });
  it("변경 0도 4 유지 — 비-git 프로젝트는 항상 이 값(null 아님, spec §3.1)", () => {
    expect(
      inferStep({ ...base, hasPlanDoc: true, hasCheckpoint: true, changedFileCount: 0, guardStatus: null }),
    ).toBe(4);
  });
});

describe("resolveOverride", () => {
  it("override 없으면 추론값", () => {
    expect(resolveOverride(null, 3)).toBe(3);
  });
  it("추론이 기록 시점과 같으면 override 유지", () => {
    expect(resolveOverride({ step: 5, baseInferred: 3 }, 3)).toBe(5);
  });
  it("추론이 바뀌면 override 자동 해제 (spec §4-2)", () => {
    expect(resolveOverride({ step: 5, baseInferred: 3 }, 4)).toBe(4);
  });
});

describe("cardStepState", () => {
  it("현재 단계가 속한 카드는 now", () => {
    expect(cardStepState("planning", 2)).toBe("now");
    expect(cardStepState("maintain", 3)).toBe("now");
    expect(cardStepState("develop", 4)).toBe("now");
    expect(cardStepState("maintain", 6)).toBe("now");
  });
  it("5️⃣은 홈에서 하는 행동이라 카드 밖 — 어느 카드도 now가 아님", () => {
    expect(cardStepState("planning", 5)).toBe("done");
    expect(cardStepState("develop", 5)).toBe("done");
    expect(cardStepState("maintain", 5)).toBe("upcoming"); // 6️⃣이 남아 있음
  });
  it("카드의 모든 단계가 지났으면 done", () => {
    expect(cardStepState("planning", 3)).toBe("done");
  });
  it("남은 단계가 있으면 upcoming (유지보수 카드=3·6이라 4에서도 upcoming)", () => {
    expect(cardStepState("develop", 3)).toBe("upcoming");
    expect(cardStepState("maintain", 4)).toBe("upcoming");
  });
});

describe("guideOverrideKey / guideBaselineKey", () => {
  it("프로젝트 경로별로 다른 키 (spec §4 진행 분리)", () => {
    expect(guideOverrideKey("/a")).not.toBe(guideOverrideKey("/b"));
    expect(guideBaselineKey("/a")).not.toBe(guideBaselineKey("/b"));
    expect(guideOverrideKey("/a")).not.toBe(guideBaselineKey("/a"));
  });
});

describe("latestCheckpointId", () => {
  it("createdAt 내림차순 최신 — backupList 정렬 순서를 가정하지 않음", () => {
    expect(
      latestCheckpointId([
        { id: "old", createdAt: "2026-06-01T10:00:00Z" },
        { id: "new", createdAt: "2026-06-10T10:00:00Z" },
      ]),
    ).toBe("new");
  });
  it("빈 목록이면 null, createdAt 없는 항목은 뒤로", () => {
    expect(latestCheckpointId([])).toBeNull();
    expect(
      latestCheckpointId([{ id: "noDate" }, { id: "dated", createdAt: "2026-06-10T10:00:00Z" }]),
    ).toBe("dated");
  });
});

describe("countChangesSinceBaseline", () => {
  it("대칭차 — 추가·삭제·복원 모두 변경으로 센다", () => {
    expect(countChangesSinceBaseline(["a.ts"], ["a.ts"])).toBe(0);
    expect(countChangesSinceBaseline(["a.ts"], ["a.ts", "b.ts"])).toBe(1); // AI가 b.ts 변경
    expect(countChangesSinceBaseline(["a.ts"], [])).toBe(1); // baseline에 있던 변경이 사라짐 = 체크포인트 시점과 달라진 것
    expect(countChangesSinceBaseline([], ["x.ts", "y.ts"])).toBe(2);
  });
  it("undo로 체크포인트 시점과 동일해지면 0 (baseline 자체가 그 시점 changed-set)", () => {
    expect(countChangesSinceBaseline(["a.ts", "b.ts"], ["b.ts", "a.ts"])).toBe(0); // 순서 무관
  });
});

describe("guideRelevantPaths", () => {
  it("vibelign/omc/git 메타데이터 경로 제외 — guard 리포트·체크포인트 churn 오인 방지", () => {
    expect(
      guideRelevantPaths([".vibelign/report.md", ".omc/state/x.json", ".git/HEAD", "src/app.ts"]),
    ).toEqual(["src/app.ts"]);
  });
});

describe("shouldCelebrate", () => {
  it("6→4 전환(저장으로 루프 닫힘) + 미축하일 때만 true", () => {
    expect(shouldCelebrate(6, 4, false)).toBe(true);
    expect(shouldCelebrate(6, 4, true)).toBe(false); // 프로젝트당 1회
    expect(shouldCelebrate(5, 4, false)).toBe(false);
    expect(shouldCelebrate(6, 5, false)).toBe(false);
  });
  it("앱 시작 직후(prev=null)·신호 로딩 전(next=null)엔 발화 금지", () => {
    expect(shouldCelebrate(null, 4, false)).toBe(false);
    expect(shouldCelebrate(6, null, false)).toBe(false);
  });
  it("guideCelebratedKey는 프로젝트 경로별", () => {
    expect(guideCelebratedKey("/a")).not.toBe(guideCelebratedKey("/b"));
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd vibelign-gui && npx vitest run src/lib/nav/guide.test.ts`
Expected: FAIL — `Cannot find module './guide'`

- [ ] **Step 3: 최소 구현**

`vibelign-gui/src/lib/nav/guide.ts`:
```ts
// === ANCHOR: NAV_GUIDE_START ===
// 강 PD 가이드 레이어 — 초보자 여정(6단계)의 단일 소스. 순수 데이터/함수.
// 근거: plans/2026-06-10-강PD-가이드레이어-design.md
import type { Page, Stage } from "./stages";

export type GuideStep = 1 | 2 | 3 | 4 | 5 | 6;
/** inferStep 반환 범위 — 1️⃣(프로젝트 열기)은 가이드가 뜨는 시점엔 항상 완료라 제외. */
export type ActiveGuideStep = 2 | 3 | 4 | 5 | 6;

export interface JourneyStep {
  step: GuideStep;
  icon: string;
  label: string;
  /** 스트립·카드에 보이는 행동 한 줄 */
  shortAction: string;
  /** 사용법 따라하기 아코디언의 상세 하우투 */
  howto: string[];
  /** "이동 →" 목적지. 1️⃣은 자동 단계라 null. */
  targetPage: Page | null;
  /** 이 단계가 속한 홈 허브 카드. 1️⃣은 카드 밖(null). */
  cardStage: Stage | null;
}

export const JOURNEY_STEPS: JourneyStep[] = [
  {
    step: 1,
    icon: "1️⃣",
    label: "프로젝트 열기",
    shortAction: "폴더를 고르면 준비 끝 — 감시가 자동으로 켜져요",
    howto: [
      "① 첫 화면에서 프로젝트 폴더를 선택해요",
      "② 코드맵과 앵커가 자동으로 준비돼요",
      "③ 감시(watch)가 자동으로 켜져요 — 코드 변경을 지켜보다 위반을 알려줘요 (백업은 아니에요)",
    ],
    targetPage: null,
    cardStage: null,
  },
  {
    step: 2,
    icon: "2️⃣",
    label: "기획하기",
    shortAction: "기획방에서 AI와 대화하며 기획안을 확정하세요",
    howto: [
      "① 기획 탭(기획방)으로 가요",
      "② \"무엇을 만들고 싶은지\" 한 줄로 적어요",
      "③ AI 페르소나들과 대화하며 생각을 다듬어요",
      "④ 기획안이 마음에 들면 확정(저장)해요",
    ],
    targetPage: "planning",
    cardStage: "planning",
  },
  {
    step: 3,
    icon: "3️⃣",
    label: "체크포인트 저장",
    shortAction: "AI에게 시키기 전에 안전 저장 — 망쳐도 되돌아올 수 있게",
    howto: [
      "① 유지보수 → 백업 탭으로 가요",
      "② \"체크포인트 저장\" 버튼을 눌러요",
      "③ 무슨 작업 전인지 설명을 짧게 적으면 끝!",
    ],
    targetPage: "backups",
    cardStage: "maintain",
  },
  {
    step: 4,
    icon: "4️⃣",
    label: "AI에게 작업 시키기",
    shortAction: "지시문을 복사해 AI 도구(Claude Code 등)에 붙여넣으세요",
    howto: [
      "① 개발 → 코드탐색 탭으로 가요",
      "② 기획안 기반 지시문을 복사해요",
      "③ Claude Code 같은 AI 도구에 붙여넣어요",
      "④ AI가 코드를 다 쓰면 다음 단계로!",
      "⑤ 아직 AI 도구가 없나요? 설정의 'AI 도구 설정'에서 설치를 도와드려요",
    ],
    targetPage: "code",
    cardStage: "develop",
  },
  {
    step: 5,
    icon: "5️⃣",
    label: "결과 검증",
    // 사용자 노출 카피에 "guard" 영문 노출 금지 — UI 버튼 라벨('상태 확인')과 동일 어휘 사용 (북극성 점검 #3)
    shortAction: "홈의 '상태 확인'으로 AI가 약속한 범위만 고쳤는지 확인하세요",
    howto: [
      "① 홈으로 가요",
      "② '상태 확인' 버튼을 눌러요 — AI가 약속한 범위만 고쳤는지 검사해줘요",
      "③ 문제가 있으면 진단으로 안내돼요 — 고치는 길을 알려줘요",
      "④ 결과가 마음에 안 들면? 검사 없이 백업 탭에서 '되돌리기'로 3️⃣ 시점으로 돌아가도 돼요",
    ],
    // guard 버튼은 진단 탭에 없고 홈에만 있다(Home.tsx handleRunGuard) — spec §2 정정.
    targetPage: "home",
    cardStage: null,
  },
  {
    step: 6,
    icon: "6️⃣",
    label: "저장 또는 되돌리기",
    shortAction: "잘 됐으면 체크포인트 저장, 망쳤으면 되돌리기",
    howto: [
      "① 결과가 마음에 들면 → 백업에서 체크포인트를 저장해요",
      "② 망쳤으면 → '되돌리기'로 3️⃣ 시점으로 돌아가요 — 없던 일로 만들 수 있어요",
      "③ 다음 작업이 있으면 4️⃣로 돌아가 반복!",
    ],
    targetPage: "backups",
    cardStage: "maintain",
  },
];

export function journeyStep(step: GuideStep): JourneyStep {
  return JOURNEY_STEPS[step - 1];
}

export interface GuideSignals {
  /** 확정 기획안 존재 (App: planningResult?.outputPath 기준) */
  hasPlanDoc: boolean;
  /** 기획 대화 진행 중(pending 메시지 존재) */
  planningPending: boolean;
  hasCheckpoint: boolean;
  /**
   * 마지막 체크포인트 이후 달라진 파일 수 — git changed-set을 체크포인트 시점
   * baseline과 대칭차 비교(countChangesSinceBaseline). git HEAD 절대 기준 아님.
   * null = 조회가 예외로 실패한 경우만 → 보수적 생략(spec §4-5).
   * 주의: 비-git 프로젝트는 null이 아니라 0 — git_status가 빈 목록을 반환(에러 아님)해
   * 자동 5️⃣ 전환이 없고, 4️⃣ affordance가 유일한 검증 진입로(spec §3.1).
   */
  changedFileCount: number | null;
  /** 마지막 변경 이후 guard 결과(홈 '상태 확인' 콜백). 변경 수 증가 시에만 App이 null로 리셋(spec §3.1). */
  guardStatus: "ok" | "issue" | null;
}

/** 신호 → 현재 단계 추론. 충돌 시 뒤 단계 우선(spec §4-1). 1️⃣은 반환하지 않음. */
export function inferStep(s: GuideSignals): ActiveGuideStep {
  let step: ActiveGuideStep = !s.hasPlanDoc || s.planningPending ? 2 : !s.hasCheckpoint ? 3 : 4;
  if (s.hasCheckpoint && s.changedFileCount !== null && s.changedFileCount > 0) {
    step = s.guardStatus === "ok" ? 6 : 5;
  }
  return step;
}

export interface GuideOverride {
  step: ActiveGuideStep;
  /** override 기록 시점의 추론값 — 추론이 바뀌면 자동 해제(spec §4-2) */
  baseInferred: ActiveGuideStep;
}

/** 수동 보정 적용: 추론이 기록 시점과 같을 때만 override 유지. */
export function resolveOverride(
  override: GuideOverride | null,
  inferred: ActiveGuideStep,
): ActiveGuideStep {
  if (override && override.baseInferred === inferred) return override.step;
  return inferred;
}

export type CardStepState = "now" | "done" | "upcoming";

/** 홈 허브 카드(3장)가 현재 여정에서 갖는 상태. 기획=[2], 개발=[4], 유지보수=[3,6]. 5️⃣은 홈 행동이라 카드 밖. */
export function cardStepState(stage: Stage, currentStep: ActiveGuideStep): CardStepState {
  const steps = JOURNEY_STEPS.filter((j) => j.cardStage === stage).map((j) => j.step);
  if (steps.includes(currentStep)) return "now";
  if (steps.every((st) => st < currentStep)) return "done";
  return "upcoming";
}

// ── 신호 배선 보조 (App.tsx가 사용, 순수) ──────────────────────────────────

/** backupList 결과에서 최신 체크포인트 id. 응답 정렬 순서를 가정하지 않는다. */
export function latestCheckpointId(
  backups: Array<{ id: string; createdAt?: string }>,
): string | null {
  if (backups.length === 0) return null;
  return [...backups].sort((a, b) => (b.createdAt ?? "").localeCompare(a.createdAt ?? ""))[0].id;
}

/** 체크포인트 시점 baseline과 현재 changed-set의 대칭차 — "체크포인트 이후 변경" 파일 수. */
export function countChangesSinceBaseline(baseline: string[], current: string[]): number {
  const b = new Set(baseline);
  const c = new Set(current);
  let n = 0;
  for (const p of c) if (!b.has(p)) n += 1;
  for (const p of b) if (!c.has(p)) n += 1;
  return n;
}

const GUIDE_IGNORED_PREFIXES = [".vibelign/", ".omc/", ".git/"];

/** 도구 메타데이터 경로 제외 — guard 리포트·체크포인트 메타 churn을 코드 변경으로 오인하지 않게. */
export function guideRelevantPaths(paths: string[]): string[] {
  return paths.filter((p) => !GUIDE_IGNORED_PREFIXES.some((pre) => p.startsWith(pre)));
}

/** 가이드 ON/OFF — 전역 1개 (숙련도는 프로젝트와 무관, spec §4) */
export const GUIDE_ENABLED_KEY = "vibelign.guide.enabled";

/** 수동 보정 — 프로젝트 경로별 (진행 위치가 프로젝트 간 새면 안 됨, spec §4) */
export function guideOverrideKey(projectDir: string): string {
  return `vibelign.guide.override.${projectDir}`;
}

/** 변경 감지 기준점 — 프로젝트 경로별. { checkpointId, changedPaths[] } JSON (spec §3.1) */
export function guideBaselineKey(projectDir: string): string {
  return `vibelign.guide.baseline.${projectDir}`;
}

/** 첫 사이클 완주 축하 1회성 플래그 — 프로젝트 경로별 (spec §3.2) */
export function guideCelebratedKey(projectDir: string): string {
  return `vibelign.guide.celebrated.${projectDir}`;
}

/**
 * 첫 사이클 완주 축하 발화 조건(spec §3.2) — 6️⃣→4️⃣ 전환(저장으로 루프가 닫힌 순간)에
 * 프로젝트당 1회. 앱 시작 직후(prev=null)·신호 로딩 전엔 발화 금지(오발 방지).
 */
export function shouldCelebrate(
  prev: ActiveGuideStep | null,
  next: ActiveGuideStep | null,
  alreadyCelebrated: boolean,
): boolean {
  return prev === 6 && next === 4 && !alreadyCelebrated;
}

/** guideBaselineKey 값의 형태. */
export interface GuideBaseline {
  checkpointId: string;
  changedPaths: string[];
}
// === ANCHOR: NAV_GUIDE_END ===
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `cd vibelign-gui && npx vitest run src/lib/nav/guide.test.ts`
Expected: PASS (모든 it 통과)

- [ ] **Step 5: 기존 테스트 회귀 확인**

Run: `cd vibelign-gui && npx vitest run src/lib/nav/`
Expected: guide.test.ts + stages.test.ts 모두 PASS

- [ ] **Step 6: 커밋**

```bash
git add vibelign-gui/src/lib/nav/guide.ts vibelign-gui/src/lib/nav/guide.test.ts
git commit -m "feat(guide): 초보자 여정 6단계 순수 모듈(guide.ts) — 추론·override·카드상태 + 테스트"
```

---

## Task 2: React 바인딩 훅 (`useGuide.ts`)

**Files:**
- Create: `vibelign-gui/src/lib/nav/useGuide.ts`

- [ ] **Step 1: 훅 작성**

`vibelign-gui/src/lib/nav/useGuide.ts`:
```ts
// === ANCHOR: NAV_USE_GUIDE_START ===
// guide.ts 순수 로직의 React 바인딩 — localStorage 영속(전역 토글 + 프로젝트별 override).
import { useEffect, useMemo, useState } from "react";
import {
  GUIDE_ENABLED_KEY,
  guideOverrideKey,
  inferStep,
  resolveOverride,
  type ActiveGuideStep,
  type GuideOverride,
  type GuideSignals,
} from "./guide";

function readOverride(projectDir: string): GuideOverride | null {
  try {
    const raw = localStorage.getItem(guideOverrideKey(projectDir));
    return raw ? (JSON.parse(raw) as GuideOverride) : null;
  } catch {
    return null;
  }
}

export interface GuideState {
  enabled: boolean;
  /** 현재 단계. null = 신호 로딩 전(렌더 금지, spec §4-4) 또는 프로젝트 없음 */
  step: ActiveGuideStep | null;
  setStep: (next: ActiveGuideStep) => void;
  setEnabled: (on: boolean) => void;
}

export function useGuide(
  projectDir: string | null,
  signals: GuideSignals,
  signalsReady: boolean,
): GuideState {
  const [enabled, setEnabledState] = useState(
    () => localStorage.getItem(GUIDE_ENABLED_KEY) !== "0",
  );
  const [override, setOverride] = useState<GuideOverride | null>(null);

  useEffect(() => {
    setOverride(projectDir ? readOverride(projectDir) : null);
  }, [projectDir]);

  const inferred = useMemo(() => inferStep(signals), [signals]);
  const step = resolveOverride(override, inferred);

  function setStep(next: ActiveGuideStep) {
    const o: GuideOverride = { step: next, baseInferred: inferred };
    setOverride(o);
    if (projectDir) localStorage.setItem(guideOverrideKey(projectDir), JSON.stringify(o));
  }

  function setEnabled(on: boolean) {
    setEnabledState(on);
    localStorage.setItem(GUIDE_ENABLED_KEY, on ? "1" : "0");
  }

  return {
    enabled,
    step: projectDir && signalsReady ? step : null,
    setStep,
    setEnabled,
  };
}
// === ANCHOR: NAV_USE_GUIDE_END ===
```

- [ ] **Step 2: 타입체크**

Run: `cd vibelign-gui && npx tsc --noEmit`
Expected: 통과 (TSC_EXIT=0). 아직 미사용이라 에러 없어야 함.

- [ ] **Step 3: 커밋**

```bash
git add vibelign-gui/src/lib/nav/useGuide.ts
git commit -m "feat(guide): useGuide 훅 — localStorage 영속(전역 토글·프로젝트별 override)"
```

---

## Task 3: 가이드 스트립 (`GuideStrip.tsx`)

**Files:**
- Create: `vibelign-gui/src/components/nav/GuideStrip.tsx`

- [ ] **Step 1: 컴포넌트 작성**

기존 App.tsx 337~355행 상태 스트립의 스타일·신호를 그대로 흡수한다(우측 유지).

`vibelign-gui/src/components/nav/GuideStrip.tsx`:
```tsx
// === ANCHOR: GUIDE_STRIP_START ===
import { journeyStep, type ActiveGuideStep } from "../../lib/nav/guide";
import { PAGE_LABELS, type Page } from "../../lib/nav/stages";

interface GuideStripProps {
  enabled: boolean;
  /** null = 신호 로딩 전 — 가이드 부분 미렌더(spec §4-4) */
  step: ActiveGuideStep | null;
  hasCheckpoint: boolean;
  planningPending: boolean;
  /** AI 도구 0개 "확정 탐지" 시에만 true — 탐지 실패·미완은 false(분기 미노출, spec §3.2) */
  aiToolMissing?: boolean;
  /** 첫 사이클 완주 축하 표시 중 — App이 6️⃣→4️⃣ 전환에서 1회 켠다(spec §3.2) */
  celebrating?: boolean;
  onNavigate: (page: Page) => void;
  onStepChange: (next: ActiveGuideStep) => void;
  onDisable: () => void;
  /** 도구 미보유 분기의 "설치 도움받기" 목적지 — 설정('AI 도구 설정') */
  onOpenSettings?: () => void;
  onCelebrateDismiss?: () => void;
}

/**
 * 탭바 아래 안내 줄 — 좌측 가이드(지금 할 일) + 우측 기존 상태 신호. A안 상태 스트립을 대체·통합.
 * 시인성(spec §3.2): 가이드 활성 시 13px + #FBBF24 강조·세로 패딩 6px, off 시 기존 얇은 줄(3px·11px).
 * 작업 탭 안에서 초보자에게 보이는 유일한 안내 표면이라 두 탭바 아래에서 묻히면 안 된다.
 */
export function GuideStrip({
  enabled,
  step,
  hasCheckpoint,
  planningPending,
  aiToolMissing = false,
  celebrating = false,
  onNavigate,
  onStepChange,
  onDisable,
  onOpenSettings,
  onCelebrateDismiss,
}: GuideStripProps) {
  const def = enabled && step ? journeyStep(step) : null;
  if (celebrating) {
    // 첫 사이클 완주 축하(spec §3.2) — 이 순간만큼은 가이드 줄 대신 완료 마디를 보여준다.
    return (
      <div
        style={{
          display: "flex",
          alignItems: "center",
          gap: 12,
          padding: "6px 12px",
          fontSize: 13,
          color: "#4DFF91",
          background: "#0E0E0E",
          borderBottom: "1px solid #1A1A1A",
        }}
      >
        <span>
          🎉 <b>첫 사이클 완주!</b> 기획부터 저장까지 해냈어요 — 이 흐름 그대로 반복하면 돼요
        </span>
        <button
          className="nav-tab"
          style={{ fontSize: 12, padding: "0 6px" }}
          onClick={() => onCelebrateDismiss?.()}
        >
          ×
        </button>
      </div>
    );
  }
  return (
    <div
      style={{
        display: "flex",
        alignItems: "center",
        gap: 16,
        padding: def ? "6px 12px" : "3px 12px",
        fontSize: 11,
        color: "#888",
        background: "#0E0E0E",
        borderBottom: "1px solid #1A1A1A",
      }}
    >
      {def && step && (
        <span style={{ display: "flex", alignItems: "center", gap: 8, fontSize: 13 }}>
          <span style={{ color: "#ccc" }}>
            🧭 지금:{" "}
            <span style={{ color: "#FBBF24", fontWeight: 700 }}>
              {def.icon} {def.label}
            </span>{" "}
            — {def.shortAction}
          </span>
          {def.targetPage && (
            <button
              className="nav-tab"
              style={{ fontSize: 12, padding: "0 8px" }}
              onClick={() => onNavigate(def.targetPage as Page)}
            >
              {PAGE_LABELS[def.targetPage]}으로 이동 →
            </button>
          )}
          {step === 4 && (
            <button
              className="nav-tab"
              style={{ fontSize: 12, padding: "0 8px", color: "#FBBF24" }}
              title="외부 AI 작업이 끝났거나 멈췄으면 확인 단계로"
              onClick={() => {
                // 외부 AI 작업 종료는 신호로 100% 감지 불가, 비-git 프로젝트는 변경이 항상
                // 0으로 읽혀 자동 전환이 아예 없음 → 수동 출구(spec §3.2 affordance).
                // "끝났거나 멈췄거나" — 중도 중단자도 자기 안내로 느끼게(완주 전제 금지).
                // override 후 5️⃣ 목적지로 이동.
                onStepChange(5);
                const verify = journeyStep(5);
                if (verify.targetPage) onNavigate(verify.targetPage as Page);
              }}
            >
              AI 작업이 끝났거나 멈췄나요? 확인하러 가기 →
            </button>
          )}
          {step === 4 && aiToolMissing && onOpenSettings && (
            <button
              className="nav-tab"
              style={{ fontSize: 12, padding: "0 8px", color: "#FBBF24" }}
              title="설정의 'AI 도구 설정'에서 설치를 도와드려요"
              onClick={onOpenSettings}
            >
              {/* 도구 미보유 입문자 분기(spec §3.2) — 4️⃣는 앱 밖으로 나가는 유일한 지점이라 최대 이탈 절벽 */}
              AI 도구가 아직 없나요? 설치 도움받기 →
            </button>
          )}
          {(step === 4 || step === 5) && (
            <button
              className="nav-tab"
              style={{ fontSize: 12, padding: "0 8px", color: "#888" }}
              title="마지막으로 저장한 시점으로 코드를 되돌릴 수 있어요"
              onClick={() => onNavigate("backups")}
            >
              {/* 포기 비상구(spec §3.2) — undo 발견이 6️⃣ 뒤에 숨지 않게. 회색톤 = 주행동 방해 금지. */}
              잘 안 됐나요? 되돌리기 →
            </button>
          )}
          <button
            className="nav-tab"
            style={{ fontSize: 12, padding: "0 6px" }}
            title="이전 단계"
            disabled={step === 2}
            onClick={() => step > 2 && onStepChange((step - 1) as ActiveGuideStep)}
          >
            ‹
          </button>
          <button
            className="nav-tab"
            style={{ fontSize: 12, padding: "0 6px" }}
            title="다음 단계"
            disabled={step === 6}
            onClick={() => step < 6 && onStepChange((step + 1) as ActiveGuideStep)}
          >
            ›
          </button>
          <button
            className="nav-tab"
            style={{ fontSize: 12, padding: "0 6px" }}
            title="가이드 끄기 (설정에서 다시 켤 수 있어요)"
            onClick={onDisable}
          >
            ×
          </button>
        </span>
      )}
      <div style={{ flex: 1 }} />
      <span style={{ color: hasCheckpoint ? "#aaa" : "#666" }}>
        {hasCheckpoint ? "✓ 백업 데이터 있음" : "백업 데이터 없음"}
      </span>
      {planningPending && <span style={{ color: "#FBBF24" }}>● 기획 진행 중</span>}
    </div>
  );
}
// === ANCHOR: GUIDE_STRIP_END ===
```

- [ ] **Step 2: 타입체크**

Run: `cd vibelign-gui && npx tsc --noEmit`
Expected: 통과 (TSC_EXIT=0)

- [ ] **Step 3: 커밋**

```bash
git add vibelign-gui/src/components/nav/GuideStrip.tsx
git commit -m "feat(guide): GuideStrip — 상태 스트립을 대체·통합하는 가이드 줄 컴포넌트"
```

---

## Task 4: App.tsx 신호 배선 + 조립 — 스트립 교체

> v2에서 확장(critic M1·M2): 원래 "null 신호 + 후속 배선"이었으나, v1에서 5️⃣·6️⃣이 추론 불가능해
> 가이드가 검증 직전에 죽는 문제 + 3️⃣이 첫 사이클 이후 재안내 불가 문제 때문에 배선을 이 Task로 승격.
> 전부 기존 프론트 API(`backupList`·`listChangedFiles`) + localStorage — 백엔드 변경 0 유지.

**Files:**
- Modify: `vibelign-gui/src/App.tsx`
- Modify: `vibelign-gui/src/pages/Home.tsx` (guard 결과 콜백 prop 1개)

- [ ] **Step 1: import 추가**

App.tsx 상단 import 블록(25행 `HomePlanningStart` import 아래)에 추가:
```ts
import { GuideStrip } from "./components/nav/GuideStrip";
import { useGuide } from "./lib/nav/useGuide";
import {
  countChangesSinceBaseline,
  guideBaselineKey,
  guideCelebratedKey,
  guideRelevantPaths,
  latestCheckpointId,
  shouldCelebrate,
  type ActiveGuideStep,
  type GuideBaseline,
} from "./lib/nav/guide";
```

그리고 App.tsx의 기존 `./lib/vib` import 구문에 `listChangedFiles`를 추가한다. (`detectInstalledTools`는 이미 export됨 — Step 6에서 사용, import에 함께 추가.)

- [ ] **Step 2: 백업 신호 확장 — `backupLoaded`·최신 체크포인트 + page 의존성**

> 기존 effect는 의존성이 `[projectDir]`뿐이라 **사용자가 3️⃣을 따라 체크포인트를 저장해도
> 스트립이 4️⃣로 안 넘어가는 갭**이 있었다(critic 리뷰 발견). page 전환 시 재조회로 해소(spec §4-6).

App.tsx:83 `const [backupCount, setBackupCount] = useState(0);` 바로 아래에 추가:
```ts
const [backupLoaded, setBackupLoaded] = useState(false);
const [latestBackupId, setLatestBackupId] = useState<string | null>(null);
```

backupList effect(121~144행)를 아래 두 effect로 교체 — 리셋 effect를 **반드시 위에** 선언
(프로젝트 전환 직후 이전 프로젝트 신호로 가이드가 잘못 뜨는 깜빡임 방지, spec §4-4):
```ts
  // 프로젝트가 바뀌면 신호 로딩 상태부터 리셋 — 아래 조회 effect보다 먼저 선언할 것.
  useEffect(() => {
    setBackupLoaded(false);
  }, [projectDir]);

  useEffect(() => {
    if (!projectDir) {
      setHasCheckpoint(false);
      setBackupCount(0);
      setLatestBackupId(null);
      setBackupLoaded(false);
      return;
    }
    let active = true;
    backupList(projectDir)
      .then((result) => {
        if (active) {
          setHasCheckpoint(result.backups.length > 0);
          setBackupCount(result.backups.length);
          setLatestBackupId(latestCheckpointId(result.backups));
        }
      })
      .catch(() => {
        if (active) {
          setHasCheckpoint(false);
          setBackupCount(0);
          setLatestBackupId(null);
        }
      })
      .finally(() => {
        if (active) setBackupLoaded(true);
      });
    return () => {
      active = false;
    };
  }, [projectDir, page]);
```
> page 전환마다 재조회하지만 `backupLoaded`는 같은 프로젝트에서 true를 유지 — 탭 이동 시
> 가이드가 깜빡이지 않는다. backupList는 가벼운 조회이고 이미 BackupDashboard가 수시 호출.

- [ ] **Step 3: 체크포인트 이후 변경 감지 effect (`changedFileCount`)**

Step 2의 effect들 아래에 추가:
```ts
  // 체크포인트 이후 변경 감지(spec §3.1) — git changed-set을 체크포인트 시점 baseline과 대칭차 비교.
  // null은 조회가 예외로 실패한 경우만. 비-git 프로젝트는 git_status가 빈 목록을 반환(에러 아님)해
  // 항상 0으로 읽힌다 — 자동 5️⃣ 전환 없음, 4️⃣ affordance가 유일한 출구(spec §4-5, critic 재리뷰 C1).
  const [changedFileCount, setChangedFileCount] = useState<number | null>(null);
  useEffect(() => {
    if (!projectDir || !backupLoaded || !latestBackupId) {
      // 체크포인트가 없으면 기준점도 없음 — 3️⃣ 안내 단계라 변경 신호 자체가 불필요.
      setChangedFileCount(null);
      return;
    }
    let active = true;
    listChangedFiles(projectDir)
      .then((entries) => {
        if (!active) return;
        const current = guideRelevantPaths(entries.map((e) => e.path)).sort();
        const key = guideBaselineKey(projectDir);
        let baseline: GuideBaseline | null = null;
        try {
          baseline = JSON.parse(localStorage.getItem(key) ?? "null") as GuideBaseline | null;
        } catch {
          baseline = null;
        }
        if (!baseline || baseline.checkpointId !== latestBackupId) {
          // 새 최신 체크포인트를 알아챈 순간의 changed-set이 새 기준점(spec §4-7) — 6️⃣ 저장 후 4️⃣ 복귀가 여기서 일어난다.
          localStorage.setItem(key, JSON.stringify({ checkpointId: latestBackupId, changedPaths: current }));
          setChangedFileCount(0);
          return;
        }
        setChangedFileCount(countChangesSinceBaseline(baseline.changedPaths, current));
      })
      .catch(() => {
        if (active) setChangedFileCount(null);
      });
    return () => {
      active = false;
    };
  }, [projectDir, page, latestBackupId, backupLoaded]);
```

- [ ] **Step 4: `guardStatus` 상태 + Home 콜백 배선**

(a) App.tsx — Step 3 아래에 추가:
```ts
  const [guardStatus, setGuardStatus] = useState<"ok" | "issue" | null>(null);
  // 변경 수가 "증가"할 때만 guard 결과를 리셋(spec §3.1, critic 재리뷰 M1).
  // 블랭킷 리셋([changedFileCount]에 무조건 null)은 guard 직후 탭 전환·사소한 재계산으로
  // 막 도달한 6️⃣을 풀어버려 초보자를 5️⃣에 가두는 오리셋을 만든다. 같은 수 재조회는
  // 값이 같아 effect가 아예 안 돌고, 감소(부분 undo)는 유지 — 6️⃣ 안내("저장 또는 undo")와 무해.
  const prevChangedRef = useRef<number | null>(null); // useRef는 App.tsx에 이미 import됨(resumingRef)
  useEffect(() => {
    const prev = prevChangedRef.current;
    prevChangedRef.current = changedFileCount;
    if (changedFileCount !== null && prev !== null && changedFileCount > prev) {
      setGuardStatus(null);
    }
  }, [changedFileCount]);
  useEffect(() => {
    setGuardStatus(null);
    prevChangedRef.current = null;
  }, [projectDir]);
```

(b) Home.tsx — `HomeProps`(Home.tsx:18~41)에 prop 추가:
```ts
  /** guard 실행 결과를 가이드 신호로 올림(spec §3.1). */
  onGuardResult?: (status: "ok" | "issue") => void;
```
구조분해(Home.tsx:45)에 `onGuardResult` 추가, `handleRunGuard`(Home.tsx:63~78)의 `setGuardResult(result);` 바로 아래에:
```ts
      onGuardResult?.(result.status === "pass" ? "ok" : "issue");
```
> 엔진의 `status` 필드 기준 — guard CLI의 판정 어휘는 **"pass"/"warn"/"fail"**(`vib_guard_cmd.py`
> `_guard_status`, "ok"라는 값은 존재하지 않음). issues 배열이 비어도 warn/fail일 수 있어
> `issues.length`로는 부족하다. **warn도 "issue"로 매핑** — warn은 약속 범위 밖 변경이 감지됐다는
> 뜻이라 5️⃣(검증·수정)에 머무는 게 여정 의도(spec §2 5️⃣)와 맞다.

(c) App.tsx — 홈 뷰 Home 호출(361행 부근, `page === "home"`)에 `onGuardResult={setGuardStatus}` 추가.

- [ ] **Step 5: `planningPendingNow` 파생값 + `useGuide` 호출 추가**

App.tsx의 `planningStatus` 파생값(284~288행) 바로 아래에 추가 — **Step 2~4의 상태 선언보다 뒤에 와야 한다**:
```ts
  const planningPendingNow = planningResult?.messages.some((m) => m.status === "pending") ?? false;
  // 가이드 레이어 — 신호 전부 배선(v2): 기획안·기획진행·체크포인트·체크포인트 이후 변경·guard.
  const guide = useGuide(
    projectDir,
    {
      hasPlanDoc: Boolean(planningResult?.outputPath),
      planningPending: planningPendingNow,
      hasCheckpoint,
      changedFileCount,
      guardStatus,
    },
    backupLoaded,
  );
```

- [ ] **Step 6: AI 도구 탐지 + 첫 완주 축하 배선 (북극성 점검 #1·#2)**

(a) AI 도구 탐지 — Step 5 아래에 추가 (도구 설치는 머신 전역이라 마운트 시 1회):
```ts
  // 4️⃣ 도구 미보유 분기(spec §3.2) — 0개 "확정 탐지"일 때만 true. 탐지 실패는 분기 미노출(보수적).
  const [aiToolMissing, setAiToolMissing] = useState(false);
  useEffect(() => {
    let active = true;
    detectInstalledTools()
      .then((tools) => {
        if (active) setAiToolMissing(tools.length === 0);
      })
      .catch(() => {
        // 탐지 실패 → false 유지(추측 안내 금지)
      });
    return () => {
      active = false;
    };
  }, []);
```

(b) 첫 완주 축하 — `guide` 선언(Step 5) 아래에 추가:
```ts
  // 첫 사이클 완주 축하(spec §3.2) — 6️⃣→4️⃣ 전환(저장으로 루프 닫힘)에 프로젝트당 1회.
  const [celebrating, setCelebrating] = useState(false);
  const prevGuideStepRef = useRef<ActiveGuideStep | null>(null);
  useEffect(() => {
    const prev = prevGuideStepRef.current;
    prevGuideStepRef.current = guide.step;
    if (!projectDir) return;
    const already = localStorage.getItem(guideCelebratedKey(projectDir)) === "1";
    if (shouldCelebrate(prev, guide.step, already)) {
      localStorage.setItem(guideCelebratedKey(projectDir), "1");
      setCelebrating(true);
    }
  }, [guide.step, projectDir]);
  useEffect(() => {
    // 프로젝트 전환 시 축하 상태·전환 추적 리셋
    setCelebrating(false);
    prevGuideStepRef.current = null;
  }, [projectDir]);
```

- [ ] **Step 7: 상태 스트립(337~355행)을 GuideStrip으로 교체**

기존(337~355행):
```tsx
            <div
              style={{
                display: "flex",
                alignItems: "center",
                gap: 16,
                padding: "3px 12px",
                fontSize: 11,
                color: "#888",
                background: "#0E0E0E",
                borderBottom: "1px solid #1A1A1A",
              }}
            >
              <span style={{ color: hasCheckpoint ? "#aaa" : "#666" }}>
                {hasCheckpoint ? "✓ 백업 데이터 있음" : "백업 데이터 없음"}
              </span>
              {(planningResult?.messages.some((message) => message.status === "pending") ?? false) && (
                <span style={{ color: "#FBBF24" }}>● 기획 진행 중</span>
              )}
            </div>
```
교체:
```tsx
            <GuideStrip
              enabled={guide.enabled}
              step={guide.step}
              hasCheckpoint={hasCheckpoint}
              planningPending={planningPendingNow}
              aiToolMissing={aiToolMissing}
              celebrating={celebrating}
              onNavigate={navigate}
              onStepChange={guide.setStep}
              onDisable={() => guide.setEnabled(false)}
              onOpenSettings={() => openSettings()}
              onCelebrateDismiss={() => setCelebrating(false)}
            />
```

- [ ] **Step 8: 타입체크**

Run: `cd vibelign-gui && npx tsc --noEmit`
Expected: 통과 (TSC_EXIT=0)

- [ ] **Step 9: 시각 확인**

Run: `cd vibelign-gui && npm run tauri dev` (또는 사용자 구동)
확인:
- 프로젝트 열면 탭바 아래 스트립에 `🧭 지금: …` + `[…으로 이동 →]` + `‹ › ×` 노출 (가이드 구간 13px·노란 강조)
- 기획안 없는 프로젝트 → "2️⃣ 기획하기" 안내
- 체크포인트 저장 후 다른 탭으로 이동 → 스트립이 4️⃣로 전환(page 재조회 — 앱 재시작 불필요)
- 4️⃣에서 git 프로젝트의 파일을 외부에서 수정 → 탭 한 번 전환하면 5️⃣로 전환 / 비-git 프로젝트는 변경이 0으로 읽혀 4️⃣ 유지(null 아님) — "확인하러 가기 →" 버튼이 유일한 5️⃣ 진입로인지 확인
- 4️⃣·5️⃣에서 회색 보조 출구 "잘 안 됐나요? 되돌리기 →" 노출 → 클릭 시 백업 탭 이동 (포기 비상구)
- 5️⃣에서 홈 '상태 확인' 실행(엔진 status=**pass**) → 6️⃣로 전환, **탭을 몇 번 전환해도 6️⃣이 유지**(guard 결과 오리셋 없음 — critic 재리뷰 M1). warn/fail이면 5️⃣ 유지
- 6️⃣에서 체크포인트 저장 → 4️⃣ 복귀 (baseline 리셋 — 루프 닫힘) / 6️⃣에서 undo 실행 → 변경이 baseline으로 돌아가 4️⃣ 복귀
- `‹`는 2단계에서 비활성, `›`로 단계 이동 가능, 이동 후 앱 재시작해도 유지(같은 프로젝트)
- `×` 클릭 → 가이드 부분만 사라지고 우측 상태 신호(백업/기획 진행)는 유지
- 신호 로딩 직후·프로젝트 전환 직후 잘못된 안내가 깜빡이지 않음
- AI 도구가 하나도 탐지되지 않는 환경(또는 강제로 `aiToolMissing` true) → 4️⃣에 "AI 도구가 아직 없나요? 설치 도움받기 →" 노출 → 설정 이동
- 6️⃣에서 체크포인트 저장으로 4️⃣ 복귀하는 순간 → "🎉 첫 사이클 완주!" 1회 노출, × 닫기 후 같은 프로젝트에서 재발 없음(재시작 포함)

- [ ] **Step 10: 커밋**

```bash
git add vibelign-gui/src/App.tsx vibelign-gui/src/pages/Home.tsx
git commit -m "feat(guide): App 신호 배선(체크포인트 baseline·guard 콜백) + 가이드 스트립 조립"
```

---

## Task 5: 홈 허브 카드 차례 표시 (`StageHubCards.tsx`)

**Files:**
- Modify: `vibelign-gui/src/components/nav/StageHubCards.tsx` (전체 53줄 — 아래 전문으로 교체)
- Modify: `vibelign-gui/src/App.tsx:361` (StageHubCards 호출)

- [ ] **Step 1: StageHubCards 수정 (파일 전문)**

```tsx
// === ANCHOR: STAGE_HUB_CARDS_START ===
import { STAGE_DEFS, pagesForStage, type Stage, type Page } from "../../lib/nav/stages";
import { cardStepState, journeyStep, type ActiveGuideStep } from "../../lib/nav/guide";

interface StageHubCardsProps {
  onNavigate: (page: Page) => void;
  planningStatus: "none" | "active" | "done";
  backupCount: number;
  /** 가이드 현재 단계. null = 가이드 꺼짐/로딩 전(기존 표시 그대로). */
  currentStep?: ActiveGuideStep | null;
}

const CARD_DESC: Record<Stage, string> = {
  planning: "아이디어를 기획안으로",
  develop: "코드 보기·탐색",
  maintain: "안전 점검·복원",
};

function badgeText(stage: Stage, planningStatus: StageHubCardsProps["planningStatus"], backupCount: number): string {
  if (stage === "planning") {
    return planningStatus === "active" ? "● 진행 중" : planningStatus === "done" ? "완료" : "대기";
  }
  if (stage === "maintain") return `백업 ${backupCount}개`;
  return "열기"; // develop: 기존 신호 없음
}

export function StageHubCards({ onNavigate, planningStatus, backupCount, currentStep = null }: StageHubCardsProps) {
  return (
    <div style={{ display: "flex", gap: 12, padding: 16 }}>
      {STAGE_DEFS.map((def) => {
        const guideState = currentStep ? cardStepState(def.key, currentStep) : null;
        return (
          <button
            key={def.key}
            onClick={() => onNavigate(pagesForStage(def.key)[0])}
            style={{
              flex: 1,
              textAlign: "left",
              padding: 16,
              background: guideState === "now" ? "#1a1813" : "#141414",
              border: guideState === "now" ? "2px solid #FBBF24" : "1px solid #2A2A2A",
              borderRadius: 8,
              color: "#ddd",
              cursor: "pointer",
            }}
          >
            <div style={{ fontSize: 20 }}>{def.icon}</div>
            <div style={{ fontWeight: 600, marginTop: 6 }}>
              {def.label}
              {guideState === "now" && (
                <span style={{ marginLeft: 8, fontSize: 11, background: "#FBBF24", color: "#000", padding: "1px 6px", borderRadius: 4, fontWeight: 700 }}>
                  지금 할 차례
                </span>
              )}
              {guideState === "done" && <span style={{ marginLeft: 8, fontSize: 11, color: "#4DFF91" }}>✓ 완료</span>}
              {guideState === "upcoming" && <span style={{ marginLeft: 8, fontSize: 11, color: "#666" }}>다음</span>}
            </div>
            <div style={{ fontSize: 12, color: "#888", marginTop: 4 }}>{CARD_DESC[def.key]}</div>
            {guideState === "now" && currentStep && (
              <div style={{ fontSize: 12, color: "#FBBF24", marginTop: 6 }}>
                → {journeyStep(currentStep).icon} {journeyStep(currentStep).shortAction}
              </div>
            )}
            <div style={{ fontSize: 12, color: def.key === "planning" && planningStatus === "active" ? "#FBBF24" : "#aaa", marginTop: 8 }}>
              {badgeText(def.key, planningStatus, backupCount)}
            </div>
          </button>
        );
      })}
    </div>
  );
}
// === ANCHOR: STAGE_HUB_CARDS_END ===
```

- [ ] **Step 2: App.tsx 호출에 prop 추가**

App.tsx:361 — 기존:
```tsx
<StageHubCards onNavigate={navigate} planningStatus={planningStatus} backupCount={backupCount} />
```
변경:
```tsx
<StageHubCards onNavigate={navigate} planningStatus={planningStatus} backupCount={backupCount} currentStep={guide.enabled ? guide.step : null} />
```

- [ ] **Step 3: 타입체크 + 시각 확인**

Run: `cd vibelign-gui && npx tsc --noEmit`
Expected: 통과 (TSC_EXIT=0)
시각: 홈에서 현재 단계 카드에 노란 테두리+"지금 할 차례"+행동 한 줄, 지난 카드 "✓ 완료", 남은 카드 "다음". 가이드 끄면(×) 카드가 기존 모습으로 복귀.

- [ ] **Step 4: 커밋**

```bash
git add vibelign-gui/src/components/nav/StageHubCards.tsx vibelign-gui/src/App.tsx
git commit -m "feat(guide): 홈 허브 카드에 여정 차례 표시 — 지금 할 차례·완료·다음"
```

---

## Task 6: 사용법 따라하기 아코디언 (`JourneyHowto.tsx` + Home 통합)

**Files:**
- Create: `vibelign-gui/src/components/nav/JourneyHowto.tsx`
- Modify: `vibelign-gui/src/pages/Home.tsx` (onNavigate 타입 확장 + props + manual_list 뷰)
- Modify: `vibelign-gui/src/components/home/AdvancedHomeCards.tsx` (onNavigate 타입 확장 — 타입만)
- Modify: `vibelign-gui/src/App.tsx:378` (manual Home 호출)

- [ ] **Step 1: JourneyHowto 작성**

`vibelign-gui/src/components/nav/JourneyHowto.tsx`:
```tsx
// === ANCHOR: JOURNEY_HOWTO_START ===
import { useEffect, useState } from "react";
import { JOURNEY_STEPS, type ActiveGuideStep, type GuideStep } from "../../lib/nav/guide";
import { PAGE_LABELS, type Page } from "../../lib/nav/stages";

interface JourneyHowtoProps {
  /** 현재 단계(가이드 추론). null이면 강조 없이 전부 접힘. */
  currentStep: ActiveGuideStep | null;
  onNavigate: (page: Page) => void;
}

/** 사용법 탭 상단 "순서대로 따라하기" 아코디언. 현재 단계가 자동으로 펼쳐진다(spec §3.4). */
export function JourneyHowto({ currentStep, onNavigate }: JourneyHowtoProps) {
  const [open, setOpen] = useState<GuideStep | null>(currentStep);
  useEffect(() => setOpen(currentStep), [currentStep]);
  return (
    <div style={{ padding: "16px 20px 4px" }}>
      <div style={{ fontWeight: 700, fontSize: 13, marginBottom: 8 }}>📖 순서대로 따라하기</div>
      {JOURNEY_STEPS.map((def) => {
        const isOpen = open === def.step;
        const isCurrent = currentStep === def.step;
        return (
          <div
            key={def.step}
            style={{
              border: isCurrent ? "1px solid #FBBF24" : "1px solid #2A2A2A",
              background: isCurrent ? "#1a1813" : "#141414",
              borderRadius: 6,
              marginBottom: 4,
            }}
          >
            <button
              onClick={() => setOpen(isOpen ? null : def.step)}
              style={{
                width: "100%",
                textAlign: "left",
                padding: "8px 12px",
                background: "transparent",
                border: "none",
                color: "#ddd",
                cursor: "pointer",
                fontSize: 13,
              }}
            >
              {def.icon} <b>{def.label}</b> — <span style={{ color: "#888" }}>{def.shortAction}</span>
              <span style={{ float: "right", color: "#666" }}>{isOpen ? "▾" : "▸"}</span>
            </button>
            {isOpen && (
              <div style={{ padding: "0 12px 10px", fontSize: 12, color: "#aaa", lineHeight: 1.8 }}>
                {def.howto.map((line) => (
                  <div key={line}>{line}</div>
                ))}
                {def.targetPage && (
                  <button
                    className="nav-tab"
                    style={{ fontSize: 12, marginTop: 6 }}
                    onClick={() => onNavigate(def.targetPage as Page)}
                  >
                    지금 하러 가기 → {PAGE_LABELS[def.targetPage]}
                  </button>
                )}
              </div>
            )}
          </div>
        );
      })}
    </div>
  );
}
// === ANCHOR: JOURNEY_HOWTO_END ===
```

- [ ] **Step 2: `onNavigate` 타입 확장 — (critic C1, 이 스텝 없이는 Step 5 tsc 실패)**

`Home.onNavigate`는 현재 `(page: "backups") => void`로 좁혀져 있어(아래 두 곳) `Page` 전체를 받는
JourneyHowto/`navigate`를 꽂으면 타입 에러가 난다. 둘 다 `(page: Page) => void`로 확장:

(a) `Home.tsx:24` — 기존 `onNavigate: (page: "backups") => void;` → 변경:
```ts
  onNavigate: (page: Page) => void;
```
import 추가: `import type { Page } from "../lib/nav/stages";`

(b) `AdvancedHomeCards.tsx:42` — 기존 `readonly onNavigate: (page: "backups") => void;` → 변경:
```ts
  readonly onNavigate: (page: Page) => void;
```
import 추가: `import type { Page } from "../../lib/nav/stages";`

> 안전 근거: leaf인 CheckpointCard/UndoCard는 `onNavigate("backups")`만 호출하고, 넓은 핸들러를
> 좁은 prop 자리에 넘기는 방향이라(파라미터 반공변) leaf 컴포넌트는 무변경. App의 기존 호출
> `onNavigate={setPage}`(홈 뷰)도 `(page: Page) => void`에 그대로 호환.

- [ ] **Step 3: Home.tsx에 prop 추가 + manual_list 뷰 통합**

(a) import 추가 — Home.tsx 상단 import 블록에:
```ts
import { JourneyHowto } from "../components/nav/JourneyHowto";
import type { ActiveGuideStep } from "../lib/nav/guide";
```

(b) `HomeProps` 인터페이스(Home.tsx:17 부근)에 추가:
```ts
  /** 가이드 현재 단계 — 사용법 따라하기 아코디언 강조용. */
  guideStep?: ActiveGuideStep | null;
```

(c) 함수 시그니처(Home.tsx:45)의 구조분해에 `guideStep = null` 추가 — 기존 `onOpenPlanningHistory }` 앞에:
```ts
..., onOpenPlanningHistory, guideStep = null }: HomeProps) {
```

(d) manual_list 뷰(Home.tsx:86~97) — 기존:
```tsx
  if (view === "manual_list") {
    return (
      <ManualCommandList
        onBack={() => setView("home")}
        onSelectCommand={(command) => {
          setSelectedCmd(command);
          setView("manual_detail");
        }}
      />
    );
  }
```
변경(아코디언 고정 + 사전은 자체 스크롤 — **flex 분할로 이중 스크롤바 선차단**. `ManualCommandList`는
이미 자체 스크롤 컨테이너를 가지므로(`ManualCommandList.tsx:14,23`) 바깥에 `overflowY:"auto"`를 또
씌우면 중첩 스크롤이 거의 확실히 발생한다 — critic m3):
```tsx
  if (view === "manual_list") {
    return (
      <div style={{ height: "100%", display: "flex", flexDirection: "column" }}>
        <div style={{ flexShrink: 0 }}>
          <JourneyHowto currentStep={guideStep} onNavigate={onNavigate} />
        </div>
        <div style={{ flex: 1, minHeight: 0 }}>
          <ManualCommandList
            onBack={() => setView("home")}
            onSelectCommand={(command) => {
              setSelectedCmd(command);
              setView("manual_detail");
            }}
          />
        </div>
      </div>
    );
  }
```
> `onNavigate`는 HomeProps 필수 prop(Step 2에서 `Page`로 확장됨)이므로 직접 전달.

- [ ] **Step 4: App.tsx manual Home 호출 수정**

App.tsx:378 — 기존 `onNavigate={setPage}` 를 `onNavigate={navigate}` 로 바꾸고 `guideStep` 추가:
```tsx
{page === "manual" && <Home key="manual" projectDir={projectDir} apiKey={apiKey} providerKeys={providerKeys} hasAnyAiKey={hasAnyAiKey} aiKeyStatusLoaded={envKeyStatusLoaded} onNavigate={navigate} onOpenSettings={openSettings} initialView="manual_list" watchOn={watchOn} setWatchOn={setWatchOn} mapMode={mapMode} setMapMode={setMapMode} onStartPlanning={(idea) => { if (projectDir) void openPlanningRoom(projectDir, idea); }} guideStep={guide.enabled ? guide.step : null} />}
```
(`navigate` 사용 이유: `setPage`와 달리 doctor 분기(launch intent 초기화, App.tsx:187-190)를 거친다 — 현재 여정 목적지에 doctor는 없지만 향후 추가에 안전)

- [ ] **Step 5: 타입체크 + 시각 확인**

Run: `cd vibelign-gui && npx tsc --noEmit`
Expected: 통과 (TSC_EXIT=0) — Step 2를 건너뛰었다면 여기서 `(page: "backups") => void` 불일치로 실패한다.
시각: 사용법 탭 → 맨 위 "📖 순서대로 따라하기" 6단계, 현재 단계가 노란 테두리로 자동 펼쳐짐, "지금 하러 가기 →" 클릭 시 해당 탭 이동, 그 아래 기존 명령어 사전 정상(자체 스크롤). 스크롤바 이중 발생 없음 확인. 작은 창에서 아코디언을 펼쳐도 사전 영역이 찌그러지지 않는지 확인.

- [ ] **Step 6: 커밋**

```bash
git add vibelign-gui/src/components/nav/JourneyHowto.tsx vibelign-gui/src/pages/Home.tsx vibelign-gui/src/components/home/AdvancedHomeCards.tsx vibelign-gui/src/App.tsx
git commit -m "feat(guide): 사용법 탭에 '순서대로 따라하기' 아코디언 — onNavigate Page 확장 포함"
```

---

## Task 7: 설정 토글 (Settings)

**Files:**
- Modify: `vibelign-gui/src/pages/Settings.tsx` (props 2개 + 토글 카드 1개)
- Modify: `vibelign-gui/src/App.tsx:395` (Settings 호출)

- [ ] **Step 1: SettingsProps에 props 추가**

Settings.tsx:24 `interface SettingsProps {` 블록 안에 추가:
```ts
  guideEnabled?: boolean;
  onGuideEnabledChange?: (on: boolean) => void;
```
Settings.tsx:33 구조분해에 추가:
```ts
export default function Settings({ apiKey, onApiKeyChange, providerKeys, onKeysUpdated, projectDir, notice, guideEnabled, onGuideEnabledChange }: SettingsProps) {
```

- [ ] **Step 2: 토글 카드 추가**

Settings.tsx:557 (AI 앵커 보강 토글 섹션 닫는 `</div>` 다음, "AI 도구 설정" 섹션 앞)에 삽입 — 기존 토글 섹션과 동일한 패턴:
```tsx
        {/* 초보자 가이드 토글 섹션 */}
        <div className="card" style={{ marginTop: 16 }}>
          <div style={{ fontWeight: 700, fontSize: 12, marginBottom: 12, textTransform: "uppercase", letterSpacing: 1 }}>
            초보자 가이드
          </div>
          <div style={{ fontSize: 13, lineHeight: 1.7, marginBottom: 10 }}>
            화면 상단에 "지금 할 일 → 다음 행동"을 안내하는 가이드 줄과 홈 카드의 차례 표시를 보여줍니다.
            익숙해지면 꺼도 좋아요. (가이드 줄의 × 버튼으로도 끌 수 있어요)
          </div>
          {guideEnabled === undefined || !onGuideEnabledChange ? (
            <div style={{ fontSize: 11, color: "#777" }}>프로젝트를 먼저 선택하세요.</div>
          ) : (
            <div style={{ display: "flex", alignItems: "center", gap: 12 }}>
              <button
                className="btn btn-sm"
                onClick={() => onGuideEnabledChange(!guideEnabled)}
                style={{
                  background: guideEnabled ? "#4DFF91" : "#1A1A1A",
                  color: guideEnabled ? "#1A1A1A" : "#888",
                  border: "2px solid #1A1A1A",
                  fontWeight: 700,
                }}
              >
                {guideEnabled ? "ON" : "OFF"}
              </button>
              <span style={{ fontSize: 11, color: "#888", fontFamily: "IBM Plex Mono, monospace" }}>
                guide_enabled: {guideEnabled ? "true" : "false"}
              </span>
            </div>
          )}
        </div>
```

- [ ] **Step 3: App.tsx Settings 호출에 props 전달**

App.tsx:395 — 기존:
```tsx
<Settings apiKey={apiKey} onApiKeyChange={setApiKey} providerKeys={providerKeys} onKeysUpdated={refreshAiKeys} projectDir={projectDir} notice={settingsNotice} />
```
변경:
```tsx
<Settings apiKey={apiKey} onApiKeyChange={setApiKey} providerKeys={providerKeys} onKeysUpdated={refreshAiKeys} projectDir={projectDir} notice={settingsNotice} guideEnabled={guide.enabled} onGuideEnabledChange={guide.setEnabled} />
```

- [ ] **Step 4: 타입체크 + 시각 확인**

Run: `cd vibelign-gui && npx tsc --noEmit`
Expected: 통과 (TSC_EXIT=0)
시각: 설정 → "초보자 가이드" 카드 ON/OFF. OFF→스트립 가이드·카드 차례 표시 사라짐, ON→복귀. 가이드 줄 ×로 끈 뒤 설정에서 다시 ON 가능.

- [ ] **Step 5: 커밋**

```bash
git add vibelign-gui/src/pages/Settings.tsx vibelign-gui/src/App.tsx
git commit -m "feat(guide): 설정에 초보자 가이드 ON/OFF 토글"
```

---

## Task 8: 전체 검증 + push

**Files:** (코드 변경 없음, 발견 시 소규모 수정)

- [ ] **Step 1: 전체 테스트**

Run: `cd vibelign-gui && npx vitest run`
Expected: guide.test.ts + stages.test.ts 포함 전부 PASS

- [ ] **Step 2: 전체 빌드**

Run: `cd vibelign-gui && npm run build`
Expected: tsc + vite build 성공

- [ ] **Step 3: 육안 회귀 체크리스트** (`npm run tauri dev`)

- [ ] 기획안 없는 프로젝트: 스트립 "2️⃣ 기획하기" / 홈 기획 카드 "지금 할 차례"
- [ ] 기획안 확정 후: "3️⃣ 체크포인트 저장"으로 전환 (override 자동 해제 확인 — 수동으로 4️⃣ 옮겨놨다가 기획안 상태 바뀌면 추론값 복귀)
- [ ] 체크포인트 저장 후 탭 전환: "4️⃣ AI에게 작업 시키기" (앱 재시작 없이 전환되는지 — page 재조회 확인)
- [ ] **전체 루프 1회**: 4️⃣ → 외부에서 파일 수정 → 5️⃣ → 홈 '상태 확인'(이슈 0) → 6️⃣ → 체크포인트 저장 → 4️⃣ 복귀
- [ ] **앱 재시작 후 기획안 복원**: 기획안 확정 → 앱 종료/재시작 → `hasPlanDoc`이 살아 있어 2️⃣로 되돌리지 않는지 (planningResult.outputPath 복원 여부 — 안 되면 별도 이슈로 기록)
- [ ] **비-git 프로젝트**: changedFileCount가 0으로 읽힘(null 아님 — git_status는 비-git에 빈 목록 반환) → 4️⃣ 유지, "AI 작업이 끝났거나 멈췄나요? 확인하러 가기 →" 버튼이 유일한 5️⃣ 진입로
- [ ] **AI 도구 미보유 분기**: 도구 0개 탐지 환경에서 4️⃣ → "설치 도움받기 →" 노출·설정 이동 / 도구 있는 환경에선 미노출
- [ ] **첫 완주 축하**: 첫 6️⃣→4️⃣ 전환에 1회 노출 → × 닫기 → 같은 프로젝트에서 재발 없음(앱 재시작 포함), 다른 프로젝트에선 독립적으로 1회
- [ ] **guard 결과 유지**: 5️⃣에서 깨끗한 guard(엔진 status=**pass** — "ok"라는 값은 없음) → 6️⃣ 전환 후 탭 전환·재방문에도 6️⃣ 유지 (변경 수 증가 시에만 리셋). warn/fail은 5️⃣ 유지
- [ ] **undo 복귀**: 5️⃣/6️⃣에서 undo로 체크포인트 시점 복원 → 변경 0 → 4️⃣ 복귀 (baseline 재캡처 없이 — latestBackupId 불변)
- [ ] **중도 포기 동선**: 4️⃣·5️⃣ 보조 출구 "되돌리기 →" → 백업 탭 → undo 실행까지 초보자 시선으로 끊김 없는지 / 사용법 5️⃣ 하우투 ④(검사 없이 undo 가능) 노출 확인
- [ ] **재시작 후퇴(의도)**: 6️⃣ 상태에서 앱 재시작 → guardStatus 소실로 5️⃣ 표시 — "시간 지났으면 재검증" 안내로 자연스러운지 (spec §4-8 의도 동작)
- [ ] **멀티 사이클 override**: 4️⃣에서 `›`로 6️⃣ 고정 → 새 기획 대화 시작(planningPending) → 추론 2️⃣로 바뀌며 override 자동 해제 확인
- [ ] 5️⃣일 때 홈 카드: 어떤 카드도 "지금 할 차례" 아님(스트립이 안내) — 어색하지 않은지 육안 판단
- [ ] **홈 이중 안내 판정(결정 항목)**: 홈에서 스트립과 허브 카드가 같은 단계를 안내 — 소음으로 판단되면 후속에서 `page === "home"`일 때 스트립의 가이드 구간만 숨기는 방향(카드가 안내 담당, 우측 상태 신호는 유지). 보강이라고 판단되면 유지. 판정 결과를 커밋 메시지나 후속 메모에 기록
- [ ] `‹ ›` 수동 보정 + 앱 재시작 후 유지(같은 프로젝트), 다른 프로젝트 열면 그 프로젝트 기준
- [ ] `×` 끄기 → 우측 상태 신호만 남음 → 설정에서 재활성
- [ ] 사용법 따라하기: 현재 단계 자동 펼침, "지금 하러 가기" 이동, 명령어 사전 무손상(이중 스크롤 없음)
- [ ] 기존 동작 회귀 없음: 단계 전환 바·서브탭·기획방 진입·진단 launch intent

- [ ] **Step 4: push**

```bash
git push origin feat/vibelign-product-renew
```

---

## 후속 (이 계획 밖, 명시)

- GuardCard(고급 카드)·진단(Doctor) 흐름의 guard/doctor 결과도 `guardStatus`로 배선 — v1은 홈 '상태 확인'만
- 4️⃣ 도구 인도 확장 — v1은 "0개 탐지 → 설정 진입로"의 최소 분기. 도구별(Claude Code/Cursor/OpenCode) 맞춤 하우투, 설치 마법사 딥링크, 사용법 탭 "AI 도구가 처음이라면" 입문 섹션은 후속
- 완주 감지 정교화 — v1 축하는 6️⃣→4️⃣ 전환 휴리스틱 1회. 사이클 횟수 추적·마일스톤(10회 등)은 후속
- 비-git 프로젝트의 자동 5️⃣ 전환 — "git인지" 구분 신호(예: 별도 커맨드 또는 listChangedFiles에 isGitRepo 플래그)가 생기면 null 처리로 승격 가능 (현재 백엔드는 비-git에 빈 목록 반환)
- 감시(watch) 이벤트를 변경 신호로 활용 — 탭 전환 폴링 없이 실시간 5️⃣ 전환 (현재는 page 전환 시 재조회)
- `guideBaseline`/`guideOverride` localStorage 키가 프로젝트 경로별로 누적 — 프루닝(최근 N개 유지) 검토
- 자동 백업(감시 연동)이 생기면 3️⃣ 단계 자동화 검토 (spec §6)
- 홈 화면 이중 안내가 소음 판정이면 `page === "home"`일 때 스트립 가이드 구간 숨김 (Task 8 결정 항목의 방향)
- 사용법 콘텐츠 외부화(노션/전자책) 연동 — 펀딩 문서 §5-3 별도 트랙

## Self-Review 결과 (작성자 점검 + 2026-06-10 critic 리뷰 2회 반영 v3)

- **critic 1차 리뷰 반영(v2):** C1(Home.onNavigate 타입 좁힘 → Task 6 Step 2 신설) / M1·M2(신호 미배선으로 5️⃣·6️⃣ 도달 불가·3️⃣ 1회성 → Task 4 배선 승격 + baseline 설계 + 4️⃣ affordance + 루프 한계 design 명시) / M3(스트립 시인성 → 13px·#FBBF24) / m3(이중 스크롤 → flex 분할 선결정) / 5️⃣ 목적지 사실 오류(guard는 홈에만 있음 → targetPage "home"·cardStage null) / backupList 갱신 갭(page 의존성) / Task 8 체크리스트에 재시작 복원·멀티 사이클·비-git 케이스 추가.
- **북극성 점검 반영(v5, "초보자 입문 인도" 렌즈 — 판정 조건부 부합→부합):** Critical #1 도구 미보유 분기(최소판: detectInstalledTools 재사용 + 스트립 분기 버튼 + 하우투 ⑤, Task 4 Step 6(a)·Task 3) / #2 첫 완주 축하(shouldCelebrate TDD + 1회성 플래그, Task 1·3·4 Step 6(b)) / #3 영문 용어 제거(guard·undo → UI 라벨 어휘). 전체 설치 마법사·도구별 맞춤·마일스톤은 후속으로 분리(범위 통제).
- **중도 포기 비상구 반영(v4, 사용자 지적):** 포기는 단계가 아니라 비상구 — 4️⃣·5️⃣ 스트립에 회색 보조 출구 "잘 안 됐나요? 되돌리기 →"(백업 탭), affordance 문구 "끝났거나 멈췄나요"로 중립화(완주 전제 제거), 5️⃣ 하우투에 "검사 없이 undo 가능" ④ 추가, 방치·재시작 수렴 동작을 spec §4-8 의도로 명문화. Task 4·8 체크리스트에 포기 동선 항목 추가.
- **critic 재리뷰 반영(v3):** C1-new(비-git은 null이 아니라 0 — `git_status.rs`가 빈 목록 반환을 코드로 확인, 모든 "비-git → null" 서술 정정 + `changedFileCount: 0` 테스트 추가 + affordance 근거 재서술) / M1-new(guardStatus 블랭킷 리셋이 6️⃣을 푸는 오리셋 → 변경 수 증가 시에만 리셋, ref 기반) / guard 매핑을 `issues.length`에서 엔진 `status` 필드로 변경 — **3차 리뷰가 잡은 회귀 정정 포함: 엔진 어휘는 "pass"/"warn"/"fail"이라 `=== "ok"`는 영원히 false(6️⃣ 도달 불가), `=== "pass"`로 수정**(빈 issues에도 warn/fail 가능, warn→issue는 의도된 결정) / undo→4️⃣ 복귀·6️⃣ 유지 체크리스트 추가 / 홈 이중 안내를 결정 항목으로 승격(방향 선결정) / localStorage 누적 후속 메모. 캐시 우려는 기각 확인 — `checkpointCreate`·`backupCreate` 모두 `clearBackupCaches` 경유(backup.ts:44,51), 대시보드·카드 버튼 둘 다 이 경로.
- **Spec 커버리지:** §2 여정=JOURNEY_STEPS(Task1) / §3.1 guide.ts·localStorage·baseline 보조 함수=Task1·2·4 / §3.2 GuideStrip(시인성·affordance)=Task3·4 / §3.3 허브 카드=Task5 / §3.4 따라하기=Task6 / 토글(§1)=Task7 / §4 엣지 7종=inferStep 뒤단계우선(테스트)·resolveOverride(테스트)·기획 건너뛰기(‹›·×)·로딩 전 미렌더(backupLoaded→step null)·신호 실패 시 보수적 생략(null 신호 테스트+affordance)·page 재조회(§4-6)·baseline 타이밍(§4-7) / §5 검증=Task1 TDD+Task8.
- **플레이스홀더 스캔:** 모든 코드 스텝에 실제 코드 전문 포함. "적절히" 류 없음.
- **타입 일관성:** `ActiveGuideStep`·`GuideSignals`·`GuideOverride`·`GuideBaseline`·`inferStep`·`resolveOverride`·`cardStepState`·`journeyStep`·`latestCheckpointId`·`countChangesSinceBaseline`·`guideRelevantPaths`·`guideBaselineKey`·`useGuide`·`guide.step`·`guide.setStep`·`guide.setEnabled` 명칭이 Task 간 일치. `GuideStrip`/`JourneyHowto`/`StageHubCards` props 명칭 일치. Home `onGuardResult`(Task 4)와 `guideStep`(Task 6)은 별개 prop.
