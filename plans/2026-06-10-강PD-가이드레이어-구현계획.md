# 강 PD 가이드 레이어 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 초보자가 "지금 뭘 해야 하는지" 항상 알 수 있도록, 6단계 표준 여정을 추론해 상단 스트립·홈 허브 카드·사용법 따라하기로 안내하는 가이드 레이어를 구현한다.

**Architecture:** `stages.ts`와 같은 패턴의 순수 모듈 `guide.ts`(여정 정의 + 추론 + override 규칙, TDD)를 토대로, React 바인딩 훅(`useGuide.ts`)과 표시 컴포넌트 2개(`GuideStrip`, `JourneyHowto`)를 만들고 App.tsx에서 조립한다. 기존 상태 스트립(App.tsx:337-355)은 GuideStrip이 대체·통합한다. 백엔드(Rust/Python) 변경 0.

**Tech Stack:** React + TypeScript (Tauri GUI), vitest(순수 함수), tsc + tauri dev(시각 검증), localStorage(영속).

> **검증 방침(중요):** 이 프로젝트는 UI 레이아웃에 단위테스트를 요구하지 않는다(CLAUDE.md "가장 작은 패치"). **순수 로직(`guide.ts`)만 TDD**, 컴포넌트·통합은 `npx tsc --noEmit` + 육안 렌더.

**근거 spec:** `plans/2026-06-10-강PD-가이드레이어-design.md` (커밋 cbb64e3)
**현재 코드 기준:** 744b8d6. App.tsx 상태 스트립=337~355행, `navigate()`=187~190행, `planningStatus`=284~288행, backupList effect=121~144행, Home(manual) 호출=378행, Settings 호출=395행. `vibGuard`는 Home에서 버튼으로만 실행되는 on-demand 신호 → **v1은 `guardStatus`/`changedFileCount`를 null(미배선)로 전달**하고 추론 규칙은 보수적으로 생략(spec §4-5). 함수 시그니처는 두 신호를 지원하므로 후속 배선 시 guide.ts 무변경.

---

## File Structure

**신규:**
- `vibelign-gui/src/lib/nav/guide.ts` — 여정 6단계 정의(`JOURNEY_STEPS`), `inferStep()`, `resolveOverride()`, `cardStepState()`, localStorage 키. 순수.
- `vibelign-gui/src/lib/nav/guide.test.ts` — vitest.
- `vibelign-gui/src/lib/nav/useGuide.ts` — guide.ts의 React 바인딩(localStorage 영속). 얇은 훅.
- `vibelign-gui/src/components/nav/GuideStrip.tsx` — 상단 스트립(가이드 + 기존 상태 신호 통합).
- `vibelign-gui/src/components/nav/JourneyHowto.tsx` — 사용법 "순서대로 따라하기" 아코디언.

**수정:**
- `vibelign-gui/src/App.tsx` — `backupLoaded` 상태, `useGuide` 조립, 상태 스트립 교체, props 전달.
- `vibelign-gui/src/components/nav/StageHubCards.tsx` — `currentStep` prop + 차례 표시.
- `vibelign-gui/src/pages/Home.tsx` — `guideStep` prop + manual_list 뷰에 JourneyHowto.
- `vibelign-gui/src/pages/Settings.tsx` — 초보자 가이드 ON/OFF 토글 카드.

---

## Task 1: 여정·추론 순수 모듈 (`guide.ts`) — TDD

**Files:**
- Create: `vibelign-gui/src/lib/nav/guide.ts`
- Test: `vibelign-gui/src/lib/nav/guide.test.ts`

- [ ] **Step 1: 실패하는 테스트 작성**

`vibelign-gui/src/lib/nav/guide.test.ts`:
```ts
import { describe, it, expect } from "vitest";
import {
  JOURNEY_STEPS,
  journeyStep,
  inferStep,
  resolveOverride,
  cardStepState,
  guideOverrideKey,
  type GuideSignals,
} from "./guide";

const base: GuideSignals = {
  hasPlanDoc: false,
  planningPending: false,
  hasCheckpoint: false,
  changedFileCount: null,
  guardStatus: null,
};

describe("JOURNEY_STEPS", () => {
  it("6단계가 순서대로", () => {
    expect(JOURNEY_STEPS.map((j) => j.step)).toEqual([1, 2, 3, 4, 5, 6]);
  });
  it("journeyStep은 step 번호로 조회", () => {
    expect(journeyStep(3).label).toBe("체크포인트 저장");
    expect(journeyStep(4).targetPage).toBe("code");
  });
  it("1️⃣은 자동 단계 — 이동 목적지 없음", () => {
    expect(journeyStep(1).targetPage).toBeNull();
    expect(journeyStep(1).cardStage).toBeNull();
  });
});

describe("inferStep", () => {
  it("기획안 없으면 2", () => {
    expect(inferStep(base)).toBe(2);
  });
  it("기획 진행 중이면 기획안 있어도 2", () => {
    expect(inferStep({ ...base, hasPlanDoc: true, planningPending: true })).toBe(2);
  });
  it("기획안 있고 체크포인트 없으면 3", () => {
    expect(inferStep({ ...base, hasPlanDoc: true })).toBe(3);
  });
  it("기획안+체크포인트, 추가 신호 없으면 4", () => {
    expect(inferStep({ ...base, hasPlanDoc: true, hasCheckpoint: true })).toBe(4);
  });
  it("변경 감지되면 5 (뒤 단계 우선 — spec §4-1)", () => {
    expect(inferStep({ ...base, hasPlanDoc: true, hasCheckpoint: true, changedFileCount: 3 })).toBe(5);
  });
  it("변경 + guard 통과면 6", () => {
    expect(
      inferStep({ ...base, hasPlanDoc: true, hasCheckpoint: true, changedFileCount: 3, guardStatus: "ok" }),
    ).toBe(6);
  });
  it("체크포인트 없으면 변경 감지돼도 5로 점프하지 않음", () => {
    expect(inferStep({ ...base, hasPlanDoc: true, changedFileCount: 3 })).toBe(3);
  });
  it("신호 미배선(null)은 보수적 생략 — 4 유지 (spec §4-5)", () => {
    expect(
      inferStep({ ...base, hasPlanDoc: true, hasCheckpoint: true, changedFileCount: null, guardStatus: null }),
    ).toBe(4);
  });
});

describe("resolveOverride", () => {
  it("override 없으면 추론값", () => {
    expect(resolveOverride(null, 3)).toBe(3);
  });
  it("추론이 기록 시점과 같으면 override 유지", () => {
    expect(resolveOverride({ step: 5, baseInferred: 3 }, 3)).toBe(5);
  });
  it("추론이 바뀌면 override 자동 해제 (spec §4-2)", () => {
    expect(resolveOverride({ step: 5, baseInferred: 3 }, 4)).toBe(4);
  });
});

describe("cardStepState", () => {
  it("현재 단계가 속한 카드는 now", () => {
    expect(cardStepState("planning", 2)).toBe("now");
    expect(cardStepState("maintain", 3)).toBe("now");
    expect(cardStepState("develop", 4)).toBe("now");
    expect(cardStepState("maintain", 5)).toBe("now");
    expect(cardStepState("maintain", 6)).toBe("now");
  });
  it("카드의 모든 단계가 지났으면 done", () => {
    expect(cardStepState("planning", 3)).toBe("done");
    expect(cardStepState("develop", 5)).toBe("done");
  });
  it("남은 단계가 있으면 upcoming (유지보수 카드=3·5·6이라 4에서도 upcoming)", () => {
    expect(cardStepState("develop", 3)).toBe("upcoming");
    expect(cardStepState("maintain", 4)).toBe("upcoming");
  });
});

describe("guideOverrideKey", () => {
  it("프로젝트 경로별로 다른 키 (spec §4 진행 분리)", () => {
    expect(guideOverrideKey("/a")).not.toBe(guideOverrideKey("/b"));
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd vibelign-gui && npx vitest run src/lib/nav/guide.test.ts`
Expected: FAIL — `Cannot find module './guide'`

- [ ] **Step 3: 최소 구현**

`vibelign-gui/src/lib/nav/guide.ts`:
```ts
// === ANCHOR: NAV_GUIDE_START ===
// 강 PD 가이드 레이어 — 초보자 여정(6단계)의 단일 소스. 순수 데이터/함수.
// 근거: plans/2026-06-10-강PD-가이드레이어-design.md
import type { Page, Stage } from "./stages";

export type GuideStep = 1 | 2 | 3 | 4 | 5 | 6;
/** inferStep 반환 범위 — 1️⃣(프로젝트 열기)은 가이드가 뜨는 시점엔 항상 완료라 제외. */
export type ActiveGuideStep = 2 | 3 | 4 | 5 | 6;

export interface JourneyStep {
  step: GuideStep;
  icon: string;
  label: string;
  /** 스트립·카드에 보이는 행동 한 줄 */
  shortAction: string;
  /** 사용법 따라하기 아코디언의 상세 하우투 */
  howto: string[];
  /** "이동 →" 목적지. 1️⃣은 자동 단계라 null. */
  targetPage: Page | null;
  /** 이 단계가 속한 홈 허브 카드. 1️⃣은 카드 밖(null). */
  cardStage: Stage | null;
}

export const JOURNEY_STEPS: JourneyStep[] = [
  {
    step: 1,
    icon: "1️⃣",
    label: "프로젝트 열기",
    shortAction: "폴더를 고르면 준비 끝 — 감시가 자동으로 켜져요",
    howto: [
      "① 첫 화면에서 프로젝트 폴더를 선택해요",
      "② 코드맵과 앵커가 자동으로 준비돼요",
      "③ 감시(watch)가 자동으로 켜져요 — 코드 변경을 지켜보다 위반을 알려줘요 (백업은 아니에요)",
    ],
    targetPage: null,
    cardStage: null,
  },
  {
    step: 2,
    icon: "2️⃣",
    label: "기획하기",
    shortAction: "기획방에서 AI와 대화하며 기획안을 확정하세요",
    howto: [
      "① 기획 탭(기획방)으로 가요",
      "② \"무엇을 만들고 싶은지\" 한 줄로 적어요",
      "③ AI 페르소나들과 대화하며 생각을 다듬어요",
      "④ 기획안이 마음에 들면 확정(저장)해요",
    ],
    targetPage: "planning",
    cardStage: "planning",
  },
  {
    step: 3,
    icon: "3️⃣",
    label: "체크포인트 저장",
    shortAction: "AI에게 시키기 전에 안전 저장 — 망쳐도 되돌아올 수 있게",
    howto: [
      "① 유지보수 → 백업 탭으로 가요",
      "② \"체크포인트 저장\" 버튼을 눌러요",
      "③ 무슨 작업 전인지 설명을 짧게 적으면 끝!",
    ],
    targetPage: "backups",
    cardStage: "maintain",
  },
  {
    step: 4,
    icon: "4️⃣",
    label: "AI에게 작업 시키기",
    shortAction: "지시문을 복사해 AI 도구(Claude Code 등)에 붙여넣으세요",
    howto: [
      "① 개발 → 코드탐색 탭으로 가요",
      "② 기획안 기반 지시문을 복사해요",
      "③ Claude Code 같은 AI 도구에 붙여넣어요",
      "④ AI가 코드를 다 쓰면 다음 단계로!",
    ],
    targetPage: "code",
    cardStage: "develop",
  },
  {
    step: 5,
    icon: "5️⃣",
    label: "결과 검증",
    shortAction: "guard 검사로 AI가 약속한 범위만 고쳤는지 확인하세요",
    howto: [
      "① 유지보수 → 진단 탭으로 가요",
      "② guard 검사를 실행해요",
      "③ 문제가 있으면 진단이 고치는 길을 알려줘요",
    ],
    targetPage: "doctor",
    cardStage: "maintain",
  },
  {
    step: 6,
    icon: "6️⃣",
    label: "저장 또는 되돌리기",
    shortAction: "잘 됐으면 체크포인트 저장, 망쳤으면 undo",
    howto: [
      "① 결과가 마음에 들면 → 백업에서 체크포인트를 저장해요",
      "② 망쳤으면 → undo로 3️⃣ 시점으로 되돌려요",
      "③ 다음 작업이 있으면 4️⃣로 돌아가 반복!",
    ],
    targetPage: "backups",
    cardStage: "maintain",
  },
];

export function journeyStep(step: GuideStep): JourneyStep {
  return JOURNEY_STEPS[step - 1];
}

export interface GuideSignals {
  /** 확정 기획안 존재 (App: planningResult?.outputPath 기준) */
  hasPlanDoc: boolean;
  /** 기획 대화 진행 중(pending 메시지 존재) */
  planningPending: boolean;
  hasCheckpoint: boolean;
  /** 변경 파일 수. null = 신호 미배선(보수적 생략, spec §4-5) */
  changedFileCount: number | null;
  /** guard 결과. null = 신호 미배선 */
  guardStatus: "ok" | "issue" | null;
}

/** 신호 → 현재 단계 추론. 충돌 시 뒤 단계 우선(spec §4-1). 1️⃣은 반환하지 않음. */
export function inferStep(s: GuideSignals): ActiveGuideStep {
  let step: ActiveGuideStep = !s.hasPlanDoc || s.planningPending ? 2 : !s.hasCheckpoint ? 3 : 4;
  if (s.hasCheckpoint && s.changedFileCount !== null && s.changedFileCount > 0) {
    step = s.guardStatus === "ok" ? 6 : 5;
  }
  return step;
}

export interface GuideOverride {
  step: ActiveGuideStep;
  /** override 기록 시점의 추론값 — 추론이 바뀌면 자동 해제(spec §4-2) */
  baseInferred: ActiveGuideStep;
}

/** 수동 보정 적용: 추론이 기록 시점과 같을 때만 override 유지. */
export function resolveOverride(
  override: GuideOverride | null,
  inferred: ActiveGuideStep,
): ActiveGuideStep {
  if (override && override.baseInferred === inferred) return override.step;
  return inferred;
}

export type CardStepState = "now" | "done" | "upcoming";

/** 홈 허브 카드(3장)가 현재 여정에서 갖는 상태. 기획=[2], 개발=[4], 유지보수=[3,5,6]. */
export function cardStepState(stage: Stage, currentStep: ActiveGuideStep): CardStepState {
  const steps = JOURNEY_STEPS.filter((j) => j.cardStage === stage).map((j) => j.step);
  if (steps.includes(currentStep)) return "now";
  if (steps.every((st) => st < currentStep)) return "done";
  return "upcoming";
}

/** 가이드 ON/OFF — 전역 1개 (숙련도는 프로젝트와 무관, spec §4) */
export const GUIDE_ENABLED_KEY = "vibelign.guide.enabled";

/** 수동 보정 — 프로젝트 경로별 (진행 위치가 프로젝트 간 새면 안 됨, spec §4) */
export function guideOverrideKey(projectDir: string): string {
  return `vibelign.guide.override.${projectDir}`;
}
// === ANCHOR: NAV_GUIDE_END ===
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `cd vibelign-gui && npx vitest run src/lib/nav/guide.test.ts`
Expected: PASS (모든 it 통과)

- [ ] **Step 5: 기존 테스트 회귀 확인**

Run: `cd vibelign-gui && npx vitest run src/lib/nav/`
Expected: guide.test.ts + stages.test.ts 모두 PASS

- [ ] **Step 6: 커밋**

```bash
git add vibelign-gui/src/lib/nav/guide.ts vibelign-gui/src/lib/nav/guide.test.ts
git commit -m "feat(guide): 초보자 여정 6단계 순수 모듈(guide.ts) — 추론·override·카드상태 + 테스트"
```

---

## Task 2: React 바인딩 훅 (`useGuide.ts`)

**Files:**
- Create: `vibelign-gui/src/lib/nav/useGuide.ts`

- [ ] **Step 1: 훅 작성**

`vibelign-gui/src/lib/nav/useGuide.ts`:
```ts
// === ANCHOR: NAV_USE_GUIDE_START ===
// guide.ts 순수 로직의 React 바인딩 — localStorage 영속(전역 토글 + 프로젝트별 override).
import { useEffect, useMemo, useState } from "react";
import {
  GUIDE_ENABLED_KEY,
  guideOverrideKey,
  inferStep,
  resolveOverride,
  type ActiveGuideStep,
  type GuideOverride,
  type GuideSignals,
} from "./guide";

function readOverride(projectDir: string): GuideOverride | null {
  try {
    const raw = localStorage.getItem(guideOverrideKey(projectDir));
    return raw ? (JSON.parse(raw) as GuideOverride) : null;
  } catch {
    return null;
  }
}

export interface GuideState {
  enabled: boolean;
  /** 현재 단계. null = 신호 로딩 전(렌더 금지, spec §4-4) 또는 프로젝트 없음 */
  step: ActiveGuideStep | null;
  setStep: (next: ActiveGuideStep) => void;
  setEnabled: (on: boolean) => void;
}

export function useGuide(
  projectDir: string | null,
  signals: GuideSignals,
  signalsReady: boolean,
): GuideState {
  const [enabled, setEnabledState] = useState(
    () => localStorage.getItem(GUIDE_ENABLED_KEY) !== "0",
  );
  const [override, setOverride] = useState<GuideOverride | null>(null);

  useEffect(() => {
    setOverride(projectDir ? readOverride(projectDir) : null);
  }, [projectDir]);

  const inferred = useMemo(() => inferStep(signals), [signals]);
  const step = resolveOverride(override, inferred);

  function setStep(next: ActiveGuideStep) {
    const o: GuideOverride = { step: next, baseInferred: inferred };
    setOverride(o);
    if (projectDir) localStorage.setItem(guideOverrideKey(projectDir), JSON.stringify(o));
  }

  function setEnabled(on: boolean) {
    setEnabledState(on);
    localStorage.setItem(GUIDE_ENABLED_KEY, on ? "1" : "0");
  }

  return {
    enabled,
    step: projectDir && signalsReady ? step : null,
    setStep,
    setEnabled,
  };
}
// === ANCHOR: NAV_USE_GUIDE_END ===
```

- [ ] **Step 2: 타입체크**

Run: `cd vibelign-gui && npx tsc --noEmit`
Expected: 통과 (TSC_EXIT=0). 아직 미사용이라 에러 없어야 함.

- [ ] **Step 3: 커밋**

```bash
git add vibelign-gui/src/lib/nav/useGuide.ts
git commit -m "feat(guide): useGuide 훅 — localStorage 영속(전역 토글·프로젝트별 override)"
```

---

## Task 3: 가이드 스트립 (`GuideStrip.tsx`)

**Files:**
- Create: `vibelign-gui/src/components/nav/GuideStrip.tsx`

- [ ] **Step 1: 컴포넌트 작성**

기존 App.tsx 337~355행 상태 스트립의 스타일·신호를 그대로 흡수한다(우측 유지).

`vibelign-gui/src/components/nav/GuideStrip.tsx`:
```tsx
// === ANCHOR: GUIDE_STRIP_START ===
import { journeyStep, type ActiveGuideStep } from "../../lib/nav/guide";
import { PAGE_LABELS, type Page } from "../../lib/nav/stages";

interface GuideStripProps {
  enabled: boolean;
  /** null = 신호 로딩 전 — 가이드 부분 미렌더(spec §4-4) */
  step: ActiveGuideStep | null;
  hasCheckpoint: boolean;
  planningPending: boolean;
  onNavigate: (page: Page) => void;
  onStepChange: (next: ActiveGuideStep) => void;
  onDisable: () => void;
}

/** 탭바 아래 얇은 줄 — 좌측 가이드(지금 할 일) + 우측 기존 상태 신호. A안 상태 스트립을 대체·통합. */
export function GuideStrip({
  enabled,
  step,
  hasCheckpoint,
  planningPending,
  onNavigate,
  onStepChange,
  onDisable,
}: GuideStripProps) {
  const def = enabled && step ? journeyStep(step) : null;
  return (
    <div
      style={{
        display: "flex",
        alignItems: "center",
        gap: 16,
        padding: "3px 12px",
        fontSize: 11,
        color: "#888",
        background: "#0E0E0E",
        borderBottom: "1px solid #1A1A1A",
      }}
    >
      {def && step && (
        <span style={{ display: "flex", alignItems: "center", gap: 8, color: "#9cf" }}>
          <span>
            🧭 지금: {def.icon} <b>{def.label}</b> — {def.shortAction}
          </span>
          {def.targetPage && (
            <button
              className="nav-tab"
              style={{ fontSize: 11, padding: "0 8px" }}
              onClick={() => onNavigate(def.targetPage as Page)}
            >
              {PAGE_LABELS[def.targetPage]}으로 이동 →
            </button>
          )}
          <button
            className="nav-tab"
            style={{ fontSize: 11, padding: "0 6px" }}
            title="이전 단계"
            disabled={step === 2}
            onClick={() => step > 2 && onStepChange((step - 1) as ActiveGuideStep)}
          >
            ‹
          </button>
          <button
            className="nav-tab"
            style={{ fontSize: 11, padding: "0 6px" }}
            title="다음 단계"
            disabled={step === 6}
            onClick={() => step < 6 && onStepChange((step + 1) as ActiveGuideStep)}
          >
            ›
          </button>
          <button
            className="nav-tab"
            style={{ fontSize: 11, padding: "0 6px" }}
            title="가이드 끄기 (설정에서 다시 켤 수 있어요)"
            onClick={onDisable}
          >
            ×
          </button>
        </span>
      )}
      <div style={{ flex: 1 }} />
      <span style={{ color: hasCheckpoint ? "#aaa" : "#666" }}>
        {hasCheckpoint ? "✓ 백업 데이터 있음" : "백업 데이터 없음"}
      </span>
      {planningPending && <span style={{ color: "#FBBF24" }}>● 기획 진행 중</span>}
    </div>
  );
}
// === ANCHOR: GUIDE_STRIP_END ===
```

- [ ] **Step 2: 타입체크**

Run: `cd vibelign-gui && npx tsc --noEmit`
Expected: 통과 (TSC_EXIT=0)

- [ ] **Step 3: 커밋**

```bash
git add vibelign-gui/src/components/nav/GuideStrip.tsx
git commit -m "feat(guide): GuideStrip — 상태 스트립을 대체·통합하는 가이드 줄 컴포넌트"
```

---

## Task 4: App.tsx 조립 — 스트립 교체 + 신호 전달

**Files:**
- Modify: `vibelign-gui/src/App.tsx`

- [ ] **Step 1: import 추가**

App.tsx 상단 import 블록(25행 `HomePlanningStart` import 아래)에 추가:
```ts
import { GuideStrip } from "./components/nav/GuideStrip";
import { useGuide } from "./lib/nav/useGuide";
```

- [ ] **Step 2: `backupLoaded` 상태 추가 (신호 로딩 전 미렌더용)**

App.tsx:83 `const [backupCount, setBackupCount] = useState(0);` 바로 아래에 추가:
```ts
const [backupLoaded, setBackupLoaded] = useState(false);
```

backupList effect(121~144행)를 수정 — 기존:
```ts
  useEffect(() => {
    if (!projectDir) {
      setHasCheckpoint(false);
      setBackupCount(0);
      return;
    }
    let active = true;
    backupList(projectDir)
      .then((result) => {
        if (active) {
          setHasCheckpoint(result.backups.length > 0);
          setBackupCount(result.backups.length);
        }
      })
      .catch(() => {
        if (active) {
          setHasCheckpoint(false);
          setBackupCount(0);
        }
      });
    return () => {
      active = false;
    };
  }, [projectDir]);
```
변경(차이: 프로젝트 없을 때 false 리셋, 시작 시 false 리셋, finally에서 true):
```ts
  useEffect(() => {
    if (!projectDir) {
      setHasCheckpoint(false);
      setBackupCount(0);
      setBackupLoaded(false);
      return;
    }
    let active = true;
    setBackupLoaded(false);
    backupList(projectDir)
      .then((result) => {
        if (active) {
          setHasCheckpoint(result.backups.length > 0);
          setBackupCount(result.backups.length);
        }
      })
      .catch(() => {
        if (active) {
          setHasCheckpoint(false);
          setBackupCount(0);
        }
      })
      .finally(() => {
        if (active) setBackupLoaded(true);
      });
    return () => {
      active = false;
    };
  }, [projectDir]);
```

- [ ] **Step 3: `planningPendingNow` 파생값 + `useGuide` 호출 추가**

App.tsx의 `planningStatus` 파생값(284~288행) 바로 아래에 추가:
```ts
  const planningPendingNow = planningResult?.messages.some((m) => m.status === "pending") ?? false;
  // 가이드 레이어 — v1 신호: 기획안·체크포인트·기획진행. guard/변경수는 미배선(null) → 보수적 생략(spec §4-5).
  const guide = useGuide(
    projectDir,
    {
      hasPlanDoc: Boolean(planningResult?.outputPath),
      planningPending: planningPendingNow,
      hasCheckpoint,
      changedFileCount: null,
      guardStatus: null,
    },
    backupLoaded,
  );
```

- [ ] **Step 4: 상태 스트립(337~355행)을 GuideStrip으로 교체**

기존(337~355행):
```tsx
            <div
              style={{
                display: "flex",
                alignItems: "center",
                gap: 16,
                padding: "3px 12px",
                fontSize: 11,
                color: "#888",
                background: "#0E0E0E",
                borderBottom: "1px solid #1A1A1A",
              }}
            >
              <span style={{ color: hasCheckpoint ? "#aaa" : "#666" }}>
                {hasCheckpoint ? "✓ 백업 데이터 있음" : "백업 데이터 없음"}
              </span>
              {(planningResult?.messages.some((message) => message.status === "pending") ?? false) && (
                <span style={{ color: "#FBBF24" }}>● 기획 진행 중</span>
              )}
            </div>
```
교체:
```tsx
            <GuideStrip
              enabled={guide.enabled}
              step={guide.step}
              hasCheckpoint={hasCheckpoint}
              planningPending={planningPendingNow}
              onNavigate={navigate}
              onStepChange={guide.setStep}
              onDisable={() => guide.setEnabled(false)}
            />
```

- [ ] **Step 5: 타입체크**

Run: `cd vibelign-gui && npx tsc --noEmit`
Expected: 통과 (TSC_EXIT=0)

- [ ] **Step 6: 시각 확인**

Run: `cd vibelign-gui && npm run tauri dev` (또는 사용자 구동)
확인:
- 프로젝트 열면 탭바 아래 스트립에 `🧭 지금: …` + `[…으로 이동 →]` + `‹ › ×` 노출
- 기획안 없는 프로젝트 → "2️⃣ 기획하기" 안내
- `‹`는 2단계에서 비활성, `›`로 단계 이동 가능, 이동 후 앱 재시작해도 유지(같은 프로젝트)
- `×` 클릭 → 가이드 부분만 사라지고 우측 상태 신호(백업/기획 진행)는 유지
- 신호 로딩 직후 잘못된 안내가 깜빡이지 않음

- [ ] **Step 7: 커밋**

```bash
git add vibelign-gui/src/App.tsx
git commit -m "feat(guide): App에 가이드 스트립 조립 — 상태 스트립 대체, 신호 로딩 가드"
```

---

## Task 5: 홈 허브 카드 차례 표시 (`StageHubCards.tsx`)

**Files:**
- Modify: `vibelign-gui/src/components/nav/StageHubCards.tsx` (전체 53줄 — 아래 전문으로 교체)
- Modify: `vibelign-gui/src/App.tsx:361` (StageHubCards 호출)

- [ ] **Step 1: StageHubCards 수정 (파일 전문)**

```tsx
// === ANCHOR: STAGE_HUB_CARDS_START ===
import { STAGE_DEFS, pagesForStage, type Stage, type Page } from "../../lib/nav/stages";
import { cardStepState, journeyStep, type ActiveGuideStep } from "../../lib/nav/guide";

interface StageHubCardsProps {
  onNavigate: (page: Page) => void;
  planningStatus: "none" | "active" | "done";
  backupCount: number;
  /** 가이드 현재 단계. null = 가이드 꺼짐/로딩 전(기존 표시 그대로). */
  currentStep?: ActiveGuideStep | null;
}

const CARD_DESC: Record<Stage, string> = {
  planning: "아이디어를 기획안으로",
  develop: "코드 보기·탐색",
  maintain: "안전 점검·복원",
};

function badgeText(stage: Stage, planningStatus: StageHubCardsProps["planningStatus"], backupCount: number): string {
  if (stage === "planning") {
    return planningStatus === "active" ? "● 진행 중" : planningStatus === "done" ? "완료" : "대기";
  }
  if (stage === "maintain") return `백업 ${backupCount}개`;
  return "열기"; // develop: 기존 신호 없음
}

export function StageHubCards({ onNavigate, planningStatus, backupCount, currentStep = null }: StageHubCardsProps) {
  return (
    <div style={{ display: "flex", gap: 12, padding: 16 }}>
      {STAGE_DEFS.map((def) => {
        const guideState = currentStep ? cardStepState(def.key, currentStep) : null;
        return (
          <button
            key={def.key}
            onClick={() => onNavigate(pagesForStage(def.key)[0])}
            style={{
              flex: 1,
              textAlign: "left",
              padding: 16,
              background: guideState === "now" ? "#1a1813" : "#141414",
              border: guideState === "now" ? "2px solid #FBBF24" : "1px solid #2A2A2A",
              borderRadius: 8,
              color: "#ddd",
              cursor: "pointer",
            }}
          >
            <div style={{ fontSize: 20 }}>{def.icon}</div>
            <div style={{ fontWeight: 600, marginTop: 6 }}>
              {def.label}
              {guideState === "now" && (
                <span style={{ marginLeft: 8, fontSize: 11, background: "#FBBF24", color: "#000", padding: "1px 6px", borderRadius: 4, fontWeight: 700 }}>
                  지금 할 차례
                </span>
              )}
              {guideState === "done" && <span style={{ marginLeft: 8, fontSize: 11, color: "#4DFF91" }}>✓ 완료</span>}
              {guideState === "upcoming" && <span style={{ marginLeft: 8, fontSize: 11, color: "#666" }}>다음</span>}
            </div>
            <div style={{ fontSize: 12, color: "#888", marginTop: 4 }}>{CARD_DESC[def.key]}</div>
            {guideState === "now" && currentStep && (
              <div style={{ fontSize: 12, color: "#FBBF24", marginTop: 6 }}>
                → {journeyStep(currentStep).icon} {journeyStep(currentStep).shortAction}
              </div>
            )}
            <div style={{ fontSize: 12, color: def.key === "planning" && planningStatus === "active" ? "#FBBF24" : "#aaa", marginTop: 8 }}>
              {badgeText(def.key, planningStatus, backupCount)}
            </div>
          </button>
        );
      })}
    </div>
  );
}
// === ANCHOR: STAGE_HUB_CARDS_END ===
```

- [ ] **Step 2: App.tsx 호출에 prop 추가**

App.tsx:361 — 기존:
```tsx
<StageHubCards onNavigate={navigate} planningStatus={planningStatus} backupCount={backupCount} />
```
변경:
```tsx
<StageHubCards onNavigate={navigate} planningStatus={planningStatus} backupCount={backupCount} currentStep={guide.enabled ? guide.step : null} />
```

- [ ] **Step 3: 타입체크 + 시각 확인**

Run: `cd vibelign-gui && npx tsc --noEmit`
Expected: 통과 (TSC_EXIT=0)
시각: 홈에서 현재 단계 카드에 노란 테두리+"지금 할 차례"+행동 한 줄, 지난 카드 "✓ 완료", 남은 카드 "다음". 가이드 끄면(×) 카드가 기존 모습으로 복귀.

- [ ] **Step 4: 커밋**

```bash
git add vibelign-gui/src/components/nav/StageHubCards.tsx vibelign-gui/src/App.tsx
git commit -m "feat(guide): 홈 허브 카드에 여정 차례 표시 — 지금 할 차례·완료·다음"
```

---

## Task 6: 사용법 따라하기 아코디언 (`JourneyHowto.tsx` + Home 통합)

**Files:**
- Create: `vibelign-gui/src/components/nav/JourneyHowto.tsx`
- Modify: `vibelign-gui/src/pages/Home.tsx` (props + manual_list 뷰)
- Modify: `vibelign-gui/src/App.tsx:378` (manual Home 호출)

- [ ] **Step 1: JourneyHowto 작성**

`vibelign-gui/src/components/nav/JourneyHowto.tsx`:
```tsx
// === ANCHOR: JOURNEY_HOWTO_START ===
import { useEffect, useState } from "react";
import { JOURNEY_STEPS, type ActiveGuideStep, type GuideStep } from "../../lib/nav/guide";
import { PAGE_LABELS, type Page } from "../../lib/nav/stages";

interface JourneyHowtoProps {
  /** 현재 단계(가이드 추론). null이면 강조 없이 전부 접힘. */
  currentStep: ActiveGuideStep | null;
  onNavigate: (page: Page) => void;
}

/** 사용법 탭 상단 "순서대로 따라하기" 아코디언. 현재 단계가 자동으로 펼쳐진다(spec §3.4). */
export function JourneyHowto({ currentStep, onNavigate }: JourneyHowtoProps) {
  const [open, setOpen] = useState<GuideStep | null>(currentStep);
  useEffect(() => setOpen(currentStep), [currentStep]);
  return (
    <div style={{ padding: "16px 20px 4px" }}>
      <div style={{ fontWeight: 700, fontSize: 13, marginBottom: 8 }}>📖 순서대로 따라하기</div>
      {JOURNEY_STEPS.map((def) => {
        const isOpen = open === def.step;
        const isCurrent = currentStep === def.step;
        return (
          <div
            key={def.step}
            style={{
              border: isCurrent ? "1px solid #FBBF24" : "1px solid #2A2A2A",
              background: isCurrent ? "#1a1813" : "#141414",
              borderRadius: 6,
              marginBottom: 4,
            }}
          >
            <button
              onClick={() => setOpen(isOpen ? null : def.step)}
              style={{
                width: "100%",
                textAlign: "left",
                padding: "8px 12px",
                background: "transparent",
                border: "none",
                color: "#ddd",
                cursor: "pointer",
                fontSize: 13,
              }}
            >
              {def.icon} <b>{def.label}</b> — <span style={{ color: "#888" }}>{def.shortAction}</span>
              <span style={{ float: "right", color: "#666" }}>{isOpen ? "▾" : "▸"}</span>
            </button>
            {isOpen && (
              <div style={{ padding: "0 12px 10px", fontSize: 12, color: "#aaa", lineHeight: 1.8 }}>
                {def.howto.map((line) => (
                  <div key={line}>{line}</div>
                ))}
                {def.targetPage && (
                  <button
                    className="nav-tab"
                    style={{ fontSize: 12, marginTop: 6 }}
                    onClick={() => onNavigate(def.targetPage as Page)}
                  >
                    지금 하러 가기 → {PAGE_LABELS[def.targetPage]}
                  </button>
                )}
              </div>
            )}
          </div>
        );
      })}
    </div>
  );
}
// === ANCHOR: JOURNEY_HOWTO_END ===
```

- [ ] **Step 2: Home.tsx에 prop 추가 + manual_list 뷰 통합**

(a) import 추가 — Home.tsx 상단 import 블록에:
```ts
import { JourneyHowto } from "../components/nav/JourneyHowto";
import type { ActiveGuideStep } from "../lib/nav/guide";
```

(b) `HomeProps` 인터페이스(Home.tsx:17 부근)에 추가:
```ts
  /** 가이드 현재 단계 — 사용법 따라하기 아코디언 강조용. */
  guideStep?: ActiveGuideStep | null;
```

(c) 함수 시그니처(Home.tsx:45)의 구조분해에 `guideStep = null` 추가 — 기존 `onOpenPlanningHistory }` 앞에:
```ts
..., onOpenPlanningHistory, guideStep = null }: HomeProps) {
```

(d) manual_list 뷰(Home.tsx:86~97) — 기존:
```tsx
  if (view === "manual_list") {
    return (
      <ManualCommandList
        onBack={() => setView("home")}
        onSelectCommand={(command) => {
          setSelectedCmd(command);
          setView("manual_detail");
        }}
      />
    );
  }
```
변경(아코디언을 위에, 사전을 아래에 — 한 페이지 스크롤):
```tsx
  if (view === "manual_list") {
    return (
      <div style={{ height: "100%", overflowY: "auto" }}>
        <JourneyHowto currentStep={guideStep} onNavigate={(p) => onNavigate?.(p)} />
        <ManualCommandList
          onBack={() => setView("home")}
          onSelectCommand={(command) => {
            setSelectedCmd(command);
            setView("manual_detail");
          }}
        />
      </div>
    );
  }
```
> 주의: `onNavigate`가 HomeProps에서 옵셔널이면 `onNavigate?.(p)` 형태 유지, 필수면 `onNavigate(p)`로. (구현 시 HomeProps 타입 확인해 한쪽으로.)

- [ ] **Step 3: App.tsx manual Home 호출 수정**

App.tsx:378 — 기존 `onNavigate={setPage}` 를 `onNavigate={navigate}` 로 바꾸고 `guideStep` 추가:
```tsx
{page === "manual" && <Home key="manual" projectDir={projectDir} apiKey={apiKey} providerKeys={providerKeys} hasAnyAiKey={hasAnyAiKey} aiKeyStatusLoaded={envKeyStatusLoaded} onNavigate={navigate} onOpenSettings={openSettings} initialView="manual_list" watchOn={watchOn} setWatchOn={setWatchOn} mapMode={mapMode} setMapMode={setMapMode} onStartPlanning={(idea) => { if (projectDir) void openPlanningRoom(projectDir, idea); }} guideStep={guide.enabled ? guide.step : null} />}
```
(`navigate` 사용 이유: 따라하기의 "진단으로 가기"가 launch intent 초기화를 거치게 — App.tsx:187-190)

- [ ] **Step 4: 타입체크 + 시각 확인**

Run: `cd vibelign-gui && npx tsc --noEmit`
Expected: 통과 (TSC_EXIT=0)
시각: 사용법 탭 → 맨 위 "📖 순서대로 따라하기" 6단계, 현재 단계가 노란 테두리로 자동 펼쳐짐, "지금 하러 가기 →" 클릭 시 해당 탭 이동, 그 아래 기존 명령어 사전 정상. **스크롤바가 이중으로 생기지 않는지 확인** — 이중이면 ManualCommandList 자체 스크롤 컨테이너와 겹친 것이므로 바깥 div의 `overflowY:"auto"`를 제거하고 ManualCommandList의 기존 스크롤 흐름에 맡긴다.

- [ ] **Step 5: 커밋**

```bash
git add vibelign-gui/src/components/nav/JourneyHowto.tsx vibelign-gui/src/pages/Home.tsx vibelign-gui/src/App.tsx
git commit -m "feat(guide): 사용법 탭에 '순서대로 따라하기' 아코디언 — 현재 단계 자동 펼침"
```

---

## Task 7: 설정 토글 (Settings)

**Files:**
- Modify: `vibelign-gui/src/pages/Settings.tsx` (props 2개 + 토글 카드 1개)
- Modify: `vibelign-gui/src/App.tsx:395` (Settings 호출)

- [ ] **Step 1: SettingsProps에 props 추가**

Settings.tsx:24 `interface SettingsProps {` 블록 안에 추가:
```ts
  guideEnabled?: boolean;
  onGuideEnabledChange?: (on: boolean) => void;
```
Settings.tsx:33 구조분해에 추가:
```ts
export default function Settings({ apiKey, onApiKeyChange, providerKeys, onKeysUpdated, projectDir, notice, guideEnabled, onGuideEnabledChange }: SettingsProps) {
```

- [ ] **Step 2: 토글 카드 추가**

Settings.tsx:557 (AI 앵커 보강 토글 섹션 닫는 `</div>` 다음, "AI 도구 설정" 섹션 앞)에 삽입 — 기존 토글 섹션과 동일한 패턴:
```tsx
        {/* 초보자 가이드 토글 섹션 */}
        <div className="card" style={{ marginTop: 16 }}>
          <div style={{ fontWeight: 700, fontSize: 12, marginBottom: 12, textTransform: "uppercase", letterSpacing: 1 }}>
            초보자 가이드
          </div>
          <div style={{ fontSize: 13, lineHeight: 1.7, marginBottom: 10 }}>
            화면 상단에 "지금 할 일 → 다음 행동"을 안내하는 가이드 줄과 홈 카드의 차례 표시를 보여줍니다.
            익숙해지면 꺼도 좋아요. (가이드 줄의 × 버튼으로도 끌 수 있어요)
          </div>
          {guideEnabled === undefined || !onGuideEnabledChange ? (
            <div style={{ fontSize: 11, color: "#777" }}>프로젝트를 먼저 선택하세요.</div>
          ) : (
            <div style={{ display: "flex", alignItems: "center", gap: 12 }}>
              <button
                className="btn btn-sm"
                onClick={() => onGuideEnabledChange(!guideEnabled)}
                style={{
                  background: guideEnabled ? "#4DFF91" : "#1A1A1A",
                  color: guideEnabled ? "#1A1A1A" : "#888",
                  border: "2px solid #1A1A1A",
                  fontWeight: 700,
                }}
              >
                {guideEnabled ? "ON" : "OFF"}
              </button>
              <span style={{ fontSize: 11, color: "#888", fontFamily: "IBM Plex Mono, monospace" }}>
                guide_enabled: {guideEnabled ? "true" : "false"}
              </span>
            </div>
          )}
        </div>
```

- [ ] **Step 3: App.tsx Settings 호출에 props 전달**

App.tsx:395 — 기존:
```tsx
<Settings apiKey={apiKey} onApiKeyChange={setApiKey} providerKeys={providerKeys} onKeysUpdated={refreshAiKeys} projectDir={projectDir} notice={settingsNotice} />
```
변경:
```tsx
<Settings apiKey={apiKey} onApiKeyChange={setApiKey} providerKeys={providerKeys} onKeysUpdated={refreshAiKeys} projectDir={projectDir} notice={settingsNotice} guideEnabled={guide.enabled} onGuideEnabledChange={guide.setEnabled} />
```

- [ ] **Step 4: 타입체크 + 시각 확인**

Run: `cd vibelign-gui && npx tsc --noEmit`
Expected: 통과 (TSC_EXIT=0)
시각: 설정 → "초보자 가이드" 카드 ON/OFF. OFF→스트립 가이드·카드 차례 표시 사라짐, ON→복귀. 가이드 줄 ×로 끈 뒤 설정에서 다시 ON 가능.

- [ ] **Step 5: 커밋**

```bash
git add vibelign-gui/src/pages/Settings.tsx vibelign-gui/src/App.tsx
git commit -m "feat(guide): 설정에 초보자 가이드 ON/OFF 토글"
```

---

## Task 8: 전체 검증 + push

**Files:** (코드 변경 없음, 발견 시 소규모 수정)

- [ ] **Step 1: 전체 테스트**

Run: `cd vibelign-gui && npx vitest run`
Expected: guide.test.ts + stages.test.ts 포함 전부 PASS

- [ ] **Step 2: 전체 빌드**

Run: `cd vibelign-gui && npm run build`
Expected: tsc + vite build 성공

- [ ] **Step 3: 육안 회귀 체크리스트** (`npm run tauri dev`)

- [ ] 기획안 없는 프로젝트: 스트립 "2️⃣ 기획하기" / 홈 기획 카드 "지금 할 차례"
- [ ] 기획안 확정 후: "3️⃣ 체크포인트 저장"으로 전환 (override 자동 해제 확인 — 수동으로 4️⃣ 옮겨놨다가 기획안 상태 바뀌면 추론값 복귀)
- [ ] 체크포인트 저장 후: "4️⃣ AI에게 작업 시키기"
- [ ] `‹ ›` 수동 보정 + 앱 재시작 후 유지(같은 프로젝트), 다른 프로젝트 열면 그 프로젝트 기준
- [ ] `×` 끄기 → 우측 상태 신호만 남음 → 설정에서 재활성
- [ ] 사용법 따라하기: 현재 단계 자동 펼침, "지금 하러 가기" 이동, 명령어 사전 무손상
- [ ] 기존 동작 회귀 없음: 단계 전환 바·서브탭·기획방 진입·진단 launch intent

- [ ] **Step 4: push**

```bash
git push origin feat/vibelign-product-renew
```

---

## 후속 (이 계획 밖, 명시)

- `changedFileCount`·`guardStatus` 신호 배선 → 5️⃣·6️⃣ 자동 추론 활성화 (guide.ts는 무변경, App 신호 전달만)
- 자동 백업(감시 연동)이 생기면 3️⃣ 단계 자동화 검토 (spec §6)
- 사용법 콘텐츠 외부화(노션/전자책) 연동 — 펀딩 문서 §5-3 별도 트랙

## Self-Review 결과 (작성자 점검)

- **Spec 커버리지:** §2 여정=JOURNEY_STEPS(Task1) / §3.1 guide.ts·localStorage=Task1·2 / §3.2 GuideStrip=Task3·4 / §3.3 허브 카드=Task5 / §3.4 따라하기=Task6 / 토글(§1)=Task7 / §4 엣지 5종=inferStep 뒤단계우선(테스트)·resolveOverride(테스트)·기획 건너뛰기(‹›·×)·로딩 전 미렌더(backupLoaded→step null)·미배선 생략(null 신호 테스트) / §5 검증=Task1 TDD+Task8. 갭 없음.
- **플레이스홀더 스캔:** 모든 코드 스텝에 실제 코드 전문 포함. "적절히" 류 없음. Task6 Step2(d)의 onNavigate 옵셔널 여부만 구현 시 확인 항목으로 명시.
- **타입 일관성:** `ActiveGuideStep`·`GuideSignals`·`GuideOverride`·`inferStep`·`resolveOverride`·`cardStepState`·`journeyStep`·`useGuide`·`guide.step`·`guide.setStep`·`guide.setEnabled` 명칭이 Task 간 일치. `GuideStrip`/`JourneyHowto`/`StageHubCards` props 명칭 일치.
