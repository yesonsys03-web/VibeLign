# 강 PD UX B안 (3단계 IA) 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 평평한 6-탭 네비게이션을 기획·개발·유지보수 3단계 IA(홈 허브 + 단계 전환 바 + 서브탭)로 교체하고 기획방을 1급 단계로 승격한다.

**Architecture:** 라우팅 상태(`page`)·페이지 컴포넌트는 보존하고 **네비게이션 셸만 교체**한다. 순수 매핑 함수 `stages.ts`(page↔stage)를 토대로, 신규 nav 컴포넌트 3개(전환 바·서브탭·허브 카드)를 만들고 `App.tsx`에서 조립한다.

**Tech Stack:** React + TypeScript (Tauri GUI), vitest(순수 함수 단위테스트), tsc + tauri dev(시각 검증).

> **검증 방침(중요):** 이 프로젝트와 설계 spec은 UI 레이아웃에 단위테스트를 요구하지 않는다(CLAUDE.md "가장 작은 패치", spec §7 "tsc + 시각 렌더"). 따라서 **순수 로직(`stages.ts`)만 TDD**하고, 컴포넌트·통합은 `tsc --noEmit` 통과 + 육안 렌더로 검증한다. 이는 superpowers 기본 TDD를 사용자 지침이 덮어쓰는 경우다.

**근거 spec:** `plans/2026-06-10-강PD-UX-B안-3단계IA-설계.md` (커밋 da8c29f)
**현재 코드 기준:** A안 적용 후(커밋 9836462). `App.tsx`의 평평한 nav-tabs = 291~333행, 상태 스트립 = 335~353행, 페이지 라우팅 = 355~379행. `type Page` = App.tsx:64. `진단`은 `openDoctorTab()`(App.tsx:170) 경유.

---

## File Structure

**신규 파일:**
- `vibelign-gui/src/lib/nav/stages.ts` — `Page`/`Stage` 타입, `STAGE_DEFS`, `stageOf()`, `pagesForStage()`, `PAGE_LABELS`. 순수.
- `vibelign-gui/src/lib/nav/stages.test.ts` — vitest 단위테스트.
- `vibelign-gui/src/components/nav/StageSwitcherBar.tsx` — 상단 전환 바(홈 + 3단계 + 우측 유틸). 항상 표시.
- `vibelign-gui/src/components/nav/StageSubnav.tsx` — 현재 단계의 서브탭(페이지 2개 이상일 때만).
- `vibelign-gui/src/components/nav/StageHubCards.tsx` — 홈 본문의 단계 카드 3장 + 가이드 뱃지.

**수정 파일:**
- `vibelign-gui/src/App.tsx` — `Page` 타입 이동(stages.ts에서 import), `backupCount` 상태 추가, nav-tabs(291~333) 교체, `navigate()` 래퍼, 홈 본문에 허브 카드.

각 신규 컴포넌트는 props 주도(presentational)로 책임을 좁게 유지한다. App.tsx는 조립·라우팅 분기만 담당한다.

---

## Task 1: 단계 매핑 순수 모듈 (`stages.ts`) — TDD

**Files:**
- Create: `vibelign-gui/src/lib/nav/stages.ts`
- Test: `vibelign-gui/src/lib/nav/stages.test.ts`

- [ ] **Step 1: 실패하는 테스트 작성**

`vibelign-gui/src/lib/nav/stages.test.ts`:
```ts
import { describe, it, expect } from "vitest";
import { stageOf, pagesForStage, STAGE_DEFS, PAGE_LABELS } from "./stages";

describe("stageOf", () => {
  it("기획방은 기획 단계", () => {
    expect(stageOf("planning")).toBe("planning");
  });
  it("코드탐색·문서는 개발 단계", () => {
    expect(stageOf("code")).toBe("develop");
    expect(stageOf("docs")).toBe("develop");
  });
  it("진단·백업·에러로그는 유지보수 단계", () => {
    expect(stageOf("doctor")).toBe("maintain");
    expect(stageOf("backups")).toBe("maintain");
    expect(stageOf("logs")).toBe("maintain");
  });
  it("홈·사용법·설정은 단계 없음(null)", () => {
    expect(stageOf("home")).toBeNull();
    expect(stageOf("manual")).toBeNull();
    expect(stageOf("settings")).toBeNull();
  });
});

describe("pagesForStage", () => {
  it("개발 단계는 코드탐색·문서 순서", () => {
    expect(pagesForStage("develop")).toEqual(["code", "docs"]);
  });
  it("유지보수 단계는 진단·백업·에러로그 순서", () => {
    expect(pagesForStage("maintain")).toEqual(["doctor", "backups", "logs"]);
  });
});

describe("STAGE_DEFS", () => {
  it("기획→개발→유지보수 순서", () => {
    expect(STAGE_DEFS.map((d) => d.key)).toEqual(["planning", "develop", "maintain"]);
  });
});

describe("PAGE_LABELS", () => {
  it("A안 한글 라벨 유지", () => {
    expect(PAGE_LABELS.docs).toBe("문서");
    expect(PAGE_LABELS.code).toBe("코드탐색");
    expect(PAGE_LABELS.doctor).toBe("진단");
    expect(PAGE_LABELS.backups).toBe("백업");
    expect(PAGE_LABELS.logs).toBe("에러로그");
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd vibelign-gui && npx vitest run src/lib/nav/stages.test.ts`
Expected: FAIL — `Cannot find module './stages'`

- [ ] **Step 3: 최소 구현**

`vibelign-gui/src/lib/nav/stages.ts`:
```ts
// === ANCHOR: NAV_STAGES_START ===
// 3단계 IA(기획·개발·유지보수)의 page↔stage 매핑. 순수 데이터/함수.
export type Page =
  | "home"
  | "manual"
  | "docs"
  | "code"
  | "doctor"
  | "backups"
  | "logs"
  | "settings"
  | "planning";

export type Stage = "planning" | "develop" | "maintain";

export interface StageDef {
  key: Stage;
  label: string;
  icon: string;
  pages: Page[];
}

export const STAGE_DEFS: StageDef[] = [
  { key: "planning", label: "기획", icon: "📋", pages: ["planning"] },
  { key: "develop", label: "개발", icon: "⚙️", pages: ["code", "docs"] },
  { key: "maintain", label: "유지보수", icon: "🛡️", pages: ["doctor", "backups", "logs"] },
];

/** page가 속한 단계. 홈/사용법/설정은 단계 없음(null). */
export function stageOf(page: Page): Stage | null {
  return STAGE_DEFS.find((d) => d.pages.includes(page))?.key ?? null;
}

/** 단계에 속한 page 목록(서브탭 순서). */
export function pagesForStage(stage: Stage): Page[] {
  return STAGE_DEFS.find((d) => d.key === stage)?.pages ?? [];
}

/** A안에서 확정한 한글 라벨. 서브탭·표시용. */
export const PAGE_LABELS: Record<Page, string> = {
  home: "홈",
  planning: "기획방",
  code: "코드탐색",
  docs: "문서",
  doctor: "진단",
  backups: "백업",
  logs: "에러로그",
  manual: "사용법",
  settings: "설정",
};
// === ANCHOR: NAV_STAGES_END ===
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `cd vibelign-gui && npx vitest run src/lib/nav/stages.test.ts`
Expected: PASS (모든 it 통과)

- [ ] **Step 5: 커밋**

```bash
git add vibelign-gui/src/lib/nav/stages.ts vibelign-gui/src/lib/nav/stages.test.ts
git commit -m "feat(nav): 3단계 IA page↔stage 매핑 모듈(stages.ts) + 테스트"
```

---

## Task 2: 상단 단계 전환 바 (`StageSwitcherBar.tsx`)

**Files:**
- Create: `vibelign-gui/src/components/nav/StageSwitcherBar.tsx`

- [ ] **Step 1: 컴포넌트 작성**

`vibelign-gui/src/components/nav/StageSwitcherBar.tsx`:
```tsx
// === ANCHOR: STAGE_SWITCHER_BAR_START ===
import { STAGE_DEFS, stageOf, pagesForStage, type Page } from "../../lib/nav/stages";

interface StageSwitcherBarProps {
  page: Page;
  projectDir: string;
  onHome: () => void;
  onNavigate: (page: Page) => void;
  onOpenManual: () => void;
  onOpenFolder: () => void;
  onExitProject: () => void;
}

export function StageSwitcherBar({
  page,
  projectDir,
  onHome,
  onNavigate,
  onOpenManual,
  onOpenFolder,
  onExitProject,
}: StageSwitcherBarProps) {
  const activeStage = stageOf(page);
  return (
    <div className="nav-tabs" style={{ paddingLeft: 8 }}>
      <button className={`nav-tab ${page === "home" ? "active" : ""}`} onClick={onHome}>
        홈
      </button>
      {STAGE_DEFS.map((def) => (
        <button
          key={def.key}
          className={`nav-tab ${activeStage === def.key ? "active" : ""}`}
          onClick={() => onNavigate(pagesForStage(def.key)[0])}
        >
          {def.icon} {def.label}
        </button>
      ))}
      <div style={{ flex: 1 }} />
      <button className={`nav-tab ${page === "manual" ? "active" : ""}`} onClick={onOpenManual}>
        사용법
      </button>
      <button className="nav-tab" onClick={onOpenFolder}>
        폴더열기
      </button>
      <button
        className="nav-tab"
        style={{
          borderRight: "none",
          fontSize: 11,
          color: "#777",
          maxWidth: 260,
          overflow: "hidden",
          textOverflow: "ellipsis",
          display: "block",
        }}
        title={projectDir}
        onClick={onExitProject}
      >
        {projectDir.replace(/\\/g, "/").split("/").filter(Boolean).slice(-1)[0] || projectDir} ↩
      </button>
    </div>
  );
}
// === ANCHOR: STAGE_SWITCHER_BAR_END ===
```

- [ ] **Step 2: 타입체크**

Run: `cd vibelign-gui && npx tsc --noEmit`
Expected: 통과 (TSC_EXIT=0). 아직 미사용 컴포넌트라 에러 없어야 함.

- [ ] **Step 3: 커밋**

```bash
git add vibelign-gui/src/components/nav/StageSwitcherBar.tsx
git commit -m "feat(nav): 상단 단계 전환 바 컴포넌트"
```

---

## Task 3: 단계별 서브탭 (`StageSubnav.tsx`)

**Files:**
- Create: `vibelign-gui/src/components/nav/StageSubnav.tsx`

- [ ] **Step 1: 컴포넌트 작성**

`vibelign-gui/src/components/nav/StageSubnav.tsx`:
```tsx
// === ANCHOR: STAGE_SUBNAV_START ===
import { stageOf, pagesForStage, PAGE_LABELS, type Page } from "../../lib/nav/stages";

interface StageSubnavProps {
  page: Page;
  onNavigate: (page: Page) => void;
}

/** 현재 단계의 서브탭. 단계에 페이지가 2개 이상일 때만 렌더(기획 단계는 숨김). */
export function StageSubnav({ page, onNavigate }: StageSubnavProps) {
  const stage = stageOf(page);
  if (!stage) return null;
  const pages = pagesForStage(stage);
  if (pages.length < 2) return null;
  return (
    <div style={{ display: "flex", gap: 4, padding: "4px 12px", borderBottom: "1px solid #1A1A1A" }}>
      {pages.map((p) => (
        <button
          key={p}
          className={`nav-tab ${page === p ? "active" : ""}`}
          style={{ fontSize: 12, padding: "2px 10px" }}
          onClick={() => onNavigate(p)}
        >
          {PAGE_LABELS[p]}
        </button>
      ))}
    </div>
  );
}
// === ANCHOR: STAGE_SUBNAV_END ===
```

- [ ] **Step 2: 타입체크**

Run: `cd vibelign-gui && npx tsc --noEmit`
Expected: 통과 (TSC_EXIT=0)

- [ ] **Step 3: 커밋**

```bash
git add vibelign-gui/src/components/nav/StageSubnav.tsx
git commit -m "feat(nav): 단계별 서브탭 컴포넌트"
```

---

## Task 4: 홈 허브 단계 카드 (`StageHubCards.tsx`)

**Files:**
- Create: `vibelign-gui/src/components/nav/StageHubCards.tsx`

- [ ] **Step 1: 컴포넌트 작성**

`vibelign-gui/src/components/nav/StageHubCards.tsx`:
```tsx
// === ANCHOR: STAGE_HUB_CARDS_START ===
import { STAGE_DEFS, pagesForStage, type Stage, type Page } from "../../lib/nav/stages";

interface StageHubCardsProps {
  onNavigate: (page: Page) => void;
  planningStatus: "none" | "active" | "done";
  backupCount: number;
}

const CARD_DESC: Record<Stage, string> = {
  planning: "아이디어를 기획안으로",
  develop: "코드 보기·탐색",
  maintain: "안전 점검·복원",
};

function badgeText(stage: Stage, planningStatus: StageHubCardsProps["planningStatus"], backupCount: number): string {
  if (stage === "planning") {
    return planningStatus === "active" ? "● 진행 중" : planningStatus === "done" ? "완료" : "대기";
  }
  if (stage === "maintain") return `백업 ${backupCount}개`;
  return "열기"; // develop: 기존 신호 없음
}

export function StageHubCards({ onNavigate, planningStatus, backupCount }: StageHubCardsProps) {
  return (
    <div style={{ display: "flex", gap: 12, padding: 16 }}>
      {STAGE_DEFS.map((def) => (
        <button
          key={def.key}
          onClick={() => onNavigate(pagesForStage(def.key)[0])}
          style={{
            flex: 1,
            textAlign: "left",
            padding: 16,
            background: "#141414",
            border: "1px solid #2A2A2A",
            borderRadius: 8,
            color: "#ddd",
            cursor: "pointer",
          }}
        >
          <div style={{ fontSize: 20 }}>{def.icon}</div>
          <div style={{ fontWeight: 600, marginTop: 6 }}>{def.label}</div>
          <div style={{ fontSize: 12, color: "#888", marginTop: 4 }}>{CARD_DESC[def.key]}</div>
          <div style={{ fontSize: 12, color: def.key === "planning" && planningStatus === "active" ? "#FBBF24" : "#aaa", marginTop: 8 }}>
            {badgeText(def.key, planningStatus, backupCount)}
          </div>
        </button>
      ))}
    </div>
  );
}
// === ANCHOR: STAGE_HUB_CARDS_END ===
```

- [ ] **Step 2: 타입체크**

Run: `cd vibelign-gui && npx tsc --noEmit`
Expected: 통과 (TSC_EXIT=0)

- [ ] **Step 3: 커밋**

```bash
git add vibelign-gui/src/components/nav/StageHubCards.tsx
git commit -m "feat(nav): 홈 허브 단계 카드 컴포넌트"
```

---

## Task 5: App.tsx 조립 — 네비 셸 교체

**Files:**
- Modify: `vibelign-gui/src/App.tsx` (타입 import, backupCount 상태, nav-tabs 교체, navigate 래퍼, 허브 카드)

- [ ] **Step 1: `Page` 타입을 stages.ts에서 import (중복 정의 제거)**

App.tsx:64 의 로컬 정의:
```ts
type Page = "home" | "manual" | "docs" | "code" | "doctor" | "backups" | "logs" | "settings" | "planning";
```
를 삭제하고, 파일 상단 import 블록(예: 다른 컴포넌트 import 근처)에 추가:
```ts
import { type Page } from "./lib/nav/stages";
import { StageSwitcherBar } from "./components/nav/StageSwitcherBar";
import { StageSubnav } from "./components/nav/StageSubnav";
import { StageHubCards } from "./components/nav/StageHubCards";
```

- [ ] **Step 2: `backupCount` 상태 추가 + backupList effect에서 채우기**

App.tsx에서 `const [hasCheckpoint, setHasCheckpoint] = useState(false);`(약 78행) 바로 아래에 추가:
```ts
const [backupCount, setBackupCount] = useState(0);
```
그리고 backupList effect(약 122~128행)의 `.then`을 수정 — 기존:
```ts
      .then((result) => {
        if (active) setHasCheckpoint(result.backups.length > 0);
      })
```
변경:
```ts
      .then((result) => {
        if (active) {
          setHasCheckpoint(result.backups.length > 0);
          setBackupCount(result.backups.length);
        }
      })
```

- [ ] **Step 3: `navigate` 래퍼 + 파생값 추가**

App.tsx의 함수 컴포넌트 본문(다른 핸들러 근처, 예: `openDoctorTab` 정의 부근)에 추가:
```ts
function navigate(next: Page) {
  if (next === "doctor") { openDoctorTab(); return; } // 진단은 launch intent 경유
  setPage(next);
}
const planningStatus: "none" | "active" | "done" = !planningResult
  ? "none"
  : planningResult.messages.some((m) => m.status === "pending")
    ? "active"
    : "done";
```

- [ ] **Step 4: 평평한 nav-tabs(291~333행)를 전환 바 + 서브탭으로 교체**

App.tsx 291~333행의 `<div className="nav-tabs" ...> ... </div>` 블록 전체를 아래로 교체:
```tsx
            <StageSwitcherBar
              page={page}
              projectDir={projectDir}
              onHome={() => setPage("home")}
              onNavigate={navigate}
              onOpenManual={() => setPage("manual")}
              onOpenFolder={() => openFolder(projectDir).catch(() => {})}
              onExitProject={() => { stopWatch().catch(() => {}); setProjectDir(null); setPlanningResult(null); setReviewSourcePath(null); setPlanningPrompt(""); setPage("home"); }}
            />
            <StageSubnav page={page} onNavigate={navigate} />
```

- [ ] **Step 5: 홈 본문에 허브 카드 추가**

App.tsx 페이지 라우팅에서 `{page === "home" && <Home ... />}`(약 357행) 를 fragment로 감싸 허브 카드를 위에 둔다:
```tsx
                {page === "home" && (
                  <>
                    <StageHubCards onNavigate={navigate} planningStatus={planningStatus} backupCount={backupCount} />
                    <Home key="home" projectDir={projectDir} apiKey={apiKey} providerKeys={providerKeys} hasAnyAiKey={hasAnyAiKey} aiKeyStatusLoaded={envKeyStatusLoaded} onNavigate={setPage} onOpenDoctor={openDoctorFromGuardIssue} onOpenSettings={openSettings} watchOn={watchOn} setWatchOn={setWatchOn} watchError={watchError} onRetryWatch={() => { void retryWatch(); }} hasCheckpoint={hasCheckpoint} mapMode={mapMode} setMapMode={setMapMode} planningPrompt={planningPrompt} planningOutputPath={planningResult?.outputPath ?? null} planningPending={planningResult?.messages.some((message) => message.status === "pending") ?? false} onOpenPlanning={planningResult ? () => setPage("planning") : undefined} onStartPlanning={(idea) => { if (projectDir) void openPlanningRoom(projectDir, idea); }} onOpenPlanningHistory={() => setShowSessionPicker(true)} />
                  </>
                )}
```
(나머지 `{page === "planning" && ...}` 등 라우팅 줄은 그대로 둔다.)

- [ ] **Step 6: 타입체크**

Run: `cd vibelign-gui && npx tsc --noEmit`
Expected: 통과 (TSC_EXIT=0). 실패 시 흔한 원인: 남은 `type Page` 중복, `navigate` 위치(컴포넌트 본문 안인지), import 경로.

- [ ] **Step 7: 시각 렌더 + 회귀 확인**

Run: `cd vibelign-gui && npm run tauri dev` (또는 사용자가 직접 구동)
확인 항목(전부 도달 가능해야 함):
- 상단 바: `홈 │ 📋 기획 │ ⚙️ 개발 │ 🛡️ 유지보수 … 사용법 · 폴더열기 · 프로젝트명↩`
- 홈: 카드 3장(기획 뱃지=기획상태 / 유지보수=백업 N개 / 개발=열기) + 기존 홈 내용
- 개발 클릭 → 코드탐색, 서브탭 [코드탐색 │ 문서] 노출, 문서 전환됨
- 유지보수 클릭 → 진단, 서브탭 [진단 │ 백업 │ 에러로그], 각 전환됨
- 기획 클릭 → 기획방(기획방 승격 확인), 서브탭 없음
- 기획방 onStartWork → 개발(코드)로 이동 보존, onBack → 홈 보존
- 사용법/폴더열기/설정(타이틀바) 정상

- [ ] **Step 8: 커밋**

```bash
git add vibelign-gui/src/App.tsx
git commit -m "feat(nav): App에 3단계 IA 셸 조립 — 전환 바·서브탭·허브 카드"
```

---

## Task 6: 마무리 검증 + 메모리/문서 갱신

**Files:**
- (코드 변경 없음, 또는 시각 다듬기 발견 시 소규모 수정)

- [ ] **Step 1: 전체 빌드 검증**

Run: `cd vibelign-gui && npm run build`
Expected: tsc + vite build 성공.

- [ ] **Step 2: 전체 테스트**

Run: `cd vibelign-gui && npx vitest run`
Expected: 기존 + Task1 신규 테스트 전부 통과.

- [ ] **Step 3: 폴리시 메모(후속) 기록**

spec §8 폴리시 항목(홈 기존 카드 vs 신규 허브 카드 시각 중복 정리, 단계 내부 상태 스트립 중복 여부, 빈 기획 세션 안내 문구)은 이 계획 범위 밖. 별도 폴리시 작업으로 남긴다 — 필요 시 이슈/메모로.

- [ ] **Step 4: push (사용자 승인 시)**

```bash
git push origin feat/vibelign-product-renew
```

---

## 폴리시/후속 (이 계획 밖, 명시)

- 홈의 기존 콘텐츠(SimpleHome 등)와 신규 허브 카드의 시각 중복 정리 — 이 계획은 허브 카드를 기존 홈 위에 *추가*만 한다(앱이 매 커밋 동작 유지). 시각 통합은 후속.
- 단계 내부 상단 상태 스트립 중복 여부 최종 결정(현재는 홈에만, 단계 내부는 전환 바만).
- 빈 상태: 기획 세션 없을 때 기획 카드/단계의 안내 문구.

## Self-Review 결과 (작성자 점검)

- **Spec 커버리지:** §2 구조(전환 바·서브탭·허브)=Task2-5 / §3 매핑=Task1 / §4 가이드 뱃지=Task4 / §5 진입 재배선=Task5(navigate·기획 카드·onStartWork 보존) / §6 컴포넌트 경계=신규 3파일 + App 조립 / §7 검증=각 Task의 tsc·vitest·시각. 갭 없음.
- **플레이스홀더 스캔:** 모든 코드 스텝에 실제 코드 포함. "적절히 처리" 류 없음.
- **타입 일관성:** `Page`/`Stage`/`StageDef`·`stageOf`·`pagesForStage`·`PAGE_LABELS`·`navigate`·`planningStatus`·`backupCount` 명칭이 Task 간 일치.
