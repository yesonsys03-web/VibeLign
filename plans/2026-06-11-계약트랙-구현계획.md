# 계약 트랙 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 기획안 확정 시 "작업 계약"(목표·손댈 범위·금지·완료 기준)을 페르소나로 추출해 `contract.json`에 저장하고, 기획안 문서·외부 AI 지시문에 주입하며, 홈 '상태 확인' 시 실제 변경과 약속 범위를 비교한 리포트를 보여준다.

**Architecture:** 추출은 readiness 판정(`judge_readiness`)과 같은 패턴 — `save_planning_chat_as_markdown`에서 `run_active_ai` 1회 호출(**readiness와 병렬 실행** — 순차면 저장이 최악 2회 CLI timeout, 외부 리뷰 H1), strict JSON 파싱, 실패 시 원인 중립 폴백(퇴행 0). 저장은 cards.json 관례(`contract.json` = 소스, Markdown은 파생 뷰). 프론트는 응답의 `contract` 필드를 받아 ② 지시문 주입(`buildPlanningWorkInstruction`)과 ③ 범위 비교 리포트(`scopeReport` 순수 함수, Home이 guard 실행 시점에 `listChangedFiles` 직접 조회 — 심플·고급 양 경로에서 갱신)에 사용한다.

**외부 리뷰 반영(2026-06-11, 4건):** H1 저장 CLI 2회 → readiness·contract **병렬 실행**(루브릭 격리 유지, 병합은 후속) / M1 실패 카피 → "이번 저장에서는 추출하지 못했어요"(원인 중립 — "활성 AI 없음"은 CLI 오류·파싱 실패 사용자에게 오진) / M2 비-git 정책 모순 → **메타 제외 후 변경 0건이면 scopeReport가 null**(리포트 미노출 — "범위 안 0건"은 성공처럼 읽히는 무정보, 비-git 자연 커버) / M3 고급 GuardCard 경로 → 리포트 갱신을 양 guard 경로로(stale 방지), 고급 카드 안 표시는 후속.

**Tech Stack:** Rust(Tauri commands — 추출·저장·렌더, cargo test), React + TypeScript(주입·리포트, vitest + @testing-library/react), 신규 Tauri 커맨드 0.

**근거 spec:** `plans/2026-06-11-계약트랙-design.md` (ff3e5de + 구현계획 작성 중 §3 extractedAt·§6 changed-set 직접 조회 구체화 반영판)

**현재 코드 기준 (이 계획이 의존하는 사실):**
- `save_planning_chat_as_markdown` = planning_chat.rs:227, 내부에서 `judge_readiness`(:250)·`read_cards`(:252)·`save_planning_markdown`(:253) 호출
- `run_active_ai(project_dir, messages, prompt) -> Option<String>` = planning_persona.rs:497 (활성 CLI 1회 실행)
- `planning_chat_success(session, messages, markdown, cards)` 호출처 **5곳**: planning_chat.rs:63·83·220·270, planning_chat_retry.rs:83 — 전부 `session_dir`이 스코프에 있음
- 기획안 렌더 = planning_chat_markdown.rs `synthesize_planning_markdown(session, messages, cards)`, 호출처는 planning_chat_synthesis.rs:20 한 곳
- 지시문 = planningInstruction.ts `buildPlanningWorkInstruction({prompt, outputPath, persona})`, 소비처는 PlanningInstructionActions.tsx:39·45 (프리뷰·복사)
- 홈 guard = Home.tsx:63 `handleRunGuard` → `setGuardResult`, 표시는 SimpleHome.tsx "프로젝트 안전 상태" 블록
- **가이드 v6 계획과 독립** — 본 트랙은 v6 미구현 상태에서도 구현·출하 가능(§6 직접 조회 결정). 단 둘 다 App.tsx·Home.tsx를 수정하므로 충돌 최소화를 위해 행 번호 대신 앵커·식별자로 참조한다.

> **검증 방침:** 추출 파싱·실존 검증·project_map 요약·Markdown 렌더 = **cargo test**(readiness/synthesis 테스트 패턴). `scopeReport`·지시문 주입 = **vitest TDD**. SimpleHome 리포트 표시 = 기존 `SimpleHome.guard.test.tsx` 확장(이 컴포넌트는 이미 RTL 테스트 보유). 나머지 UI 배선 = `tsc --noEmit` + 육안.

---

## File Structure

**신규:**
- `vibelign-gui/src-tauri/src/commands/planning_chat_contract.rs` — `PlanningContract`·`ScopeEntry` 타입, 루브릭, 프롬프트 조립(project_map 요약 포함), strict JSON 파싱, scope 실존 검증, `contract.json` 읽기/쓰기. 추출의 단일 책임 모듈.
- `vibelign-gui/src/lib/home/scopeReport.ts` + `scopeReport.test.ts` — 범위 비교 순수 함수(메타 경로 제외 포함).

**수정:**
- `vibelign-gui/src-tauri/src/commands/mod.rs` — 모듈 등록 1줄.
- `vibelign-gui/src-tauri/src/commands/planning_chat_readiness.rs` — `extract_json`을 `pub(crate)`로 (재사용).
- `vibelign-gui/src-tauri/src/commands/planning_chat.rs` — save 흐름에 추출 통합, `planning_chat_success` 호출 4곳에 contract 인자.
- `vibelign-gui/src-tauri/src/commands/planning_chat_retry.rs` — 호출 1곳에 contract 인자.
- `vibelign-gui/src-tauri/src/commands/planning_chat_types.rs` — 응답에 `contract` 필드 + success/error 헬퍼 시그니처.
- `vibelign-gui/src-tauri/src/commands/planning_chat_markdown.rs` — `contract_section` + `synthesize_planning_markdown` 파라미터 추가.
- `vibelign-gui/src-tauri/src/commands/planning_chat_synthesis.rs`(+`_tests.rs`) — contract 전달.
- `vibelign-gui/src/lib/vib/types.ts` — `PlanningContract` TS 타입 + 응답 필드.
- `vibelign-gui/src/lib/code-explorer/planningInstruction.ts`(+`.test.ts`) — 계약 블록 주입.
- `vibelign-gui/src/components/code-explorer/PlanningInstructionActions.tsx`·`CodeExplorerPlanningContext.tsx`, `vibelign-gui/src/pages/CodeExplorer.tsx` — contract prop 스레딩.
- `vibelign-gui/src/pages/Home.tsx` — guard 실행 시 changed-set 조회 + 리포트 계산 + prop.
- `vibelign-gui/src/components/home/SimpleHome.tsx`(+`.guard.test.tsx`) — 리포트 표시.
- `vibelign-gui/src/App.tsx` — Home(home 뷰)·CodeExplorer 호출에 `planningResult?.contract` 전달.

---

## Task 1: 계약 타입 + JSON 파싱 (`planning_chat_contract.rs`) — Rust TDD

**Files:**
- Create: `vibelign-gui/src-tauri/src/commands/planning_chat_contract.rs`
- Modify: `vibelign-gui/src-tauri/src/commands/mod.rs` (등록)
- Modify: `vibelign-gui/src-tauri/src/commands/planning_chat_readiness.rs` (`extract_json` 공개)

- [ ] **Step 1: readiness의 `extract_json` 재사용 준비**

planning_chat_readiness.rs:132 — 기존:
```rust
fn extract_json(text: &str) -> Option<&str> {
```
변경 (호출처는 같은 파일 + 신규 contract 모듈 둘뿐):
```rust
pub(crate) fn extract_json(text: &str) -> Option<&str> {
```

- [ ] **Step 2: 모듈 등록**

commands/mod.rs:7 `pub(crate) mod planning_chat;` 아래(알파벳 순서 유지)에 추가:
```rust
pub(crate) mod planning_chat_contract;
```

- [ ] **Step 3: 타입 + 파싱 구현과 실패 테스트를 한 파일로 작성**

`vibelign-gui/src-tauri/src/commands/planning_chat_contract.rs` 신규 (이 Step에서는 타입·파싱·테스트만 — 추출/검증/저장은 Task 2·3에서 같은 파일에 추가):
```rust
// === ANCHOR: PLANNING_CHAT_CONTRACT_START ===
// 작업 계약 — 기획안 확정 시 페르소나로 추출하는 목표·손댈 범위·금지·완료 기준.
// 근거: plans/2026-06-11-계약트랙-design.md. readiness(판정)·cards(결정) 추출 패턴과 동일 계열.
use std::path::{Path, PathBuf};

use serde::{Deserialize, Serialize};

use super::planning_chat_readiness::extract_json;

#[derive(Serialize, Deserialize, Clone, Copy, PartialEq, Eq, Debug)]
#[serde(rename_all = "lowercase")]
pub(crate) enum ScopeKind {
    File,
    Dir,
}

#[derive(Serialize, Deserialize, Clone, Debug, PartialEq, Eq)]
#[serde(rename_all = "camelCase")]
pub(crate) struct ScopeEntry {
    pub(crate) path: String,
    pub(crate) kind: ScopeKind,
    /// 초보자 노출 텍스트 — 한국어, 영문 용어 금지(루브릭에서 강제).
    pub(crate) reason: String,
}

#[derive(Serialize, Deserialize, Clone, Debug, PartialEq, Eq)]
#[serde(rename_all = "camelCase")]
pub(crate) struct PlanningContract {
    pub(crate) version: u32,
    /// epoch ms 문자열 — 메시지 createdAt 등 기존 타임스탬프 관례와 동일.
    pub(crate) extracted_at: String,
    pub(crate) goal: String,
    pub(crate) scope: Vec<ScopeEntry>,
    pub(crate) exclusions: Vec<String>,
    pub(crate) done_criteria: Vec<String>,
}

fn string_list(value: &serde_json::Value, key: &str) -> Vec<String> {
    value
        .get(key)
        .and_then(|v| v.as_array())
        .map(|items| {
            items
                .iter()
                .filter_map(|item| item.as_str())
                .map(|s| s.trim().to_string())
                .filter(|s| !s.is_empty())
                .collect()
        })
        .unwrap_or_default()
}

/// CLI 출력 텍스트 → 계약. goal이 비면 계약 전체를 실패로 본다(빈 계약은 무가치).
/// scope의 kind가 "file"/"dir"가 아니면 그 entry만 버린다(불확실하면 통과시키지 않는다 — readiness와 동일 보수성).
pub(crate) fn parse_contract(text: &str, now: &str) -> Option<PlanningContract> {
    let json = extract_json(text)?;
    let value = serde_json::from_str::<serde_json::Value>(json).ok()?;
    let goal = value.get("goal").and_then(|v| v.as_str()).map(str::trim).unwrap_or("");
    if goal.is_empty() {
        return None;
    }
    let scope = value
        .get("scope")
        .and_then(|v| v.as_array())
        .map(|items| {
            items
                .iter()
                .filter_map(|item| {
                    let path = item.get("path")?.as_str()?.trim().to_string();
                    if path.is_empty() {
                        return None;
                    }
                    let kind = match item.get("kind").and_then(|v| v.as_str()).map(str::trim) {
                        Some("file") => ScopeKind::File,
                        Some("dir") => ScopeKind::Dir,
                        _ => return None,
                    };
                    let reason = item
                        .get("reason")
                        .and_then(|v| v.as_str())
                        .map(|s| s.trim().to_string())
                        .unwrap_or_default();
                    Some(ScopeEntry { path, kind, reason })
                })
                .collect()
        })
        .unwrap_or_default();
    Some(PlanningContract {
        version: 1,
        extracted_at: now.to_string(),
        goal: goal.to_string(),
        scope,
        exclusions: string_list(&value, "exclusions"),
        done_criteria: string_list(&value, "doneCriteria"),
    })
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn parse_extracts_contract_from_fenced_json() {
        let text = r#"결과:
```json
{
  "goal": "예약 화면을 만든다",
  "scope": [
    { "path": "src/pages/Home.tsx", "kind": "file", "reason": "예약 진입 버튼이 있는 곳" },
    { "path": "src/components/nav/", "kind": "dir", "reason": "이동 안내를 더하는 곳" }
  ],
  "exclusions": ["결제 흐름은 건드리지 않음"],
  "doneCriteria": ["예약 버튼을 누르면 예약 화면이 뜬다"]
}
```
끝."#;
        let contract = parse_contract(text, "123").expect("parsed");
        assert_eq!(contract.version, 1);
        assert_eq!(contract.extracted_at, "123");
        assert_eq!(contract.goal, "예약 화면을 만든다");
        assert_eq!(contract.scope.len(), 2);
        assert_eq!(contract.scope[0].kind, ScopeKind::File);
        assert_eq!(contract.scope[1].kind, ScopeKind::Dir);
        assert_eq!(contract.exclusions, vec!["결제 흐름은 건드리지 않음".to_string()]);
        assert_eq!(contract.done_criteria.len(), 1);
    }

    #[test]
    fn parse_fails_without_goal_and_on_broken_json() {
        assert!(parse_contract("JSON을 못 만들었어요.", "1").is_none());
        assert!(parse_contract(r#"{ "scope": [] }"#, "1").is_none()); // goal 없음 = 실패
        assert!(parse_contract(r#"{ "goal": "  " }"#, "1").is_none()); // 빈 goal = 실패
    }

    #[test]
    fn parse_drops_scope_entries_with_unknown_kind_or_empty_path() {
        let text = r#"{ "goal": "g", "scope": [
            { "path": "a.ts", "kind": "maybe", "reason": "" },
            { "path": "", "kind": "file", "reason": "" },
            { "path": "b.ts", "kind": "file", "reason": "이유" }
        ] }"#;
        let contract = parse_contract(text, "1").expect("parsed");
        assert_eq!(contract.scope.len(), 1);
        assert_eq!(contract.scope[0].path, "b.ts");
    }

    #[test]
    fn contract_serializes_camel_case() {
        let contract = PlanningContract {
            version: 1,
            extracted_at: "1".to_string(),
            goal: "g".to_string(),
            scope: Vec::new(),
            exclusions: Vec::new(),
            done_criteria: Vec::new(),
        };
        let json = serde_json::to_string(&contract).expect("json");
        assert!(json.contains("extractedAt"));
        assert!(json.contains("doneCriteria"));
    }
}
// === ANCHOR: PLANNING_CHAT_CONTRACT_END ===
```

- [ ] **Step 4: 테스트 실행**

Run: `cd vibelign-gui/src-tauri && cargo test planning_chat_contract`
Expected: PASS (4개)

- [ ] **Step 5: 커밋**

```bash
git add vibelign-gui/src-tauri/src/commands/planning_chat_contract.rs vibelign-gui/src-tauri/src/commands/mod.rs vibelign-gui/src-tauri/src/commands/planning_chat_readiness.rs
git commit -m "feat(contract): 작업 계약 타입 + strict JSON 파싱 — goal 필수, 불확실 entry 폐기"
```

---

## Task 2: project_map 요약 + scope 실존 검증 — Rust TDD

**Files:**
- Modify: `vibelign-gui/src-tauri/src/commands/planning_chat_contract.rs` (이어서 추가)

경로 탈출 가드(품질 리뷰): 절대경로·`..`는 실존 검사 전에 차단 — `safe_relative_target`과 동일 기준, join()의 base 교체/.. 추종으로 외부 파일이 실존 통과하는 구멍 봉쇄.

- [ ] **Step 1: 구현 + 테스트 추가**

planning_chat_contract.rs의 `parse_contract` 아래에 추가:
```rust
/// 페르소나 프롬프트에 넣을 project_map 요약 상한. core 카테고리 우선.
const PROJECT_MAP_SUMMARY_LIMIT: usize = 200;

/// `.vibelign/project_map.json` → "path (category)" 줄 목록 + 잘림 여부.
/// 파일이 없거나 깨졌으면 빈 목록(루브릭이 "목록에 있는 경로만"이라 scope가 비게 됨 — 안전한 강등).
pub(crate) fn project_map_summary(project_dir: &Path) -> (Vec<String>, bool) {
    let raw = match std::fs::read_to_string(project_dir.join(".vibelign/project_map.json")) {
        Ok(raw) => raw,
        Err(_) => return (Vec::new(), false),
    };
    let Ok(value) = serde_json::from_str::<serde_json::Value>(&raw) else {
        return (Vec::new(), false);
    };
    let Some(files) = value.get("files").and_then(|v| v.as_object()) else {
        return (Vec::new(), false);
    };
    let mut rows: Vec<(bool, String)> = files
        .iter()
        .map(|(path, meta)| {
            let category = meta.get("category").and_then(|v| v.as_str()).unwrap_or("other");
            (category == "core", format!("{path} ({category})"))
        })
        .collect();
    // core 우선, 같은 우선순위 안에서는 경로순(결정적 출력 — 테스트 가능성).
    rows.sort_by(|a, b| b.0.cmp(&a.0).then_with(|| a.1.cmp(&b.1)));
    let truncated = rows.len() > PROJECT_MAP_SUMMARY_LIMIT;
    rows.truncate(PROJECT_MAP_SUMMARY_LIMIT);
    (rows.into_iter().map(|(_, line)| line).collect(), truncated)
}

/// 환각 경로 2차 차단(1차는 루브릭): project_map 또는 디스크에 실존하는 경로만 통과.
/// dir entry는 끝 '/'를 보장해 prefix 매칭(scopeReport)의 모호성을 없앤다.
pub(crate) fn validate_scope(project_dir: &Path, scope: Vec<ScopeEntry>) -> Vec<ScopeEntry> {
    scope
        .into_iter()
        .filter_map(|mut entry| {
            let normalized = entry.path.replace('\\', "/");
            entry.path = normalized.trim_start_matches("./").to_string();
            // 프로젝트 밖 탈출 차단(safe_relative_target과 동일 기준): 절대경로·상위 참조는
            // LLM 환각이며, join()이 base를 버리거나 ..를 따라가 외부 파일을 실존 판정할 수 있다.
            let candidate = std::path::Path::new(&entry.path);
            if candidate.is_absolute()
                || candidate
                    .components()
                    .any(|component| matches!(component, std::path::Component::ParentDir))
            {
                return None;
            }
            match entry.kind {
                ScopeKind::File => {
                    if project_dir.join(&entry.path).is_file() {
                        Some(entry)
                    } else {
                        None
                    }
                }
                ScopeKind::Dir => {
                    let trimmed = entry.path.trim_end_matches('/').to_string();
                    if trimmed.is_empty() {
                        return None; // 루트 전체를 scope로 잡는 건 무의미 — 버림
                    }
                    if project_dir.join(&trimmed).is_dir() {
                        entry.path = format!("{trimmed}/");
                        Some(entry)
                    } else {
                        None
                    }
                }
            }
        })
        .collect()
}
```

같은 파일 `mod tests` 안에 추가:
```rust
    use tempfile::TempDir;

    fn entry(path: &str, kind: ScopeKind) -> ScopeEntry {
        ScopeEntry { path: path.to_string(), kind, reason: String::new() }
    }

    #[test]
    fn project_map_summary_prefers_core_and_reports_truncation() {
        let root = TempDir::new().unwrap();
        std::fs::create_dir_all(root.path().join(".vibelign")).unwrap();
        std::fs::write(
            root.path().join(".vibelign/project_map.json"),
            r#"{ "files": {
                "zz_other.py": { "category": "other" },
                "aa_core.rs": { "category": "core" }
            } }"#,
        )
        .unwrap();
        let (lines, truncated) = project_map_summary(root.path());
        assert_eq!(lines[0], "aa_core.rs (core)"); // core가 other보다 먼저
        assert_eq!(lines.len(), 2);
        assert!(!truncated);
    }

    #[test]
    fn project_map_summary_is_empty_when_map_missing() {
        let root = TempDir::new().unwrap();
        let (lines, truncated) = project_map_summary(root.path());
        assert!(lines.is_empty());
        assert!(!truncated);
    }

    #[test]
    fn validate_scope_keeps_existing_and_drops_hallucinated() {
        let root = TempDir::new().unwrap();
        std::fs::create_dir_all(root.path().join("src/pages")).unwrap();
        std::fs::write(root.path().join("src/pages/Home.tsx"), b"x").unwrap();

        let validated = validate_scope(
            root.path(),
            vec![
                entry("src/pages/Home.tsx", ScopeKind::File),
                entry("src/pages", ScopeKind::Dir),       // 끝 '/' 없음 → 보정
                entry("src/ghost.tsx", ScopeKind::File),   // 실존 안 함 → 버림
                entry("imaginary/", ScopeKind::Dir),       // 실존 안 함 → 버림
                entry("/", ScopeKind::Dir),                // 루트 → 버림
                entry("../outside.ts", ScopeKind::File),   // 상위 참조 → 차단
                entry("/etc/passwd", ScopeKind::File),     // 절대경로 → 차단
            ],
        );
        let paths: Vec<&str> = validated.iter().map(|e| e.path.as_str()).collect();
        assert_eq!(paths, vec!["src/pages/Home.tsx", "src/pages/"]);
    }
```

- [ ] **Step 2: 테스트 실행**

Run: `cd vibelign-gui/src-tauri && cargo test planning_chat_contract`
Expected: PASS (7개 — Task 1의 4개 + 신규 3개). `tempfile`은 dev-dependencies에 이미 존재(git_status.rs 테스트가 사용 중).

- [ ] **Step 3: 커밋**

```bash
git add vibelign-gui/src-tauri/src/commands/planning_chat_contract.rs
git commit -m "feat(contract): project_map 요약(core 우선·상한) + scope 실존 검증 — 환각 경로 2차 차단"
```

---

## Task 3: 루브릭·프롬프트·추출 함수 + `contract.json` 저장 — Rust

**Files:**
- Modify: `vibelign-gui/src-tauri/src/commands/planning_chat_contract.rs` (이어서 추가)

- [ ] **Step 1: 루브릭 + 프롬프트 조립 + 추출 + 저장 구현**

planning_chat_contract.rs 상단 `use` 블록을 다음으로 확장:
```rust
use std::path::{Path, PathBuf};

use serde::{Deserialize, Serialize};

use super::planning_chat_readiness::extract_json;
use super::planning_chat_types::PlanningChatMessage;
use super::planning_persona::run_active_ai;
```

`validate_scope` 아래에 추가:
```rust
const CONTRACT_RUBRIC: &str = r#"너는 확정된 기획 대화를 읽고 'AI 작업 계약'을 추출하는 정리자다.
계약은 외부 AI 코딩 도구에게 줄 작업의 목표·손댈 범위·금지·완료 기준이다.

규칙:
- goal: 기획의 한 줄 목표. 사용자가 처음 말한 목표를 대화에서 합의된 형태로 다듬는다.
- scope: 이 작업에서 손댈 파일/디렉터리 후보. 반드시 아래 '프로젝트 파일 목록'에 있는 경로만 쓴다.
  목록에 없는 경로는 절대 만들어내지 않는다. 확신이 없으면 파일 대신 디렉터리 단위로 적는다
  (kind를 "dir"로, 경로 끝에 '/'). 어디를 고칠지 모르겠으면 빈 배열 []로 둔다.
  reason은 초보자가 읽는 한국어 한 줄이다. 영문 용어를 쓰지 않는다.
- exclusions: 대화에서 '하지 않기로 한 것'. 없으면 [].
- doneCriteria: 사용자가 '됐다'를 확인하는 방법. 대화에 수용 기준이 있으면 그걸 쓴다. 없으면 [].

반드시 아래 JSON만 출력한다(설명 금지):
{
  "goal": "...",
  "scope": [ { "path": "src/...", "kind": "file", "reason": "..." } ],
  "exclusions": ["..."],
  "doneCriteria": ["..."]
}
"#;

fn build_contract_prompt(
    messages: &[PlanningChatMessage],
    map_lines: &[String],
    map_truncated: bool,
) -> String {
    let mut prompt = String::from(CONTRACT_RUBRIC);
    prompt.push_str("\n프로젝트 파일 목록");
    if map_truncated {
        prompt.push_str(" (일부만 표시 — 핵심 파일 우선, 디렉터리 단위로 적는 게 안전)");
    }
    prompt.push_str(":\n");
    if map_lines.is_empty() {
        prompt.push_str("(목록 없음 — scope는 빈 배열로 둘 것)\n");
    }
    for line in map_lines {
        prompt.push_str(line);
        prompt.push('\n');
    }
    prompt.push_str("\n지금까지의 대화:\n");
    for message in messages {
        if message.status != "ok" {
            continue;
        }
        let speaker = if message.role == "user" { "사용자" } else { "AI" };
        prompt.push_str(speaker);
        prompt.push_str(": ");
        prompt.push_str(message.content.trim());
        prompt.push('\n');
    }
    prompt.push_str("\n위 대화의 작업 계약 JSON을 출력해.");
    prompt
}

/// 확정 시점 1회 추출. AI 없음·실패·파싱 실패 = None — 원인 중립 폴백(퇴행 0, 외부 리뷰 M1).
pub(crate) fn extract_contract(
    project_dir: &Path,
    messages: &[PlanningChatMessage],
    now: &str,
) -> Option<PlanningContract> {
    let (map_lines, map_truncated) = project_map_summary(project_dir);
    let prompt = build_contract_prompt(messages, &map_lines, map_truncated);
    let text = run_active_ai(project_dir, messages, &prompt)?;
    let mut contract = parse_contract(&text, now)?;
    contract.scope = validate_scope(project_dir, contract.scope);
    Some(contract)
}

fn contract_path(session_dir: &Path) -> PathBuf {
    session_dir.join("contract.json")
}

/// cards.json 관례 — 파일 없음/깨짐 = None(계약 없음으로 강등).
pub(crate) fn read_contract(session_dir: &Path) -> Option<PlanningContract> {
    let raw = std::fs::read_to_string(contract_path(session_dir)).ok()?;
    serde_json::from_str::<PlanningContract>(&raw).ok()
}

pub(crate) fn write_contract(session_dir: &Path, contract: &PlanningContract) -> Result<(), String> {
    let json = serde_json::to_string_pretty(contract).map_err(|error| error.to_string())?;
    std::fs::write(contract_path(session_dir), json).map_err(|error| error.to_string())
}
```

`mod tests`에 추가:
```rust
    #[test]
    fn build_prompt_includes_rubric_map_and_conversation() {
        let messages = vec![PlanningChatMessage {
            id: "m1".to_string(),
            role: "user".to_string(),
            persona_id: None,
            content: "예약 앱 만들고 싶어".to_string(),
            status: "ok".to_string(),
            created_at: "0".to_string(),
            provider_used: None,
            fallback_reason: None,
        }];
        let prompt = build_contract_prompt(&messages, &["src/a.ts (core)".to_string()], true);
        assert!(prompt.contains("작업 계약"));
        assert!(prompt.contains("src/a.ts (core)"));
        assert!(prompt.contains("일부만 표시"));
        assert!(prompt.contains("예약 앱 만들고 싶어"));
    }

    #[test]
    fn contract_roundtrips_through_file() {
        let dir = TempDir::new().unwrap();
        let contract = PlanningContract {
            version: 1,
            extracted_at: "1".to_string(),
            goal: "g".to_string(),
            scope: vec![entry("src/", ScopeKind::Dir)],
            exclusions: vec!["x".to_string()],
            done_criteria: vec!["d".to_string()],
        };
        write_contract(dir.path(), &contract).expect("write");
        assert_eq!(read_contract(dir.path()), Some(contract));
    }

    #[test]
    fn read_contract_is_none_when_missing_or_broken() {
        let dir = TempDir::new().unwrap();
        assert_eq!(read_contract(dir.path()), None);
        std::fs::write(dir.path().join("contract.json"), "깨진 json").unwrap();
        assert_eq!(read_contract(dir.path()), None);
    }
```

- [ ] **Step 2: 테스트 실행**

Run: `cd vibelign-gui/src-tauri && cargo test planning_chat_contract`
Expected: PASS (10개)

- [ ] **Step 3: 커밋**

```bash
git add vibelign-gui/src-tauri/src/commands/planning_chat_contract.rs
git commit -m "feat(contract): 추출 루브릭·프롬프트(project_map 주입)·extract_contract + contract.json 입출력"
```

---

## Task 4: 기획안 Markdown "## 작업 계약" 렌더 — Rust

**Files:**
- Modify: `vibelign-gui/src-tauri/src/commands/planning_chat_markdown.rs`
- Modify: `vibelign-gui/src-tauri/src/commands/planning_chat_synthesis.rs` + `planning_chat_synthesis_tests.rs`

- [ ] **Step 1: `contract_section` + synthesize 파라미터 추가**

planning_chat_markdown.rs 상단 use에 추가:
```rust
use super::planning_chat_contract::PlanningContract;
```

`synthesize_planning_markdown` 시그니처 변경 — 기존:
```rust
pub(crate) fn synthesize_planning_markdown(
    session: &StoredPlanningChatSession,
    messages: &[PlanningChatMessage],
    cards: &[Card],
) -> String {
```
변경:
```rust
pub(crate) fn synthesize_planning_markdown(
    session: &StoredPlanningChatSession,
    messages: &[PlanningChatMessage],
    cards: &[Card],
    contract: Option<&PlanningContract>,
) -> String {
```

본문에서 `markdown.push_str(&readiness_header(session.readiness.as_ref()));` **바로 아래**에 추가
(준비 상태 판정 다음 = 문서 머리 — 계약은 "구현 도구가 그대로 읽는 지시서" 카피와 같은 자리):
```rust
    markdown.push_str(&contract_section(contract));
```

`readiness_header` 함수 아래에 추가:
```rust
/// 작업 계약 섹션 — 읽기 전용 노출(spec §1 Q3). 실패 카피는 원인 중립(외부 리뷰 M1):
/// AI 미설정·CLI 실패·파싱 실패를 사용자에게 구분해 보일 수 없으므로 원인을 단정하지 않는다.
pub(crate) fn contract_section(contract: Option<&PlanningContract>) -> String {
    let Some(contract) = contract else {
        return "## 작업 계약: 이번 저장에서는 추출하지 못했어요\n\n".to_string();
    };
    let mut out = String::from(
        "## 작업 계약\n> 이 블록은 외부 AI 도구에게 주는 지시문에 그대로 들어갑니다.\n\n",
    );
    out.push_str(&format!("- 목표: {}\n", contract.goal));
    if contract.scope.is_empty() {
        out.push_str("- 손댈 범위: 추출하지 못함 — 작업 도구가 코드를 읽고 스스로 정합니다\n");
    } else {
        out.push_str("- 손댈 범위 후보:\n");
        for entry in &contract.scope {
            if entry.reason.is_empty() {
                out.push_str(&format!("  - `{}`\n", entry.path));
            } else {
                out.push_str(&format!("  - `{}` — {}\n", entry.path, entry.reason));
            }
        }
    }
    for exclusion in &contract.exclusions {
        out.push_str(&format!("- 건드리지 말 것: {exclusion}\n"));
    }
    for criteria in &contract.done_criteria {
        out.push_str(&format!("- 완료 기준: {criteria}\n"));
    }
    out.push('\n');
    out
}
```

같은 파일 `mod tests`에 추가:
```rust
    use crate::commands::planning_chat_contract::{PlanningContract, ScopeEntry, ScopeKind};

    fn sample_contract() -> PlanningContract {
        PlanningContract {
            version: 1,
            extracted_at: "1".to_string(),
            goal: "예약 화면을 만든다".to_string(),
            scope: vec![ScopeEntry {
                path: "src/pages/".to_string(),
                kind: ScopeKind::Dir,
                reason: "화면이 모인 곳".to_string(),
            }],
            exclusions: vec!["결제는 건드리지 않음".to_string()],
            done_criteria: vec!["예약 버튼이 동작한다".to_string()],
        }
    }

    #[test]
    fn contract_section_renders_all_fields() {
        let section = contract_section(Some(&sample_contract()));
        assert!(section.contains("## 작업 계약"));
        assert!(section.contains("목표: 예약 화면을 만든다"));
        assert!(section.contains("`src/pages/` — 화면이 모인 곳"));
        assert!(section.contains("건드리지 말 것: 결제는 건드리지 않음"));
        assert!(section.contains("완료 기준: 예약 버튼이 동작한다"));
    }

    #[test]
    fn contract_section_is_honest_when_missing_or_scopeless() {
        // 원인 중립 카피(외부 리뷰 M1) — "활성 AI 없음" 같은 원인 단정 금지.
        assert!(contract_section(None).contains("이번 저장에서는 추출하지 못했어요"));
        assert!(!contract_section(None).contains("활성 AI 없음"));
        let mut scopeless = sample_contract();
        scopeless.scope.clear();
        assert!(contract_section(Some(&scopeless)).contains("추출하지 못함"));
    }
```

- [ ] **Step 2: synthesis 호출 갱신**

planning_chat_synthesis.rs — use에 추가:
```rust
use super::planning_chat_contract::PlanningContract;
```
`save_planning_markdown` 시그니처에 파라미터 추가(기존 `cards: &[Card],` 아래):
```rust
    contract: Option<&PlanningContract>,
```
:20 호출 변경:
```rust
    let markdown = synthesize_planning_markdown(session, messages, cards, contract);
```

planning_chat_synthesis_tests.rs의 `save_planning_markdown(...)` 호출들(:93·:109 테스트)에 인자 `None` 추가 — 기존 인자 목록 끝(`target_path` 앞이 아니라 `cards` 다음 자리)에 `None,`을 삽입한다. 컴파일러가 누락 지점을 전부 알려주므로 `cargo test`로 확인.

- [ ] **Step 3: 테스트 실행**

Run: `cd vibelign-gui/src-tauri && cargo test planning_chat`
Expected: PASS — contract 신규 + markdown 신규 2개 + 기존 synthesis/markdown/readiness/cards 전부. (이 시점에 planning_chat.rs:253의 `save_planning_markdown` 호출이 컴파일 에러 — Task 5 Step 1에서 함께 고치므로, 이 Step에서는 임시로 호출에 `None,`을 넣어 컴파일을 통과시킨다:)
```rust
        let saved = match save_planning_markdown(
            &project_dir,
            &mut session,
            &messages,
            &cards,
            None,
            request.target_path.as_deref(),
        ) {
```

- [ ] **Step 4: 커밋**

```bash
git add vibelign-gui/src-tauri/src/commands/planning_chat_markdown.rs vibelign-gui/src-tauri/src/commands/planning_chat_synthesis.rs vibelign-gui/src-tauri/src/commands/planning_chat_synthesis_tests.rs vibelign-gui/src-tauri/src/commands/planning_chat.rs
git commit -m "feat(contract): 기획안에 '작업 계약' 읽기 전용 섹션 렌더 — 실패는 원인 중립 표기"
```

---

## Task 5: save 흐름 통합 + 응답 `contract` 필드 — Rust

**Files:**
- Modify: `vibelign-gui/src-tauri/src/commands/planning_chat_types.rs`
- Modify: `vibelign-gui/src-tauri/src/commands/planning_chat.rs` (4곳)
- Modify: `vibelign-gui/src-tauri/src/commands/planning_chat_retry.rs` (1곳)

- [ ] **Step 1: 응답 타입 확장**

planning_chat_types.rs — use에 추가:
```rust
use super::planning_chat_contract::PlanningContract;
```
`PlanningChatSessionResponse` struct에 필드 추가(`cards: Vec<Card>,` 아래):
```rust
    contract: Option<PlanningContract>,
```
`planning_chat_error`의 리터럴에 `contract: None,` 추가(`cards: Vec::new(),` 아래).
`planning_chat_success` 시그니처에 파라미터 추가 + 리터럴 반영:
```rust
pub(crate) fn planning_chat_success(
    session: StoredPlanningChatSession,
    messages: Vec<PlanningChatMessage>,
    markdown: Option<String>,
    cards: Vec<Card>,
    contract: Option<PlanningContract>,
) -> PlanningChatSessionResponse {
```
리터럴에 `contract,` 추가(`cards,` 아래).

- [ ] **Step 2: save 커맨드에 추출 통합 — readiness와 병렬 실행**

planning_chat.rs `save_planning_chat_as_markdown`의 **기존 readiness 할당(:250~251)**:
```rust
        session.readiness =
            Some(super::planning_chat_readiness::judge_readiness(&project_dir, &messages));
```
을 아래 병렬 블록으로 **교체** (둘 다 CLI 1회씩 — 순차면 저장 버튼이 최악 2회 CLI timeout에 묶인다, 외부 리뷰 H1.
이미 `spawn_blocking` 안이므로 스레드 1개 추가는 안전. `PlanningChatMessage`는 `Clone` derive, `project_dir`는 `PathBuf`라 clone 가능):
```rust
        // readiness 판정과 계약 추출은 각각 CLI 1회(spec §4) — 병렬 실행으로 저장 지연을 ~1회분으로 유지.
        // 루브릭은 서로 격리(병합 안 함) — 기존 readiness 품질 보호. 병합(호출 1회화)은 후속 검토.
        let now = timestamp_ms().to_string();
        let (readiness, contract) = {
            let readiness_dir = project_dir.clone();
            let readiness_messages = messages.clone();
            let readiness_handle = std::thread::spawn(move || {
                super::planning_chat_readiness::judge_readiness(&readiness_dir, &readiness_messages)
            });
            // 계약 추출은 현재 스레드에서 — 두 CLI 프로세스가 동시에 돈다.
            let contract = super::planning_chat_contract::extract_contract(&project_dir, &messages, &now);
            let readiness = readiness_handle
                .join()
                .unwrap_or_else(|_| super::planning_chat_readiness::ReadinessReport::unavailable());
            (readiness, contract)
        };
        session.readiness = Some(readiness);
        if let Some(ref parsed) = contract {
            // 저장 실패는 응답을 막지 않는다(best-effort) — 응답에는 추출본이 그대로 실린다.
            let _ = super::planning_chat_contract::write_contract(&session_dir, parsed);
        }
```
Task 4 Step 3에서 임시로 `None,`을 넣었던 `save_planning_markdown` 호출을 교체:
```rust
            contract.as_ref(),
```
같은 커맨드의 `planning_chat_success(session, messages, Some(saved.markdown), cards)`(:270) 변경:
```rust
        planning_chat_success(session, messages, Some(saved.markdown), cards, contract)
```

- [ ] **Step 3: 나머지 `planning_chat_success` 호출 4곳에 저장본 전달**

각 호출을 다음으로 변경 — 전부 `session_dir`(또는 그에 준하는 경로 변수)이 스코프에 있다. 변수명이 다르면(예: `dir`) 그 변수를 쓴다:

(a) planning_chat.rs:63 (세션 생성 직후 — contract.json이 아직 없어 자연히 None):
```rust
        planning_chat_success(session, messages, None, Vec::new(), super::planning_chat_contract::read_contract(&session_dir))
```
(b) planning_chat.rs:83 (최신 세션 로드):
```rust
    planning_chat_success(session, messages, markdown, cards, super::planning_chat_contract::read_contract(&session_dir))
```
(c) planning_chat.rs:220 (턴 추가 — 재대화 시작. 기존 계약은 저장 전까지 그대로 노출):
```rust
        planning_chat_success(session, messages, None, cards, super::planning_chat_contract::read_contract(&session_dir))
```
(d) planning_chat_retry.rs:83:
```rust
        planning_chat_success(session, messages, None, cards, super::planning_chat_contract::read_contract(&session_dir))
```

- [ ] **Step 4: Rust 전체 테스트**

Run: `cd vibelign-gui/src-tauri && cargo test`
Expected: PASS 전부 (컴파일러가 호출처 누락을 잡아줬는지 이 시점에 확정)

- [ ] **Step 5: 커밋**

```bash
git add vibelign-gui/src-tauri/src/commands/planning_chat_types.rs vibelign-gui/src-tauri/src/commands/planning_chat.rs vibelign-gui/src-tauri/src/commands/planning_chat_retry.rs
git commit -m "feat(contract): 확정 시 계약 추출·저장 + 모든 기획 응답에 contract 필드"
```

---

## Task 6: TS 타입 + 지시문 주입 (`buildPlanningWorkInstruction`) — vitest TDD

**Files:**
- Modify: `vibelign-gui/src/lib/vib/types.ts`
- Modify: `vibelign-gui/src/lib/code-explorer/planningInstruction.ts`
- Test: `vibelign-gui/src/lib/code-explorer/planningInstruction.test.ts`

- [ ] **Step 1: TS 타입 추가**

types.ts — `PlanningChatSessionResponse` 인터페이스(types.ts:254) 위에 추가:
```ts
export type ContractScopeKind = "file" | "dir";

export interface ContractScopeEntry {
  path: string;
  kind: ContractScopeKind;
  /** 초보자 노출 한국어 한 줄. */
  reason: string;
}

/** 작업 계약 — 기획안 확정 시 추출(plans/2026-06-11-계약트랙-design.md §3). */
export interface PlanningContract {
  version: number;
  /** epoch ms 문자열(메시지 createdAt 관례와 동일). */
  extractedAt: string;
  goal: string;
  scope: ContractScopeEntry[];
  exclusions: string[];
  doneCriteria: string[];
}
```
`PlanningChatSessionResponse`에 필드 추가(`cards?: readonly Card[];` 아래):
```ts
  contract?: PlanningContract | null;
```

- [ ] **Step 2: 실패하는 테스트 추가**

planningInstruction.test.ts의 describe 안에 추가:
```ts
  const CONTRACT = {
    version: 1,
    extractedAt: "1",
    goal: "예약 화면을 만든다",
    scope: [
      { path: "src/pages/Home.tsx", kind: "file" as const, reason: "예약 진입 버튼" },
      { path: "src/components/nav/", kind: "dir" as const, reason: "이동 안내" },
    ],
    exclusions: ["결제 흐름은 건드리지 않음"],
    doneCriteria: ["예약 버튼을 누르면 예약 화면이 뜬다"],
  };

  test("injects_contract_block_when_contract_present", () => {
    const instruction = buildPlanningWorkInstruction({
      prompt: "예약 앱 만들고 싶어",
      outputPath: "plans/예약.md",
      contract: CONTRACT,
    });

    expect(instruction).toContain("이번 작업의 계약(기획안에서 추출):");
    expect(instruction).toContain("- 목표: 예약 화면을 만든다");
    expect(instruction).toContain("- 손댈 범위(이 밖은 건드리기 전에 사용자에게 확인): src/pages/Home.tsx, src/components/nav/");
    expect(instruction).toContain("- 건드리지 말 것: 결제 흐름은 건드리지 않음");
    expect(instruction).toContain("- 완료 기준: 예약 버튼을 누르면 예약 화면이 뜬다");
    expect(instruction).toContain("토큰, 쿠키, 세션 파일을 읽지 마세요"); // 기존 안전 수칙 유지
  });

  test("omits_scope_line_when_scope_empty_and_is_identical_without_contract", () => {
    const scopeless = buildPlanningWorkInstruction({
      prompt: "p",
      outputPath: "plans/p.md",
      contract: { ...CONTRACT, scope: [], exclusions: [], doneCriteria: [] },
    });
    expect(scopeless).toContain("- 목표: 예약 화면을 만든다");
    expect(scopeless).not.toContain("손댈 범위");

    // 계약 부재 = 현행 출력과 완전 동일(퇴행 0 — spec §5·§8)
    const without = buildPlanningWorkInstruction({ prompt: "p", outputPath: "plans/p.md" });
    const withNull = buildPlanningWorkInstruction({ prompt: "p", outputPath: "plans/p.md", contract: null });
    expect(withNull).toBe(without);
  });
```

- [ ] **Step 3: 테스트 실패 확인**

Run: `cd vibelign-gui && npx vitest run src/lib/code-explorer/planningInstruction.test.ts`
Expected: FAIL — `contract` 입력을 받지 않음 (tsc 에러 또는 미주입 출력)

- [ ] **Step 4: 구현**

planningInstruction.ts — import에 추가:
```ts
import type { PlanningContract } from "../vib/types";
```
`PlanningWorkInstructionInput`에 필드 추가:
```ts
  readonly contract?: PlanningContract | null;
```
`buildPlanningWorkInstruction` 구조분해에 `contract` 추가, 본문의 `"작업 기준:",` **바로 위**에 삽입:
```ts
    ...(contract
      ? [
          "이번 작업의 계약(기획안에서 추출):",
          `- 목표: ${contract.goal}`,
          ...(contract.scope.length > 0
            ? [`- 손댈 범위(이 밖은 건드리기 전에 사용자에게 확인): ${contract.scope.map((s) => s.path).join(", ")}`]
            : []),
          ...contract.exclusions.map((item) => `- 건드리지 말 것: ${item}`),
          ...contract.doneCriteria.map((item) => `- 완료 기준: ${item}`),
          "",
        ]
      : []),
```

- [ ] **Step 5: 테스트 통과 확인**

Run: `cd vibelign-gui && npx vitest run src/lib/code-explorer/planningInstruction.test.ts`
Expected: PASS (기존 3개 + 신규 2개)

- [ ] **Step 6: 커밋**

```bash
git add vibelign-gui/src/lib/vib/types.ts vibelign-gui/src/lib/code-explorer/planningInstruction.ts vibelign-gui/src/lib/code-explorer/planningInstruction.test.ts
git commit -m "feat(contract): 지시문에 계약 블록 주입 — 계약 부재 시 현행 출력과 동일(퇴행 0)"
```

---

## Task 7: contract prop 배선 (App → CodeExplorer → 지시문 액션)

**Files:**
- Modify: `vibelign-gui/src/components/code-explorer/PlanningInstructionActions.tsx`
- Modify: `vibelign-gui/src/components/code-explorer/CodeExplorerPlanningContext.tsx`
- Modify: `vibelign-gui/src/pages/CodeExplorer.tsx`
- Modify: `vibelign-gui/src/App.tsx`

- [ ] **Step 1: PlanningInstructionActions**

props 인터페이스에 추가(import도 함께):
```ts
import type { PlanningContract } from "../../lib/vib";
```
```ts
  readonly contract?: PlanningContract | null;
```
구조분해에 `contract = null` 추가, `buildPlanningWorkInstruction` 호출 2곳(:39 프리뷰, :45 복사)에 `contract` 전달:
```ts
  const previewInstruction = fallbackInstruction || buildPlanningWorkInstruction({ prompt, outputPath, persona: previewPersona, contract });
```
```ts
    const instruction = buildPlanningWorkInstruction({ prompt, outputPath, persona, contract });
```
(`PlanningContract`가 `lib/vib` index에서 re-export되지 않으면 `../../lib/vib/types`에서 직접 import — `lib/vib/index.ts`는 `types`를 `export type *` 하는지 확인하고, 안 하면 types 직접 경로 사용. 아래 Step들도 동일.)

- [ ] **Step 2: CodeExplorerPlanningContext**

props에 `readonly contract?: PlanningContract | null;` 추가(import 동일), `<PlanningInstructionActions prompt={prompt} outputPath={outputPath} />`(:30)를:
```tsx
      <PlanningInstructionActions prompt={prompt} outputPath={outputPath} contract={contract} />
```

- [ ] **Step 3: CodeExplorer**

`CodeExplorerProps`(:14 부근)에 추가:
```ts
  planningContract?: PlanningContract | null;
```
구조분해(:19)에 `planningContract = null` 추가, `planningContext={...}`(:120)의 `CodeExplorerPlanningContext`에 `contract={planningContract}` 전달.

- [ ] **Step 4: App**

App.tsx의 CodeExplorer 호출(`page === "code"` 분기 — `planningOutputPath`를 이미 넘기는 곳)에 추가:
```tsx
  planningContract={planningResult?.contract ?? null}
```

- [ ] **Step 5: 타입체크 + 커밋**

Run: `cd vibelign-gui && npx tsc --noEmit`
Expected: 통과 (TSC_EXIT=0)

```bash
git add vibelign-gui/src/components/code-explorer/PlanningInstructionActions.tsx vibelign-gui/src/components/code-explorer/CodeExplorerPlanningContext.tsx vibelign-gui/src/pages/CodeExplorer.tsx vibelign-gui/src/App.tsx
git commit -m "feat(contract): 계약을 코드탐색 지시문 액션까지 배선 — 복사·미리보기 모두 계약 포함"
```

---

## Task 8: 범위 비교 순수 함수 (`scopeReport`) — vitest TDD

**Files:**
- Create: `vibelign-gui/src/lib/home/scopeReport.ts`
- Test: `vibelign-gui/src/lib/home/scopeReport.test.ts`

- [ ] **Step 1: 실패하는 테스트 작성**

`vibelign-gui/src/lib/home/scopeReport.test.ts`:
```ts
import { describe, expect, it } from "vitest";

import { scopeReport } from "./scopeReport";
import type { ContractScopeEntry } from "../vib/types";

const SCOPE: ContractScopeEntry[] = [
  { path: "src/pages/Home.tsx", kind: "file", reason: "" },
  { path: "src/components/nav/", kind: "dir", reason: "" },
];

describe("scopeReport", () => {
  it("파일은 완전일치, 디렉터리는 prefix로 안/밖을 가른다", () => {
    const report = scopeReport(SCOPE, [
      "src/pages/Home.tsx",            // 안 (file 일치)
      "src/components/nav/Guide.tsx",  // 안 (dir prefix)
      "src/pages/Settings.tsx",        // 밖
    ]);
    expect(report.inScope).toBe(2);
    expect(report.outOfScope).toEqual(["src/pages/Settings.tsx"]);
  });

  it("도구 메타데이터 경로만 있으면 null — 실질 변경 0건 (.vibelign/.omc/.git 제외)", () => {
    expect(scopeReport(SCOPE, [".vibelign/report.md", ".omc/state.json", ".git/HEAD"])).toBeNull();
  });

  it("scope가 비면 null — 리포트 자체가 성립 안 함 (spec §7-2)", () => {
    expect(scopeReport([], ["src/a.ts"])).toBeNull();
  });

  it("변경이 없으면 null — '범위 안 0건'은 성공처럼 읽히는 무정보. 비-git(항상 빈 목록)도 이 규칙으로 미노출 (spec §6·§7-1, 외부 리뷰 M2)", () => {
    expect(scopeReport(SCOPE, [])).toBeNull();
  });

  it("dir 경로는 끝 '/' 보장돼 들어오지만, 유사 prefix 오탐은 없어야 한다", () => {
    // "src/components/nav/"가 "src/components/navbar.tsx"에 매칭되면 안 됨
    const report = scopeReport(SCOPE, ["src/components/navbar.tsx"]);
    expect(report?.outOfScope).toEqual(["src/components/navbar.tsx"]);
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd vibelign-gui && npx vitest run src/lib/home/scopeReport.test.ts`
Expected: FAIL — `Cannot find module './scopeReport'`

- [ ] **Step 3: 최소 구현**

`vibelign-gui/src/lib/home/scopeReport.ts`:
```ts
// === ANCHOR: SCOPE_REPORT_START ===
// 작업 계약 scope vs 실제 changed-set 비교 — 판정이 아니라 리포트(spec §6 불개입 원칙).
import type { ContractScopeEntry } from "../vib/types";

export interface ScopeReportResult {
  inScope: number;
  outOfScope: string[];
}

// 가이드 v6 guideRelevantEntries와 같은 목록 — 미구현 모듈 의존 금지로 자체 보유(spec §6).
const META_PREFIXES = [".vibelign/", ".omc/", ".git/"];

/**
 * scope가 비거나 실질 변경이 0건이면 null(리포트 성립 불가 — 표시하지 않는다).
 * "약속 범위 안 0건"은 성공처럼 읽히는 무정보이고, 비-git(항상 빈 changed-set)도
 * 이 규칙으로 자연 미노출된다(spec §6·§7-1, 외부 리뷰 M2).
 * 매칭: kind "file" = 완전일치, kind "dir" = prefix(검증 단계에서 끝 '/' 보장됨 — 유사 prefix 오탐 없음).
 */
export function scopeReport(
  scope: ContractScopeEntry[],
  changedPaths: string[],
): ScopeReportResult | null {
  if (scope.length === 0) return null;
  const relevant = changedPaths.filter(
    (path) => !META_PREFIXES.some((prefix) => path.startsWith(prefix)),
  );
  if (relevant.length === 0) return null;
  const inScopeOf = (path: string): boolean =>
    scope.some((entry) =>
      entry.kind === "file" ? entry.path === path : path.startsWith(entry.path),
    );
  const outOfScope = relevant.filter((path) => !inScopeOf(path));
  return { inScope: relevant.length - outOfScope.length, outOfScope };
}
// === ANCHOR: SCOPE_REPORT_END ===
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `cd vibelign-gui && npx vitest run src/lib/home/scopeReport.test.ts`
Expected: PASS (5개)

- [ ] **Step 5: 커밋**

```bash
git add vibelign-gui/src/lib/home/scopeReport.ts vibelign-gui/src/lib/home/scopeReport.test.ts
git commit -m "feat(contract): scopeReport 순수 함수 — 약속 범위 안/밖 분류(메타 경로 제외, 리포트 전용)"
```

---

## Task 9: 홈 리포트 — guard 실행 시 계산 + SimpleHome 표시

**Files:**
- Modify: `vibelign-gui/src/pages/Home.tsx`
- Modify: `vibelign-gui/src/components/home/SimpleHome.tsx`
- Test: `vibelign-gui/src/components/home/SimpleHome.guard.test.tsx` (확장)
- Modify: `vibelign-gui/src/App.tsx` (home 뷰 Home 호출)

- [ ] **Step 1: SimpleHome 테스트 확장 (RED)**

SimpleHome.guard.test.tsx의 describe 안에 추가 — 기존 render 패턴에 `scopeReport` prop만 추가:
```ts
  test("shows_scope_report_with_out_of_scope_paths", () => {
    render(
      <SimpleHome
        guardResult={PASSING_GUARD}
        watchOn={false}
        watchError={null}
        hasCheckpoint={false}
        guardCheckPending={false}
        guardCheckError={null}
        scopeReport={{ inScope: 2, outOfScope: ["src/pages/Settings.tsx"] }}
        onRetryWatch={() => undefined}
        onRunGuard={() => undefined}
        onShowAdvanced={() => undefined}
        onNavigateBackups={() => undefined}
        onOpenGuardDetails={() => undefined}
      />
    );

    expect(screen.getByText(/약속 범위 안 변경 2건/)).toBeInTheDocument();
    expect(screen.getByText(/범위 밖 1건/)).toBeInTheDocument();
    expect(screen.getByText("src/pages/Settings.tsx")).toBeInTheDocument();
    // 판정 어조 금지 — 안내 문구 확인 (spec §6)
    expect(screen.getByText(/의도한 작업인지 확인해보세요/)).toBeInTheDocument();
  });

  test("hides_scope_report_when_absent", () => {
    render(
      <SimpleHome
        guardResult={PASSING_GUARD}
        watchOn={false}
        watchError={null}
        hasCheckpoint={false}
        guardCheckPending={false}
        guardCheckError={null}
        scopeReport={null}
        onRetryWatch={() => undefined}
        onRunGuard={() => undefined}
        onShowAdvanced={() => undefined}
        onNavigateBackups={() => undefined}
        onOpenGuardDetails={() => undefined}
      />
    );

    expect(screen.queryByText(/약속 범위/)).not.toBeInTheDocument();
  });
```

Run: `cd vibelign-gui && npx vitest run src/components/home/SimpleHome.guard.test.tsx`
Expected: FAIL — `scopeReport` prop 없음

- [ ] **Step 2: SimpleHome 구현 (GREEN)**

SimpleHome.tsx — import 추가:
```ts
import type { ScopeReportResult } from "../../lib/home/scopeReport";
```
`SimpleHomeProps`에 추가:
```ts
  readonly scopeReport?: ScopeReportResult | null;
```
구조분해에 `scopeReport = null` 추가. "프로젝트 안전 상태" `HomeStatusBlock` 안, `{hasGuardProblemDetails ? ... : null}` **위**에 삽입:
```tsx
          {scopeReport ? (
            <div style={{ marginTop: 8, fontSize: 12, lineHeight: 1.55, color: scopeReport.outOfScope.length > 0 ? "#A63A00" : "#555" }}>
              <div>
                약속 범위 안 변경 {scopeReport.inScope}건
                {scopeReport.outOfScope.length > 0 ? ` · 범위 밖 ${scopeReport.outOfScope.length}건` : ""}
              </div>
              {scopeReport.outOfScope.length > 0 ? (
                <details style={{ marginTop: 4 }}>
                  <summary style={{ cursor: "pointer" }}>범위 밖 변경이 있어요 — 의도한 작업인지 확인해보세요</summary>
                  {scopeReport.outOfScope.map((path) => (
                    <div key={path} style={{ fontFamily: "IBM Plex Mono, monospace", fontSize: 11 }}>{path}</div>
                  ))}
                </details>
              ) : null}
            </div>
          ) : null}
```

Run: `cd vibelign-gui && npx vitest run src/components/home/SimpleHome.guard.test.tsx`
Expected: PASS (기존 2개 + 신규 2개)

- [ ] **Step 3: Home에서 guard 실행 시 리포트 계산**

Home.tsx — import 추가(기존 `lib/vib` import 구문에 `listChangedFiles` 추가 + 신규):
```ts
import { scopeReport, type ScopeReportResult } from "../lib/home/scopeReport";
import type { PlanningContract } from "../lib/vib";
```
`HomeProps`에 추가:
```ts
  /** 활성 작업 계약 — 범위 비교 리포트용(없으면 리포트 미노출). */
  planningContract?: PlanningContract | null;
```
구조분해에 `planningContract = null` 추가. state 추가(`guardCheckError` 아래):
```ts
  const [scopeReportResult, setScopeReportResult] = useState<ScopeReportResult | null>(null);
```
`handleRunGuard` 함수 **위**에 헬퍼 추가 — guard 실행 경로는 심플('상태 확인')과 고급(GuardCard) **둘**이라,
한쪽만 갱신하면 고급에서 guard 재실행 후 심플로 복귀할 때 stale 리포트가 새 guard 결과 옆에 남는다(외부 리뷰 M3):
```ts
  // 계약 범위 비교(spec §6) — guard와 같은 순간의 changed-set을 직접 조회.
  // 체크포인트 유무·가이드 v6 구현 여부와 무관(설계 §6 직접 조회 결정).
  // 심플·고급 양 guard 경로 모두에서 호출(stale 방지). 표시는 v1 SimpleHome만(고급 카드 안 표시는 후속).
  async function refreshScopeReport() {
    if (projectDir && planningContract && planningContract.scope.length > 0) {
      try {
        const entries = await listChangedFiles(projectDir);
        setScopeReportResult(scopeReport(planningContract.scope, entries.map((e) => e.path)));
        return;
      } catch {
        // 조회 실패 = 리포트 생략(추측 안내 금지) — 아래 공통 null로
      }
    }
    setScopeReportResult(null);
  }
```
`handleRunGuard`의 `setGuardResult(result);` 바로 아래에 추가:
```ts
      await refreshScopeReport();
```
고급 카드 경로 — `AdvancedHomeCards` 호출의 `onGuardResult` 콜백(`setGuardResult(result); setGuardModal(true);` 부분)에 추가:
```ts
              onGuardResult={(result) => {
                setGuardResult(result);
                setGuardModal(true);
                void refreshScopeReport();
              }}
```
SimpleHome 호출(:150 부근)에 prop 추가:
```tsx
            scopeReport={scopeReportResult}
```

- [ ] **Step 4: App에서 계약 전달**

App.tsx의 **home 뷰** Home 호출(`page === "home"`)에 추가 (manual 뷰는 리포트 표면이 없어 불필요):
```tsx
  planningContract={planningResult?.contract ?? null}
```

- [ ] **Step 5: 타입체크 + 전체 vitest**

Run: `cd vibelign-gui && npx tsc --noEmit && npx vitest run`
Expected: 둘 다 통과

- [ ] **Step 6: 커밋**

```bash
git add vibelign-gui/src/pages/Home.tsx vibelign-gui/src/components/home/SimpleHome.tsx vibelign-gui/src/components/home/SimpleHome.guard.test.tsx vibelign-gui/src/App.tsx
git commit -m "feat(contract): 홈 '상태 확인'에 약속 범위 비교 리포트 — 판정 불개입, 리포트 전용"
```

---

## Task 10: 전체 검증 + push

**Files:** (코드 변경 없음, 발견 시 소규모 수정)

- [ ] **Step 1: 전체 테스트·빌드**

Run: `cd vibelign-gui/src-tauri && cargo test`
Expected: PASS 전부

Run: `cd vibelign-gui && npx vitest run && npm run build`
Expected: PASS + 빌드 성공

- [ ] **Step 2: 육안 체크리스트** (`npm run tauri dev`, AI CLI 설정된 환경)

- [ ] **확정 → 계약 생성**: 기획방에서 대화 후 기획안 저장 → 저장된 Markdown에 "## 작업 계약" 섹션(목표·손댈 범위 후보·금지·완료 기준), `.vibelign/planning/<session>/contract.json` 생성
- [ ] **scope 실존성**: contract.json의 scope 경로가 전부 프로젝트에 실존(파일 또는 디렉터리), dir 경로는 끝 `/`
- [ ] **지시문 주입**: 코드탐색 → 지시문 복사/미리보기에 "이번 작업의 계약" 블록 포함, 페르소나별 복사에도 동일 포함
- [ ] **AI 미설정 폴백(퇴행 0)**: AI CLI 없는 환경에서 저장 → Markdown에 "## 작업 계약: 이번 저장에서는 추출하지 못했어요" 한 줄(원인 단정 문구 없음), contract.json 미생성, 지시문은 기존 보일러플레이트 그대로, 홈 리포트 미노출 — **기존 저장 흐름 어떤 것도 깨지지 않음**
- [ ] **저장 지연(병렬 확인)**: AI CLI 있는 환경에서 저장 소요가 기존(readiness 단독) 대비 크게 늘지 않음 — 두 CLI가 동시에 도는지 체감 확인
- [ ] **리포트 — 범위 안**: 계약 있는 프로젝트에서 scope 안 파일만 수정 → 홈 '상태 확인' → "약속 범위 안 변경 N건"(회색), 범위 밖 문구 없음
- [ ] **리포트 — 범위 밖**: scope 밖 파일 수정 → '상태 확인' → "범위 밖 M건" + 접기 안에 경로 목록 + "의도한 작업인지 확인해보세요" 어조. **guard 판정(pass/warn/fail)은 영향 없음** 확인
- [ ] **계약 없음/scope 빈 배열**: 기획 없이 연 프로젝트·scope가 빈 계약 → 리포트 미노출, '상태 확인'은 기존과 동일
- [ ] **세션 전환**: 이전 기획 세션 불러오기 → 그 세션의 계약이 지시문·리포트에 반영(응답 contract 필드 경유)
- [ ] **재확정**: 같은 세션 재저장 → contract.json 갱신(extractedAt 변경)
- [ ] **양 경로 갱신(stale 없음)**: 심플에서 '상태 확인'(리포트 생성) → 고급 카드에서 guard 재실행 → 심플 복귀 → 리포트가 **마지막 실행 기준**으로 갱신돼 있음(옛 리포트 잔존 없음)
- [ ] **비-git 프로젝트·변경 0건**: 변경이 빈 목록으로 읽혀 리포트 미노출(null 규칙) — "범위 안 0건" 같은 무정보 표시가 없는지 확인
- [ ] 기존 회귀: 기획방 대화·카드·readiness·세션 휴지통·guard 모달 정상

- [ ] **Step 3: push**

```bash
git push origin feat/vibelign-product-renew
```

---

## 후속 (이 계획 밖, 명시 — spec §9)

- guard 판정 연동(범위 밖 변경 → warn 승격) — scope 추정 정확도 검증 후
- anchor intent/protect 자동 설정 — 계약 scope를 앵커 계약으로 내리는 트랙
- 계약 편집 UI(카드 `update_card` 패턴 재사용)
- 계약 나이·stale 표시
- readiness+contract 루브릭 병합(CLI 호출 1회화 — 구독 비용 절감, 출력 비대·파싱 결합 리스크 검토 후 — 외부 리뷰 H1 후속)
- 고급 GuardCard **안의** 리포트 표시 — v1은 갱신만 양 경로, 표시는 SimpleHome (외부 리뷰 M3 후속)
- readiness 실패 카피 "(활성 AI 없음)" 원인 중립화 — 계약과 같은 결함(planning_chat_markdown.rs:266), 본 트랙 범위 밖 (외부 리뷰 M1 후속)
- 가이드 v6 howto 카피 동기화 — 2️⃣·4️⃣에 "작업 계약" 어휘 반영(계약 출하 후, 거짓말 금지 선 재점검)
- `scopeReport`의 메타 경로 목록과 가이드 v6 `guideRelevantEntries` 공용화(둘 다 출하된 뒤 중복 제거)
- Markdown 인라인 새니타이즈 공용 헬퍼(구현 품질 리뷰 기록): LLM 산출 필드(goal·reason·exclusions·doneCriteria, 그리고 기존 카드 summary·bullets)가 개행·`#` 포함 시 문서 구조를 깨뜨릴 수 있음 — `contract_section`·`push_bullets_or_default`·`push_confirmed_decisions` 공통의 기존 노출 클래스라 별도 하드닝 패스로
- 뷰 전환 stale 리포트(구현 품질 리뷰 기록): 고급에서 guard 실행 후 재실행 없이 심플로 전환하면 직전 리포트가 남음 — 리포트는 "마지막 상태 확인의 스냅샷" 의미라 v1 수용, 거슬리면 advancedOpen 전환 시 초기화 검토

## Self-Review 결과 (작성자 점검 + 2026-06-11 외부 리뷰 4건 반영)

- **외부 리뷰 반영:** H1(저장 CLI 순차 2회 → `std::thread` 병렬, Task 5 Step 2 — 루브릭 격리 유지, 병합은 후속. 리뷰의 "결정적 추출+scope만 CLI" 안은 호출 수를 줄이지 못해 미채택, 병합 안은 후속) / M1(실패 카피 원인 단정 → "이번 저장에서는 추출하지 못했어요", Task 4 — readiness 동일 결함은 후속 메모) / M2(비-git 정책 모순 → "메타 제외 후 변경 0건 = null" 단일 규칙으로 설계·구현·체크리스트 통일, Task 8) / M3(고급 GuardCard 경로 → `refreshScopeReport` 헬퍼를 양 경로에서 호출, Task 9 — 리뷰 지적에 더해 stale 리포트 잔존 버그까지 해소, 고급 카드 안 표시는 후속).

- **Spec 커버리지:** §2 아키텍처(저장·응답·렌더)=Task 3·4·5 / §3 스키마=Task 1(Rust)·6(TS) / §4 추출(루브릭·요약·이중 차단·실패 모드)=Task 2·3 / §5 지시문 주입(byte 동일 회귀 포함)=Task 6·7 / §6 리포트(직접 조회·불개입·어조)=Task 8·9 / §7 엣지 5종=scope 빈 배열(Task 8 테스트·Task 9 게이트)·비-git(Task 10 체크)·기획 없음(Task 9 게이트)·stale(후속)·어조(루브릭+UI 문구) / §8 검증=각 Task TDD + Task 10 / §9 비목표=후속 절.
- **설계 정정 반영:** extractedAt=epoch ms(`timestamp_ms()` 관례), changed-set은 Home이 guard 실행 시 직접 조회(설계 문서에 동반 정정 완료 — 체크포인트 무관·시점 일관·v6 독립).
- **플레이스홀더 스캔:** 모든 코드 스텝에 실제 코드 전문. "적절히" 류 없음. Task 4 Step 3의 임시 `None,`은 Task 5 Step 2에서 교체됨을 양쪽에 명시.
- **타입 일관성:** Rust `PlanningContract`/`ScopeEntry`/`ScopeKind`(camelCase 직렬화) ↔ TS `PlanningContract`/`ContractScopeEntry`/`ContractScopeKind` 필드명 일치(extractedAt·doneCriteria). `scopeReport` 반환 `ScopeReportResult`가 SimpleHome prop·Home state와 일치. `planning_chat_success` 5번째 인자 순서가 5개 호출처 전부와 일치. `synthesize_planning_markdown` 4번째 인자 = synthesis 1곳 + tests 2곳.
- **독립 출하성:** Task 1~5(백엔드)만으로도 기획안 문서가 좋아지고, 6~7(지시문)·8~9(리포트)는 각각 독립 가치 — 중간 어디서 멈춰도 출하 가능.
