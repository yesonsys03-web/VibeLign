# 기획방 카드 버튼 (A2) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 카드 상태 전환(동의·보류·빼기)을 사용자 버튼 클릭으로 결정적으로 처리하고, 추출 LLM은 결정 감지(add)만 하도록 좁혀 "이어감=confirm" 오분류를 제거한다.

**Architecture:** 새 Tauri 명령 `update_card`가 카드 1장에 단일 델타 연산(기존 `apply_card_ops` 재사용)을 적용해 `cards.json`을 갱신한다. 추출 루브릭은 add 전용으로 단순화한다. `PlanningCardsPanel`이 draft/held 카드에 버튼을 띄우고 명령을 호출한다.

**Tech Stack:** Rust (Tauri command, serde_json), React/TypeScript, 테스트 `cargo test` + `vitest`/`tsc`.

---

## 배경 명세

`plans/기획방-카드버튼-A2-design.md`(spec)를 구현한다. A(카드 흐름)의 후속으로, LLM의
confirm/reject/hold 추론을 버튼으로 대체한다("이어가면 자동 확정" 폐기).

## 파일 구조
- **수정** `commands/planning_chat_cards.rs` — `update_card` 명령 + 요청/응답 타입 + `action_to_op`; `CARD_RUBRIC` add 전용화.
- **수정** `lib.rs` — `update_card` 등록.
- **수정** `src/lib/vib/types.ts` — `UpdateCardRequest`/`CardUpdateResponse`.
- **수정** `src/lib/vib/planning.ts` + `index.ts` — `updateCard` 래퍼.
- **수정** `src/pages/planning/PlanningCardsPanel.tsx` — 버튼 + 핸들러.
- **수정** `src/pages/PlanningRoom.tsx` — 패널에 props 전달.

---

## Task 1: update_card 명령 (Rust)

**Files:**
- Modify: `vibelign-gui/src-tauri/src/commands/planning_chat_cards.rs`
- Modify: `vibelign-gui/src-tauri/src/lib.rs:120`

- [ ] **Step 1: action_to_op 실패 테스트 작성**

`planning_chat_cards.rs`의 `tests` 모듈에 추가:

```rust
    #[test]
    fn action_to_op_maps_known_and_rejects_unknown() {
        assert_eq!(action_to_op("confirm", "c1".to_string()), Some(CardOp::Confirm { id: "c1".to_string() }));
        assert_eq!(action_to_op("hold", "c1".to_string()), Some(CardOp::Hold { id: "c1".to_string() }));
        assert_eq!(action_to_op("reject", "c1".to_string()), Some(CardOp::Reject { id: "c1".to_string() }));
        assert_eq!(action_to_op("frob", "c1".to_string()), None);
    }
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd vibelign-gui/src-tauri && cargo test planning_chat_cards`
Expected: FAIL — `action_to_op` 미정의.

- [ ] **Step 3: 명령 + 매핑 구현**

`extract_and_apply` 아래(테스트 모듈 위)에 추가:

```rust
fn action_to_op(action: &str, card_id: String) -> Option<CardOp> {
    match action {
        "confirm" => Some(CardOp::Confirm { id: card_id }),
        "hold" => Some(CardOp::Hold { id: card_id }),
        "reject" => Some(CardOp::Reject { id: card_id }),
        _ => None,
    }
}

#[derive(serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct UpdateCardRequest {
    pub(crate) project_dir: String,
    pub(crate) session_id: String,
    pub(crate) card_id: String,
    pub(crate) action: String,
}

#[derive(serde::Serialize)]
#[serde(rename_all = "camelCase")]
pub struct CardUpdateResponse {
    ok: bool,
    cards: Vec<Card>,
    error: Option<String>,
}

#[tauri::command]
pub(crate) async fn update_card(request: UpdateCardRequest) -> CardUpdateResponse {
    let project_dir = PathBuf::from(&request.project_dir);
    if !project_dir.is_absolute() {
        return CardUpdateResponse {
            ok: false,
            cards: Vec::new(),
            error: Some("projectDir must be absolute".to_string()),
        };
    }
    tauri::async_runtime::spawn_blocking(move || {
        let Some(op) = action_to_op(&request.action, request.card_id.clone()) else {
            return CardUpdateResponse {
                ok: false,
                cards: Vec::new(),
                error: Some(format!("unknown action: {}", request.action)),
            };
        };
        let session_dir =
            super::planning_chat_store::planning_dir(&project_dir).join(&request.session_id);
        let now = std::time::SystemTime::now()
            .duration_since(std::time::UNIX_EPOCH)
            .map_or(0, |d| d.as_millis())
            .to_string();
        let cards = read_cards(&session_dir);
        let updated = apply_card_ops(cards, &[op], &now);
        if let Err(error) = write_cards(&session_dir, &updated) {
            return CardUpdateResponse {
                ok: false,
                cards: updated,
                error: Some(error),
            };
        }
        CardUpdateResponse {
            ok: true,
            cards: updated,
            error: None,
        }
    })
    .await
    .unwrap_or_else(|error| CardUpdateResponse {
        ok: false,
        cards: Vec::new(),
        error: Some(error.to_string()),
    })
}
```

- [ ] **Step 4: lib.rs 등록**

`lib.rs`의 `commands::planning_chat::save_planning_chat_as_markdown,`(line 120) 다음 줄에 추가:

```rust
            commands::planning_chat_cards::update_card,
```

- [ ] **Step 5: 컴파일 + 테스트**

Run: `cd vibelign-gui/src-tauri && cargo test planning_chat_cards && cargo build`
Expected: PASS (기존 9 + action_to_op 1 = 10개), 빌드 성공.

> `planning_dir`는 `planning_chat_store`에 `pub(crate)`로 존재한다. `PathBuf`는 파일 상단에서 이미 import됨. `serde::{Deserialize, Serialize}`도 이미 import됨.

- [ ] **Step 6: 커밋**

```bash
git add vibelign-gui/src-tauri/src/commands/planning_chat_cards.rs vibelign-gui/src-tauri/src/lib.rs
git commit -m "feat(planning): update_card command for button-driven state transitions"
```

---

## Task 2: 추출 루브릭 add 전용화 (Rust)

**Files:**
- Modify: `vibelign-gui/src-tauri/src/commands/planning_chat_cards.rs`

- [ ] **Step 1: CARD_RUBRIC 교체**

`const CARD_RUBRIC: &str = r#"..."#;` 전체를 아래로 교체:

```rust
const CARD_RUBRIC: &str = r#"너는 기획 대화를 읽고 '결정'을 카드로 감지하는 추출기다.
이번 턴의 대화를 보고, 새로 생긴 결정만 카드로 추가하는 add 연산을 JSON으로 낸다.

규칙:
- 결정에만 반응한다. 인사·잡담·질문이면 ops는 빈 배열 [].
- 새 결정이 생기면 add (흐릿한 초안 카드 생성).
- 아래 '현재 카드'에 이미 있는 결정은 다시 add하지 않는다(중복 금지).
- 확정/거절/보류는 사용자가 버튼으로 직접 한다. 추출기는 confirm/reject/hold를 절대 내지 않는다. add만 낸다.

반드시 아래 JSON만 출력한다(설명 금지):
{ "ops": [
  { "op": "add", "title": "...", "summary": "...", "reason": "..." }
] }
"#;
```

- [ ] **Step 2: 기존 테스트 갱신**

`build_prompt_includes_rules_current_cards_and_turn` 테스트의 마지막 단언 `assert!(prompt.contains("confirm"));`은 이제 루브릭에 "confirm"이 등장하긴 한다("confirm/reject/hold를 절대 내지 않는다"). 안전하게 검증을 바꾼다 — 그 줄을 아래로 교체:

```rust
        assert!(prompt.contains("add만 낸다"));
```

- [ ] **Step 3: 테스트 통과 확인**

Run: `cd vibelign-gui/src-tauri && cargo test planning_chat_cards`
Expected: PASS (10개).

- [ ] **Step 4: 커밋**

```bash
git add vibelign-gui/src-tauri/src/commands/planning_chat_cards.rs
git commit -m "feat(planning): narrow extraction rubric to add-only (transitions via buttons)"
```

---

## Task 3: TS 타입 + updateCard 래퍼

**Files:**
- Modify: `vibelign-gui/src/lib/vib/types.ts`
- Modify: `vibelign-gui/src/lib/vib/planning.ts`
- Modify: `vibelign-gui/src/lib/vib/index.ts`

- [ ] **Step 1: 타입 추가**

`types.ts`의 `Card` 인터페이스 아래에 추가:

```typescript
export interface UpdateCardRequest {
  projectDir: string;
  sessionId: string;
  cardId: string;
  action: "confirm" | "hold" | "reject";
}

export interface CardUpdateResponse {
  ok: boolean;
  cards: readonly Card[];
  error?: string | null;
}
```

- [ ] **Step 2: invoke 래퍼 추가**

`planning.ts`의 import 타입 목록(`import type { ... } from "./types";`)에 `CardUpdateResponse, UpdateCardRequest`를 추가하고, 파일 끝(`PLANNING_END` 앵커 주석 위)에 추가:

```typescript
// === ANCHOR: PLANNING_UPDATECARD_START ===
export function updateCard(request: UpdateCardRequest): Promise<CardUpdateResponse> {
  return invoke<CardUpdateResponse>("update_card", { request });
}
// === ANCHOR: PLANNING_UPDATECARD_END ===
```

- [ ] **Step 3: index.ts에서 export**

`index.ts`의 `export { ... } from "./planning";` 목록에 `updateCard`를 추가한다(알파벳/기존 순서 유지).

- [ ] **Step 4: 타입체크**

Run: `cd vibelign-gui && npx tsc --noEmit`
Expected: 에러 없음.

- [ ] **Step 5: 커밋**

```bash
git add vibelign-gui/src/lib/vib/types.ts vibelign-gui/src/lib/vib/planning.ts vibelign-gui/src/lib/vib/index.ts
git commit -m "feat(planning): updateCard invoke wrapper + types"
```

---

## Task 4: 카드 버튼 (TS)

**Files:**
- Modify: `vibelign-gui/src/pages/planning/PlanningCardsPanel.tsx`

- [ ] **Step 1: props 확장 + 버튼 추가**

`PlanningCardsPanel.tsx` 전체를 아래로 교체:

```tsx
import { useState } from "react";
import type { CSSProperties } from "react";

import { updateCard } from "../../lib/vib";
import type { Card, CardState } from "../../lib/vib/types";

const STATE_STYLE: Record<CardState, CSSProperties> = {
  draft: { borderStyle: "dashed", opacity: 0.6 },
  held: { borderStyle: "dashed", opacity: 0.6, marginLeft: 24 },
  confirmed: { borderStyle: "solid", opacity: 1 },
};

const STATE_LABEL: Record<CardState, string> = {
  draft: "초안",
  held: "보류",
  confirmed: "확정",
};

type CardAction = "confirm" | "hold" | "reject";

interface PlanningCardsPanelProps {
  readonly cards: readonly Card[] | null | undefined;
  readonly projectDir: string;
  readonly sessionId: string | null;
  readonly onCardsChange: (cards: readonly Card[]) => void;
}

export function PlanningCardsPanel({ cards, projectDir, sessionId, onCardsChange }: PlanningCardsPanelProps) {
  const [busyId, setBusyId] = useState<string | null>(null);

  if (!cards || cards.length === 0) {
    return null;
  }

  async function handleAction(cardId: string, action: CardAction) {
    if (!sessionId || busyId) {
      return;
    }
    setBusyId(cardId);
    try {
      const result = await updateCard({ projectDir, sessionId, cardId, action });
      if (result.ok) {
        onCardsChange(result.cards);
      }
    } finally {
      setBusyId(null);
    }
  }

  return (
    <div style={{ display: "grid", gap: 8 }}>
      {cards.map((card) => (
        <CardItem key={card.id} card={card} busy={busyId === card.id} onAction={handleAction} />
      ))}
    </div>
  );
}

function CardItem({
  card,
  busy,
  onAction,
}: {
  readonly card: Card;
  readonly busy: boolean;
  readonly onAction: (cardId: string, action: CardAction) => void;
}) {
  const [open, setOpen] = useState(false);
  const showButtons = card.state === "draft" || card.state === "held";
  return (
    <div
      style={{
        border: "2px solid #1A1A1A",
        background: "#FEFBF0",
        padding: "10px 12px",
        transition: "opacity 0.3s ease, border-style 0.3s ease",
        ...STATE_STYLE[card.state],
      }}
    >
      <div style={{ display: "flex", justifyContent: "space-between", gap: 8 }}>
        <strong style={{ fontSize: 12 }}>{card.title}</strong>
        <span style={{ fontSize: 10, opacity: 0.7 }}>{STATE_LABEL[card.state]}</span>
      </div>
      {card.summary && <div style={{ fontSize: 11, marginTop: 4 }}>{card.summary}</div>}
      {card.reason && (
        <button
          type="button"
          onClick={() => setOpen((value) => !value)}
          style={{ fontSize: 10, marginTop: 6, background: "none", border: "none", cursor: "pointer", padding: 0, textDecoration: "underline" }}
        >
          {open ? "이유 접기" : "이유 보기"}
        </button>
      )}
      {open && card.reason && <div style={{ fontSize: 11, marginTop: 4, opacity: 0.85 }}>{card.reason}</div>}
      {showButtons && (
        <div style={{ display: "flex", gap: 6, marginTop: 8 }}>
          <button type="button" disabled={busy} onClick={() => onAction(card.id, "confirm")} style={cardBtnStyle}>✓ 동의</button>
          <button type="button" disabled={busy} onClick={() => onAction(card.id, "hold")} style={cardBtnStyle}>⏸ 보류</button>
          <button type="button" disabled={busy} onClick={() => onAction(card.id, "reject")} style={cardBtnStyle}>✕ 빼기</button>
        </div>
      )}
    </div>
  );
}

const cardBtnStyle: CSSProperties = {
  fontSize: 10,
  fontWeight: 700,
  border: "1.5px solid #1A1A1A",
  background: "#fff",
  padding: "3px 8px",
  cursor: "pointer",
};
```

- [ ] **Step 2: 타입체크**

Run: `cd vibelign-gui && npx tsc --noEmit`
Expected: 에러 (PlanningRoom이 아직 새 props 안 줌) — 이는 Task 5에서 해결. 단, `PlanningCardsPanel.tsx` 자체에 타입 에러는 없어야 함. `npx tsc --noEmit` 결과에서 PlanningRoom.tsx의 props 누락 에러만 남는지 확인.

> 이 task는 PlanningRoom과 함께 컴파일이 맞춰지므로, 커밋은 Task 5와 묶어도 되지만 분리 유지를 위해 여기서 커밋한다. tsc가 PlanningRoom props 에러만 보고하면 정상.

- [ ] **Step 3: 커밋**

```bash
git add vibelign-gui/src/pages/planning/PlanningCardsPanel.tsx
git commit -m "feat(planning): card action buttons (동의/보류/빼기)"
```

---

## Task 5: PlanningRoom 배선 (TS)

**Files:**
- Modify: `vibelign-gui/src/pages/PlanningRoom.tsx`

- [ ] **Step 1: 패널 props 전달**

`<PlanningCardsPanel cards={result.cards} />`를 아래로 교체:

```tsx
            <PlanningCardsPanel
              cards={result.cards}
              projectDir={projectDir}
              sessionId={result.sessionId ?? null}
              onCardsChange={(cards) => onResultChange({ ...result, cards })}
            />
```

- [ ] **Step 2: 타입체크 + 빌드**

Run: `cd vibelign-gui && npx tsc --noEmit`
Expected: 에러 없음(이제 PlanningCardsPanel props 충족).

> `projectDir`는 PlanningRoom props에 이미 있다(`PlanningRoomProps`). `onResultChange`도 있다. `result.sessionId`도 응답 타입에 있다.

- [ ] **Step 3: 커밋**

```bash
git add vibelign-gui/src/pages/PlanningRoom.tsx
git commit -m "feat(planning): wire card buttons (projectDir/session/onCardsChange) in planning room"
```

---

## Task 6: 라이브 수동 검증

LLM/GUI 의존이라 수동.

- [ ] **Step 1: 앱에서 확인**

`npm run tauri dev`로 띄우고 기획방에서:
1. 결정 한 줄("X로 하자") → **흐릿한 draft 카드**가 뜬다.
2. 다음 입력으로 단순히 대화를 이어간다 → **자동 confirm 안 됨**(draft 그대로). ← 핵심 회귀 확인
3. 카드의 **[✓ 동의]** 클릭 → **confirmed(또렷)**, 버튼 사라짐.
4. 다른 draft에서 **[✕ 빼기]** → 사라짐. **[⏸ 보류]** → held(비켜둠).
5. 앱 재시작(또는 방 재진입) 후에도 카드 상태 유지(cards.json 영속).

- [ ] **Step 2: 결과 기록**

자동 confirm이 사라지고 버튼만으로 상태가 바뀌면 통과. 추출이 여전히 confirm/reject를 내면 `CARD_RUBRIC`을 더 강하게 조정.

---

## Self-Review (작성자 점검)

**Spec 커버리지:**
- §1 update_card 명령 → Task 1. ✅
- §2 add 전용 루브릭 → Task 2. ✅
- §3 프론트(타입·래퍼·버튼·배선) → Task 3·4·5. ✅
- §6 테스트 → Task 1(action_to_op 단위) + Task 6(라이브). ✅

**플레이스홀더 스캔:** 없음. 모든 코드 스텝 완전 코드. ✅

**타입 일관성:** `UpdateCardRequest`/`CardUpdateResponse`/`action_to_op`/`updateCard`/`CardAction`("confirm"|"hold"|"reject") 가 Rust(camelCase serde)↔TS 일치. `apply_card_ops` 재사용. `PlanningCardsPanel` props(projectDir/sessionId/onCardsChange)가 Task 4 정의와 Task 5 호출에서 일치. ✅

**주의:** Task 4 단독 커밋 시 tsc가 PlanningRoom props 누락을 보고하는 게 정상(Task 5에서 해소). 두 task는 연속 실행한다.
