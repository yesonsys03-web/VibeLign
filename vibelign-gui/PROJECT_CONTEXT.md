<!-- VibeLign Transfer Context -->
<!--
  ⚡ AI Transfer Context — Generated by VibeLign
  이 파일을 읽으면 이 프로젝트를 즉시 파악할 수 있어요.
  Generated: 2026-04-05 22:10:34 | `vib transfer`

  📌 AI 툴에게: Session Handoff 블록을 먼저 읽고 작업을 시작하세요.
-->

## Session Handoff
> ⚠️ This block is session-specific and time-sensitive. Read this first.

Generated: 2026-04-05 22:10
Handoff source: file_fallback
Handoff quality: auto-drafted
Latest checkpoint: 카드 드래그 순서 변경 + store 저장 완료

VibeLign patch rules
- Split composite requests into intent / source / destination / behavior_constraint
- Treat delete + move as move + preservation unless removal is explicit
- Resolve source and destination by role, not with one shared rule
- If patch contract or codespeak shape changes, update tests and docs together

- Today's work: fix: add store plugin permissions to capabilities, fix: explicitly call store.save() after set to persist card order, feat: card drag-and-drop reorder with persistent store, feat: add useCardOrder hook with tauri-plugin-store persistence, feat: register tauri-plugin-store plugin
- Changed files: `PROJECT_CONTEXT.md`, `vibelign-gui/PROJECT_CONTEXT.md`, `vibelign-gui/src/lib/commands.ts`, `.superpowers/`, `vibelign-gui/src/lib/commandData.ts` … (+1)
- Completed work: (not provided)
- Unfinished work: (not provided)
- Next action: (not provided)

---

# ⚡ vibelign-gui — AI Transfer Context

> 이 파일은 `vib transfer`로 자동 생성되었습니다.
> **AI 툴이 바뀌어도 이 파일 하나로 즉시 이어서 작업 가능합니다.**

---

## 1. 지금 무엇을 작업 중인가

- **마지막 작업**: 20260405T130950206608Z_카드_드래그_순서_변경_store_저장_완료 — `카드 드래그 순서 변경 + store 저장 완료`
- **상태**: 작업 진행 중 (아래 체크포인트 기록 참고)

---

## 2. 프로젝트 요약

- **프로젝트명**: vibelign-gui
- **기술 스택**: React
- **루트 경로**: /Users/topsphinx/YesonENT Dropbox/top sphinx/Mac/Documents/coding/VibeLign/vibelign-gui

---

## 3. 파일 구조

```
vibelign-gui/
├── docs/
│   └── superpowers/
│       ├── plans/
│       └── specs/
├── public/
├── src/
│   ├── assets/
│   ├── components/
│   │   ├── cards/
│   │   ├── CustomTitleBar.tsx
│   │   └── GuiCliOutputBlock.tsx
│   ├── hooks/
│   │   └── useCardOrder.ts
│   ├── lib/
│   │   ├── commandData.ts
│   │   ├── commandData2.ts
│   │   ├── commands.ts
│   │   ├── helpData.ts
│   │   └── vib.ts
│   ├── pages/
│   │   ├── Checkpoints.tsx
│   │   ├── Doctor.tsx
│   │   ├── Home.tsx
│   │   ├── Onboarding.tsx
│   │   └── Settings.tsx
│   ├── styles/
│   │   └── brutalism.css
│   ├── App.css
│   ├── App.tsx
│   ├── main.tsx
│   └── vite-env.d.ts
├── src-tauri/
│   ├── binaries/
│   │   ├── vib-aarch64-apple-darwin
│   │   └── vib-x86_64-apple-darwin
│   ├── capabilities/
│   │   └── default.json
│   ├── gen/
│   │   └── schemas/
│   ├── icons/
│   │   ├── android/
│   │   ├── ios/
│   │   └── icon.icns
│   ├── src/
│   │   ├── lib.rs
│   │   ├── main.rs  ⭐
│   │   └── vib_path.rs
│   ├── target/
│   │   ├── debug/
│   │   └── CACHEDIR.TAG
│   ├── Cargo.toml  ⭐
│   ├── build.rs
│   └── tauri.conf.json
├── PROJECT_CONTEXT.md
├── README.md  ⭐
├── eslint.config.js
├── index.html
├── package-lock.json
├── package.json  ⭐
├── tsconfig.json
├── tsconfig.node.json
└── vite.config.ts
```

> ⭐ = 핵심 진입점 파일

---

## 4. 최근 체크포인트 (작업 이력)

| 시간 | 작업 내용 |
|------|-----------|
| 20260405T130950206608Z_카드_드래그_순서_변경_store_저장_완료 | 카드 드래그 순서 변경 + store 저장 완료 |

> 전체 기록: `vib history`

---

## 5. AI 작업 규칙 (AGENTS.md)

```
(AGENTS.md 없음 — `vib start` 실행 권장)
```

---

## 6. 실행 방법

```bash
  npm run dev
```

---

## 7. VibeLign 필수 명령어

```bash
vib checkpoint "설명"  # 작업 전 반드시 저장
vib undo               # 잘못됐으면 되돌리기
vib history            # 작업 이력 보기
vib transfer           # 이 파일 갱신
```

---

*Generated by [VibeLign](https://github.com/yesonsys03-web/VibeLign) — AI 코딩 안전망*
